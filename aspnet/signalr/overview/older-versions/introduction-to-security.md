---
uid: signalr/overview/older-versions/introduction-to-security
title: Wprowadzenie do zabezpieczeń sygnalizujących (sygnał 1. x) | Microsoft Docs
author: bradygaster
description: Opisuje problemy z zabezpieczeniami, które należy wziąć pod uwagę podczas tworzenia aplikacji sygnalizującej.
ms.author: bradyg
ms.date: 10/17/2013
ms.assetid: 715a4059-d307-4631-abbb-c789c95d6eb4
msc.legacyurl: /signalr/overview/older-versions/introduction-to-security
msc.type: authoredcontent
ms.openlocfilehash: 34172c0a2a15a7ab0d782704d5831ce236f5c989
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/06/2020
ms.locfileid: "78536730"
---
# <a name="introduction-to-signalr-security-signalr-1x"></a><span data-ttu-id="cb4ef-103">Wprowadzenie do zabezpieczeń usługi SignalR (SignalR 1.x)</span><span class="sxs-lookup"><span data-stu-id="cb4ef-103">Introduction to SignalR Security (SignalR 1.x)</span></span>

<span data-ttu-id="cb4ef-104">[Fletcher Patryk](https://github.com/pfletcher), [Tomasz FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="cb4ef-104">by [Patrick Fletcher](https://github.com/pfletcher), [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

[!INCLUDE [Consider ASP.NET Core SignalR](~/includes/signalr/signalr-version-disambiguation.md)]

> <span data-ttu-id="cb4ef-105">W tym artykule opisano problemy związane z zabezpieczeniami, które należy wziąć pod uwagę podczas tworzenia aplikacji sygnalizującej.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-105">This article describes the security issues you must consider when developing a SignalR application.</span></span>

## <a name="overview"></a><span data-ttu-id="cb4ef-106">Omówienie</span><span class="sxs-lookup"><span data-stu-id="cb4ef-106">Overview</span></span>

<span data-ttu-id="cb4ef-107">Ten dokument zawiera następujące sekcje:</span><span class="sxs-lookup"><span data-stu-id="cb4ef-107">This document contains the following sections:</span></span>

- [<span data-ttu-id="cb4ef-108">Pojęcia dotyczące zabezpieczeń sygnałów</span><span class="sxs-lookup"><span data-stu-id="cb4ef-108">SignalR Security Concepts</span></span>](#concepts)

    - [<span data-ttu-id="cb4ef-109">Uwierzytelnianie i autoryzacja</span><span class="sxs-lookup"><span data-stu-id="cb4ef-109">Authentication and authorization</span></span>](#authentication)
    - [<span data-ttu-id="cb4ef-110">Token połączenia</span><span class="sxs-lookup"><span data-stu-id="cb4ef-110">Connection token</span></span>](#connectiontoken)
    - [<span data-ttu-id="cb4ef-111">Ponowne łączenie grup po ponownym połączeniu</span><span class="sxs-lookup"><span data-stu-id="cb4ef-111">Rejoining groups when reconnecting</span></span>](#rejoingroup)
- [<span data-ttu-id="cb4ef-112">Jak sygnalizujący zapobiega podrabianiu żądań między lokacjami</span><span class="sxs-lookup"><span data-stu-id="cb4ef-112">How SignalR prevents Cross-Site Request Forgery</span></span>](#csrf)
- [<span data-ttu-id="cb4ef-113">Zalecenia dotyczące zabezpieczeń sygnałów</span><span class="sxs-lookup"><span data-stu-id="cb4ef-113">SignalR Security Recommendations</span></span>](#recommendations)

    - [<span data-ttu-id="cb4ef-114">Protokół SSL (Secure Sockets)</span><span class="sxs-lookup"><span data-stu-id="cb4ef-114">Secure Socket Layers (SSL) protocol</span></span>](#ssl)
    - [<span data-ttu-id="cb4ef-115">Nie używaj grup jako mechanizmu zabezpieczeń</span><span class="sxs-lookup"><span data-stu-id="cb4ef-115">Do not use groups as a security mechanism</span></span>](#groupsecurity)
    - [<span data-ttu-id="cb4ef-116">Bezpieczne obsługiwanie danych wejściowych z klientów</span><span class="sxs-lookup"><span data-stu-id="cb4ef-116">Safely handling input from clients</span></span>](#input)
    - [<span data-ttu-id="cb4ef-117">Uzgadnianie zmiany stanu użytkownika z aktywnym połączeniem</span><span class="sxs-lookup"><span data-stu-id="cb4ef-117">Reconciling a change in user status with an active connection</span></span>](#reconcile)
    - [<span data-ttu-id="cb4ef-118">Automatycznie generowane pliki proxy JavaScript</span><span class="sxs-lookup"><span data-stu-id="cb4ef-118">Automatically generated JavaScript proxy files</span></span>](#autogen)
    - [<span data-ttu-id="cb4ef-119">Wyjątki</span><span class="sxs-lookup"><span data-stu-id="cb4ef-119">Exceptions</span></span>](#exceptions)

<a id="concepts"></a>

## <a name="signalr-security-concepts"></a><span data-ttu-id="cb4ef-120">Pojęcia dotyczące zabezpieczeń sygnałów</span><span class="sxs-lookup"><span data-stu-id="cb4ef-120">SignalR Security Concepts</span></span>

<a id="authentication"></a>

### <a name="authentication-and-authorization"></a><span data-ttu-id="cb4ef-121">Uwierzytelnianie i autoryzacja</span><span class="sxs-lookup"><span data-stu-id="cb4ef-121">Authentication and authorization</span></span>

<span data-ttu-id="cb4ef-122">Sygnał został zaprojektowany do zintegrowania z istniejącą strukturą uwierzytelniania dla aplikacji.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-122">SignalR is designed to be integrated into the existing authentication structure for an application.</span></span> <span data-ttu-id="cb4ef-123">Nie udostępnia żadnych funkcji uwierzytelniania użytkowników.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-123">It does not provide any features for authenticating users.</span></span> <span data-ttu-id="cb4ef-124">Zamiast tego uwierzytelniasz użytkowników jako normalnie w aplikacji, a następnie pracujesz z wynikami uwierzytelniania w kodzie sygnalizującym.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-124">Instead, you authenticate users as you would normally in your application, and then work with the results of the authentication in your SignalR code.</span></span> <span data-ttu-id="cb4ef-125">Można na przykład uwierzytelniać użytkowników za pomocą uwierzytelniania ASP.NET Forms, a następnie w centrum, wymusić, którzy użytkownicy lub które role mają autoryzację, aby wywołać metodę.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-125">For example, you might authenticate your users with ASP.NET forms authentication, and then in your hub, enforce which users or roles are authorized to call a method.</span></span> <span data-ttu-id="cb4ef-126">W centrum można także przekazać informacje uwierzytelniania, takie jak nazwa użytkownika lub czy użytkownik należy do roli, do klienta programu.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-126">In your hub, you can also pass authentication information, such as user name or whether a user belongs to a role, to the client.</span></span>

<span data-ttu-id="cb4ef-127">Sygnalizujący udostępnia atrybut [Autoryzuj](https://msdn.microsoft.com/library/microsoft.aspnet.signalr.authorizeattribute(v=vs.111).aspx) , aby określić, którzy użytkownicy mają dostęp do centrum lub metody.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-127">SignalR provides the [Authorize](https://msdn.microsoft.com/library/microsoft.aspnet.signalr.authorizeattribute(v=vs.111).aspx) attribute to specify which users have access to a hub or method.</span></span> <span data-ttu-id="cb4ef-128">Należy zastosować atrybut Autoryzuj do koncentratora lub określonych metod w centrum.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-128">You apply the Authorize attribute to either a hub or particular methods in a hub.</span></span> <span data-ttu-id="cb4ef-129">Bez atrybutu Autoryzuj wszystkie metody publiczne w centrum są dostępne dla klienta, który jest podłączony do centrum.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-129">Without the Authorize attribute, all public methods on the hub are available to a client that is connected to the hub.</span></span> <span data-ttu-id="cb4ef-130">Aby uzyskać więcej informacji na temat centrów, zobacz [uwierzytelnianie i autoryzacja dla centrów sygnałów](../security/hub-authorization.md).</span><span class="sxs-lookup"><span data-stu-id="cb4ef-130">For more information about hubs, see [Authentication and Authorization for SignalR Hubs](../security/hub-authorization.md).</span></span>

<span data-ttu-id="cb4ef-131">Atrybut `Authorize` jest używany tylko z koncentratorami.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-131">The `Authorize` attribute is used only with hubs.</span></span> <span data-ttu-id="cb4ef-132">Aby wymusić reguły autoryzacji przy użyciu `PersistentConnection` należy zastąpić metodę `AuthorizeRequest`.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-132">To enforce authorization rules when using a `PersistentConnection` you must override the `AuthorizeRequest` method.</span></span> <span data-ttu-id="cb4ef-133">Aby uzyskać więcej informacji na temat połączeń trwałych, zobacz [uwierzytelnianie i autoryzacja dla połączeń trwałych](../security/persistent-connection-authorization.md).</span><span class="sxs-lookup"><span data-stu-id="cb4ef-133">For more information about persistent connections, see [Authentication and Authorization for SignalR Persistent Connections](../security/persistent-connection-authorization.md).</span></span>

<a id="connectiontoken"></a>

### <a name="connection-token"></a><span data-ttu-id="cb4ef-134">Token połączenia</span><span class="sxs-lookup"><span data-stu-id="cb4ef-134">Connection token</span></span>

<span data-ttu-id="cb4ef-135">Program sygnalizujący zmniejsza ryzyko wykonania złośliwych poleceń przez zweryfikowanie tożsamości nadawcy.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-135">SignalR mitigates the risk of executing malicious commands by validating the identity of the sender.</span></span> <span data-ttu-id="cb4ef-136">Token połączenia zawierający identyfikator i nazwę użytkownika dla uwierzytelnionych użytkowników jest przesyłany między klientem i serwerem dla każdego żądania.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-136">A connection token, containing the connection id and username for authenticated users, is passed between the client and server for each request.</span></span> <span data-ttu-id="cb4ef-137">Identyfikator połączenia jest unikatowym identyfikatorem, który jest generowana losowo przez serwer w przypadku utworzenia nowego połączenia i jest utrwalany na czas trwania połączenia.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-137">The connection id is a unique identifier that is randomly generated by the server when a new connection is created, and is persisted for the duration of the connection.</span></span> <span data-ttu-id="cb4ef-138">Nazwa użytkownika jest udostępniana przez mechanizm uwierzytelniania dla aplikacji sieci Web.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-138">The username is provided by the authentication mechanism for the web application.</span></span> <span data-ttu-id="cb4ef-139">Token połączenia jest chroniony przy użyciu szyfrowania i podpisu cyfrowego.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-139">The connection token is protected with encryption and a digital signature.</span></span>

![](introduction-to-security/_static/image2.png)

<span data-ttu-id="cb4ef-140">Dla każdego żądania serwer sprawdza zawartość tokenu, aby upewnić się, że żądanie pochodzi od określonego użytkownika.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-140">For each request, the server validates the contents of the token to ensure that the request is coming from the specified user.</span></span> <span data-ttu-id="cb4ef-141">Nazwa użytkownika musi odpowiadać identyfikatorowi połączenia. Sprawdzając poprawność zarówno identyfikatora połączenia, jak i nazwy użytkownika, sygnalizujący uniemożliwia złośliwemu użytkownikowi łatwe personifikowanie innego użytkownika.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-141">The username must correspond to the connection id. By validating both the connection id and the username, SignalR prevents a malicious user from easily impersonating another user.</span></span> <span data-ttu-id="cb4ef-142">Jeśli serwer nie może zweryfikować tokenu połączenia, żądanie nie powiedzie się.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-142">If the server cannot validate the connection token, the request fails.</span></span>

![](introduction-to-security/_static/image4.png)

<span data-ttu-id="cb4ef-143">Ponieważ identyfikator połączenia jest częścią procesu weryfikacji, nie należy ujawniać identyfikatora połączenia jednego użytkownika innym użytkownikom ani przechowywać wartości na komputerze klienckim, na przykład w pliku cookie.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-143">Because the connection id is part of the verification process, you should not reveal one user's connection id to other users or store the value on the client, such as in a cookie.</span></span>

<a id="rejoingroup"></a>

### <a name="rejoining-groups-when-reconnecting"></a><span data-ttu-id="cb4ef-144">Ponowne łączenie grup po ponownym połączeniu</span><span class="sxs-lookup"><span data-stu-id="cb4ef-144">Rejoining groups when reconnecting</span></span>

<span data-ttu-id="cb4ef-145">Domyślnie aplikacja sygnalizująca automatycznie ponownie przypisze użytkownika do odpowiednich grup podczas ponownego nawiązywania połączenia po tymczasowym przerwaniu, na przykład w przypadku porzucenia i ponownego ustanowienia połączenia przed upływem limitu czasu połączenia. Po ponownym nawiązaniu połączenia klient przekazuje token grupy zawierający identyfikator połączenia i przypisane grupy.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-145">By default, the SignalR application will automatically re-assign a user to the appropriate groups when reconnecting from a temporary disruption, such as when a connection is dropped and re-established before the connection times out. When reconnecting, the client passes a group token that includes the connection id and the assigned groups.</span></span> <span data-ttu-id="cb4ef-146">Token grupy jest podpisany cyfrowo i szyfrowany.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-146">The group token is digitally signed and encrypted.</span></span> <span data-ttu-id="cb4ef-147">Klient zachowuje ten sam identyfikator połączenia po ponownej nawiązaniu połączenia; w związku z tym identyfikator połączenia przekazaną przez ponownie podłączony klient musi być zgodny z poprzednim identyfikatorem połączenia używanym przez klienta.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-147">The client retains the same connection id after a reconnection; therefore, the connection id passed from the reconnected client must match the previous connection id used by the client.</span></span> <span data-ttu-id="cb4ef-148">Ta weryfikacja uniemożliwia złośliwemu użytkownikowi przekazanie żądań dołączenia do nieautoryzowanych grup podczas ponownego nawiązywania połączenia.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-148">This verification prevents a malicious user from passing requests to join unauthorized groups when reconnecting.</span></span>

<span data-ttu-id="cb4ef-149">Należy jednak pamiętać, że token grupy nie wygasa.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-149">However, it is important to note, that the group token does not expire.</span></span> <span data-ttu-id="cb4ef-150">Jeśli użytkownik należy do grupy w przeszłości, ale został zabroniony z tej grupy, może być w stanie naśladować token grupy zawierający zabronioną grupę.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-150">If a user belonged to a group in the past, but was banned from that group, that user may be able to mimic a group token that includes the prohibited group.</span></span> <span data-ttu-id="cb4ef-151">Jeśli musisz bezpiecznie zarządzać użytkownikami należącymi do grup, musisz przechowywać te dane na serwerze, na przykład w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-151">If you need to securely manage which users belong to which groups, you need to store that data on the server, such as in a database.</span></span> <span data-ttu-id="cb4ef-152">Następnie należy dodać logikę do aplikacji, która weryfikuje na serwerze, czy użytkownik należy do grupy.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-152">Then, add logic to your application that verifies on the server whether a user belongs to a group.</span></span> <span data-ttu-id="cb4ef-153">Aby zapoznać się z przykładem weryfikowania członkostwa w grupie, zobacz [Praca z grupami](../guide-to-the-api/working-with-groups.md).</span><span class="sxs-lookup"><span data-stu-id="cb4ef-153">For an example of verifying group membership, see [Working with groups](../guide-to-the-api/working-with-groups.md).</span></span>

<span data-ttu-id="cb4ef-154">Automatyczne Ponowne przyłączanie grup ma zastosowanie tylko wtedy, gdy połączenie jest ponownie nawiązywane po tymczasowym przerwie.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-154">Automatically rejoining groups only applies when a connection is reconnected after a temporary disruption.</span></span> <span data-ttu-id="cb4ef-155">Jeśli użytkownik odłączy się przez przejście do aplikacji lub ponowne uruchomienie aplikacji, aplikacja musi obsłużyć procedurę dodawania tego użytkownika do odpowiednich grup.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-155">If a user disconnects by navigating away from the application or the application restarts, your application must handle how to add that user to the correct groups.</span></span> <span data-ttu-id="cb4ef-156">Aby uzyskać więcej informacji, zobacz [Praca z grupami](../guide-to-the-api/working-with-groups.md).</span><span class="sxs-lookup"><span data-stu-id="cb4ef-156">For more information, see [Working with groups](../guide-to-the-api/working-with-groups.md).</span></span>

<a id="csrf"></a>

## <a name="how-signalr-prevents-cross-site-request-forgery"></a><span data-ttu-id="cb4ef-157">Jak sygnalizujący zapobiega podrabianiu żądań między lokacjami</span><span class="sxs-lookup"><span data-stu-id="cb4ef-157">How SignalR prevents Cross-Site Request Forgery</span></span>

<span data-ttu-id="cb4ef-158">Fałszerstwo żądania między lokacjami (CSRF) to atak polegający na tym, że złośliwa witryna wysyła żądanie do zagrożonej lokacji, w której użytkownik jest aktualnie zalogowany.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-158">Cross-Site Request Forgery (CSRF) is an attack where a malicious site sends a request to a vulnerable site where the user is currently logged in.</span></span> <span data-ttu-id="cb4ef-159">Program sygnalizujący uniemożliwia CSRF, uniemożliwiając złośliwej lokacji utworzenie prawidłowego żądania dla aplikacji sygnalizującej.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-159">SignalR prevents CSRF by making it extremely unlikely for a malicious site to create a valid request for your SignalR application.</span></span>

### <a name="description-of-csrf-attack"></a><span data-ttu-id="cb4ef-160">Opis ataku CSRF</span><span class="sxs-lookup"><span data-stu-id="cb4ef-160">Description of CSRF attack</span></span>

<span data-ttu-id="cb4ef-161">Oto przykład ataku CSRF:</span><span class="sxs-lookup"><span data-stu-id="cb4ef-161">Here is an example of a CSRF attack:</span></span>

1. <span data-ttu-id="cb4ef-162">Użytkownik loguje się do `www.example.com`przy użyciu uwierzytelniania formularzy.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-162">A user logs into `www.example.com`, using forms authentication.</span></span>
2. <span data-ttu-id="cb4ef-163">Serwer uwierzytelnia użytkownika.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-163">The server authenticates the user.</span></span> <span data-ttu-id="cb4ef-164">Odpowiedź z serwera zawiera plik cookie uwierzytelniania.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-164">The response from the server includes an authentication cookie.</span></span>
3. <span data-ttu-id="cb4ef-165">Bez wylogowywania użytkownik odwiedza złośliwą witrynę sieci Web.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-165">Without logging out, the user visits a malicious web site.</span></span> <span data-ttu-id="cb4ef-166">Ta złośliwa witryna zawiera następującą postać HTML:</span><span class="sxs-lookup"><span data-stu-id="cb4ef-166">This malicious site contains the following HTML form:</span></span> 

    [!code-html[Main](introduction-to-security/samples/sample1.html)]

   <span data-ttu-id="cb4ef-167">Należy zauważyć, że w ramach akcji formularza są umieszczane wpisy w zagrożonej witrynie, a nie w złośliwej witrynie.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-167">Notice that the form action posts to the vulnerable site, not to the malicious site.</span></span> <span data-ttu-id="cb4ef-168">To jest część "wiele witryn" z CSRF.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-168">This is the "cross-site" part of CSRF.</span></span>
4. <span data-ttu-id="cb4ef-169">Użytkownik klika przycisk Prześlij.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-169">The user clicks the submit button.</span></span> <span data-ttu-id="cb4ef-170">Przeglądarka zawiera plik cookie uwierzytelniania z żądaniem.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-170">The browser includes the authentication cookie with the request.</span></span>
5. <span data-ttu-id="cb4ef-171">Żądanie jest uruchamiane na serwerze example.com z kontekstem uwierzytelniania użytkownika i może wykonywać wszystkie czynności, które mogą wykonać uwierzytelnionego użytkownika.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-171">The request runs on the example.com server with the user's authentication context, and can do anything that an authenticated user is allowed to do.</span></span>

<span data-ttu-id="cb4ef-172">Mimo że ten przykład wymaga, aby użytkownik klikał przycisk formularza, złośliwa strona może jak równie łatwo uruchomić skrypt wysyłający żądanie AJAX do aplikacji sygnalizującej.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-172">Although this example requires the user to click the form button, the malicious page could just as easily run a script that sends an AJAX request to your SignalR application.</span></span> <span data-ttu-id="cb4ef-173">Ponadto użycie protokołu SSL nie uniemożliwia ataku CSRF, ponieważ złośliwa witryna może wysłać żądanie "https://".</span><span class="sxs-lookup"><span data-stu-id="cb4ef-173">Moreover, using SSL does not prevent a CSRF attack, because the malicious site can send an "https://" request.</span></span>

<span data-ttu-id="cb4ef-174">Zazwyczaj ataki CSRF są dostępne dla witryn sieci Web, które używają plików cookie do uwierzytelniania, ponieważ przeglądarki wysyłają wszystkie odpowiednie pliki cookie do docelowej witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-174">Typically, CSRF attacks are possible against web sites that use cookies for authentication, because browsers send all relevant cookies to the destination web site.</span></span> <span data-ttu-id="cb4ef-175">Jednak ataki CSRF nie ograniczają się do korzystania z plików cookie.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-175">However, CSRF attacks are not limited to exploiting cookies.</span></span> <span data-ttu-id="cb4ef-176">Na przykład uwierzytelnianie podstawowe i szyfrowane jest również zagrożone.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-176">For example, Basic and Digest authentication are also vulnerable.</span></span> <span data-ttu-id="cb4ef-177">Gdy użytkownik zaloguje się przy użyciu uwierzytelniania podstawowego lub szyfrowanego, przeglądarka automatycznie wyśle poświadczenia do momentu zakończenia sesji.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-177">After a user logs in with Basic or Digest authentication, the browser automatically sends the credentials until the session ends.</span></span>

### <a name="csrf-mitigations-taken-by-signalr"></a><span data-ttu-id="cb4ef-178">CSRF środki zaradcze wykonywane przez sygnalizującego</span><span class="sxs-lookup"><span data-stu-id="cb4ef-178">CSRF mitigations taken by SignalR</span></span>

<span data-ttu-id="cb4ef-179">Program sygnalizujący wykonuje poniższe kroki, aby uniemożliwić złośliwym lokacjom Tworzenie prawidłowych żądań do aplikacji sygnalizującej.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-179">SignalR takes the following steps to prevent a malicious site from creating valid requests to your SignalR application.</span></span> <span data-ttu-id="cb4ef-180">Te kroki są domyślnie podejmowane i nie wymagają żadnych akcji w kodzie.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-180">These steps are taken by default and do not require any action in your code.</span></span>

- <span data-ttu-id="cb4ef-181">**Wyłącz żądania między domenami**</span><span class="sxs-lookup"><span data-stu-id="cb4ef-181">**Disable cross domain requests**</span></span>  
 <span data-ttu-id="cb4ef-182">Domyślnie żądania między domenami są wyłączone w aplikacji sygnalizującej, aby uniemożliwić użytkownikom wywoływanie punktu końcowego sygnalizującego z domeny zewnętrznej.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-182">By default, cross domain requests are disabled in a SignalR application to prevent users from calling a SignalR endpoint from an external domain.</span></span> <span data-ttu-id="cb4ef-183">Wszystkie żądania, które pochodzą z domeny zewnętrznej, są automatycznie traktowane jako nieprawidłowe i zablokowane.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-183">Any request that comes from an external domain is automatically considered invalid and is blocked.</span></span> <span data-ttu-id="cb4ef-184">Zalecane jest zachowanie tego zachowania domyślnego; w przeciwnym razie złośliwa witryna może dowolnych użytkowników do wysyłania poleceń do witryny.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-184">It is recommended that you keep this default behavior; otherwise, a malicious site could trick users into sending commands to your site.</span></span> <span data-ttu-id="cb4ef-185">Jeśli konieczne jest użycie żądań między domenami, zobacz [jak ustanowić połączenie między domenami](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain) .</span><span class="sxs-lookup"><span data-stu-id="cb4ef-185">If you need to use cross domain requests, see     [How to establish a cross-domain connection](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain) .</span></span>
- <span data-ttu-id="cb4ef-186">**Przekaż token połączenia w ciągu zapytania, a nie w pliku cookie**</span><span class="sxs-lookup"><span data-stu-id="cb4ef-186">**Pass connection token in query string, not cookie**</span></span>  
 <span data-ttu-id="cb4ef-187">Sygnał przekazuje token połączenia jako wartość ciągu zapytania, a nie jako plik cookie.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-187">SignalR passes the connection token as a query string value, instead of as a cookie.</span></span> <span data-ttu-id="cb4ef-188">W przypadku braku przechowywania tokenu połączenia jako pliku cookie token połączenia nie jest przypadkowo przekazywany przez przeglądarkę w przypadku napotkania złośliwego kodu.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-188">By not storing the connection token as a cookie, the connection token is not inadvertently forwarded by the browser when malicious code is encountered.</span></span> <span data-ttu-id="cb4ef-189">Ponadto token połączenia nie jest utrwalany poza bieżącym połączeniem.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-189">Also, the connection token is not persisted beyond the current connection.</span></span> <span data-ttu-id="cb4ef-190">W związku z tym złośliwy użytkownik nie może wykonać żądania w ramach poświadczeń uwierzytelniania innego użytkownika.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-190">Therefore, a malicious user cannot make a request under another user's authentication credentials.</span></span>
- <span data-ttu-id="cb4ef-191">**Weryfikuj token połączenia**</span><span class="sxs-lookup"><span data-stu-id="cb4ef-191">**Verify connection token**</span></span>  
 <span data-ttu-id="cb4ef-192">Zgodnie z opisem w sekcji [token połączenia](#connectiontoken) serwer wie, który identyfikator połączenia jest skojarzony z każdym uwierzytelnionym użytkownikiem.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-192">As described in the [Connection token](#connectiontoken) section, the server knows which connection id is associated with each authenticated user.</span></span> <span data-ttu-id="cb4ef-193">Serwer nie przetwarza żadnych żądań z identyfikatora połączenia, który nie jest zgodny z nazwą użytkownika.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-193">The server does not process any request from a connection id that does not match the user name.</span></span> <span data-ttu-id="cb4ef-194">Jest mało prawdopodobne, że złośliwy użytkownik może odgadnąć prawidłowe żądanie, ponieważ złośliwy użytkownik musi znać nazwę użytkownika i bieżący wygenerowany losowo identyfikator połączenia. Ten identyfikator połączenia będzie nieprawidłowy, gdy tylko zakończy się połączenie.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-194">It is unlikely a malicious user could guess a valid request because the malicious user would have to know the user name and the current randomly-generated connection id. That connection id becomes invalid as soon as the connection is ended.</span></span> <span data-ttu-id="cb4ef-195">Użytkownicy anonimowi nie powinni mieć dostępu do żadnych poufnych informacji.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-195">Anonymous users should not have access to any sensitive information.</span></span>

<a id="recommendations"></a>

## <a name="signalr-security-recommendations"></a><span data-ttu-id="cb4ef-196">Zalecenia dotyczące zabezpieczeń sygnałów</span><span class="sxs-lookup"><span data-stu-id="cb4ef-196">SignalR Security Recommendations</span></span>

<a id="ssl"></a>

### <a name="secure-socket-layers-ssl-protocol"></a><span data-ttu-id="cb4ef-197">Protokół SSL (Secure Sockets)</span><span class="sxs-lookup"><span data-stu-id="cb4ef-197">Secure Socket Layers (SSL) protocol</span></span>

<span data-ttu-id="cb4ef-198">Protokół SSL używa szyfrowania w celu zabezpieczenia transportu danych między klientem i serwerem.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-198">The SSL protocol uses encryption to secure the transport of data between a client and server.</span></span> <span data-ttu-id="cb4ef-199">Jeśli aplikacja sygnalizująca przesyła poufne informacje między klientem a serwerem, należy użyć protokołu SSL do transportu.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-199">If your SignalR application transmits sensitive information between the client and server, use SSL for the transport.</span></span> <span data-ttu-id="cb4ef-200">Aby uzyskać więcej informacji o konfigurowaniu protokołu SSL, zobacz [jak skonfigurować protokół SSL w usługach IIS 7](https://www.iis.net/learn/manage/configuring-security/how-to-set-up-ssl-on-iis).</span><span class="sxs-lookup"><span data-stu-id="cb4ef-200">For more information about setting up SSL, see [How to set up SSL on IIS 7](https://www.iis.net/learn/manage/configuring-security/how-to-set-up-ssl-on-iis).</span></span>

<a id="groupsecurity"></a>

### <a name="do-not-use-groups-as-a-security-mechanism"></a><span data-ttu-id="cb4ef-201">Nie używaj grup jako mechanizmu zabezpieczeń</span><span class="sxs-lookup"><span data-stu-id="cb4ef-201">Do not use groups as a security mechanism</span></span>

<span data-ttu-id="cb4ef-202">Grupy to wygodny sposób zbierania powiązanych użytkowników, ale nie są bezpiecznym mechanizmem ograniczania dostępu do poufnych informacji.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-202">Groups are a convenient way of collecting related users, but they are not a secure mechanism for limiting access to sensitive information.</span></span> <span data-ttu-id="cb4ef-203">Jest to szczególnie prawdziwe, gdy użytkownicy mogą automatycznie odłączać grupy podczas ponownego nawiązywania połączenia.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-203">This is especially true when users can automatically rejoin groups during a reconnect.</span></span> <span data-ttu-id="cb4ef-204">Zamiast tego należy rozważyć dodanie uprzywilejowanych użytkowników do roli i ograniczenie dostępu do metody centrum tylko do członków tej roli.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-204">Instead, consider adding privileged users to a role and limiting access to a hub method to only members of that role.</span></span> <span data-ttu-id="cb4ef-205">Aby uzyskać przykład ograniczenia dostępu na podstawie roli, zobacz [uwierzytelnianie i autoryzacja dla centrów sygnałów](../security/hub-authorization.md).</span><span class="sxs-lookup"><span data-stu-id="cb4ef-205">For an example of restricting access based on a role, see [Authentication and Authorization for SignalR Hubs](../security/hub-authorization.md).</span></span> <span data-ttu-id="cb4ef-206">Aby zapoznać się z przykładem sprawdzania dostępu użytkowników do grup podczas ponownego nawiązywania połączenia, zobacz [Praca z grupami](../guide-to-the-api/working-with-groups.md).</span><span class="sxs-lookup"><span data-stu-id="cb4ef-206">For an example of checking user access to groups when reconnecting, see [Working with groups](../guide-to-the-api/working-with-groups.md).</span></span>

<a id="input"></a>

### <a name="safely-handling-input-from-clients"></a><span data-ttu-id="cb4ef-207">Bezpieczne obsługiwanie danych wejściowych z klientów</span><span class="sxs-lookup"><span data-stu-id="cb4ef-207">Safely handling input from clients</span></span>

<span data-ttu-id="cb4ef-208">Wszystkie dane wejściowe z klientów, które są przeznaczone do emisji do innych klientów, muszą zostać zakodowane w celu zapewnienia, że złośliwy użytkownik nie wyśle skryptu do innych użytkowników.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-208">All input from clients that is intended for broadcast to other clients must be encoded to ensure that a malicious user does not send script to other users.</span></span> <span data-ttu-id="cb4ef-209">Najlepszym rozwiązaniem jest kodowanie komunikatów na klientach otrzymujących zamiast na serwerze, ponieważ aplikacja sygnalizująca może mieć wiele różnych typów klientów.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-209">It is best to encode messages on the receiving clients rather than the server, because your SignalR application may have many different types of clients.</span></span> <span data-ttu-id="cb4ef-210">W związku z tym kodowanie HTML działa dla klienta sieci Web, ale nie dla innych typów klientów.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-210">Therefore, HTML-encoding works for a web client, but not for other types of clients.</span></span> <span data-ttu-id="cb4ef-211">Na przykład Metoda klienta sieci Web, aby wyświetlić komunikat rozmowy, bezpiecznie obsłużyć nazwę użytkownika i komunikat, wywołując funkcję `html()`.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-211">For example, a web client method to display a chat message would safely handle the user name and message by calling the `html()` function.</span></span>

[!code-html[Main](introduction-to-security/samples/sample2.html?highlight=3-4)]

<a id="reconcile"></a>

### <a name="reconciling-a-change-in-user-status-with-an-active-connection"></a><span data-ttu-id="cb4ef-212">Uzgadnianie zmiany stanu użytkownika z aktywnym połączeniem</span><span class="sxs-lookup"><span data-stu-id="cb4ef-212">Reconciling a change in user status with an active connection</span></span>

<span data-ttu-id="cb4ef-213">Jeśli stan uwierzytelnienia użytkownika zostanie zmieniony, gdy istnieje aktywne połączenie, użytkownik otrzyma komunikat o błędzie z informacją "tożsamość użytkownika nie może ulec zmianie podczas aktywnego połączenia sygnalizującego".</span><span class="sxs-lookup"><span data-stu-id="cb4ef-213">If a user's authentication status changes while an active connection exists, the user will receive an error that states, "The user identity cannot change during an active SignalR connection."</span></span> <span data-ttu-id="cb4ef-214">W takim przypadku aplikacja powinna ponownie połączyć się z serwerem, aby upewnić się, że identyfikator połączenia i nazwa użytkownika są skoordynowane.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-214">In that case, your application should re-connect to the server to make sure the connection id and username are coordinated.</span></span> <span data-ttu-id="cb4ef-215">Na przykład jeśli aplikacja zezwala użytkownikowi na wylogowanie się, gdy istnieje aktywne połączenie, nazwa użytkownika dla połączenia nie będzie już zgodna z nazwą przekazaną dla następnego żądania.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-215">For example, if your application allows the user to log out while an active connection exists, the username for the connection will no longer match the name that is passed in for the next request.</span></span> <span data-ttu-id="cb4ef-216">Należy zatrzymać połączenie przed wylogowaniem użytkownika, a następnie uruchomić go ponownie.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-216">You will want to stop the connection before the user logs out, and then restart it.</span></span>

<span data-ttu-id="cb4ef-217">Należy jednak pamiętać, że większość aplikacji nie będzie musiała ręcznie zatrzymać i uruchomić połączenia.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-217">However, it is important to note that most applications will not need to manually stop and start the connection.</span></span> <span data-ttu-id="cb4ef-218">Jeśli aplikacja przekierowuje użytkowników do osobnej strony po wylogowaniu, takiej jak domyślne zachowanie w aplikacji formularzy sieci Web lub aplikacji MVC, lub odświeża bieżącą stronę po wylogowaniu, aktywne połączenie zostanie automatycznie rozłączone i nie Wymagaj wszelkich dodatkowych akcji.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-218">If your application redirects users to a separate page after logging out, such as the default behavior in a Web Forms application or MVC application, or refreshes the current page after logging out, the active connection is automatically disconnected and does not require any additional action.</span></span>

<span data-ttu-id="cb4ef-219">Poniższy przykład pokazuje, jak zatrzymać i uruchomić połączenie, gdy stan użytkownika został zmieniony.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-219">The following example shows how to stop and start a connection when the user status has changed.</span></span>

[!code-html[Main](introduction-to-security/samples/sample3.html)]

<span data-ttu-id="cb4ef-220">Można również zmienić stan uwierzytelniania użytkownika, jeśli w lokacji jest używane okresowe wygaśnięcie z uwierzytelnianiem formularzy i nie ma aktywności, aby zachować prawidłowy plik cookie uwierzytelniania.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-220">Or, the user's authentication status may change if your site uses sliding expiration with Forms Authentication, and there is no activity to keep the authentication cookie valid.</span></span> <span data-ttu-id="cb4ef-221">W takim przypadku użytkownik zostanie wylogowany, a nazwa użytkownika nie będzie już zgodna z nazwą użytkownika w tokenie połączenia.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-221">In that case, the user will be logged out and the user name will no longer match the user name in the connection token.</span></span> <span data-ttu-id="cb4ef-222">Ten problem można rozwiązać, dodając jakiś skrypt, który okresowo żąda zasobu na serwerze sieci Web, aby zachować prawidłowy plik cookie uwierzytelniania.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-222">You can fix this problem by adding some script that periodically requests a resource on the web server to keep the authentication cookie valid.</span></span> <span data-ttu-id="cb4ef-223">Poniższy przykład pokazuje, jak zażądać zasobu co 30 minut.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-223">The following example shows how to request a resource every 30 minutes.</span></span>

[!code-javascript[Main](introduction-to-security/samples/sample4.js)]

<a id="autogen"></a>

### <a name="automatically-generated-javascript-proxy-files"></a><span data-ttu-id="cb4ef-224">Automatycznie generowane pliki proxy JavaScript</span><span class="sxs-lookup"><span data-stu-id="cb4ef-224">Automatically generated JavaScript proxy files</span></span>

<span data-ttu-id="cb4ef-225">Jeśli nie chcesz uwzględniać wszystkich centrów i metod w pliku proxy JavaScript dla każdego użytkownika, możesz wyłączyć automatyczne generowanie pliku.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-225">If you do not want to include all of the hubs and methods in the JavaScript proxy file for each user, you can disable the automatic generation of the file.</span></span> <span data-ttu-id="cb4ef-226">Możesz wybrać tę opcję, jeśli masz wiele centrów i metod, ale nie chcesz, aby każdy użytkownik miał świadomość wszystkich metod.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-226">You might choose this option if you have multiple hubs and methods, but do not want every user to be aware of all of the methods.</span></span> <span data-ttu-id="cb4ef-227">Aby wyłączyć generowanie automatyczne, należy ustawić **EnableJavaScriptProxies** na **false**.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-227">You disable automatic generation by setting **EnableJavaScriptProxies** to **false**.</span></span>

[!code-csharp[Main](introduction-to-security/samples/sample5.cs)]

<span data-ttu-id="cb4ef-228">Aby uzyskać więcej informacji na temat plików serwera proxy języka JavaScript, zobacz [wygenerowany serwer proxy i jego zawartość](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy).</span><span class="sxs-lookup"><span data-stu-id="cb4ef-228">For more information about the JavaScript proxy files, see [The generated proxy and what it does for you](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy).</span></span> <a id="exceptions"></a>

### <a name="exceptions"></a><span data-ttu-id="cb4ef-229">Wyjątki</span><span class="sxs-lookup"><span data-stu-id="cb4ef-229">Exceptions</span></span>

<span data-ttu-id="cb4ef-230">Należy unikać przekazywania obiektów wyjątków do klientów, ponieważ obiekty mogą uwidaczniać klientom poufne informacje.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-230">You should avoid passing exception objects to clients because the objects may expose sensitive information to the clients.</span></span> <span data-ttu-id="cb4ef-231">Zamiast tego należy wywołać metodę na kliencie, który wyświetla odpowiedni komunikat o błędzie.</span><span class="sxs-lookup"><span data-stu-id="cb4ef-231">Instead, call a method on the client that displays the relevant error message.</span></span>

[!code-csharp[Main](introduction-to-security/samples/sample6.cs)]
