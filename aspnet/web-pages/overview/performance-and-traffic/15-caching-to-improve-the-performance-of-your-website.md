---
uid: web-pages/overview/performance-and-traffic/15-caching-to-improve-the-performance-of-your-website
title: Buforowanie danych we wzorcu ASP.NET Web Pages lokacji (Razor) zapewnienia lepszej wydajności | Dokumentacja firmy Microsoft
author: Rick-Anderson
description: Można przyspieszyć witryny sieci Web przez on przechowywać — czyli pamięci podręcznej — wyniki danych, który normalnie zajęłoby znaczną ilość czasu pobierania lub przetwarzać...
ms.author: riande
ms.date: 02/14/2014
ms.assetid: 961e525b-7700-469e-8a68-d7010b6fb68c
msc.legacyurl: /web-pages/overview/performance-and-traffic/15-caching-to-improve-the-performance-of-your-website
msc.type: authoredcontent
ms.openlocfilehash: ede341e02869a9c81cbe2fa7ef97345dc87519a1
ms.sourcegitcommit: 24b1f6decbb17bb22a45166e5fdb0845c65af498
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/01/2019
ms.locfileid: "57068426"
---
<a name="caching-data-in-an-aspnet-web-pages-razor-site-for-better-performance"></a><span data-ttu-id="c805c-103">Buforowanie danych w witrynie ASP.NET Web Pages (Razor) w celu zapewnienia lepszej wydajności</span><span class="sxs-lookup"><span data-stu-id="c805c-103">Caching Data in an ASP.NET Web Pages (Razor) Site for Better Performance</span></span>
====================
<span data-ttu-id="c805c-104">przez [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="c805c-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="c805c-105">W tym artykule wyjaśniono, jak użyć pomocnika do informacji o pamięci podręcznej, aby zwiększyć wydajność w witrynie internetowej ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="c805c-105">This article explains how to use a helper to cache information for faster performance in an ASP.NET Web Pages (Razor) website.</span></span> <span data-ttu-id="c805c-106">Można przyspieszyć witryny sieci Web, konfigurując go przechowywać &#8212; oznacza to, że w pamięci podręcznej &#8212; wyników danych, który normalnie zajęłoby znaczną ilość czasu, można pobrać lub przetwarzania, a która nie zmienia się często.</span><span class="sxs-lookup"><span data-stu-id="c805c-106">You can speed up your website by having it store &#8212; that is, cache &#8212; the results of data that ordinarily would take considerable time to retrieve or process and that does not change often.</span></span>
> 
> <span data-ttu-id="c805c-107">**Zawartość:**</span><span class="sxs-lookup"><span data-stu-id="c805c-107">**What you'll learn:**</span></span> 
> 
> - <span data-ttu-id="c805c-108">Jak używać buforowania, można zwiększyć szybkość reakcji witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="c805c-108">How to use caching to improve the responsiveness of your website.</span></span>
> 
> <span data-ttu-id="c805c-109">Poniżej przedstawiono funkcje platformy ASP.NET, wprowadzona w artykule:</span><span class="sxs-lookup"><span data-stu-id="c805c-109">These are the ASP.NET features introduced in the article:</span></span>
> 
> - <span data-ttu-id="c805c-110">`WebCache` Pomocnika.</span><span class="sxs-lookup"><span data-stu-id="c805c-110">The `WebCache` helper.</span></span>
>   
> 
> ## <a name="software-versions-used-in-the-tutorial"></a><span data-ttu-id="c805c-111">Wersje oprogramowania używanego w tym samouczku</span><span class="sxs-lookup"><span data-stu-id="c805c-111">Software versions used in the tutorial</span></span>
> 
> 
> - <span data-ttu-id="c805c-112">ASP.NET Web Pages (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="c805c-112">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="c805c-113">W tym samouczku współpracuje również z wzorca ASP.NET Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="c805c-113">This tutorial also works with ASP.NET Web Pages 2.</span></span>


<span data-ttu-id="c805c-114">Za każdym razem, gdy ktoś zgłasza żądanie strony z lokacji, serwer sieci web ma wykonania dodatkowych czynności w celu spełnienia żądania.</span><span class="sxs-lookup"><span data-stu-id="c805c-114">Every time someone requests a page from your site, the web server has to do some work in order to fulfill the request.</span></span> <span data-ttu-id="c805c-115">W przypadku niektórych stron serwer może być konieczne przeprowadzenie zadań, które długo (stosunkowo), takie jak pobieranie danych z bazy danych.</span><span class="sxs-lookup"><span data-stu-id="c805c-115">For some of your pages, the server might have to perform tasks that take a (comparatively) long time, such as retrieving data from a database.</span></span> <span data-ttu-id="c805c-116">Nawet jeśli te zadania nie wykona długo w liczbach bezwzględnych, jeśli wiele ruchu witryny, całego szeregu poszczególnych żądań, które powodują serwer sieci web do wykonania zadania skomplikowany lub powolne możliwe jest dodanie do sporego nakładu pracy.</span><span class="sxs-lookup"><span data-stu-id="c805c-116">Even if these tasks don't take long in absolute terms, if your site experiences a lot of traffic, a whole series of individual requests that cause the web server to perform the complicated or slow task can add up to a lot of work.</span></span> <span data-ttu-id="c805c-117">Ostatecznie może to wpłynąć na wydajność lokacji.</span><span class="sxs-lookup"><span data-stu-id="c805c-117">This can ultimately affect the performance of the site.</span></span>

<span data-ttu-id="c805c-118">Jednym ze sposobów, aby zwiększyć wydajność witryny sieci Web w sytuacjach, takich jak to jest danych w pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="c805c-118">One way to improve the performance of your website in circumstances like this is to cache data.</span></span> <span data-ttu-id="c805c-119">Jeśli lokacji pobiera ponownych żądań tych samych informacji i informacji nie musi zostać zmodyfikowany dla każdej osoby, a nie jest to czas uwzględniana wielkość liter, zamiast ponownie podczas pobierania lub ponowne obliczanie, możesz pobrać dane jeden raz i następnie zapisać wyniki.</span><span class="sxs-lookup"><span data-stu-id="c805c-119">If your site gets repeated requests for the same information, and the information does not need to be modified for each person, and it's not time sensitive, instead of re-fetching or recalculating it, you can fetch the data once and then store the results.</span></span> <span data-ttu-id="c805c-120">Przy następnym dotrze żądanie, tym informacji, po prostu jego uzyskaniu z pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="c805c-120">The next time a request comes in for that information, you just get it out of the cache.</span></span>

<span data-ttu-id="c805c-121">Ogólnie rzecz biorąc należy w pamięci podręcznej informacje, które nie zmieniają się często.</span><span class="sxs-lookup"><span data-stu-id="c805c-121">In general, you cache information that doesn't change frequently.</span></span> <span data-ttu-id="c805c-122">Kiedy wprowadzasz informacje w pamięci podręcznej znajduje się w pamięci na serwerze sieci web.</span><span class="sxs-lookup"><span data-stu-id="c805c-122">When you put information in the cache, it's stored in memory on the web server.</span></span> <span data-ttu-id="c805c-123">Można określić, jak długo go powinno być buforowane, od sekund do dni.</span><span class="sxs-lookup"><span data-stu-id="c805c-123">You can specify how long it should be cached, from seconds to days.</span></span> <span data-ttu-id="c805c-124">Po wygaśnięciu okresu buforowania informacji zostanie automatycznie usunięty z pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="c805c-124">When the caching period expires, the information is automatically removed from the cache.</span></span>

> [!NOTE]
> <span data-ttu-id="c805c-125">Wpisy w pamięci podręcznej mogą zostać usunięte powodów innych niż, po wygaśnięciu.</span><span class="sxs-lookup"><span data-stu-id="c805c-125">Entries in the cache might be removed for reasons other than that they've expired.</span></span> <span data-ttu-id="c805c-126">Na przykład serwer sieci web może być tymczasowo brakować pamięci i jednym ze sposobów jej odzyskania pamięci jest zgłaszanie wpisów z pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="c805c-126">For example, the web server might temporarily run low on memory, and one way it can reclaim memory is by throwing entries out of the cache.</span></span> <span data-ttu-id="c805c-127">Jak można zauważyć, nawet jeśli została utworzona informacje w pamięci podręcznej, masz upewnij się, że jest on jeszcze gdy ich potrzebujesz.</span><span class="sxs-lookup"><span data-stu-id="c805c-127">As you'll see, even if you've put information into the cache, you have to check to be sure it's still there when you need it.</span></span>


<span data-ttu-id="c805c-128">Załóżmy, że witryna ma stronę, która zawiera aktualną temperaturę i prognozy pogody.</span><span class="sxs-lookup"><span data-stu-id="c805c-128">Imagine your website has a page that displays the current temperature and weather forecast.</span></span> <span data-ttu-id="c805c-129">Aby uzyskać informacje tego typu, może wysyłać żądania do usługi zewnętrznej.</span><span class="sxs-lookup"><span data-stu-id="c805c-129">To get this type of information, you might send a request to an external service.</span></span> <span data-ttu-id="c805c-130">Ponieważ te informacje nie zmienia się wiele (w ciągu dwóch godzin okresu, na przykład), a ponieważ połączeniami zewnętrznymi wymaga czasu i przepustowość, jest dobrym kandydatem do buforowania.</span><span class="sxs-lookup"><span data-stu-id="c805c-130">Since this information doesn't change much (within a two-hour time period, for example) and since external calls require time and bandwidth, it's a good candidate for caching.</span></span>

## <a name="adding-caching-to-a-page"></a><span data-ttu-id="c805c-131">Dodanie do strony</span><span class="sxs-lookup"><span data-stu-id="c805c-131">Adding Caching to a Page</span></span>

<span data-ttu-id="c805c-132">Program ASP.NET zawiera `WebCache` pomocnika, która ułatwia dodawanie funkcji buforowania do swojej witryny i dodać dane do pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="c805c-132">ASP.NET includes a `WebCache` helper that makes it easy to add caching to your site and add data to the cache.</span></span> <span data-ttu-id="c805c-133">W tej procedurze utworzysz strona, która zapisuje bieżący czas w pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="c805c-133">In this procedure, you'll create a page that caches the current time.</span></span> <span data-ttu-id="c805c-134">Jest to przykład rzeczywistych, ponieważ bieżąca godzina to coś, który zmienia się często, a ponadto nie jest to skomplikowane obliczenia.</span><span class="sxs-lookup"><span data-stu-id="c805c-134">This isn't a real-world example, since the current time is something that does change often, and that moreover isn't complex to calculate.</span></span> <span data-ttu-id="c805c-135">Jednak jest dobrym sposobem ilustrowania buforowania.</span><span class="sxs-lookup"><span data-stu-id="c805c-135">However, it's a good way to illustrate caching in action.</span></span>

1. <span data-ttu-id="c805c-136">Dodaj nową stronę o nazwie *WebCache.cshtml* do witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="c805c-136">Add a new page named *WebCache.cshtml* to the website.</span></span>
2. <span data-ttu-id="c805c-137">Na stronie, Dodaj następujący kod i znaczników:</span><span class="sxs-lookup"><span data-stu-id="c805c-137">Add the following code and markup to the page:</span></span>

    [!code-cshtml[Main](15-caching-to-improve-the-performance-of-your-website/samples/sample1.cshtml)]

    <span data-ttu-id="c805c-138">Po użytkownik dane z pamięci podręcznej, został on umieszczony w pamięci podręcznej przy użyciu nazwy, to jest unikatowa w obrębie witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="c805c-138">When you cache data, you put it into the cache using a name this is unique across the website.</span></span> <span data-ttu-id="c805c-139">W tym przypadku użyjemy wpis pamięci podręcznej o nazwie `CachedTime`.</span><span class="sxs-lookup"><span data-stu-id="c805c-139">In this case, you'll use a cache entry named `CachedTime`.</span></span> <span data-ttu-id="c805c-140">Jest to `cacheItemKey` pokazano w przykładzie kodu.</span><span class="sxs-lookup"><span data-stu-id="c805c-140">This is the `cacheItemKey` shown in the code example.</span></span>

    <span data-ttu-id="c805c-141">Kod najpierw odczytuje `CachedTime` wpisu pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="c805c-141">The code first reads the `CachedTime` cache entry.</span></span> <span data-ttu-id="c805c-142">Jeśli wartość jest zwracana (to znaczy, jeśli wpis pamięci podręcznej nie jest null), kod po prostu ustawia wartość zmiennej czasu buforowania danych.</span><span class="sxs-lookup"><span data-stu-id="c805c-142">If a value is returned (that is, if the cache entry isn't null), the code just sets the value of the time variable to the cache data.</span></span>

    <span data-ttu-id="c805c-143">Jednak jeśli nie istnieje wpis pamięci podręcznej (czyli jest wartość null), ten kod ustawia wartość czasu, dodaje ją do pamięci podręcznej i ustawia wartość dla wygaśnięcia na jedną minutę.</span><span class="sxs-lookup"><span data-stu-id="c805c-143">However, if the cache entry doesn't exist (that is, it's null), the code sets the time value, adds it to the cache, and sets an expiration value to one minute.</span></span> <span data-ttu-id="c805c-144">Po jednej minucie wpisu pamięci podręcznej jest odrzucany.</span><span class="sxs-lookup"><span data-stu-id="c805c-144">After one minute, the cache entry is discarded.</span></span> <span data-ttu-id="c805c-145">(Wartość domyślna wygaśnięcie elementu w pamięci podręcznej jest 20 minut). Polecenie `WebCache.Set(cacheItemKey, time, 1, false)` pokazuje, jak dodać wartość bieżącej godziny do pamięci podręcznej i ustawić jej wygaśnięcia na 1 minutę.</span><span class="sxs-lookup"><span data-stu-id="c805c-145">(The default expiration value for an item in the cache is 20 minutes.) The command `WebCache.Set(cacheItemKey, time, 1, false)` shows how to add the current time value to the cache and set its expiration to 1 minute.</span></span> <span data-ttu-id="c805c-146">Ustawienie *slidingExpiration* parametr `false` oznacza, że czas wygaśnięcia nie zostanie odnowiony każdorazowo żądanie.</span><span class="sxs-lookup"><span data-stu-id="c805c-146">Setting the *slidingExpiration* parameter to `false` means the expiration time is not renewed each time it is requested.</span></span> <span data-ttu-id="c805c-147">Wygaśnięcia dokładnie 1 minuta po pierwotnie został dodany do pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="c805c-147">It will expire exactly 1 minute after it was originally added to the cache.</span></span> <span data-ttu-id="c805c-148">Jeśli ta wartość jest ustawiona na `true` każdego czas żądania wartość z pamięci podręcznej jest resetowany przy 1 minuta czasu wygaśnięcia.</span><span class="sxs-lookup"><span data-stu-id="c805c-148">If you set this value to `true` the 1 minute expiration time is reset each time the value is requested from the cache.</span></span>

    <span data-ttu-id="c805c-149">Ten kod ilustruje wzorzec, który należy zawsze używać podczas buforowania danych.</span><span class="sxs-lookup"><span data-stu-id="c805c-149">This code illustrates the pattern you should always use when you cache data.</span></span> <span data-ttu-id="c805c-150">Przed zagłębieniem się coś z pamięci podręcznej, zawsze najpierw sprawdzić czy `WebCache.Get` metoda zwróciła wartość null.</span><span class="sxs-lookup"><span data-stu-id="c805c-150">Before you get something out of the cache, always check first whether the `WebCache.Get` method has returned null.</span></span> <span data-ttu-id="c805c-151">Należy pamiętać, że wpis pamięci podręcznej wygasła lub mógł zostać usunięty innego powodu, aby dany wpis nigdy nie musi znajdować się w pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="c805c-151">Remember that the cache entry might have expired or might have been removed for some other reason, so any given entry is never guaranteed to be in the cache.</span></span>
3. <span data-ttu-id="c805c-152">Uruchom *WebCache.cshtml* w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="c805c-152">Run *WebCache.cshtml* in a browser.</span></span> <span data-ttu-id="c805c-153">(Upewnij się, że strona jest zaznaczona w **pliki** obszaru roboczego przed jej uruchomieniem.) Żądanie strony, po raz pierwszy danych czasowych nie znajduje się w pamięci podręcznej, a kod musi dodać wartości w czasie do pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="c805c-153">(Make sure the page is selected in the **Files** workspace before you run it.) The first time you request the page, the time data isn't in the cache, and the code has to add the time value to the cache.</span></span>

    ![pamięć podręczna-1](15-caching-to-improve-the-performance-of-your-website/_static/image1.jpg)
4. <span data-ttu-id="c805c-155">Odśwież *WebCache.cshtml* w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="c805c-155">Refresh *WebCache.cshtml* in the browser.</span></span> <span data-ttu-id="c805c-156">Tym razem danych czasowych jest w pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="c805c-156">This time, the time data is in the cache.</span></span> <span data-ttu-id="c805c-157">Należy zauważyć, że czas nie zmieniła się od czasu ostatniego przeglądać strony.</span><span class="sxs-lookup"><span data-stu-id="c805c-157">Notice that the time hasn't changed since the last time you viewed the page.</span></span>

    ![pamięć podręczna 2](15-caching-to-improve-the-performance-of-your-website/_static/image2.jpg)
5. <span data-ttu-id="c805c-159">Poczekaj jedną minutę na można opróżnić pamięć podręczną, a następnie odśwież stronę.</span><span class="sxs-lookup"><span data-stu-id="c805c-159">Wait one minute for the cache to be emptied, and then refresh the page.</span></span> <span data-ttu-id="c805c-160">Strona ponownie wskazuje, że danych czasowych nie został znaleziony w pamięci podręcznej i czas aktualizacji jest dodawany do pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="c805c-160">The page again indicates that the time data wasn't found in the cache, and the updated time is added to the cache.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="c805c-161">Dodatkowe zasoby</span><span class="sxs-lookup"><span data-stu-id="c805c-161">Additional Resources</span></span>


- [<span data-ttu-id="c805c-162">Wyświetlanie danych na wykresie</span><span class="sxs-lookup"><span data-stu-id="c805c-162">Displaying Data in a Chart</span></span>](https://go.microsoft.com/fwlink/?LinkId=202895)
- <span data-ttu-id="c805c-163">[Dokumentacja interfejsu API WebCache](https://msdn.microsoft.com/library/system.web.helpers.webcache(v=vs.99).aspx) (MSDN)</span><span class="sxs-lookup"><span data-stu-id="c805c-163">[WebCache API reference](https://msdn.microsoft.com/library/system.web.helpers.webcache(v=vs.99).aspx) (MSDN)</span></span>
