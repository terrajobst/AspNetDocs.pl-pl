---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: Wyświetlanie danych na wykresie przy użyciu stron ASP.NET Web Pages (Razor) | Dokumentacja firmy Microsoft
author: microsoft
description: W tym rozdziale wyjaśniono sposób wyświetlania danych na wykresie. W poprzednich akapitach pokazaliśmy ci, jak wyświetlać dane ręcznie, jak i w siatce. W tym rozdziale wyjaśniono...
ms.author: riande
ms.date: 05/22/2012
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: f97f214abeaeb88634dd10aaebacc0d58e91ab84
ms.sourcegitcommit: 0f1119340e4464720cfd16d0ff15764746ea1fea
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/17/2019
ms.locfileid: "59422463"
---
# <a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="4492a-105">Wyświetlanie danych na wykresie przy użyciu wzorca ASP.NET Web Pages (Razor)</span><span class="sxs-lookup"><span data-stu-id="4492a-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>

<span data-ttu-id="4492a-106">przez [firmy Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="4492a-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="4492a-107">W tym artykule wyjaśniono, jak używać wykresu do wyświetlania danych w witrynie internetowej ASP.NET Web Pages (Razor) przy użyciu `Chart` pomocnika.</span><span class="sxs-lookup"><span data-stu-id="4492a-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="4492a-108">**Dowiesz się**:</span><span class="sxs-lookup"><span data-stu-id="4492a-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="4492a-109">Jak wyświetlać dane na wykresie.</span><span class="sxs-lookup"><span data-stu-id="4492a-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="4492a-110">Jak do nadawania stylu wykresów przy użyciu wbudowanych motywów.</span><span class="sxs-lookup"><span data-stu-id="4492a-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="4492a-111">Jak zapisywać wykresy oraz jak je skonfigurować buforowanie lepszą wydajność.</span><span class="sxs-lookup"><span data-stu-id="4492a-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="4492a-112">Poniżej przedstawiono funkcje wprowadzone w artykule programowania programu ASP.NET:</span><span class="sxs-lookup"><span data-stu-id="4492a-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="4492a-113">`Chart` Pomocnika.</span><span class="sxs-lookup"><span data-stu-id="4492a-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="4492a-114">Informacje przedstawione w tym artykule dotyczy 1.0 stron sieci Web platformy ASP.NET i Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="4492a-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>


<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="4492a-115">Pomocnik wykresu</span><span class="sxs-lookup"><span data-stu-id="4492a-115">The Chart Helper</span></span>

<span data-ttu-id="4492a-116">Jeśli chcesz wyświetlać dane w formie graficznej, można użyć `Chart` pomocnika.</span><span class="sxs-lookup"><span data-stu-id="4492a-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="4492a-117">`Chart` Pomocnika można renderować obraz, który wyświetla dane w różnych typów wykresów.</span><span class="sxs-lookup"><span data-stu-id="4492a-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="4492a-118">Obsługuje ona wiele opcji formatowania i etykietowania.</span><span class="sxs-lookup"><span data-stu-id="4492a-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="4492a-119">`Chart` Pomocnika umożliwiający renderowanie ponad 30 typów wykresów, w tym wszystkich typów wykresów, które mogą być znane z programu Microsoft Excel lub innych narzędzi &#8212; wykresy warstwowe, wykresy słupkowe, wykresy kolumnowe, wykresów liniowych i wykresy kołowe, wraz z więcej Wykresy specjalne, takie jak podstawowe wykresy.</span><span class="sxs-lookup"><span data-stu-id="4492a-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="4492a-120">**Wykres warstwowy** ![opis: Obraz obszaru typ wykresu](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="4492a-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="4492a-121">**Wykres słupkowy** ![opis: Obraz typu wykresu słupkowego](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="4492a-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="4492a-122">**Wykres kolumnowy** ![opis: Obraz wykresu kolumnowego](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="4492a-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="4492a-123">**Wykres liniowy** ![opis: Obraz wiersza typ wykresu](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="4492a-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="4492a-124">**Wykres kołowy** ![opis: Obraz typu wykresu kołowego](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="4492a-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="4492a-125">**Wykres giełdowy** ![opis: Obraz akcji typ wykresu](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="4492a-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="4492a-126">Elementy wykresu</span><span class="sxs-lookup"><span data-stu-id="4492a-126">Chart Elements</span></span>

<span data-ttu-id="4492a-127">Wykresy pokazują dane i dodatkowe elementy, takie jak legendami, osiami, serii i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="4492a-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="4492a-128">Na poniższej ilustracji przedstawiono wiele elementów wykresu, które można dostosować, korzystając z `Chart` pomocnika.</span><span class="sxs-lookup"><span data-stu-id="4492a-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="4492a-129">W tym artykule pokazano, jak ustawić niektóre (nie wszystkie) z tych elementów.</span><span class="sxs-lookup"><span data-stu-id="4492a-129">This article shows you how to set some (not all) of these elements.</span></span>

![Opis: Obraz przedstawiający elementy wykresu](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="4492a-131">Tworzenie wykresu na podstawie danych</span><span class="sxs-lookup"><span data-stu-id="4492a-131">Creating a Chart from Data</span></span>

<span data-ttu-id="4492a-132">Dane, które są wyświetlane na wykresie może być z tablicy, na liście wyników zwrócony z bazy danych lub danych, który znajduje się w pliku XML.</span><span class="sxs-lookup"><span data-stu-id="4492a-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="4492a-133">Przy użyciu tablicy</span><span class="sxs-lookup"><span data-stu-id="4492a-133">Using an Array</span></span>

<span data-ttu-id="4492a-134">Jak wyjaśniono w [wprowadzenie do platformy ASP.NET Web Pages programowania z użyciem składni Razor](https://go.microsoft.com/fwlink/?LinkId=202890), Tablica umożliwia przechowywanie kolekcji podobnych elementów w zmiennej.</span><span class="sxs-lookup"><span data-stu-id="4492a-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="4492a-135">Tablice umożliwia zawierają dane, które mają zostać uwzględnione w planie.</span><span class="sxs-lookup"><span data-stu-id="4492a-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="4492a-136">W tej procedurze pokazano, jak utworzyć wykres z danymi w macierzy, przy użyciu domyślnego typu wykresu.</span><span class="sxs-lookup"><span data-stu-id="4492a-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="4492a-137">Pokazano również, jak wyświetlić wykres na stronie.</span><span class="sxs-lookup"><span data-stu-id="4492a-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="4492a-138">Utwórz nowy plik o nazwie *ChartArrayBasic.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="4492a-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="4492a-139">Zastąp istniejącą zawartość następujących czynności:</span><span class="sxs-lookup"><span data-stu-id="4492a-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="4492a-140">Ten kod najpierw tworzy nowy wykres i ustawia jego szerokość i wysokość.</span><span class="sxs-lookup"><span data-stu-id="4492a-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="4492a-141">Należy określić tytuł wykresu za pomocą `AddTitle` metody.</span><span class="sxs-lookup"><span data-stu-id="4492a-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="4492a-142">Aby dodać dane, należy użyć `AddSeries` metody.</span><span class="sxs-lookup"><span data-stu-id="4492a-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="4492a-143">W tym przykładzie używamy `name`, `xValue`, i `yValues` parametry `AddSeries` metody.</span><span class="sxs-lookup"><span data-stu-id="4492a-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="4492a-144">`name` Parametru jest wyświetlany w legendzie wykresu.</span><span class="sxs-lookup"><span data-stu-id="4492a-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="4492a-145">`xValue` Parametr zawiera tablicę danych wyświetlanych na osi poziomej wykresu.</span><span class="sxs-lookup"><span data-stu-id="4492a-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="4492a-146">`yValues` Parametr zawiera tablicę danych służącą do nakreślenia pionowych punktów wykresu.</span><span class="sxs-lookup"><span data-stu-id="4492a-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="4492a-147">`Write` Metoda renderuje faktycznie wykresu.</span><span class="sxs-lookup"><span data-stu-id="4492a-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="4492a-148">W tym przypadku, ponieważ nie określono typu wykresu `Chart` pomocnika powoduje wyświetlenie jego wykresu domyślny, który jest wykres kolumnowy.</span><span class="sxs-lookup"><span data-stu-id="4492a-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="4492a-149">Uruchom stronę w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="4492a-149">Run the page in the browser.</span></span> <span data-ttu-id="4492a-150">W przeglądarce pojawi się wykres.</span><span class="sxs-lookup"><span data-stu-id="4492a-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="4492a-151">Za pomocą zapytania do bazy danych dla danych wykresu</span><span class="sxs-lookup"><span data-stu-id="4492a-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="4492a-152">Jeśli informacje, które chcesz wykresu znajduje się w bazie danych, można uruchomić zapytania do bazy danych i następnie użyć danych z wyników, aby utworzyć wykres.</span><span class="sxs-lookup"><span data-stu-id="4492a-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="4492a-153">Ta procedura dowiesz się, jak czytać i wyświetlić dane z bazy danych utworzonej w artykule [wprowadzenie do pracy z bazą danych w witrynach stron sieci Web platformy ASP.NET](https://go.microsoft.com/fwlink/?LinkId=202893).</span><span class="sxs-lookup"><span data-stu-id="4492a-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="4492a-154">Dodaj *aplikacji\_danych* folder w katalogu głównym witryny sieci Web, jeśli folder nie istnieje.</span><span class="sxs-lookup"><span data-stu-id="4492a-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="4492a-155">W *aplikacji\_danych* folderu, Dodaj plik bazy danych o nazwie *SmallBakery.sdf* opisanym w [wprowadzenie do pracy z bazą danych w witrynach stron sieci Web platformy ASP.NET](https://go.microsoft.com/fwlink/?LinkId=202893).</span><span class="sxs-lookup"><span data-stu-id="4492a-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="4492a-156">Utwórz nowy plik o nazwie *ChartDataQuery.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="4492a-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="4492a-157">Zastąp istniejącą zawartość następujących czynności:</span><span class="sxs-lookup"><span data-stu-id="4492a-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="4492a-158">Ten kod najpierw otwarcie bazy danych, SmallBakery i przypisuje go do zmiennej o nazwie `db`.</span><span class="sxs-lookup"><span data-stu-id="4492a-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="4492a-159">Ta zmienna reprezentuje `Database` obiekt, który może służyć do odczytu i zapisu w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="4492a-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="4492a-160">Następnie kod uruchamia zapytanie SQL w celu uzyskania nazwy i cenę każdego produktu.</span><span class="sxs-lookup"><span data-stu-id="4492a-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="4492a-161">Ten kod tworzy nowy wykres i przekazuje zapytanie bazy danych przez wywołanie metody na wykresie `DataBindTable` metody.</span><span class="sxs-lookup"><span data-stu-id="4492a-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="4492a-162">Ta metoda przyjmuje dwa parametry: `dataSource` parametr jest używany dane z zapytania, a `xField` parametr umożliwia określenie, która kolumna danych jest używany dla osi x wykresu.</span><span class="sxs-lookup"><span data-stu-id="4492a-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="4492a-163">Jako alternatywa dla użycia `DataBindTable` metody, można użyć `AddSeries` metody `Chart` pomocnika.</span><span class="sxs-lookup"><span data-stu-id="4492a-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="4492a-164">`AddSeries` Metoda pozwala na ustalanie `xValue` i `yValues` parametrów.</span><span class="sxs-lookup"><span data-stu-id="4492a-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="4492a-165">Na przykład, zamiast `DataBindTable` metoda następująco:</span><span class="sxs-lookup"><span data-stu-id="4492a-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="4492a-166">Możesz użyć `AddSeries` metoda następująco:</span><span class="sxs-lookup"><span data-stu-id="4492a-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="4492a-167">Renderuj oba te same wyniki.</span><span class="sxs-lookup"><span data-stu-id="4492a-167">Both render the same results.</span></span> <span data-ttu-id="4492a-168">`AddSeries` Metody jest bardziej elastyczna, ponieważ można określić typ wykresu i danych bardziej jawny sposób, ale `DataBindTable` metodą jest łatwiejszy w obsłudze, jeśli jest potrzebna dodatkowa elastyczność.</span><span class="sxs-lookup"><span data-stu-id="4492a-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="4492a-169">Uruchom stronę w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="4492a-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="4492a-170">Korzystanie z danych XML</span><span class="sxs-lookup"><span data-stu-id="4492a-170">Using XML Data</span></span>

<span data-ttu-id="4492a-171">Trzecia opcja dla wykresów jest używany jest plik XML jako źródło danych dla wykresu.</span><span class="sxs-lookup"><span data-stu-id="4492a-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="4492a-172">Wymaga to, że plik XML również mieć plik schematu (*XSD* pliku), który opisuje strukturze XML.</span><span class="sxs-lookup"><span data-stu-id="4492a-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="4492a-173">Ta procedura pokazuje, jak odczytywać dane z pliku XML.</span><span class="sxs-lookup"><span data-stu-id="4492a-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="4492a-174">W *aplikacji\_danych* folderu, Utwórz nowy plik XML o nazwie *data.xml*.</span><span class="sxs-lookup"><span data-stu-id="4492a-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="4492a-175">Zastąp istniejący plik XML przy użyciu poniższych składników, które to dane XML o pracownikach w fikcyjnej firmie.</span><span class="sxs-lookup"><span data-stu-id="4492a-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="4492a-176">W *aplikacji\_danych* folderu, Utwórz nowy plik XML o nazwie *data.xsd*.</span><span class="sxs-lookup"><span data-stu-id="4492a-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="4492a-177">(Należy pamiętać, że rozszerzenie jest w tej chwili *XSD*.)</span><span class="sxs-lookup"><span data-stu-id="4492a-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="4492a-178">Zastąp istniejący kod XML następujących czynności:</span><span class="sxs-lookup"><span data-stu-id="4492a-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="4492a-179">W katalogu głównym witryny sieci Web, Utwórz nowy plik o nazwie *ChartDataXML.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="4492a-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="4492a-180">Zastąp istniejącą zawartość następujących czynności:</span><span class="sxs-lookup"><span data-stu-id="4492a-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="4492a-181">Kod najpierw tworzy `DataSet` obiektu.</span><span class="sxs-lookup"><span data-stu-id="4492a-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="4492a-182">Ten obiekt jest używany do zarządzania danymi, które są odczytywane z pliku XML i organizowania go zgodnie z informacjami w pliku schematu.</span><span class="sxs-lookup"><span data-stu-id="4492a-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="4492a-183">(Zwróć uwagę, że górnej części kodu zawiera instrukcję `using SystemData`.</span><span class="sxs-lookup"><span data-stu-id="4492a-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="4492a-184">Jest to wymagane, aby można było pracować z `DataSet` obiektu.</span><span class="sxs-lookup"><span data-stu-id="4492a-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="4492a-185">Aby uzyskać więcej informacji, zobacz [ &quot;Using&quot; instrukcji i w pełni kwalifikowane nazwy](#SB_UsingStatements) w dalszej części tego artykułu.)</span><span class="sxs-lookup"><span data-stu-id="4492a-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="4492a-186">Następnie kod tworzy `DataView` obiektu na podstawie zestawu danych.</span><span class="sxs-lookup"><span data-stu-id="4492a-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="4492a-187">Widok danych zawiera obiekt, który można powiązać wykres &#8212; oznacza to, Odczyt i wykreślania.</span><span class="sxs-lookup"><span data-stu-id="4492a-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="4492a-188">Wykres, który tworzy powiązanie danych przy użyciu `AddSeries` metody, jak był wyświetlany w przypadku wykresów danych, chyba że teraz `xValue` i `yValues` parametry są ustawione na `DataView` obiektu.</span><span class="sxs-lookup"><span data-stu-id="4492a-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="4492a-189">W tym przykładzie przedstawiono również sposób określania typu wykresu określonego</span><span class="sxs-lookup"><span data-stu-id="4492a-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="4492a-190">Gdy dane są dodawane w `AddSeries` metody `chartType` parametru jest również ustawiona na wyświetlania wykresu kołowego.</span><span class="sxs-lookup"><span data-stu-id="4492a-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="4492a-191">Uruchom stronę w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="4492a-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP]
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="4492a-192">Instrukcje "Za pomocą" i w pełni kwalifikowane nazwy</span><span class="sxs-lookup"><span data-stu-id="4492a-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="4492a-193">.NET Framework, ASP.NET Web Pages o składni Razor oparty na składa się z wielu tysięcy składników (grupy).</span><span class="sxs-lookup"><span data-stu-id="4492a-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="4492a-194">Aby umożliwić łatwe w zarządzaniu do pracy z tych klas, są zorganizowane w *przestrzenie nazw*, które są nieco, takich jak biblioteki.</span><span class="sxs-lookup"><span data-stu-id="4492a-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="4492a-195">Na przykład `System.Web` przestrzeń nazw zawiera klasy, które obsługują komunikację przeglądarką a serwerem `System.Xml` przestrzeń nazw zawiera klasy, które są używane do tworzenia i odczytywania plików XML i `System.Data` przestrzeń nazw zawiera klasy, które pozwalają pracować z danymi.</span><span class="sxs-lookup"><span data-stu-id="4492a-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="4492a-196">Aby uzyskać dostęp do danej klasy w .NET Framework, kod musi wiedzieć, nie tylko nazwę klasy, ale także przestrzeni nazw, który znajduje się w klasie.</span><span class="sxs-lookup"><span data-stu-id="4492a-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="4492a-197">Na przykład, aby można było używać `Chart` pomocnika, kod musi znaleźć `System.Web.Helpers.Chart` klasy, która łączy przestrzeni nazw (`System.Web.Helpers`) o nazwie klasy (`Chart`).</span><span class="sxs-lookup"><span data-stu-id="4492a-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="4492a-198">Jest to nazywane klasy *pełną* nazwa &#8212; pełną, jednoznaczną lokalizacji w ramach vastness programu .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="4492a-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="4492a-199">W kodzie to będzie wyglądać następująco:</span><span class="sxs-lookup"><span data-stu-id="4492a-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="4492a-200">Jednak jest kłopotliwe (i podatne) musi używać tych nazw długie, w pełni kwalifikowanej, za każdym razem, gdy chcesz odwołać się do klasy lub pomocnika.</span><span class="sxs-lookup"><span data-stu-id="4492a-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="4492a-201">W związku z tym, aby ułatwić nazwy klas, możesz *zaimportować* przestrzenie nazw interesuje Cię, który zazwyczaj jest to tylko kilka spośród wielu przestrzeniach nazw programu .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="4492a-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="4492a-202">Jeśli zaimportowane przestrzeni nazw, można użyć tylko nazwy klasy (`Chart`) zamiast z w pełni kwalifikowana nazwa (`System.Web.Helpers.Chart`).</span><span class="sxs-lookup"><span data-stu-id="4492a-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="4492a-203">Kod uruchamia i napotka nazwy klasy, można wygląda w właśnie przestrzeni nazw zaimportowanymi można znaleźć tej klasy.</span><span class="sxs-lookup"><span data-stu-id="4492a-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="4492a-204">Korzystając z ASP.NET Web Pages o składni Razor do tworzenia stron sieci web, zazwyczaj używasz tego samego zestawu klas za każdym razem w tym `WebPage` klasy, różnych wątków i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="4492a-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="4492a-205">Aby zapisać pracę importowania odpowiednie przestrzenie nazw, za każdym razem, gdy tworzysz witrynę sieci Web, ASP.NET jest skonfigurowany tak automatycznie importuje zestaw podstawowej przestrzeni nazw, dla każdej witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="4492a-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="4492a-206">Dlatego nie ma do czynienia z przestrzeni nazw lub zaimportowanie do chwili obecnej; wszystkie klasy, do których się znajdują się w przestrzeni nazw, które zostały już zaimportowane dla Ciebie.</span><span class="sxs-lookup"><span data-stu-id="4492a-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="4492a-207">Jednak czasami musisz pracować z klasą, która nie znajduje się w przestrzeni nazw, który jest importowany automatycznie dla Ciebie.</span><span class="sxs-lookup"><span data-stu-id="4492a-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="4492a-208">W takim przypadku możesz użyć tej klasy w pełni kwalifikowaną nazwę lub można ręcznie zaimportować obszar nazw, który zawiera klasę.</span><span class="sxs-lookup"><span data-stu-id="4492a-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="4492a-209">Aby zaimportować przestrzeni nazw, należy użyć `using` — instrukcja (`import` w języku Visual Basic), jak pokazano w przykładzie wcześniej tego artykułu.</span><span class="sxs-lookup"><span data-stu-id="4492a-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="4492a-210">Na przykład `DataSet` klasa się zebrała `System.Data` przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="4492a-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="4492a-211">`System.Data` Przestrzeni nazw nie jest automatycznie dostępny dla stron Razor programu ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="4492a-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="4492a-212">W związku z tym aby pracować z `DataSet` przy użyciu jego w pełni kwalifikowana nazwa, możesz użyć kodu w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="4492a-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="4492a-213">Jeśli trzeba użyć `DataSet` wielokrotnie klasy można importować przestrzeni nazw w następujący sposób i następnie użyć tylko nazwę klasy w kodzie:</span><span class="sxs-lookup"><span data-stu-id="4492a-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="4492a-214">Możesz dodać `using` instrukcji dla żadnych innych .NET Framework przestrzenie nazw, które ma dotyczyć odwołanie.</span><span class="sxs-lookup"><span data-stu-id="4492a-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="4492a-215">Jak wspomniano, nie będzie potrzebny, w tym celu często, ponieważ większość klas, którą będziesz pracować, które znajdują się w przestrzeni nazw, które są automatycznie importowane przez platformę ASP.NET do użycia w *.cshtml* i *.vbhtml* stron.</span><span class="sxs-lookup"><span data-stu-id="4492a-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>


<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="4492a-216">Wyświetlanie wykresów na stronie sieci Web</span><span class="sxs-lookup"><span data-stu-id="4492a-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="4492a-217">W przykładach przedstawiono do tej pory utworzyć wykres, a następnie renderowania wykresu bezpośrednio do przeglądarki jako grafika.</span><span class="sxs-lookup"><span data-stu-id="4492a-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="4492a-218">W wielu przypadkach jednak chcesz wyświetlić wykres jako część strony, nie tylko przez siebie w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="4492a-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="4492a-219">Aby to zrobić wymaga procesem dwuetapowym.</span><span class="sxs-lookup"><span data-stu-id="4492a-219">To do that requires a two-step process.</span></span> <span data-ttu-id="4492a-220">Pierwszym krokiem jest utworzyć stronę, która generuje wykres, zgodnie z wyświetlonymi już wcześniej.</span><span class="sxs-lookup"><span data-stu-id="4492a-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="4492a-221">Drugim krokiem jest do wyświetlenia obraz wynikowy w innej strony.</span><span class="sxs-lookup"><span data-stu-id="4492a-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="4492a-222">Aby wyświetlić obraz, należy użyć kodu HTML `<img>` elementu w taki sam sposób, w jaki do wyświetlania dowolnego obrazu.</span><span class="sxs-lookup"><span data-stu-id="4492a-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="4492a-223">Jednak zamiast odwoływać się do *.jpg* lub *.png* pliku `<img>` odwołania do elementu *.cshtml* pliku, który zawiera `Chart` pomocnika, Tworzy wykres.</span><span class="sxs-lookup"><span data-stu-id="4492a-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="4492a-224">Po uruchomieniu strony ekranu, `<img>` element pobiera dane wyjściowe `Chart` pomocnika i renderuje wykresu.</span><span class="sxs-lookup"><span data-stu-id="4492a-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="4492a-225">Utwórz plik o nazwie *ShowChart.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="4492a-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="4492a-226">Zastąp istniejącą zawartość następujących czynności:</span><span class="sxs-lookup"><span data-stu-id="4492a-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="4492a-227">Kod używa `<img>` element, aby wyświetlić wykres, który został wcześniej utworzony w *ChartArrayBasic.cshtml* pliku.</span><span class="sxs-lookup"><span data-stu-id="4492a-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="4492a-228">Uruchom stronę sieci web w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="4492a-228">Run the web page in a browser.</span></span> <span data-ttu-id="4492a-229">*ShowChart.cshtml* plik zawiera obraz wykresu, w oparciu o kod zawarty w *ChartArrayBasic.cshtml* pliku.</span><span class="sxs-lookup"><span data-stu-id="4492a-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="4492a-230">Ustawianie stylów na wykresie</span><span class="sxs-lookup"><span data-stu-id="4492a-230">Styling a Chart</span></span>

<span data-ttu-id="4492a-231">`Chart` Pomocnika obsługuje dużą liczbę opcji, które umożliwiają dostosowanie wyglądu wykresu.</span><span class="sxs-lookup"><span data-stu-id="4492a-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="4492a-232">Można ustawić kolory, czcionki, obramowania i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="4492a-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="4492a-233">Łatwe dostosowywanie wyglądu wykresu jest użycie *motyw*.</span><span class="sxs-lookup"><span data-stu-id="4492a-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="4492a-234">Motywy to kolekcje informacji określających sposób renderowania wykresu przy użyciu czcionek, kolorów, etykiet, palet, obramowania i efektów.</span><span class="sxs-lookup"><span data-stu-id="4492a-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="4492a-235">(Zwróć uwagę, że styl wykresu nie wskazuje typ wykresu).</span><span class="sxs-lookup"><span data-stu-id="4492a-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="4492a-236">Poniższa tabela zawiera listę wbudowanych motywów.</span><span class="sxs-lookup"><span data-stu-id="4492a-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="4492a-237">Motyw</span><span class="sxs-lookup"><span data-stu-id="4492a-237">Theme</span></span> | <span data-ttu-id="4492a-238">Opis</span><span class="sxs-lookup"><span data-stu-id="4492a-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="4492a-239">Wyświetla czerwony kolumn na białym tle.</span><span class="sxs-lookup"><span data-stu-id="4492a-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="4492a-240">Wyświetla niebieskie kolumn na niebieskim tle gradientu.</span><span class="sxs-lookup"><span data-stu-id="4492a-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="4492a-241">Wyświetla niebieski kolumn znajdujących się zielony gradientu tła.</span><span class="sxs-lookup"><span data-stu-id="4492a-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="4492a-242">Wyświetla pomarańczowy kolumn w żółte tło gradientu.</span><span class="sxs-lookup"><span data-stu-id="4492a-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="4492a-243">Wyświetla 3-czerwony kolumn na białym tle.</span><span class="sxs-lookup"><span data-stu-id="4492a-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="4492a-244">Można określić motywu, który ma używać podczas tworzenia nowego wykresu.</span><span class="sxs-lookup"><span data-stu-id="4492a-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="4492a-245">Utwórz nowy plik o nazwie *ChartStyleGreen.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="4492a-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="4492a-246">Zastąp istniejącą zawartość na stronie następujących czynności:</span><span class="sxs-lookup"><span data-stu-id="4492a-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="4492a-247">Ten kod jest taki sam jak poprzedniego przykładu, który korzysta z bazy danych dla danych, ale dodaje `theme` parametru podczas tworzenia `Chart` obiektu.</span><span class="sxs-lookup"><span data-stu-id="4492a-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="4492a-248">Poniżej przedstawiono zmiany kodu:</span><span class="sxs-lookup"><span data-stu-id="4492a-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="4492a-249">Uruchom stronę w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="4492a-249">Run the page in a browser.</span></span> <span data-ttu-id="4492a-250">Zobacz te same dane, co przed, ale bardziej dopracowane wyglądu wykresu:</span><span class="sxs-lookup"><span data-stu-id="4492a-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="4492a-251">Zapisanie wykresu</span><span class="sxs-lookup"><span data-stu-id="4492a-251">Saving a Chart</span></span>

<span data-ttu-id="4492a-252">Kiedy używasz `Chart` pomocnika, jak w tym samouczku do tej pory w tym artykule pomocnika ponownie tworzy wykres od podstaw każdym razem jest wywoływana.</span><span class="sxs-lookup"><span data-stu-id="4492a-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="4492a-253">Jeśli to konieczne, kod dla wykresu również ponownie zapytania bazy danych lub ponownie odczytuje plik XML, aby pobrać dane.</span><span class="sxs-lookup"><span data-stu-id="4492a-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="4492a-254">W niektórych przypadkach, w ten sposób może być złożonych operacji, takich jak jeśli zadajesz zapytanie do bazy danych jest duży, czy plik XML zawiera dużą ilość danych.</span><span class="sxs-lookup"><span data-stu-id="4492a-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="4492a-255">Nawet jeśli wykres nie wymaga dużej ilości danych, proces dynamicznie Tworzenie obrazu zajmuje zasoby serwera, a jeśli wiele osób żądanie strony, które wyświetlić wykres, mogą być wpływ na wydajność witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="4492a-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="4492a-256">Pozwala zmniejszyć potencjalne obniżenie wydajności podczas tworzenia wykresu, można utworzyć wykres przy jego pierwszym czas potrzebny, a następnie zapisz go.</span><span class="sxs-lookup"><span data-stu-id="4492a-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="4492a-257">Gdy wykres jest ponownie potrzebny, zamiast ponownego generowania, możesz po prostu pobrać zapisaną wersję i renderować.</span><span class="sxs-lookup"><span data-stu-id="4492a-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="4492a-258">Wykres można zapisać w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="4492a-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="4492a-259">Wykres w pamięci komputera (na serwerze) w pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="4492a-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="4492a-260">Zapisz wykres jako plik obrazu.</span><span class="sxs-lookup"><span data-stu-id="4492a-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="4492a-261">Zapisz wykres jako plik XML.</span><span class="sxs-lookup"><span data-stu-id="4492a-261">Save the chart as an XML file.</span></span> <span data-ttu-id="4492a-262">Ta opcja pozwala zmodyfikować wykres, przed jej zapisaniem.</span><span class="sxs-lookup"><span data-stu-id="4492a-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="4492a-263">Buforowanie wykresu</span><span class="sxs-lookup"><span data-stu-id="4492a-263">Caching a Chart</span></span>

<span data-ttu-id="4492a-264">Po utworzeniu wykresu możesz pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="4492a-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="4492a-265">Buforowanie wykres oznacza, że nie ma zostać utworzony ponownie, jeśli należy go ponownie wyświetlone.</span><span class="sxs-lookup"><span data-stu-id="4492a-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="4492a-266">Podczas zapisywania wykresu w pamięci podręcznej, możesz nadać mu kluczem, który musi być unikatowa dla tego wykresu.</span><span class="sxs-lookup"><span data-stu-id="4492a-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="4492a-267">Wykresy zapisane w pamięci podręcznej może zostać usunięty, jeśli serwer jest za mało pamięci.</span><span class="sxs-lookup"><span data-stu-id="4492a-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="4492a-268">Ponadto pamięć podręczna jest czyszczona, jeśli aplikacja uruchamia ponownie jakiegokolwiek powodu.</span><span class="sxs-lookup"><span data-stu-id="4492a-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="4492a-269">W związku z tym standardowy sposób pracy z pamięci podręcznej wykresu jest zawsze najpierw sprawdzić, czy jest dostępna w pamięci podręcznej, a w przeciwnym razie, a następnie utwórz lub utwórz go ponownie.</span><span class="sxs-lookup"><span data-stu-id="4492a-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="4492a-270">W katalogu głównym witryny sieci Web, Utwórz plik o nazwie *ShowCachedChart.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="4492a-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="4492a-271">Zastąp istniejącą zawartość następujących czynności:</span><span class="sxs-lookup"><span data-stu-id="4492a-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="4492a-272">`<img>` Znacznik obejmuje `src` atrybut, który wskazuje na *ChartSaveToCache.cshtml* plik i przekazuje klucz do strony jako ciąg zapytania.</span><span class="sxs-lookup"><span data-stu-id="4492a-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="4492a-273">Klucza zawiera wartość &quot;myChartKey&quot;.</span><span class="sxs-lookup"><span data-stu-id="4492a-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="4492a-274">*ChartSaveToCache.cshtml* plik zawiera `Chart` pomocnika, która tworzy wykres.</span><span class="sxs-lookup"><span data-stu-id="4492a-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="4492a-275">Utworzysz tę stronę za chwilę.</span><span class="sxs-lookup"><span data-stu-id="4492a-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="4492a-276">Na końcu strony znajduje się link do strony o nazwie *ClearCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="4492a-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="4492a-277">To strona, która utworzysz też wkrótce.</span><span class="sxs-lookup"><span data-stu-id="4492a-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="4492a-278">Potrzebujesz *ClearCache.cshtml* tylko w celu testowania buforowania w tym przykładzie — nie jest łącze lub strona, która zwykle należy dołączyć podczas pracy z wykresami pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="4492a-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="4492a-279">W katalogu głównym witryny sieci Web, Utwórz nowy plik o nazwie *ChartSaveToCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="4492a-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="4492a-280">Zastąp istniejącą zawartość następujących czynności:</span><span class="sxs-lookup"><span data-stu-id="4492a-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="4492a-281">Ten kod najpierw sprawdza, czy przekazano wszystko jako wartości klucza w ciągu zapytania.</span><span class="sxs-lookup"><span data-stu-id="4492a-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="4492a-282">Jeśli tak, kod próbuje odczytać wykres z pamięci podręcznej przez wywołanie metody `GetFromCache` metody i przekazanie do niej klucza.</span><span class="sxs-lookup"><span data-stu-id="4492a-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="4492a-283">Okazuje się, że nie ma nic w pamięci podręcznej, w tym kluczu (co się stanie po raz pierwszy wykres jest wymagane), ten kod tworzy wykres w zwykły sposób.</span><span class="sxs-lookup"><span data-stu-id="4492a-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="4492a-284">Po zakończeniu wykresu kodu zapisuje je w pamięci podręcznej przez wywołanie metody `SaveToCache`.</span><span class="sxs-lookup"><span data-stu-id="4492a-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="4492a-285">Ta metoda wymaga klucza (tak, aby później można żądać wykres) i czas, który chcesz zapisać wykres w pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="4492a-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="4492a-286">(Dokładny czas będzie buforować wykres będzie zależeć od jak często użytkownik uważa, że dane, które reprezentuje mogą ulec zmianie). `SaveToCache` Wymaga również metoda `slidingExpiration` parametru &#8212; Jeśli ta opcja jest ustawiona na wartość true, limit czasu licznik jest resetowany każdorazowo wykresu jest dostępny.</span><span class="sxs-lookup"><span data-stu-id="4492a-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="4492a-287">W tym przypadku w praktyce oznacza to, że wpis pamięci podręcznej wykresu wygaśnie 2 minuty po ostatnim ktoś uzyskać dostęp do wykresu.</span><span class="sxs-lookup"><span data-stu-id="4492a-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="4492a-288">(Zamiast przedłużanie ważności jest bezwzględna wygaśnięcia, co oznacza, że wpis pamięci podręcznej upływa dokładnie 2 minuty po został umieszczony w pamięci podręcznej, niezależnie od tego, jak często ma były używane).</span><span class="sxs-lookup"><span data-stu-id="4492a-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="4492a-289">Na koniec kod używa `WriteFromCache` metodę, aby pobrać i renderować wykres z pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="4492a-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="4492a-290">Należy zauważyć, że ta metoda jest poza `if` blok, który sprawdza, czy pamięć podręczna, ponieważ pobierze wykres z pamięci podręcznej czy wykres został ma rozpoczynać się, czy ma zostać wygenerowany i zapisane w pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="4492a-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="4492a-291">Należy zauważyć, że w tym przykładzie `AddTitle` metoda zawiera sygnaturę czasową.</span><span class="sxs-lookup"><span data-stu-id="4492a-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="4492a-292">(Dodaje bieżącą datę i godzinę &#8212; `DateTime.Now` &#8212; tytuł.)</span><span class="sxs-lookup"><span data-stu-id="4492a-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="4492a-293">Utwórz nową stronę o nazwie *ClearCache.cshtml* i zastąp jego zawartość następującym kodem:</span><span class="sxs-lookup"><span data-stu-id="4492a-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="4492a-294">Ta strona używa `WebCache` pomocnika wykres, który jest buforowany w *ChartSaveToCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="4492a-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="4492a-295">Jak wspomniano wcześniej, zwykle nie trzeba mieć stronę następująco.</span><span class="sxs-lookup"><span data-stu-id="4492a-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="4492a-296">Tworzysz tu tylko po to, aby ułatwić testowanie buforowania.</span><span class="sxs-lookup"><span data-stu-id="4492a-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="4492a-297">Uruchom *ShowCachedChart.cshtml* strony sieci web w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="4492a-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="4492a-298">Zostanie wyświetlona strona obraz wykresu, w oparciu o kod zawarty w *ChartSaveToCache.cshtml* pliku.</span><span class="sxs-lookup"><span data-stu-id="4492a-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="4492a-299">Zanotuj sygnaturę czasową mówi w tytuł wykresu.</span><span class="sxs-lookup"><span data-stu-id="4492a-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![Opis: Obraz podstawowego wykresu z sygnaturą czasową w tytuł wykresu](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="4492a-301">Zamknij przeglądarkę.</span><span class="sxs-lookup"><span data-stu-id="4492a-301">Close the browser.</span></span>
8. <span data-ttu-id="4492a-302">Uruchom *ShowCachedChart.cshtml* ponownie.</span><span class="sxs-lookup"><span data-stu-id="4492a-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="4492a-303">Należy zauważyć, że sygnatura czasowa jest taka sama jak wcześniej, co oznacza, że wykres nie został ponownie wygenerowany, ale zamiast tego została odczytana z pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="4492a-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="4492a-304">W *ShowCachedChart.cshtml*, kliknij przycisk **wyczyścić pamięć podręczną** łącza.</span><span class="sxs-lookup"><span data-stu-id="4492a-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="4492a-305">Spowoduje to przejście do *ClearCache.cshtml*, który zgłasza, że pamięć podręczna została wyczyszczona.</span><span class="sxs-lookup"><span data-stu-id="4492a-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="4492a-306">Kliknij przycisk **powrócić do ShowCachedChart.cshtml** połączyć lub uruchomić ponownie *ShowCachedChart.cshtml* z programu WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="4492a-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="4492a-307">Zwróć uwagę, że teraz sygnaturę czasową została zmieniona, ponieważ pamięć podręczna została wyczyszczona.</span><span class="sxs-lookup"><span data-stu-id="4492a-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="4492a-308">W związku z tym w tym celu należy wygenerować ponownie wykresu, a następnie przestawić je ponownie w pamięci podręcznej musiały kodu.</span><span class="sxs-lookup"><span data-stu-id="4492a-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="4492a-309">Zapisanie wykresu jako pliku obrazu</span><span class="sxs-lookup"><span data-stu-id="4492a-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="4492a-310">Można również zapisać wykres jako plik obrazu (na przykład, jako *.jpg* pliku) na serwerze.</span><span class="sxs-lookup"><span data-stu-id="4492a-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="4492a-311">Można następnie użyć pliku obrazu sposób, w jaki dowolny obraz.</span><span class="sxs-lookup"><span data-stu-id="4492a-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="4492a-312">Zaletą jest to plik jest przechowywany zamiast zapisane w tymczasowej pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="4492a-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="4492a-313">Można zapisać nowego obrazu wykresu w różnym czasie (na przykład co godzinę) i następnie rejestrować je trwałych zmian, które występują w czasie.</span><span class="sxs-lookup"><span data-stu-id="4492a-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="4492a-314">Pamiętaj, że należy się upewnić, że aplikacja sieci web ma uprawnienia do zapisania pliku do folderu na serwerze, na którym chcesz umieścić plik obrazu.</span><span class="sxs-lookup"><span data-stu-id="4492a-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="4492a-315">W katalogu głównym witryny sieci Web utwórz folder o nazwie  *\_ChartFiles* Jeśli jeszcze nie istnieje.</span><span class="sxs-lookup"><span data-stu-id="4492a-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="4492a-316">W katalogu głównym witryny sieci Web, Utwórz nowy plik o nazwie *ChartSave.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="4492a-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="4492a-317">Zastąp istniejącą zawartość następujących czynności:</span><span class="sxs-lookup"><span data-stu-id="4492a-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="4492a-318">Ten kod najpierw sprawdza, czy *.jpg* plik istnieje, wywołując `File.Exists` metody.</span><span class="sxs-lookup"><span data-stu-id="4492a-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="4492a-319">Jeśli plik nie istnieje, kod tworzy nową `Chart` z tablicy.</span><span class="sxs-lookup"><span data-stu-id="4492a-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="4492a-320">Tym razem, kod wywołuje `Save` metody i przekazuje `path` parametru, aby określić ścieżkę i nazwę pliku, gdzie zapisać wykres.</span><span class="sxs-lookup"><span data-stu-id="4492a-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="4492a-321">W treści strony `<img>` element używa ścieżki, aby wskazywał *.jpg* plik, aby wyświetlić.</span><span class="sxs-lookup"><span data-stu-id="4492a-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="4492a-322">Uruchom *ChartSave.cshtml* pliku.</span><span class="sxs-lookup"><span data-stu-id="4492a-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="4492a-323">Wróć do programu WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="4492a-323">Return to WebMatrix.</span></span> <span data-ttu-id="4492a-324">Należy zauważyć, że plik obrazu o nazwie *chart01.jpg* został zapisany w  *\_ChartFiles* folderu.</span><span class="sxs-lookup"><span data-stu-id="4492a-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="4492a-325">Zapisywanie wykres jako plik XML</span><span class="sxs-lookup"><span data-stu-id="4492a-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="4492a-326">Na koniec można zapisać wykres jako plik XML na serwerze.</span><span class="sxs-lookup"><span data-stu-id="4492a-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="4492a-327">Zaletą tej metody w pamięci podręcznej wykresu lub zapisywania wykresu do pliku jest, czy można zmodyfikować plik XML, przed wyświetleniem wykresu, jeśli chce się.</span><span class="sxs-lookup"><span data-stu-id="4492a-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="4492a-328">Aplikacja musi mieć uprawnienia odczytu/zapisu do folderu na serwerze, w którym chcesz umieścić plik obrazu.</span><span class="sxs-lookup"><span data-stu-id="4492a-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="4492a-329">W katalogu głównym witryny sieci Web, Utwórz nowy plik o nazwie *ChartSaveXml.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="4492a-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="4492a-330">Zastąp istniejącą zawartość następujących czynności:</span><span class="sxs-lookup"><span data-stu-id="4492a-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="4492a-331">Ten kod jest podobny do kodu, który był wyświetlany zapisywania wykresu w pamięci podręcznej, z tą różnicą, że używa pliku XML.</span><span class="sxs-lookup"><span data-stu-id="4492a-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="4492a-332">Ten kod najpierw sprawdza, czy plik XML istnieje, wywołując `File.Exists` metody.</span><span class="sxs-lookup"><span data-stu-id="4492a-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="4492a-333">Jeśli plik istnieje, kod tworzy nową `Chart` obiektu i przekazuje nazwę pliku jako `themePath` parametru.</span><span class="sxs-lookup"><span data-stu-id="4492a-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="4492a-334">Spowoduje to utworzenie wykresu oparte na dowolnie znajduje się w pliku XML.</span><span class="sxs-lookup"><span data-stu-id="4492a-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="4492a-335">Jeśli jeszcze nie istnieje w pliku XML, ten kod tworzy wykres, takich jak normalne, a następnie wywołuje `SaveXml` go zapisać.</span><span class="sxs-lookup"><span data-stu-id="4492a-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="4492a-336">Wykres jest renderowany przy użyciu `Write` metody, jak przedstawiono przed.</span><span class="sxs-lookup"><span data-stu-id="4492a-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="4492a-337">Podobnie jak w przypadku strony, która wykazała, buforowanie, ten kod zawiera sygnaturę czasową tytuł wykresu.</span><span class="sxs-lookup"><span data-stu-id="4492a-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="4492a-338">Utwórz nową stronę o nazwie *ChartDisplayXMLChart.cshtml* i Dodaj następujący kod do niego:</span><span class="sxs-lookup"><span data-stu-id="4492a-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="4492a-339">Uruchom *ChartDisplayXMLChart.cshtml* strony.</span><span class="sxs-lookup"><span data-stu-id="4492a-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="4492a-340">Wykres zostanie wyświetlony.</span><span class="sxs-lookup"><span data-stu-id="4492a-340">The chart is displayed.</span></span> <span data-ttu-id="4492a-341">Zanotuj sygnaturę czasową tytuł wykresu.</span><span class="sxs-lookup"><span data-stu-id="4492a-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="4492a-342">Zamknij przeglądarkę.</span><span class="sxs-lookup"><span data-stu-id="4492a-342">Close the browser.</span></span>
6. <span data-ttu-id="4492a-343">W programie WebMatrix, kliknij prawym przyciskiem myszy  *\_ChartFiles* folderu, kliknij przycisk **Odśwież**, a następnie otwórz folder.</span><span class="sxs-lookup"><span data-stu-id="4492a-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="4492a-344">*XMLChart.xml* plik w tym folderze został utworzony przez `Chart` pomocnika.</span><span class="sxs-lookup"><span data-stu-id="4492a-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![Opis: Folder _ChartFiles przedstawiający plik XMLChart.xml utworzony przez pomocnika wykresu.](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="4492a-346">Uruchom *ChartDisplayXMLChart.cshtml* strony.</span><span class="sxs-lookup"><span data-stu-id="4492a-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="4492a-347">Wykres ten przedstawia tego samego znacznika czasu jako pierwszego uruchomienia strony.</span><span class="sxs-lookup"><span data-stu-id="4492a-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="4492a-348">To, ponieważ wykresu jest generowany z pliku XML został wcześniej zapisany.</span><span class="sxs-lookup"><span data-stu-id="4492a-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="4492a-349">W programie WebMatrix Otwórz  *\_ChartFiles* folder i Usuń *XMLChart.xml* pliku.</span><span class="sxs-lookup"><span data-stu-id="4492a-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="4492a-350">Uruchom *ChartDisplayXMLChart.cshtml* stronie jeszcze raz.</span><span class="sxs-lookup"><span data-stu-id="4492a-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="4492a-351">Tym razem zaktualizować sygnaturę czasową, ponieważ `Chart` pomocnika musieli ponownie utworzyć plik XML.</span><span class="sxs-lookup"><span data-stu-id="4492a-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="4492a-352">Jeśli chcesz, sprawdź  *\_ChartFiles* folder i zwróć uwagę, że plik XML jest ponownie.</span><span class="sxs-lookup"><span data-stu-id="4492a-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="4492a-353">Dodatkowe zasoby</span><span class="sxs-lookup"><span data-stu-id="4492a-353">Additional Resources</span></span>

- [<span data-ttu-id="4492a-354">Wprowadzenie do pracy z bazą danych we wzorcu ASP.NET Web Pages witryn</span><span class="sxs-lookup"><span data-stu-id="4492a-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="4492a-355">Przy użyciu buforowania we wzorcu ASP.NET Web Pages lokacjach w celu zwiększenia wydajności</span><span class="sxs-lookup"><span data-stu-id="4492a-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="4492a-356">[Klasa wykresu](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (Dokumentacja interfejsu API platformy ASP.NET Web Pages w witrynie MSDN)</span><span class="sxs-lookup"><span data-stu-id="4492a-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
