---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: Wyświetlanie danych na wykresie przy użyciu stron sieci Web ASP.NET (Razor) | Microsoft Docs
author: microsoft
description: W tym rozdziale wyjaśniono, jak wyświetlać dane na wykresie. W poprzednich działach przedstawiono sposób ręcznego wyświetlania danych i w siatce. W tym rozdziale wyjaśniono...
ms.author: riande
ms.date: 05/22/2012
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: 6dad67d4e3d38d57a761c567d937d714a3184ea9
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/06/2020
ms.locfileid: "78627492"
---
# <a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="7d7ba-105">Wyświetlanie danych na wykresie przy użyciu stron sieci Web ASP.NET (Razor)</span><span class="sxs-lookup"><span data-stu-id="7d7ba-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>

<span data-ttu-id="7d7ba-106">przez [firmę Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="7d7ba-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="7d7ba-107">W tym artykule wyjaśniono, jak używać wykresu do wyświetlania danych w witrynie internetowej ASP.NET Web Pages (Razor) za pomocą pomocnika `Chart`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="7d7ba-108">Dowiesz **się**:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="7d7ba-109">Jak wyświetlić dane na wykresie.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="7d7ba-110">Tworzenie stylów wykresów przy użyciu wbudowanych motywów.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="7d7ba-111">Jak zaoszczędzić wykresy i jak buforować je w celu zapewnienia lepszej wydajności.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="7d7ba-112">Są to funkcje programowania ASP.NET wprowadzone w artykule:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="7d7ba-113">Pomocnik `Chart`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="7d7ba-114">Informacje przedstawione w tym artykule dotyczą ASP.NET Web Pages 1,0 i Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>

<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="7d7ba-115">Pomocnik wykresu</span><span class="sxs-lookup"><span data-stu-id="7d7ba-115">The Chart Helper</span></span>

<span data-ttu-id="7d7ba-116">Aby wyświetlić dane w formie graficznej, można użyć pomocnika `Chart`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="7d7ba-117">Pomocnik `Chart` może renderować obraz, który wyświetla dane w różnych typach wykresów.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="7d7ba-118">Obsługuje wiele opcji formatowania i etykietowania.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="7d7ba-119">Pomocnik `Chart` może renderować więcej niż 30 typów wykresów, w tym wszystkie typy wykresów, które mogą być znane z programu Microsoft Excel lub inne wykresy warstwowe narzędzi &#8212; , wykresy słupkowe, wykresy liniowe i wykresy kołowe oraz bardziej wyspecjalizowane wykresy, takie jak wykresy giełdowe.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="7d7ba-120">**Wykres warstwowy** ![opis: obraz typu wykresu warstwowego](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="7d7ba-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="7d7ba-121">**Wykres słupkowy** ![opis: obraz typu wykresu słupkowego](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="7d7ba-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="7d7ba-122">**Wykres kolumnowy** ![opis: obraz typu wykresu kolumnowego](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="7d7ba-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="7d7ba-123">**Wykres liniowy** ![opis: obraz typu wykresu liniowego](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="7d7ba-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="7d7ba-124">Opis ![**wykresu kołowego** : obraz typu wykresu kołowego](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="7d7ba-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="7d7ba-125">Opis ![**wykresu giełdowego** : obraz typu wykresu giełdowego](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="7d7ba-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="7d7ba-126">Elementy wykresu</span><span class="sxs-lookup"><span data-stu-id="7d7ba-126">Chart Elements</span></span>

<span data-ttu-id="7d7ba-127">Wykresy przedstawiają dane i dodatkowe elementy, takie jak legendy, osie, serie i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="7d7ba-128">Na poniższej ilustracji przedstawiono wiele elementów wykresu, które można dostosować podczas korzystania z pomocnika `Chart`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="7d7ba-129">W tym artykule pokazano, jak ustawić niektóre (nie wszystkie) tych elementów.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-129">This article shows you how to set some (not all) of these elements.</span></span>

![Opis: obraz przedstawiający elementy wykresu](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="7d7ba-131">Tworzenie wykresu na podstawie danych</span><span class="sxs-lookup"><span data-stu-id="7d7ba-131">Creating a Chart from Data</span></span>

<span data-ttu-id="7d7ba-132">Dane wyświetlane na wykresie mogą pochodzić z tablicy, od wyników zwróconych z bazy danych lub z danych znajdujących się w pliku XML.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="7d7ba-133">Korzystanie z tablicy</span><span class="sxs-lookup"><span data-stu-id="7d7ba-133">Using an Array</span></span>

<span data-ttu-id="7d7ba-134">Zgodnie z opisem w temacie [wprowadzenie do ASP.NET stron sieci Web przy użyciu składni Razor](https://go.microsoft.com/fwlink/?LinkId=202890), tablica umożliwia przechowywanie kolekcji podobnych elementów w jednej zmiennej.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="7d7ba-135">Tablice mogą zawierać dane, które mają zostać uwzględnione na wykresie.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="7d7ba-136">Ta procedura przedstawia sposób tworzenia wykresu na podstawie danych w tablicach przy użyciu domyślnego typu wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="7d7ba-137">Przedstawiono w nim również sposób wyświetlania wykresu na stronie.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="7d7ba-138">Utwórz nowy plik o nazwie *ChartArrayBasic. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="7d7ba-139">Zastąp istniejącą zawartość następującym:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="7d7ba-140">Kod najpierw tworzy nowy wykres i ustawia jego szerokość i wysokość.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="7d7ba-141">Należy określić tytuł wykresu przy użyciu metody `AddTitle`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="7d7ba-142">Aby dodać dane, użyj metody `AddSeries`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="7d7ba-143">W tym przykładzie użyto parametrów `name`, `xValue` i `yValues` metody `AddSeries`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="7d7ba-144">Parametr `name` jest wyświetlany w legendzie wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="7d7ba-145">Parametr `xValue` zawiera tablicę danych wyświetlaną wzdłuż osi poziomej wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="7d7ba-146">Parametr `yValues` zawiera tablicę danych, która jest używana do wykreślania pionowych punktów wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="7d7ba-147">Metoda `Write` w rzeczywistości renderuje wykres.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="7d7ba-148">W tym przypadku, ponieważ nie określono typu wykresu, pomocnik `Chart` renderuje swój domyślny wykres, który jest wykresem kolumnowym.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="7d7ba-149">Uruchom stronę w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-149">Run the page in the browser.</span></span> <span data-ttu-id="7d7ba-150">Przeglądarka wyświetla wykres.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="7d7ba-151">Używanie zapytania bazy danych na potrzeby danych wykresu</span><span class="sxs-lookup"><span data-stu-id="7d7ba-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="7d7ba-152">Jeśli informacje, które chcesz utworzyć, są zawarte w bazie danych programu, możesz uruchomić zapytanie bazy danych, a następnie użyć danych z wyników do utworzenia wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="7d7ba-153">Ta procedura pokazuje, jak odczytywać i wyświetlać dane z bazy danych utworzonej w artykule [wprowadzenie do pracy z bazą danych w witrynach sieci Web ASP.NET](https://go.microsoft.com/fwlink/?LinkId=202893).</span><span class="sxs-lookup"><span data-stu-id="7d7ba-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="7d7ba-154">Jeśli folder nie istnieje, Dodaj folder *danych\_aplikacji* do katalogu głównego witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="7d7ba-155">W folderze *dane\_aplikacji* Dodaj plik bazy danych o nazwie *SmallBakery. sdf* , który został opisany w artykule [wprowadzenie do pracy z bazą danych w witrynie ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=202893).</span><span class="sxs-lookup"><span data-stu-id="7d7ba-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="7d7ba-156">Utwórz nowy plik o nazwie *ChartDataQuery. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="7d7ba-157">Zastąp istniejącą zawartość następującym:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="7d7ba-158">Kod najpierw otwiera bazę danych SmallBakery i przypisuje ją do zmiennej o nazwie `db`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="7d7ba-159">Ta zmienna reprezentuje obiekt `Database`, którego można użyć do odczytu i zapisu w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="7d7ba-160">Następnie kod uruchamia zapytanie SQL, aby uzyskać nazwę i cenę każdego produktu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="7d7ba-161">Kod tworzy nowy wykres i przekazuje do niego zapytanie bazy danych, wywołując metodę `DataBindTable` wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="7d7ba-162">Ta metoda przyjmuje dwa parametry: parametr `dataSource` jest przeznaczony dla danych z zapytania, a parametr `xField` pozwala ustawić kolumnę danych, która będzie używana dla osi x wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="7d7ba-163">Alternatywnie przy użyciu metody `DataBindTable` można użyć metody `AddSeries` pomocnika `Chart`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="7d7ba-164">Metoda `AddSeries` umożliwia ustawienie parametrów `xValue` i `yValues`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="7d7ba-165">Na przykład zamiast używać metody `DataBindTable` w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="7d7ba-166">Można użyć metody `AddSeries` podobnej do tego:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="7d7ba-167">Obie renderują te same wyniki.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-167">Both render the same results.</span></span> <span data-ttu-id="7d7ba-168">Metoda `AddSeries` jest bardziej elastyczna, ponieważ można jawnie określić typ i dane wykresu, ale metoda `DataBindTable` jest łatwiejsza do użycia, jeśli nie potrzebujesz dodatkowej elastyczności.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="7d7ba-169">Uruchom stronę w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="7d7ba-170">Korzystanie z danych XML</span><span class="sxs-lookup"><span data-stu-id="7d7ba-170">Using XML Data</span></span>

<span data-ttu-id="7d7ba-171">Trzecią opcją dla wykresów jest użycie pliku XML jako danych wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="7d7ba-172">Wymaga to, aby plik XML miał również plik schematu (plik*XSD* ), który OPISUJE strukturę XML.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="7d7ba-173">Ta procedura pokazuje, jak odczytywać dane z pliku XML.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="7d7ba-174">W folderze *dane\_aplikacji* Utwórz nowy plik XML o nazwie *Data. XML*.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="7d7ba-175">Zastąp istniejący kod XML następującym, który stanowi pewne dane XML o pracownikach w fikcyjnej firmie.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="7d7ba-176">W folderze *dane\_aplikacji* Utwórz nowy plik XML o nazwie *Data. xsd*.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="7d7ba-177">(Należy pamiętać, że rozszerzenie tego czasu to *XSD*.)</span><span class="sxs-lookup"><span data-stu-id="7d7ba-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="7d7ba-178">Zastąp istniejący kod XML następującym:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="7d7ba-179">W folderze głównym witryny sieci Web Utwórz nowy plik o nazwie *ChartDataXML. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="7d7ba-180">Zastąp istniejącą zawartość następującym:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="7d7ba-181">Najpierw kod tworzy obiekt `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="7d7ba-182">Ten obiekt jest używany do zarządzania danymi, które są odczytywane z pliku XML i organizuje je zgodnie z informacjami w pliku schematu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="7d7ba-183">(Zauważ, że górna część kodu zawiera instrukcję `using SystemData`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="7d7ba-184">Jest to wymagane w celu umożliwienia pracy z obiektem `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="7d7ba-185">Aby uzyskać więcej informacji, zobacz [&quot;przy użyciu instrukcji&quot; i w pełni kwalifikowanych nazw](#SB_UsingStatements) w dalszej części tego artykułu.)</span><span class="sxs-lookup"><span data-stu-id="7d7ba-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="7d7ba-186">Następnie kod tworzy obiekt `DataView` na podstawie zestawu danych.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="7d7ba-187">Widok danych zawiera obiekt, który można powiązać &#8212; z wykresem, czyli odczyt i wykres.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="7d7ba-188">Wykres wiąże się z danymi przy użyciu metody `AddSeries`, jak pokazano wcześniej podczas tworzenia wykresów danych tablicy, z tą różnicą, że w tym czasie parametry `xValue` i `yValues` są ustawione na obiekt `DataView`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="7d7ba-189">Ten przykład pokazuje również, jak określić konkretny typ wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="7d7ba-190">Gdy dane zostaną dodane w metodzie `AddSeries`, parametr `chartType` jest również ustawiany do wyświetlania wykresu kołowego.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="7d7ba-191">Uruchom stronę w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP]
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="7d7ba-192">Instrukcje "Using" i w pełni kwalifikowane nazwy</span><span class="sxs-lookup"><span data-stu-id="7d7ba-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="7d7ba-193">.NET Framework, które ASP.NET strony sieci Web z składnia Razor opierają się na wielu tysiącach składników (klas).</span><span class="sxs-lookup"><span data-stu-id="7d7ba-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="7d7ba-194">Aby można było zarządzać nimi w celu współpracy ze wszystkimi tymi klasami, są one zorganizowane w *przestrzenie nazw*, które są nieco podobne do bibliotek.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="7d7ba-195">Na przykład przestrzeń nazw `System.Web` zawiera klasy, które obsługują komunikację przeglądarki/serwera, `System.Xml` przestrzeń nazw zawiera klasy, które są używane do tworzenia i odczytywania plików XML, a przestrzeń nazw `System.Data` zawiera klasy umożliwiające pracy z danymi.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="7d7ba-196">Aby uzyskać dostęp do każdej klasy w .NET Framework, kod musi znać nie tylko nazwę klasy, ale również przestrzeń nazw, w której znajduje się Klasa.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="7d7ba-197">Na przykład aby można było użyć pomocnika `Chart`, kod musi znaleźć klasę `System.Web.Helpers.Chart`, która łączy przestrzeń nazw (`System.Web.Helpers`) z nazwą klasy (`Chart`).</span><span class="sxs-lookup"><span data-stu-id="7d7ba-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="7d7ba-198">Nazywa się to w *pełni kwalifikowaną* nazwą &#8212; klasy kompletną, niejednoznaczną lokalizację w ramach ogromnej części .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="7d7ba-199">W kodzie, będzie wyglądać następująco:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="7d7ba-200">Jednak jest to nieskomplikowana (i podatna na błędy), aby używać tych długich, w pełni kwalifikowanych nazw, za każdym razem, gdy chcesz odwołać się do klasy lub pomocnika.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="7d7ba-201">W związku z tym, aby ułatwić korzystanie z nazw klas, można *zaimportować* odpowiednie przestrzenie nazw, które zwykle są kilku spośród wielu przestrzeni nazw w .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="7d7ba-202">Jeśli zaimportowano przestrzeń nazw, można użyć tylko nazwy klasy (`Chart`) zamiast w pełni kwalifikowanej nazwy (`System.Web.Helpers.Chart`).</span><span class="sxs-lookup"><span data-stu-id="7d7ba-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="7d7ba-203">Gdy kod jest uruchamiany i napotyka nazwę klasy, może wyszukać tylko te przestrzenie nazw, które zostały zaimportowane, aby znaleźć tę klasę.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="7d7ba-204">Gdy używasz stron sieci Web ASP.NET z składnia Razor do tworzenia stron sieci Web, zwykle używasz tego samego zestawu klas za każdym razem, łącznie z klasą `WebPage`, różnymi pomocnikami i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="7d7ba-205">Aby zapisać zadania importowania odpowiednich przestrzeni nazw za każdym razem, gdy tworzysz witrynę sieci Web, ASP.NET jest skonfigurowany tak, aby automatycznie importował zestaw podstawowych przestrzeni nazw dla każdej witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="7d7ba-206">To dlatego, że nie musisz już zajmować się przestrzenią nazw lub zaimportować do tej pory. wszystkie klasy, z którymi pracowałeś, znajdują się w przestrzeniach nazw, które zostały już zaimportowane.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="7d7ba-207">Jednak czasami trzeba będzie współpracować z klasą, która nie znajduje się w przestrzeni nazw, która jest automatycznie importowana.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="7d7ba-208">W takim przypadku można użyć w pełni kwalifikowanej nazwy klasy lub można ręcznie zaimportować przestrzeń nazw, która zawiera klasę.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="7d7ba-209">Aby zaimportować przestrzeń nazw, należy użyć instrukcji `using` (`import` w Visual Basic), jak pokazano w przykładzie wcześniej w artykule.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="7d7ba-210">Na przykład Klasa `DataSet` znajduje się w przestrzeni nazw `System.Data`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="7d7ba-211">Przestrzeń nazw `System.Data` nie jest automatycznie dostępna do ASP.NET stron Razor.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="7d7ba-212">W związku z tym, aby współpracować z klasą `DataSet` przy użyciu jej w pełni kwalifikowanej nazwy, można użyć kodu w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="7d7ba-213">Jeśli musisz wielokrotnie używać klasy `DataSet`, możesz zaimportować przestrzeń nazw w taki sposób, a następnie użyć samej nazwy klasy w kodzie:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="7d7ba-214">Możesz dodać instrukcje `using` dla wszystkich innych przestrzeni nazw .NET Framework, do których chcesz się odwołać.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="7d7ba-215">Jednak nie trzeba tego robić często, ponieważ większość klas, z których będziesz korzystać, znajduje się w przestrzeniach nazw, które są importowane automatycznie przez ASP.NET do użycia na stronach *. cshtml* i *. vbhtml* .</span><span class="sxs-lookup"><span data-stu-id="7d7ba-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>

<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="7d7ba-216">Wyświetlanie wykresów wewnątrz strony sieci Web</span><span class="sxs-lookup"><span data-stu-id="7d7ba-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="7d7ba-217">W przykładach, które były już widoczne, tworzysz wykres, a następnie wykres jest renderowany bezpośrednio w przeglądarce jako ilustracja.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="7d7ba-218">W wielu przypadkach, mimo że chcesz wyświetlić wykres jako część strony, a nie tylko w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="7d7ba-219">Aby to zrobić, wymaga dwuetapowego procesu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-219">To do that requires a two-step process.</span></span> <span data-ttu-id="7d7ba-220">Pierwszym krokiem jest utworzenie strony, która generuje wykres, jak już widzisz.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="7d7ba-221">Drugim krokiem jest wyświetlenie uzyskanego obrazu na innej stronie.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="7d7ba-222">Aby wyświetlić obraz, należy użyć elementu HTML `<img>` w taki sam sposób jak w przypadku wyświetlania dowolnego obrazu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="7d7ba-223">Jednak zamiast odwoływania się do pliku *jpg* lub *png* , element `<img>` odwołuje się do pliku *. cshtml* , który zawiera `Chart` pomocnika, który tworzy wykres.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="7d7ba-224">Po uruchomieniu strony wyświetlania element `<img>` pobiera dane wyjściowe pomocnika `Chart` i renderuje wykres.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="7d7ba-225">Utwórz plik o nazwie *ShowChart. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="7d7ba-226">Zastąp istniejącą zawartość następującym:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="7d7ba-227">Kod używa elementu `<img>`, aby wyświetlić wykres utworzony wcześniej w pliku *ChartArrayBasic. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="7d7ba-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="7d7ba-228">Uruchom stronę sieci Web w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-228">Run the web page in a browser.</span></span> <span data-ttu-id="7d7ba-229">Plik *ShowChart. cshtml* wyświetla obraz wykresu na podstawie kodu zawartego w pliku *ChartArrayBasic. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="7d7ba-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="7d7ba-230">Określanie stylu wykresu</span><span class="sxs-lookup"><span data-stu-id="7d7ba-230">Styling a Chart</span></span>

<span data-ttu-id="7d7ba-231">Pomocnik `Chart` obsługuje dużą liczbę opcji, które umożliwiają dostosowanie wyglądu wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="7d7ba-232">Można ustawić kolory, czcionki, obramowania i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="7d7ba-233">Prostą metodą dostosowywania wyglądu wykresu jest użycie *motywu*.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="7d7ba-234">Motywy to kolekcje informacji określających sposób renderowania wykresu przy użyciu czcionek, kolorów, etykiet, palet, obramowania i efektów.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="7d7ba-235">(Należy zauważyć, że styl wykresu nie wskazuje na typ wykresu).</span><span class="sxs-lookup"><span data-stu-id="7d7ba-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="7d7ba-236">Poniższa tabela zawiera listę wbudowanych motywów.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="7d7ba-237">Tematów</span><span class="sxs-lookup"><span data-stu-id="7d7ba-237">Theme</span></span> | <span data-ttu-id="7d7ba-238">Opis</span><span class="sxs-lookup"><span data-stu-id="7d7ba-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="7d7ba-239">Wyświetla czerwone kolumny na białym tle.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="7d7ba-240">Wyświetla niebieskie kolumny na niebieskim tle gradientowym.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="7d7ba-241">Wyświetla niebieskie kolumny na zielonym tle gradientowym.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="7d7ba-242">Wyświetla pomarańczowe kolumny na żółtym tle gradientu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="7d7ba-243">Wyświetla czerwone kolumny trójwymiarowe na białym tle.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="7d7ba-244">Możesz określić motyw, który ma być używany podczas tworzenia nowego wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="7d7ba-245">Utwórz nowy plik o nazwie *ChartStyleGreen. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="7d7ba-246">Zastąp istniejącą zawartość na stronie następującymi:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="7d7ba-247">Ten kod jest taki sam jak w poprzednim przykładzie, który używa bazy danych dla danych, ale dodaje parametr `theme` podczas tworzenia obiektu `Chart`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="7d7ba-248">Poniżej przedstawiono zmieniony kod:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="7d7ba-249">Uruchom stronę w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-249">Run the page in a browser.</span></span> <span data-ttu-id="7d7ba-250">Zobaczysz te same dane jak wcześniej, ale wykres wygląda bardziej dopracowany:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="7d7ba-251">Zapisywanie wykresu</span><span class="sxs-lookup"><span data-stu-id="7d7ba-251">Saving a Chart</span></span>

<span data-ttu-id="7d7ba-252">W przypadku korzystania z pomocnika `Chart`, jak dotąd zostało to zrobione w tym artykule, pomocnik odtworzy wykres od początku przy każdym wywołaniu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="7d7ba-253">W razie potrzeby kod wykresu również wykonuje ponowne zapytania do bazy danych lub odczytuje plik XML w celu uzyskania danych.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="7d7ba-254">W niektórych przypadkach może to być złożona operacja, na przykład jeśli baza danych, która jest używana do wykonywania zapytań, jest duża, lub jeśli plik XML zawiera wiele danych.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="7d7ba-255">Nawet jeśli wykres nie obejmuje dużej ilości danych, proces dynamicznego tworzenia obrazu pobiera zasoby serwera, a wiele osób żąda strony lub stron, na których jest wyświetlany wykres, może mieć wpływ na wydajność witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="7d7ba-256">Aby pomóc w zmniejszeniu potencjalnego wpływu na wydajność tworzenia wykresu, można utworzyć wykres, gdy będzie on potrzebny po raz pierwszy, a następnie zapisać go.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="7d7ba-257">Gdy wykres będzie ponownie wymagany, a nie jego wygenerowanie, można po prostu pobrać zapisaną wersję i renderować ją.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="7d7ba-258">Wykres można zapisać w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="7d7ba-259">Buforowanie wykresu w pamięci komputera (na serwerze).</span><span class="sxs-lookup"><span data-stu-id="7d7ba-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="7d7ba-260">Zapisz wykres jako plik obrazu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="7d7ba-261">Zapisz wykres jako plik XML.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-261">Save the chart as an XML file.</span></span> <span data-ttu-id="7d7ba-262">Ta opcja umożliwia zmodyfikowanie wykresu przed jego zapisaniem.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="7d7ba-263">Buforowanie wykresu</span><span class="sxs-lookup"><span data-stu-id="7d7ba-263">Caching a Chart</span></span>

<span data-ttu-id="7d7ba-264">Po utworzeniu wykresu można go buforować.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="7d7ba-265">Buforowanie wykresu oznacza, że nie trzeba go ponownie tworzyć, jeśli trzeba będzie go ponownie wyświetlić.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="7d7ba-266">Gdy zapisujesz wykres w pamięci podręcznej, nadajesz mu klucz, który musi być unikatowy dla tego wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="7d7ba-267">Wykresy zapisane w pamięci podręcznej mogą zostać usunięte, jeśli na serwerze jest za mało pamięci.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="7d7ba-268">Ponadto pamięć podręczna jest czyszczona, jeśli aplikacja zostanie ponownie uruchomiona z dowolnego powodu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="7d7ba-269">W związku z tym standardowym sposobem pracy z buforowanym wykresem jest zawsze sprawdzenie, czy jest on dostępny w pamięci podręcznej, a jeśli nie, wówczas można go utworzyć lub utworzyć ponownie.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="7d7ba-270">W katalogu głównym witryny sieci Web Utwórz plik o nazwie *ShowCachedChart. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="7d7ba-271">Zastąp istniejącą zawartość następującym:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="7d7ba-272">Tag `<img>` zawiera atrybut `src`, który wskazuje na plik *ChartSaveToCache. cshtml* i przekazuje klucz do strony jako ciąg zapytania.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="7d7ba-273">Klucz zawiera wartość &quot;myChartKey&quot;.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="7d7ba-274">Plik *ChartSaveToCache. cshtml* zawiera pomocnika `Chart`, który tworzy wykres.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="7d7ba-275">Ta strona zostanie utworzona w chwilę.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="7d7ba-276">Na końcu strony znajduje się link do strony o nazwie *ClearCache. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="7d7ba-277">To jest strona, którą utworzysz również wkrótce.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="7d7ba-278">Dla tego przykładu potrzebujesz tylko *ClearCache. cshtml* , aby przetworzyć buforowanie. nie jest to link ani Strona, która zwykle jest uwzględniana podczas pracy z buforowanymi wykresami.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="7d7ba-279">W katalogu głównym witryny sieci Web Utwórz nowy plik o nazwie *ChartSaveToCache. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="7d7ba-280">Zastąp istniejącą zawartość następującym:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="7d7ba-281">Kod najpierw sprawdza, czy wszystkie elementy zostały przesłane jako wartość klucza w ciągu zapytania.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="7d7ba-282">Jeśli tak, kod próbuje odczytać wykres z pamięci podręcznej, wywołując metodę `GetFromCache` i przekazując klucz.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="7d7ba-283">Jeśli wyłączy się, że w pamięci podręcznej nie ma nic w tym kluczu (co się stanie po pierwszym zażądaniu wykresu), kod tworzy wykres w zwykły sposób.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="7d7ba-284">Po zakończeniu wykresu kod zapisuje go w pamięci podręcznej, wywołując `SaveToCache`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="7d7ba-285">Ta metoda wymaga klucza (aby można było żądać wykresu później) oraz czas, w którym wykres powinien być zapisany w pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="7d7ba-286">(Dokładny czas przetworzenia pamięci podręcznej wykresu zależy od tego, jak często sądzisz, że dane, które reprezentuje, mogą ulec zmianie). Metoda `SaveToCache` wymaga również, aby parametr &#8212; `slidingExpiration`, jeśli jest ustawiona na wartość true, licznik limitu czasu jest resetowany za każdym razem, gdy uzyskuje się dostęp do wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="7d7ba-287">W takim przypadku w efekcie oznacza to, że wpis pamięci podręcznej wykresu wygaśnie 2 minuty po ostatnim uzyskaniu dostępu do wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="7d7ba-288">(Alternatywą dla okresu ważności jest bezwzględne wygaśnięcie, co oznacza, że wpis pamięci podręcznej wygaśnie dokładnie 2 minuty po umieszczeniu w pamięci podręcznej, niezależnie od tego, jak często uzyskano dostęp.)</span><span class="sxs-lookup"><span data-stu-id="7d7ba-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="7d7ba-289">Na koniec kod używa metody `WriteFromCache` do pobierania i renderowania wykresu z pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="7d7ba-290">Należy zauważyć, że ta metoda jest poza blokiem `if`, który sprawdza pamięć podręczną, ponieważ pobiera wykres z pamięci podręcznej, czy wykres był w tej tabeli, czy musiał zostać wygenerowany i zapisany w pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="7d7ba-291">Zwróć uwagę, że w przykładzie metoda `AddTitle` obejmuje sygnaturę czasową.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="7d7ba-292">(Dodaje bieżącą datę i godzinę &#8212; `DateTime.Now` &#8212; do tytułu).</span><span class="sxs-lookup"><span data-stu-id="7d7ba-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="7d7ba-293">Utwórz nową stronę o nazwie *ClearCache. cshtml* i Zastąp jej zawartość następującym:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="7d7ba-294">Ta strona używa pomocnika `WebCache` do usuwania wykresu, który jest buforowany w *ChartSaveToCache. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="7d7ba-295">Jak wspomniano wcześniej, zazwyczaj nie trzeba mieć takiej strony.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="7d7ba-296">Tworzysz ją tutaj tylko, aby ułatwić testowanie pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="7d7ba-297">Uruchom stronę sieci Web *ShowCachedChart. cshtml* w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="7d7ba-298">Na stronie zostanie wyświetlony obraz wykresu na podstawie kodu zawartego w pliku *ChartSaveToCache. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="7d7ba-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="7d7ba-299">Zanotuj, jakie sygnatury czasowe widnieją w tytule wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![Opis: obraz wykresu podstawowego z sygnaturą czasową w tytule wykresu](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="7d7ba-301">Zamknij okno przeglądarki.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-301">Close the browser.</span></span>
8. <span data-ttu-id="7d7ba-302">Ponownie uruchom *ShowCachedChart. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="7d7ba-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="7d7ba-303">Należy zauważyć, że sygnatura czasowa jest taka sama jak przed, co oznacza, że wykres nie został wygenerowany ponownie, ale zamiast tego jest odczytywany z pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="7d7ba-304">W *ShowCachedChart. cshtml*kliknij link **Wyczyść pamięć podręczną** .</span><span class="sxs-lookup"><span data-stu-id="7d7ba-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="7d7ba-305">Spowoduje to przejście do *ClearCache. cshtml*, które informuje, że pamięć podręczna została wyczyszczona.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="7d7ba-306">Kliknij link **Wróć do ShowCachedChart. cshtml** lub ponownie uruchom *ShowCachedChart. cshtml* w programie WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="7d7ba-307">Należy zauważyć, że ta godzina została zmieniona, ponieważ pamięć podręczna została wyczyszczona.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="7d7ba-308">W związku z tym kod musiał ponownie wygenerować wykres i umieścić go z powrotem w pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="7d7ba-309">Zapisywanie wykresu jako pliku obrazu</span><span class="sxs-lookup"><span data-stu-id="7d7ba-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="7d7ba-310">Wykres można również zapisać jako plik obrazu (na przykład jako plik *jpg* ) na serwerze.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="7d7ba-311">Następnie możesz użyć pliku obrazu w taki sam sposób, jak w przypadku dowolnego obrazu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="7d7ba-312">Zalety polega na tym, że plik jest przechowywany zamiast zapisywać w tymczasowej pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="7d7ba-313">Nowy obraz wykresu można zapisać w różnych godzinach (na przykład co godzinę), a następnie zachować stały rekord zmian, które wystąpiły w czasie.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="7d7ba-314">Pamiętaj, że musisz upewnić się, że aplikacja sieci Web ma uprawnienia do zapisywania pliku w folderze na serwerze, na którym chcesz umieścić plik obrazu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="7d7ba-315">W katalogu głównym witryny sieci Web Utwórz folder o nazwie *\_ChartFiles* , jeśli jeszcze nie istnieje.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="7d7ba-316">W katalogu głównym witryny sieci Web Utwórz nowy plik o nazwie *ChartSave. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="7d7ba-317">Zastąp istniejącą zawartość następującym:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="7d7ba-318">Kod najpierw sprawdza, czy plik *. jpg* istnieje, wywołując metodę `File.Exists`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="7d7ba-319">Jeśli plik nie istnieje, kod tworzy nowy `Chart` z tablicy.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="7d7ba-320">Tym razem kod wywołuje metodę `Save` i przekazuje parametr `path`, aby określić ścieżkę pliku i nazwę pliku, w którym ma zostać zapisany wykres.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="7d7ba-321">W treści strony element `<img>` używa ścieżki, aby wskazać plik *. jpg* do wyświetlenia.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="7d7ba-322">Uruchom plik *ChartSave. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="7d7ba-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="7d7ba-323">Wróć do WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-323">Return to WebMatrix.</span></span> <span data-ttu-id="7d7ba-324">Zwróć uwagę, że plik obrazu o nazwie *chart01. jpg* został zapisany w folderze *\_ChartFiles* .</span><span class="sxs-lookup"><span data-stu-id="7d7ba-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="7d7ba-325">Zapisywanie wykresu jako pliku XML</span><span class="sxs-lookup"><span data-stu-id="7d7ba-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="7d7ba-326">Na koniec można zapisać wykres jako plik XML na serwerze.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="7d7ba-327">Zaletą użycia tej metody w celu przełączenia wykresu lub zapisania wykresu w pliku jest możliwość modyfikacji kodu XML przed wyświetleniem wykresu, jeśli chcesz.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="7d7ba-328">Aplikacja musi mieć uprawnienia do odczytu/zapisu do folderu na serwerze, na którym ma zostać umieszczony plik obrazu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="7d7ba-329">W katalogu głównym witryny sieci Web Utwórz nowy plik o nazwie *ChartSaveXml. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="7d7ba-330">Zastąp istniejącą zawartość następującym:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="7d7ba-331">Ten kod jest podobny do kodu, który został wcześniej wyświetlony do przechowywania wykresu w pamięci podręcznej, z tą różnicą, że używa pliku XML.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="7d7ba-332">Kod najpierw sprawdza, czy plik XML istnieje, wywołując metodę `File.Exists`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="7d7ba-333">Jeśli plik istnieje, kod tworzy nowy obiekt `Chart` i przekazuje nazwę pliku jako parametr `themePath`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="7d7ba-334">Spowoduje to utworzenie wykresu na podstawie dowolnego elementu w pliku XML.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="7d7ba-335">Jeśli plik XML jeszcze nie istnieje, kod tworzy wykres, taki jak normalny, a następnie wywołuje `SaveXml`, aby go zapisać.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="7d7ba-336">Wykres jest renderowany przy użyciu metody `Write`, tak jak wcześniej.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="7d7ba-337">Podobnie jak w przypadku strony, która przedstawia buforowanie, ten kod zawiera sygnaturę czasową w tytule wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="7d7ba-338">Utwórz nową stronę o nazwie *ChartDisplayXMLChart. cshtml* i Dodaj do niej następujący znacznik:</span><span class="sxs-lookup"><span data-stu-id="7d7ba-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="7d7ba-339">Uruchom stronę *ChartDisplayXMLChart. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="7d7ba-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="7d7ba-340">Zostanie wyświetlony wykres.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-340">The chart is displayed.</span></span> <span data-ttu-id="7d7ba-341">Zanotuj sygnaturę czasową w tytule wykresu.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="7d7ba-342">Zamknij okno przeglądarki.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-342">Close the browser.</span></span>
6. <span data-ttu-id="7d7ba-343">W programie WebMatrix kliknij prawym przyciskiem myszy folder *\_ChartFiles* , kliknij przycisk **Odśwież**, a następnie otwórz folder.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="7d7ba-344">Plik *xmlchart. XML* w tym folderze został utworzony przez pomocnika `Chart`.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![Opis: folder _ChartFiles przedstawiający plik xmlchart. XML utworzony przez pomocnika wykresu.](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="7d7ba-346">Ponownie uruchom stronę *ChartDisplayXMLChart. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="7d7ba-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="7d7ba-347">Na wykresie jest wyświetlana taka sama sygnatura czasowa pierwszego uruchomienia strony.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="7d7ba-348">Dzieje się tak, ponieważ wykres jest generowany na podstawie zapisanego wcześniej kodu XML.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="7d7ba-349">W programie WebMatrix otwórz folder *\_ChartFiles* i Usuń plik *xmlchart. XML* .</span><span class="sxs-lookup"><span data-stu-id="7d7ba-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="7d7ba-350">Uruchom ponownie stronę *ChartDisplayXMLChart. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="7d7ba-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="7d7ba-351">Tym razem sygnatura czasowa jest aktualizowana, ponieważ pomocnik `Chart` musiał odtworzyć plik XML.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="7d7ba-352">Jeśli chcesz, sprawdź folder *\_ChartFiles* i zwróć uwagę na to, że plik XML jest ponownie.</span><span class="sxs-lookup"><span data-stu-id="7d7ba-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="7d7ba-353">Dodatkowe materiały</span><span class="sxs-lookup"><span data-stu-id="7d7ba-353">Additional Resources</span></span>

- [<span data-ttu-id="7d7ba-354">Wprowadzenie do pracy z bazą danych w witrynach stron sieci Web ASP.NET</span><span class="sxs-lookup"><span data-stu-id="7d7ba-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="7d7ba-355">Korzystanie z buforowania w witrynach stron sieci Web ASP.NET w celu zwiększenia wydajności</span><span class="sxs-lookup"><span data-stu-id="7d7ba-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="7d7ba-356">[Klasa wykresu](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (Dokumentacja interfejsu API stron sieci Web ASP.NET w witrynie MSDN)</span><span class="sxs-lookup"><span data-stu-id="7d7ba-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
