---
uid: web-pages/overview/data/working-with-files
title: Praca z plikami w witrynie ASP.NET Web Pages (Razor) | Microsoft Docs
author: Rick-Anderson
description: W tym rozdziale wyjaśniono, jak odczytywać, zapisywać, dołączać, usuwać i ładować pliki.
ms.author: riande
ms.date: 02/20/2014
ms.assetid: eee916e4-ba4c-439a-a24e-68df7d45a569
msc.legacyurl: /web-pages/overview/data/working-with-files
msc.type: authoredcontent
ms.openlocfilehash: 684c47a8a8480dc040e5144144577c94c35d39e5
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/06/2020
ms.locfileid: "78642367"
---
# <a name="working-with-files-in-an-aspnet-web-pages-razor-site"></a><span data-ttu-id="59551-103">Praca z plikami w witrynie ASP.NET Web Pages (Razor)</span><span class="sxs-lookup"><span data-stu-id="59551-103">Working with Files in an ASP.NET Web Pages (Razor) Site</span></span>

<span data-ttu-id="59551-104">Autor [FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="59551-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="59551-105">W tym artykule opisano sposób odczytywania, zapisywania, dołączania, usuwania i przekazywania plików w witrynie ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="59551-105">This article explains how to read, write, append, delete, and upload files in an ASP.NET Web Pages (Razor) site.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="59551-106">Jeśli chcesz przekazać obrazy i manipulować nimi (na przykład przerzucić lub zmienić ich rozmiar), zobacz [Praca z obrazami w witrynie ASP.NET Web Pages](/aspnet/web-pages/overview/ui-layouts-and-themes/9-working-with-images).</span><span class="sxs-lookup"><span data-stu-id="59551-106">If you want to upload images and manipulate them (for example, flip or resize them), see [Working with Images in an ASP.NET Web Pages Site](/aspnet/web-pages/overview/ui-layouts-and-themes/9-working-with-images).</span></span>
> 
> 
> <span data-ttu-id="59551-107">**Dowiesz się:**</span><span class="sxs-lookup"><span data-stu-id="59551-107">**What you'll learn:**</span></span> 
> 
> - <span data-ttu-id="59551-108">Jak utworzyć plik tekstowy i zapisać w nim dane.</span><span class="sxs-lookup"><span data-stu-id="59551-108">How to create a text file and write data to it.</span></span>
> - <span data-ttu-id="59551-109">Jak dołączyć dane do istniejącego pliku.</span><span class="sxs-lookup"><span data-stu-id="59551-109">How to append data to an existing file.</span></span>
> - <span data-ttu-id="59551-110">Jak odczytać plik i wyświetlić go.</span><span class="sxs-lookup"><span data-stu-id="59551-110">How to read a file and display from it.</span></span>
> - <span data-ttu-id="59551-111">Jak usunąć pliki z witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="59551-111">How to delete files from a website.</span></span>
> - <span data-ttu-id="59551-112">Jak umożliwić użytkownikom przekazywanie jednego pliku lub wielu plików.</span><span class="sxs-lookup"><span data-stu-id="59551-112">How to let users upload one file or multiple files.</span></span>
> 
> <span data-ttu-id="59551-113">Są to funkcje programowania ASP.NET wprowadzone w artykule:</span><span class="sxs-lookup"><span data-stu-id="59551-113">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="59551-114">Obiekt `File`, który zapewnia sposób zarządzania plikami.</span><span class="sxs-lookup"><span data-stu-id="59551-114">The `File` object, which provides a way to manage files.</span></span>
> - <span data-ttu-id="59551-115">Pomocnik `FileUpload`.</span><span class="sxs-lookup"><span data-stu-id="59551-115">The `FileUpload` helper.</span></span>
> - <span data-ttu-id="59551-116">Obiekt `Path`, który dostarcza metody, które umożliwiają manipulowanie ścieżkami i nazwami plików.</span><span class="sxs-lookup"><span data-stu-id="59551-116">The `Path` object, which provides methods that let you manipulate path and file names.</span></span>
>   
> 
> ## <a name="software-versions-used-in-the-tutorial"></a><span data-ttu-id="59551-117">Wersje oprogramowania używane w samouczku</span><span class="sxs-lookup"><span data-stu-id="59551-117">Software versions used in the tutorial</span></span>
> 
> 
> - <span data-ttu-id="59551-118">ASP.NET strony sieci Web (Razor) 2</span><span class="sxs-lookup"><span data-stu-id="59551-118">ASP.NET Web Pages (Razor) 2</span></span>
> - <span data-ttu-id="59551-119">WebMatrix 2</span><span class="sxs-lookup"><span data-stu-id="59551-119">WebMatrix 2</span></span>
>   
> 
> <span data-ttu-id="59551-120">Ten samouczek współpracuje również z programem WebMatrix 3.</span><span class="sxs-lookup"><span data-stu-id="59551-120">This tutorial also works with WebMatrix 3.</span></span>

<a id="Creating_a_Text_File"></a>
## <a name="creating-a-text-file-and-writing-data-to-it"></a><span data-ttu-id="59551-121">Tworzenie pliku tekstowego i zapisywanie w nim danych</span><span class="sxs-lookup"><span data-stu-id="59551-121">Creating a Text File and Writing Data to It</span></span>

<span data-ttu-id="59551-122">Oprócz korzystania z bazy danych w witrynie sieci Web, możesz współpracować z plikami.</span><span class="sxs-lookup"><span data-stu-id="59551-122">In addition to using a database in your website, you might work with files.</span></span> <span data-ttu-id="59551-123">Można na przykład używać plików tekstowych jako prostego sposobu przechowywania danych dla witryny.</span><span class="sxs-lookup"><span data-stu-id="59551-123">For example, you might use text files as a simple way to store data for the site.</span></span> <span data-ttu-id="59551-124">(Plik tekstowy, który jest używany do przechowywania danych, jest czasami nazywany *plikiem prostym*). Pliki tekstowe mogą znajdować się w różnych formatach, takich jak *txt*, *XML*lub *CSV* (wartości rozdzielane przecinkami).</span><span class="sxs-lookup"><span data-stu-id="59551-124">(A text file that's used to store data is sometimes called a *flat file*.) Text files can be in different formats, like *.txt*, *.xml*, or *.csv* (comma-delimited values).</span></span>

<span data-ttu-id="59551-125">Jeśli chcesz przechowywać dane w pliku tekstowym, możesz użyć metody `File.WriteAllText`, aby określić plik do utworzenia oraz dane, które mają być w nim zapisane.</span><span class="sxs-lookup"><span data-stu-id="59551-125">If you want to store data in a text file, you can use the `File.WriteAllText` method to specify the file to create and the data to write to it.</span></span> <span data-ttu-id="59551-126">W tej procedurze utworzysz stronę zawierającą prosty formularz z trzema `input` elementami (imię, nazwisko i adres e-mail) i przycisk **Prześlij** .</span><span class="sxs-lookup"><span data-stu-id="59551-126">In this procedure, you'll create a page that contains a simple form with three `input` elements (first name, last name, and email address) and a **Submit** button.</span></span> <span data-ttu-id="59551-127">Po przesłaniu formularza użytkownik będzie przechowywał dane wejściowe użytkownika w pliku tekstowym.</span><span class="sxs-lookup"><span data-stu-id="59551-127">When the user submits the form, you'll store the user's input in a text file.</span></span>

1. <span data-ttu-id="59551-128">Utwórz nowy folder o nazwie *App\_Data*, jeśli jeszcze nie istnieje.</span><span class="sxs-lookup"><span data-stu-id="59551-128">Create a new folder named *App\_Data*, if it doesn't exist already.</span></span>
2. <span data-ttu-id="59551-129">W katalogu głównym witryny sieci Web Utwórz nowy plik o nazwie *UserData. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="59551-129">At the root of your website, create a new file named *UserData.cshtml*.</span></span>
3. <span data-ttu-id="59551-130">Zastąp istniejącą zawartość następującym:</span><span class="sxs-lookup"><span data-stu-id="59551-130">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](working-with-files/samples/sample1.cshtml)]

    <span data-ttu-id="59551-131">Znacznik HTML tworzy formularz z trzema polami tekstowymi.</span><span class="sxs-lookup"><span data-stu-id="59551-131">The HTML markup creates the form with the three text boxes.</span></span> <span data-ttu-id="59551-132">W kodzie można użyć właściwości `IsPost`, aby określić, czy strona została przesłana przed rozpoczęciem przetwarzania.</span><span class="sxs-lookup"><span data-stu-id="59551-132">In the code, you use the `IsPost` property to determine whether the page has been submitted before you start processing.</span></span>

    <span data-ttu-id="59551-133">Pierwsze zadanie polega na uzyskaniu danych wejściowych użytkownika i przypisaniu go do zmiennych.</span><span class="sxs-lookup"><span data-stu-id="59551-133">The first task is to get the user input and assign it to variables.</span></span> <span data-ttu-id="59551-134">Następnie kod łączy wartości oddzielnych zmiennych w jeden ciąg rozdzielany przecinkami, który jest następnie przechowywany w innej zmiennej.</span><span class="sxs-lookup"><span data-stu-id="59551-134">The code then concatenates the values of the separate variables into one comma-delimited string, which is then stored in a different variable.</span></span> <span data-ttu-id="59551-135">Należy zauważyć, że separator przecinka jest ciągiem zawartym w cudzysłowie (","), ponieważ jest to dosłownie osadzenie przecinka w ciągu, który tworzysz.</span><span class="sxs-lookup"><span data-stu-id="59551-135">Notice that the comma separator is a string contained in quotation marks (","), because you're literally embedding a comma into the big string that you're creating.</span></span> <span data-ttu-id="59551-136">Na końcu połączonych danych należy dodać `Environment.NewLine`.</span><span class="sxs-lookup"><span data-stu-id="59551-136">At the end of the data that you concatenate together, you add `Environment.NewLine`.</span></span> <span data-ttu-id="59551-137">Spowoduje to dodanie podziału wiersza (znaku nowego wiersza).</span><span class="sxs-lookup"><span data-stu-id="59551-137">This adds a line break (a newline character).</span></span> <span data-ttu-id="59551-138">To, co tworzysz przy użyciu wszystkich takich złączek, jest ciągiem, który wygląda następująco:</span><span class="sxs-lookup"><span data-stu-id="59551-138">What you're creating with all this concatenation is a string that looks like this:</span></span>

    [!code-css[Main](working-with-files/samples/sample2.css)]

    <span data-ttu-id="59551-139">(Z niewidocznym podziałem wiersza na końcu).</span><span class="sxs-lookup"><span data-stu-id="59551-139">(With an invisible line break at the end.)</span></span>

    <span data-ttu-id="59551-140">Następnie utworzysz zmienną (`dataFile`), która zawiera lokalizację i nazwę pliku, w którym będą przechowywane dane.</span><span class="sxs-lookup"><span data-stu-id="59551-140">You then create a variable (`dataFile`) that contains the location and name of the file to store the data in.</span></span> <span data-ttu-id="59551-141">Ustawienie lokalizacji wymaga szczególnej obsługi.</span><span class="sxs-lookup"><span data-stu-id="59551-141">Setting the location requires some special handling.</span></span> <span data-ttu-id="59551-142">W witrynach sieci Web jest to niewłaściwe rozwiązanie do odwoływania się w kodzie do ścieżek bezwzględnych, takich jak *C:\Folder\File.txt* dla plików na serwerze sieci Web.</span><span class="sxs-lookup"><span data-stu-id="59551-142">In websites, it's a bad practice to refer in code to absolute paths like *C:\Folder\File.txt* for files on the web server.</span></span> <span data-ttu-id="59551-143">Jeśli witryna sieci Web jest przenoszona, ścieżka bezwzględna będzie niewłaściwa.</span><span class="sxs-lookup"><span data-stu-id="59551-143">If a website is moved, an absolute path will be wrong.</span></span> <span data-ttu-id="59551-144">Ponadto w przypadku witryny hostowanej (w przeciwieństwie do komputera) zazwyczaj nie wiadomo, co jest poprawna ścieżka podczas pisania kodu.</span><span class="sxs-lookup"><span data-stu-id="59551-144">Moreover, for a hosted site (as opposed to on your own computer) you typically don't even know what the correct path is when you're writing the code.</span></span>

    <span data-ttu-id="59551-145">Jednak czasami (podobnie jak w przypadku pisania pliku) potrzebna jest kompletna ścieżka.</span><span class="sxs-lookup"><span data-stu-id="59551-145">But sometimes (like now, for writing a file) you do need a complete path.</span></span> <span data-ttu-id="59551-146">Rozwiązaniem jest użycie metody `MapPath` obiektu `Server`.</span><span class="sxs-lookup"><span data-stu-id="59551-146">The solution is to use the `MapPath` method of the `Server` object.</span></span> <span data-ttu-id="59551-147">Spowoduje to zwrócenie pełnej ścieżki do witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="59551-147">This returns the complete path to your website.</span></span> <span data-ttu-id="59551-148">Aby uzyskać ścieżkę do katalogu głównego witryny sieci Web, użytkownik może korzystać z operatora `~` (w celu represen wirtualnego katalogu głównego witryny) `MapPath`.</span><span class="sxs-lookup"><span data-stu-id="59551-148">To get the path for the website root, you user the `~` operator (to represen the site's virtual root) to `MapPath`.</span></span> <span data-ttu-id="59551-149">(Można również przekazać do niego nazwę podfolderu, na przykład *~/App\_Data/* , aby uzyskać ścieżkę dla tego podfolderu). Następnie można połączyć dodatkowe informacje z dowolną metodą zwracaną w celu utworzenia pełnej ścieżki.</span><span class="sxs-lookup"><span data-stu-id="59551-149">(You can also pass a subfolder name to it, like *~/App\_Data/*, to get the path for that subfolder.) You can then concatenate additional information onto whatever the method returns in order to create a complete path.</span></span> <span data-ttu-id="59551-150">W tym przykładzie należy dodać nazwę pliku.</span><span class="sxs-lookup"><span data-stu-id="59551-150">In this example, you add a file name.</span></span> <span data-ttu-id="59551-151">(Więcej informacji na temat sposobu pracy z ścieżkami plików i folderów można znaleźć w artykule [wprowadzenie do programowania ASP.NET stron sieci Web przy użyciu składni Razor](https://go.microsoft.com/fwlink/?LinkId=195205#ID_WorkingWithFileAndFolderPaths)).</span><span class="sxs-lookup"><span data-stu-id="59551-151">(You can read more about how to work with file and folder paths in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=195205#ID_WorkingWithFileAndFolderPaths).)</span></span>

    <span data-ttu-id="59551-152">Plik jest zapisywany w folderze *danych\_aplikacji* .</span><span class="sxs-lookup"><span data-stu-id="59551-152">The file is saved in the *App\_Data* folder.</span></span> <span data-ttu-id="59551-153">Ten folder jest specjalnym folderem w ASP.NET, który jest używany do przechowywania plików danych, zgodnie z opisem w artykule [wprowadzenie do pracy z bazą danych w witrynach sieci Web ASP.NET](https://go.microsoft.com/fwlink/?LinkId=195209).</span><span class="sxs-lookup"><span data-stu-id="59551-153">This folder is a special folder in ASP.NET that's used to store data files, as described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=195209).</span></span>

    <span data-ttu-id="59551-154">Metoda `WriteAllText` obiektu `File` zapisuje dane do pliku.</span><span class="sxs-lookup"><span data-stu-id="59551-154">The `WriteAllText` method of the `File` object writes the data to the file.</span></span> <span data-ttu-id="59551-155">Ta metoda przyjmuje dwa parametry: Nazwa (ścieżka) pliku do zapisu i rzeczywiste dane do zapisu.</span><span class="sxs-lookup"><span data-stu-id="59551-155">This method takes two parameters: the name (with path) of the file to write to, and the actual data to write.</span></span> <span data-ttu-id="59551-156">Należy zauważyć, że nazwa pierwszego parametru ma `@` znak jako prefiks.</span><span class="sxs-lookup"><span data-stu-id="59551-156">Notice that the name of the first parameter has an `@` character as a prefix.</span></span> <span data-ttu-id="59551-157">Oznacza to, że ASP.NET, że podajesz Verbatim literał ciągu i że znaki takie jak "/" nie powinny być interpretowane w specjalny sposób.</span><span class="sxs-lookup"><span data-stu-id="59551-157">This tells ASP.NET that you're providing a verbatim string literal, and that characters like "/" should not be interpreted in special ways.</span></span> <span data-ttu-id="59551-158">(Aby uzyskać więcej informacji, zobacz [wprowadzenie do programowania w ASP.NET sieci Web przy użyciu składni Razor](https://go.microsoft.com/fwlink/?LinkId=195205#ID_WorkingWithFileAndFolderPaths)).</span><span class="sxs-lookup"><span data-stu-id="59551-158">(For more information, see [Introduction to ASP.NET Web Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=195205#ID_WorkingWithFileAndFolderPaths).)</span></span>

    > [!NOTE]
    > <span data-ttu-id="59551-159">Aby kod zapisywał pliki w folderze *danych\_aplikacji* , aplikacja musi mieć uprawnienia do odczytu i zapisu dla tego folderu.</span><span class="sxs-lookup"><span data-stu-id="59551-159">In order for your code to save files in the *App\_Data* folder, the application needs read-write permissions for that folder.</span></span> <span data-ttu-id="59551-160">Na komputerze deweloperskim zwykle nie jest to problem.</span><span class="sxs-lookup"><span data-stu-id="59551-160">On your development computer this is not typically an issue.</span></span> <span data-ttu-id="59551-161">Jednak po opublikowaniu lokacji programu na serwerze sieci Web dostawcy hostingu może być konieczne jawne ustawienie tych uprawnień.</span><span class="sxs-lookup"><span data-stu-id="59551-161">However, when you publish your site to a hosting provider's web server, you might need to explicitly set those permissions.</span></span> <span data-ttu-id="59551-162">Jeśli ten kod jest uruchamiany na serwerze dostawcy hostingu i pojawia się błąd, należy skontaktować się z dostawcą hostingu, aby dowiedzieć się, jak ustawić te uprawnienia.</span><span class="sxs-lookup"><span data-stu-id="59551-162">If you run this code on a hosting provider's server and get errors, check with the hosting provider to find out how to set those permissions.</span></span>

- <span data-ttu-id="59551-163">Uruchom stronę w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="59551-163">Run the page in a browser.</span></span> 

    ![](working-with-files/_static/image1.jpg)
- <span data-ttu-id="59551-164">Wprowadź wartości w polach, a następnie kliknij przycisk **Prześlij**.</span><span class="sxs-lookup"><span data-stu-id="59551-164">Enter values into the fields and then click **Submit**.</span></span>
- <span data-ttu-id="59551-165">Zamknij okno przeglądarki.</span><span class="sxs-lookup"><span data-stu-id="59551-165">Close the browser.</span></span>
- <span data-ttu-id="59551-166">Wróć do projektu i Odśwież widok.</span><span class="sxs-lookup"><span data-stu-id="59551-166">Return to the project and refresh the view.</span></span>
- <span data-ttu-id="59551-167">Otwórz plik *Data. txt* .</span><span class="sxs-lookup"><span data-stu-id="59551-167">Open the *data.txt* file.</span></span> <span data-ttu-id="59551-168">Dane przesłane w formularzu są w pliku.</span><span class="sxs-lookup"><span data-stu-id="59551-168">The data you submitted in the form is in the file.</span></span> 

    ![Image](working-with-files/_static/image2.jpg)
- <span data-ttu-id="59551-170">Zamknij plik *Data. txt* .</span><span class="sxs-lookup"><span data-stu-id="59551-170">Close the *data.txt* file.</span></span>

<a id="Appending_Data"></a>
## <a name="appending-data-to-an-existing-file"></a><span data-ttu-id="59551-171">Dołączanie danych do istniejącego pliku</span><span class="sxs-lookup"><span data-stu-id="59551-171">Appending Data to an Existing File</span></span>

<span data-ttu-id="59551-172">W poprzednim przykładzie użyto `WriteAllText`, aby utworzyć plik tekstowy, który ma tylko jedną część danych.</span><span class="sxs-lookup"><span data-stu-id="59551-172">In the previous example, you used `WriteAllText` to create a text file that's got just one piece of data in it.</span></span> <span data-ttu-id="59551-173">Jeśli wywołasz metodę ponownie i przekażesz ją do tej samej nazwy pliku, istniejący plik zostanie całkowicie nadpisany.</span><span class="sxs-lookup"><span data-stu-id="59551-173">If you call the method again and pass it the same file name, the existing file is completely overwritten.</span></span> <span data-ttu-id="59551-174">Jednak po utworzeniu pliku często należy dodać nowe dane na końcu pliku.</span><span class="sxs-lookup"><span data-stu-id="59551-174">However, after you've created a file you often want to add new data to the end of the file.</span></span> <span data-ttu-id="59551-175">Można to zrobić za pomocą metody `AppendAllText` obiektu `File`.</span><span class="sxs-lookup"><span data-stu-id="59551-175">You can do that using the `AppendAllText` method of the `File` object.</span></span>

1. <span data-ttu-id="59551-176">W witrynie sieci Web Utwórz kopię pliku *UserData. cshtml* i nadaj jej nazwę copy *UserDataMultiple. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="59551-176">In the website, make a copy of the *UserData.cshtml* file and name the copy *UserDataMultiple.cshtml*.</span></span>
2. <span data-ttu-id="59551-177">Zastąp blok kodu przed otwierającym tagiem `<!DOCTYPE html>` i następującym blokiem kodu:</span><span class="sxs-lookup"><span data-stu-id="59551-177">Replace the code block before the opening `<!DOCTYPE html>` tag with the following code block:</span></span> 

    [!code-cshtml[Main](working-with-files/samples/sample3.cshtml)]

    <span data-ttu-id="59551-178">Ten kod zawiera jedną zmianę z poprzedniego przykładu.</span><span class="sxs-lookup"><span data-stu-id="59551-178">This code has one change in it from the previous example.</span></span> <span data-ttu-id="59551-179">Zamiast używać `WriteAllText`, używa metody `the AppendAllText`.</span><span class="sxs-lookup"><span data-stu-id="59551-179">Instead of using `WriteAllText`, it uses `the AppendAllText` method.</span></span> <span data-ttu-id="59551-180">Metody są podobne, z tą różnicą, że `AppendAllText` dodaje dane na końcu pliku.</span><span class="sxs-lookup"><span data-stu-id="59551-180">The methods are similar, except that `AppendAllText` adds the data to the end of the file.</span></span> <span data-ttu-id="59551-181">Podobnie jak w przypadku `WriteAllText`, `AppendAllText` tworzy plik, jeśli jeszcze nie istnieje.</span><span class="sxs-lookup"><span data-stu-id="59551-181">As with `WriteAllText`, `AppendAllText` creates the file if it doesn't already exist.</span></span>
3. <span data-ttu-id="59551-182">Uruchom stronę w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="59551-182">Run the page in a browser.</span></span>
4. <span data-ttu-id="59551-183">Wprowadź wartości dla pól, a następnie kliknij przycisk **Prześlij**.</span><span class="sxs-lookup"><span data-stu-id="59551-183">Enter values for the fields and then click **Submit**.</span></span>
5. <span data-ttu-id="59551-184">Dodaj więcej danych i ponownie prześlij formularz.</span><span class="sxs-lookup"><span data-stu-id="59551-184">Add more data and submit the form again.</span></span>
6. <span data-ttu-id="59551-185">Wróć do projektu, kliknij prawym przyciskiem myszy folder projektu, a następnie kliknij polecenie **Odśwież**.</span><span class="sxs-lookup"><span data-stu-id="59551-185">Return to your project, right-click the project folder, and then click **Refresh**.</span></span>
7. <span data-ttu-id="59551-186">Otwórz plik *Data. txt* .</span><span class="sxs-lookup"><span data-stu-id="59551-186">Open the *data.txt* file.</span></span> <span data-ttu-id="59551-187">Teraz zawiera nowe dane, które zostały wprowadzone.</span><span class="sxs-lookup"><span data-stu-id="59551-187">It now contains the new data that you just entered.</span></span> 

    ![Image](working-with-files/_static/image3.jpg)

<a id="Reading_and_Displaying_Data"></a>
## <a name="reading-and-displaying-data-from-a-file"></a><span data-ttu-id="59551-189">Odczytywanie i wyświetlanie danych z pliku</span><span class="sxs-lookup"><span data-stu-id="59551-189">Reading and Displaying Data from a File</span></span>

<span data-ttu-id="59551-190">Nawet jeśli nie musisz pisać danych do pliku tekstowego, prawdopodobnie czasami trzeba będzie odczytywać dane z jednego z nich.</span><span class="sxs-lookup"><span data-stu-id="59551-190">Even if you don't need to write data to a text file, you'll probably sometimes need to read data from one.</span></span> <span data-ttu-id="59551-191">Aby to zrobić, możesz ponownie użyć obiektu `File`.</span><span class="sxs-lookup"><span data-stu-id="59551-191">To do this, you can again use the `File` object.</span></span> <span data-ttu-id="59551-192">Można użyć obiektu `File`, aby odczytać każdy wiersz pojedynczo (rozdzielony podziałem wierszy) lub odczytać poszczególne elementy niezależnie od tego, jak są oddzielone.</span><span class="sxs-lookup"><span data-stu-id="59551-192">You can use the `File` object to read each line individually (separated by line breaks) or to read individual item no matter how they're separated.</span></span>

<span data-ttu-id="59551-193">Ta procedura pokazuje, jak odczytywać i wyświetlać dane, które zostały utworzone w poprzednim przykładzie.</span><span class="sxs-lookup"><span data-stu-id="59551-193">This procedure shows you how to read and display the data that you created in the previous example.</span></span>

1. <span data-ttu-id="59551-194">W katalogu głównym witryny sieci Web Utwórz nowy plik o nazwie *DisplayData. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="59551-194">At the root of your website, create a new file named *DisplayData.cshtml*.</span></span>
2. <span data-ttu-id="59551-195">Zastąp istniejącą zawartość następującym:</span><span class="sxs-lookup"><span data-stu-id="59551-195">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](working-with-files/samples/sample4.cshtml)]

    <span data-ttu-id="59551-196">Kod rozpocznie się, odczytując plik, który został utworzony w poprzednim przykładzie do zmiennej o nazwie `userData`, przy użyciu tego wywołania metody:</span><span class="sxs-lookup"><span data-stu-id="59551-196">The code starts by reading the file that you created in the previous example into a variable named `userData`, using this method call:</span></span>

    [!code-css[Main](working-with-files/samples/sample5.css)]

    <span data-ttu-id="59551-197">Kod, aby to zrobić, znajduje się w instrukcji `if`.</span><span class="sxs-lookup"><span data-stu-id="59551-197">The code to do this is inside an `if` statement.</span></span> <span data-ttu-id="59551-198">Jeśli chcesz odczytać plik, dobrym pomysłem jest użycie metody `File.Exists`, aby określić, czy plik jest dostępny.</span><span class="sxs-lookup"><span data-stu-id="59551-198">When you want to read a file, it's a good idea to use the `File.Exists` method to determine first whether the file is available.</span></span> <span data-ttu-id="59551-199">Kod sprawdza również, czy plik jest pusty.</span><span class="sxs-lookup"><span data-stu-id="59551-199">The code also checks whether the file is empty.</span></span>

    <span data-ttu-id="59551-200">Treść strony zawiera dwie `foreach` pętle, jeden zagnieżdżony w drugim.</span><span class="sxs-lookup"><span data-stu-id="59551-200">The body of the page contains two `foreach` loops, one nested inside the other.</span></span> <span data-ttu-id="59551-201">Pętla zewnętrzna `foreach` pobiera jeden wiersz jednocześnie z pliku danych.</span><span class="sxs-lookup"><span data-stu-id="59551-201">The outer `foreach` loop gets one line at a time from the data file.</span></span> <span data-ttu-id="59551-202">W takim przypadku linie są definiowane przez podziały wierszy w pliku &#8212; , który jest, każdy element danych znajduje się w osobnym wierszu.</span><span class="sxs-lookup"><span data-stu-id="59551-202">In this case, the lines are defined by line breaks in the file &#8212; that is, each data item is on its own line.</span></span> <span data-ttu-id="59551-203">Pętla zewnętrzna tworzy nowy element (`<li>` element) wewnątrz listy uporządkowanej (`<ol>` elementu).</span><span class="sxs-lookup"><span data-stu-id="59551-203">The outer loop creates a new item (`<li>` element) inside an ordered list (`<ol>` element).</span></span>

    <span data-ttu-id="59551-204">Pętla wewnętrzna dzieli każdy wiersz danych na elementy (pola) przy użyciu przecinka jako ogranicznika.</span><span class="sxs-lookup"><span data-stu-id="59551-204">The inner loop splits each data line into items (fields) using a comma as a delimiter.</span></span> <span data-ttu-id="59551-205">(Na podstawie poprzedniego przykładu oznacza to, że każdy wiersz zawiera trzy pola &#8212; imię, nazwisko i adres e-mail, z których każda jest oddzielona przecinkami). Pętla wewnętrzna tworzy również listę `<ul>` i wyświetla jeden element listy dla każdego pola w wierszu danych.</span><span class="sxs-lookup"><span data-stu-id="59551-205">(Based on the previous example, this means that each line contains three fields &#8212; the first name, last name, and email address, each separated by a comma.) The inner loop also creates a `<ul>` list and displays one list item for each field in the data line.</span></span>

    <span data-ttu-id="59551-206">Kod ilustruje sposób używania dwóch typów danych, tablicy i typu danych `char`.</span><span class="sxs-lookup"><span data-stu-id="59551-206">The code illustrates how to use two data types, an array and the `char` data type.</span></span> <span data-ttu-id="59551-207">Tablica jest wymagana, ponieważ metoda `File.ReadAllLines` zwraca dane jako tablicę.</span><span class="sxs-lookup"><span data-stu-id="59551-207">The array is required because the `File.ReadAllLines` method returns data as an array.</span></span> <span data-ttu-id="59551-208">Typ danych `char` jest wymagany, ponieważ metoda `Split` zwraca `array`, w którym każdy element jest typu `char`.</span><span class="sxs-lookup"><span data-stu-id="59551-208">The `char` data type is required because the `Split` method returns an `array` in which each element is of the type `char`.</span></span> <span data-ttu-id="59551-209">(Aby uzyskać informacje na temat tablic, zobacz [wprowadzenie do programowania w sieci Web ASP.NET przy użyciu składni Razor](https://go.microsoft.com/fwlink/?LinkId=202890#ID_CollectionsAndObjects)).</span><span class="sxs-lookup"><span data-stu-id="59551-209">(For information about arrays, see [Introduction to ASP.NET Web Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890#ID_CollectionsAndObjects).)</span></span>
3. <span data-ttu-id="59551-210">Uruchom stronę w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="59551-210">Run the page in a browser.</span></span> <span data-ttu-id="59551-211">Zostaną wyświetlone dane wprowadzone dla poprzednich przykładów.</span><span class="sxs-lookup"><span data-stu-id="59551-211">The data you entered for the previous examples is displayed.</span></span> 

    ![Image](working-with-files/_static/image4.jpg)

> [!TIP] 
> 
> <span data-ttu-id="59551-213">**Wyświetlanie danych z pliku rozdzielanego przecinkami programu Microsoft Excel**</span><span class="sxs-lookup"><span data-stu-id="59551-213">**Displaying Data from a Microsoft Excel Comma-Delimited File**</span></span>
> 
> <span data-ttu-id="59551-214">Możesz użyć programu Microsoft Excel, aby zapisać dane zawarte w arkuszu kalkulacyjnym jako plik rozdzielany przecinkami (plik*CSV* ).</span><span class="sxs-lookup"><span data-stu-id="59551-214">You can use Microsoft Excel to save the data contained in a spreadsheet as a comma-delimited file (*.csv* file).</span></span> <span data-ttu-id="59551-215">Gdy to zrobisz, plik zostanie zapisany w postaci zwykłego tekstu, a nie w formacie programu Excel.</span><span class="sxs-lookup"><span data-stu-id="59551-215">When you do, the file is saved in plain text, not in Excel format.</span></span> <span data-ttu-id="59551-216">Każdy wiersz w arkuszu kalkulacyjnym jest oddzielony podziałem wiersza w pliku tekstowym, a każdy element danych jest rozdzielony przecinkami.</span><span class="sxs-lookup"><span data-stu-id="59551-216">Each row in the spreadsheet is separated by a line break in the text file, and each data item is separated by a comma.</span></span> <span data-ttu-id="59551-217">Możesz użyć kodu pokazanego w poprzednim przykładzie, aby odczytać plik rozdzielany przecinkami programu Excel, zmieniając nazwę pliku danych w kodzie.</span><span class="sxs-lookup"><span data-stu-id="59551-217">You can use the code shown in the previous example to read an Excel comma-delimited file just by changing the name of the data file in your code.</span></span>

<a id="Deleting_Files"></a>
## <a name="deleting-files"></a><span data-ttu-id="59551-218">Usuwanie plików</span><span class="sxs-lookup"><span data-stu-id="59551-218">Deleting Files</span></span>

<span data-ttu-id="59551-219">Aby usunąć pliki z witryny sieci Web, można użyć metody `File.Delete`.</span><span class="sxs-lookup"><span data-stu-id="59551-219">To delete files from your website, you can use the `File.Delete` method.</span></span> <span data-ttu-id="59551-220">Ta procedura pokazuje, jak zezwolić użytkownikom na usuwanie obrazu (pliku*jpg* ) z folderu *obrazy* , jeśli zna nazwę pliku.</span><span class="sxs-lookup"><span data-stu-id="59551-220">This procedure shows how to let users delete an image (*.jpg* file) from an *images* folder if they know the name of the file.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="59551-221">**Ważne** W produkcyjnej witrynie sieci Web zwykle ogranicza się, kto może wprowadzać zmiany w danych.</span><span class="sxs-lookup"><span data-stu-id="59551-221">**Important** In a production website, you typically restrict who's allowed to make changes to the data.</span></span> <span data-ttu-id="59551-222">Informacje o sposobie konfigurowania członkostwa i sposobach autoryzacji użytkowników do wykonywania zadań w witrynie można znaleźć w temacie [Dodawanie zabezpieczeń i członkostwa do witryny ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=202904).</span><span class="sxs-lookup"><span data-stu-id="59551-222">For information about how to set up membership and about ways to authorize users to perform tasks on the site, see [Adding Security and Membership to an ASP.NET Web Pages Site](https://go.microsoft.com/fwlink/?LinkId=202904).</span></span>

1. <span data-ttu-id="59551-223">W witrynie sieci Web utwórz podfolder o nazwie *obrazy*.</span><span class="sxs-lookup"><span data-stu-id="59551-223">In the website, create a subfolder named *images*.</span></span>
2. <span data-ttu-id="59551-224">Skopiuj jeden lub więcej plików *. jpg* do folderu *obrazy* .</span><span class="sxs-lookup"><span data-stu-id="59551-224">Copy one or more *.jpg* files into the *images* folder.</span></span>
3. <span data-ttu-id="59551-225">W folderze głównym witryny sieci Web Utwórz nowy plik o nazwie *operacja filedelete. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="59551-225">In the root of the website, create a new file named *FileDelete.cshtml*.</span></span>
4. <span data-ttu-id="59551-226">Zastąp istniejącą zawartość następującym:</span><span class="sxs-lookup"><span data-stu-id="59551-226">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](working-with-files/samples/sample6.cshtml)]

    <span data-ttu-id="59551-227">Ta strona zawiera formularz, w którym użytkownicy mogą wprowadzić nazwę pliku obrazu.</span><span class="sxs-lookup"><span data-stu-id="59551-227">This page contains a form where users can enter the name of an image file.</span></span> <span data-ttu-id="59551-228">Nie wprowadzają one rozszerzenia nazwy pliku *. jpg* ; ograniczając nazwę pliku w ten sposób, uniemożliwiasz użytkownikom usuwanie dowolnych plików w witrynie.</span><span class="sxs-lookup"><span data-stu-id="59551-228">They don't enter the *.jpg* file-name extension; by restricting the file name like this, you help prevents users from deleting arbitrary files on your site.</span></span>

    <span data-ttu-id="59551-229">Kod odczytuje nazwę pliku wprowadzoną przez użytkownika, a następnie konstruuje kompletną ścieżkę.</span><span class="sxs-lookup"><span data-stu-id="59551-229">The code reads the file name that the user has entered and then constructs a complete path.</span></span> <span data-ttu-id="59551-230">Aby można było utworzyć ścieżkę, kod używa bieżącej ścieżki witryny sieci Web (w sposób zwrócony przez metodę `Server.MapPath`), nazwy folderu *obrazów* , nazwy podanej przez użytkownika i ". jpg" jako ciągu literału.</span><span class="sxs-lookup"><span data-stu-id="59551-230">To create the path, the code uses the current website path (as returned by the `Server.MapPath` method), the *images* folder name, the name that the user has provided, and ".jpg" as a literal string.</span></span>

    <span data-ttu-id="59551-231">Aby usunąć plik, kod wywołuje metodę `File.Delete`, przekazując ją pełną ścieżkę, która właśnie została skonstruowana.</span><span class="sxs-lookup"><span data-stu-id="59551-231">To delete the file, the code calls the `File.Delete` method, passing it the full path that you just constructed.</span></span> <span data-ttu-id="59551-232">Na końcu znacznika kod wyświetla komunikat potwierdzający, że plik został usunięty.</span><span class="sxs-lookup"><span data-stu-id="59551-232">At the end of the markup, code displays a confirmation message that the file was deleted.</span></span>
5. <span data-ttu-id="59551-233">Uruchom stronę w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="59551-233">Run the page in a browser.</span></span> 

    ![Image](working-with-files/_static/image5.jpg)
6. <span data-ttu-id="59551-235">Wprowadź nazwę pliku do usunięcia, a następnie kliknij przycisk **Prześlij**.</span><span class="sxs-lookup"><span data-stu-id="59551-235">Enter the name of the file to delete and then click **Submit**.</span></span> <span data-ttu-id="59551-236">Jeśli plik został usunięty, nazwa pliku zostanie wyświetlona w dolnej części strony.</span><span class="sxs-lookup"><span data-stu-id="59551-236">If the file was deleted, the name of the file is displayed at the bottom of the page.</span></span>

<a id="Letting_Users_Upload_a_File"></a>
## <a name="letting-users-upload-a-file"></a><span data-ttu-id="59551-237">Zezwalanie użytkownikom na przekazywanie pliku</span><span class="sxs-lookup"><span data-stu-id="59551-237">Letting Users Upload a File</span></span>

<span data-ttu-id="59551-238">Pomocnik `FileUpload` umożliwia użytkownikom przekazywanie plików do witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="59551-238">The `FileUpload` helper lets users upload files to your website.</span></span> <span data-ttu-id="59551-239">W poniższej procedurze pokazano, jak umożliwić użytkownikom przekazywanie pojedynczego pliku.</span><span class="sxs-lookup"><span data-stu-id="59551-239">The procedure below shows you how to let users upload a single file.</span></span>

1. <span data-ttu-id="59551-240">Dodaj bibliotekę pomocników sieci Web ASP.NET do witryny internetowej zgodnie z opisem w temacie [Instalowanie pomocników w witrynie ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=252372), jeśli nie została ona wcześniej dodana.</span><span class="sxs-lookup"><span data-stu-id="59551-240">Add the ASP.NET Web Helpers Library to your website as described in [Installing Helpers in an ASP.NET Web Pages Site](https://go.microsoft.com/fwlink/?LinkId=252372), if you didn't add it previously.</span></span>
2. <span data-ttu-id="59551-241">W folderze *dane\_aplikacji* Utwórz nowy folder i nadaj mu nazwę *UploadedFiles*.</span><span class="sxs-lookup"><span data-stu-id="59551-241">In the *App\_Data* folder, create a new a folder and name it *UploadedFiles*.</span></span>
3. <span data-ttu-id="59551-242">W katalogu głównym Utwórz nowy plik o nazwie *FileUpload. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="59551-242">In the root, create a new file named *FileUpload.cshtml*.</span></span>
4. <span data-ttu-id="59551-243">Zastąp istniejącą zawartość na stronie następującymi:</span><span class="sxs-lookup"><span data-stu-id="59551-243">Replace the existing content in the page with the following:</span></span> 

    [!code-cshtml[Main](working-with-files/samples/sample7.cshtml)]

    <span data-ttu-id="59551-244">Część treści strony używa pomocnika `FileUpload` do utworzenia pola przekazywania i przycisków, które są prawdopodobnie znane:</span><span class="sxs-lookup"><span data-stu-id="59551-244">The body portion of the page uses the `FileUpload` helper to create the upload box and buttons that you're probably familiar with:</span></span>

    ![Image](working-with-files/_static/image6.jpg)

    <span data-ttu-id="59551-246">Właściwości ustawione dla pomocnika `FileUpload` określają, że chcesz, aby plik został przekazany do jednego pola, i że chcesz, aby przycisk przesyłania przeczytał **przekazywanie**.</span><span class="sxs-lookup"><span data-stu-id="59551-246">The properties that you set for the `FileUpload` helper specify that you want a single box for the file to upload and that you want the submit button to read **Upload**.</span></span> <span data-ttu-id="59551-247">(W dalszej części artykułu dodasz więcej pól).</span><span class="sxs-lookup"><span data-stu-id="59551-247">(You'll add more boxes later in the article.)</span></span>

    <span data-ttu-id="59551-248">Gdy użytkownik kliknie przycisk **Przekaż**, kod w górnej części strony pobiera plik i zapisuje go.</span><span class="sxs-lookup"><span data-stu-id="59551-248">When the user clicks **Upload**, the code at the top of the page gets the file and saves it.</span></span> <span data-ttu-id="59551-249">Obiekt `Request`, który zwykle jest używany do pobierania wartości z pól formularza, ma również tablicę `Files`, która zawiera plik (lub pliki), które zostały przekazane.</span><span class="sxs-lookup"><span data-stu-id="59551-249">The `Request` object that you normally use to get values from form fields also has a `Files` array that contains the file (or files) that have been uploaded.</span></span> <span data-ttu-id="59551-250">Możesz pobrać pojedyncze pliki z określonych pozycji w tablicy &#8212; na przykład, aby uzyskać pierwszy przekazany plik, uzyskasz `Request.Files[0]`, aby uzyskać drugi plik, uzyskasz `Request.Files[1]`i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="59551-250">You can get individual files out of specific positions in the array &#8212; for example, to get the first uploaded file, you get `Request.Files[0]`, to get the second file, you get `Request.Files[1]`, and so on.</span></span> <span data-ttu-id="59551-251">(Pamiętaj, że w programowaniu funkcja liczenia zwykle zaczyna się od zera).</span><span class="sxs-lookup"><span data-stu-id="59551-251">(Remember that in programming, counting usually starts at zero.)</span></span>

    <span data-ttu-id="59551-252">Po pobraniu przekazanego pliku należy umieścić go w zmiennej (tutaj `uploadedFile`), aby umożliwić manipulowanie nim.</span><span class="sxs-lookup"><span data-stu-id="59551-252">When you fetch an uploaded file, you put it in a variable (here, `uploadedFile`) so that you can manipulate it.</span></span> <span data-ttu-id="59551-253">Aby określić nazwę przekazanego pliku, wystarczy uzyskać jego właściwość `FileName`.</span><span class="sxs-lookup"><span data-stu-id="59551-253">To determine the name of the uploaded file, you just get its `FileName` property.</span></span> <span data-ttu-id="59551-254">Jeśli jednak użytkownik przekaże plik, `FileName` zawiera oryginalną nazwę użytkownika, która obejmuje całą ścieżkę.</span><span class="sxs-lookup"><span data-stu-id="59551-254">However, when the user uploads a file, `FileName` contains the user's original name, which includes the entire path.</span></span> <span data-ttu-id="59551-255">Może wyglądać następująco:</span><span class="sxs-lookup"><span data-stu-id="59551-255">It might look like this:</span></span>

    <span data-ttu-id="59551-256">*C:\Users\Public\Sample.txt*</span><span class="sxs-lookup"><span data-stu-id="59551-256">*C:\Users\Public\Sample.txt*</span></span>

    <span data-ttu-id="59551-257">Nie chcesz, aby wszystkie informacje o ścieżce były takie same jak ścieżki na komputerze użytkownika, a nie na serwerze.</span><span class="sxs-lookup"><span data-stu-id="59551-257">You don't want all that path information, though, because that's the path on the user's computer, not for your server.</span></span> <span data-ttu-id="59551-258">Wystarczy tylko rzeczywista nazwa pliku (*Sample. txt*).</span><span class="sxs-lookup"><span data-stu-id="59551-258">You just want the actual file name (*Sample.txt*).</span></span> <span data-ttu-id="59551-259">Można rozdzielić tylko plik ze ścieżki przy użyciu metody `Path.GetFileName`, tak jak to:</span><span class="sxs-lookup"><span data-stu-id="59551-259">You can strip out just the file from a path by using the `Path.GetFileName` method, like this:</span></span>

    [!code-csharp[Main](working-with-files/samples/sample8.cs)]

    <span data-ttu-id="59551-260">Obiekt `Path` jest narzędziem, które ma wiele metod, takich jak to, których można użyć do rozdzielania ścieżek, łączenia ścieżek itd.</span><span class="sxs-lookup"><span data-stu-id="59551-260">The `Path` object is a utility that has a number of methods like this that you can use to strip paths, combine paths, and so on.</span></span>

    <span data-ttu-id="59551-261">Po otrzymaniu nazwy przekazanego pliku można utworzyć nową ścieżkę, w której ma zostać zapisany przekazany plik w witrynie sieci Web.</span><span class="sxs-lookup"><span data-stu-id="59551-261">Once you've gotten the name of the uploaded file, you can build a new path for where you want to store the uploaded file in your website.</span></span> <span data-ttu-id="59551-262">W takim przypadku należy połączyć `Server.MapPath`, nazwy folderów (*App\_Data/UploadedFiles*) oraz nowo rozkrytą nazwę pliku w celu utworzenia nowej ścieżki.</span><span class="sxs-lookup"><span data-stu-id="59551-262">In this case, you combine `Server.MapPath`, the folder names (*App\_Data/UploadedFiles*), and the newly stripped file name to create a new path.</span></span> <span data-ttu-id="59551-263">Następnie można wywołać metodę `SaveAs` przekazanego pliku, aby rzeczywiście zapisać plik.</span><span class="sxs-lookup"><span data-stu-id="59551-263">You can then call the uploaded file's `SaveAs` method to actually save the file.</span></span>
5. <span data-ttu-id="59551-264">Uruchom stronę w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="59551-264">Run the page in a browser.</span></span> 

    ![Image](working-with-files/_static/image7.jpg)
6. <span data-ttu-id="59551-266">Kliknij przycisk **Przeglądaj** , a następnie wybierz plik do przekazania.</span><span class="sxs-lookup"><span data-stu-id="59551-266">Click **Browse** and then select a file to upload.</span></span> 

    ![Image](working-with-files/_static/image8.jpg)

    <span data-ttu-id="59551-268">Pole tekstowe obok przycisku **przeglądania** będzie zawierać ścieżkę i lokalizację pliku.</span><span class="sxs-lookup"><span data-stu-id="59551-268">The text box next to the **Browse** button will contain the path and file location.</span></span>

    ![Image](working-with-files/_static/image9.jpg)
7. <span data-ttu-id="59551-270">Kliknij pozycję **Przekaż**.</span><span class="sxs-lookup"><span data-stu-id="59551-270">Click **Upload**.</span></span>
8. <span data-ttu-id="59551-271">W witrynie sieci Web kliknij prawym przyciskiem myszy folder projektu, a następnie kliknij polecenie **Odśwież**.</span><span class="sxs-lookup"><span data-stu-id="59551-271">In the website, right-click the project folder and then click **Refresh**.</span></span>
9. <span data-ttu-id="59551-272">Otwórz folder *UploadedFiles* .</span><span class="sxs-lookup"><span data-stu-id="59551-272">Open the *UploadedFiles* folder.</span></span> <span data-ttu-id="59551-273">Przekazany plik znajduje się w folderze.</span><span class="sxs-lookup"><span data-stu-id="59551-273">The file that you uploaded is in the folder.</span></span> 

    ![Image](working-with-files/_static/image10.jpg)

<a id="Letting_Users_Upload_Multiple_Files"></a>
## <a name="letting-users-upload-multiple-files"></a><span data-ttu-id="59551-275">Zezwalanie użytkownikom na przekazywanie wielu plików</span><span class="sxs-lookup"><span data-stu-id="59551-275">Letting Users Upload Multiple Files</span></span>

<span data-ttu-id="59551-276">W poprzednim przykładzie można umożliwić użytkownikom przekazywanie jednego pliku.</span><span class="sxs-lookup"><span data-stu-id="59551-276">In the previous example, you let users upload one file.</span></span> <span data-ttu-id="59551-277">Można jednak użyć pomocnika `FileUpload`, aby przekazać więcej niż jeden plik jednocześnie.</span><span class="sxs-lookup"><span data-stu-id="59551-277">But you can use the `FileUpload` helper to upload more than one file at a time.</span></span> <span data-ttu-id="59551-278">Jest to przydatne w przypadku scenariuszy, takich jak przekazywanie fotografii, gdzie przekazywanie jednego pliku w danym momencie jest żmudnym.</span><span class="sxs-lookup"><span data-stu-id="59551-278">This is handy for scenarios like uploading photos, where uploading one file at a time is tedious.</span></span> <span data-ttu-id="59551-279">(Informacje o przekazywaniu zdjęć w [pracy z obrazami można znaleźć w witrynie ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=202897)). W tym przykładzie pokazano, jak umożliwić użytkownikom przekazywanie dwóch naraz, chociaż możesz użyć tej samej techniki, aby przekazywać więcej niż te informacje.</span><span class="sxs-lookup"><span data-stu-id="59551-279">(You can read about uploading photos in [Working with Images in an ASP.NET Web Pages Site](https://go.microsoft.com/fwlink/?LinkId=202897).) This example shows how to let users upload two at a time, although you can use the same technique to upload more than that.</span></span>

1. <span data-ttu-id="59551-280">Dodaj bibliotekę pomocników sieci Web ASP.NET do witryny sieci Web zgodnie z opisem w temacie [Instalowanie pomocników w witrynie ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=252372)(jeśli jeszcze nie zostało to zrobione).</span><span class="sxs-lookup"><span data-stu-id="59551-280">Add the ASP.NET Web Helpers Library to your website as described in [Installing Helpers in an ASP.NET Web Pages Site](https://go.microsoft.com/fwlink/?LinkId=252372), if you haven't already.</span></span>
2. <span data-ttu-id="59551-281">Utwórz nową stronę o nazwie *FileUploadMultiple. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="59551-281">Create a new page named *FileUploadMultiple.cshtml*.</span></span>
3. <span data-ttu-id="59551-282">Zastąp istniejącą zawartość na stronie następującymi:</span><span class="sxs-lookup"><span data-stu-id="59551-282">Replace the existing content in the page with the following:</span></span>  

    [!code-cshtml[Main](working-with-files/samples/sample9.cshtml)]

    <span data-ttu-id="59551-283">W tym przykładzie pomocnik `FileUpload` w treści strony jest skonfigurowany tak, aby umożliwić użytkownikom przekazywanie domyślnie dwóch plików.</span><span class="sxs-lookup"><span data-stu-id="59551-283">In this example, the `FileUpload` helper in the body of the page is configured to let users upload two files by default.</span></span> <span data-ttu-id="59551-284">Ponieważ `allowMoreFilesToBeAdded` jest ustawiona na `true`, pomocnik renderuje link, który umożliwia użytkownikowi dodawanie kolejnych pól przekazywania:</span><span class="sxs-lookup"><span data-stu-id="59551-284">Because `allowMoreFilesToBeAdded` is set to `true`, the helper renders a link that lets user add more upload boxes:</span></span>

    ![Image](working-with-files/_static/image11.jpg)

    <span data-ttu-id="59551-286">Aby przetworzyć pliki, które przekazuje użytkownik, kod używa tej samej podstawowej techniki, która została użyta w poprzednim przykładzie &#8212; Pobierz plik z `Request.Files` a następnie zapisz go.</span><span class="sxs-lookup"><span data-stu-id="59551-286">To process the files that the user uploads, the code uses the same basic technique that you used in the previous example &#8212; get a file from `Request.Files` and then save it.</span></span> <span data-ttu-id="59551-287">(W tym różne czynności, które należy wykonać, aby uzyskać właściwą nazwę i ścieżkę pliku). Innowacje w tym czasie mogą przekazywać wiele plików, a użytkownik nie wie wielu.</span><span class="sxs-lookup"><span data-stu-id="59551-287">(Including the various things you need to do to get the right file name and path.) The innovation this time is that the user might be uploading multiple files and you don't know many.</span></span> <span data-ttu-id="59551-288">Aby dowiedzieć się, możesz uzyskać `Request.Files.Count`.</span><span class="sxs-lookup"><span data-stu-id="59551-288">To find out, you can get `Request.Files.Count`.</span></span>

    <span data-ttu-id="59551-289">Dzięki tej liczbie można przechodzić między `Request.Files`, pobierać każdy plik z kolei i zapisywać go.</span><span class="sxs-lookup"><span data-stu-id="59551-289">With this number in hand, you can loop through `Request.Files`, fetch each file in turn, and save it.</span></span> <span data-ttu-id="59551-290">Gdy chcesz zapętlać znaną liczbę razy za pomocą kolekcji, możesz użyć pętli `for`, tak jak to:</span><span class="sxs-lookup"><span data-stu-id="59551-290">When you want to loop a known number of times through a collection, you can use a `for` loop, like this:</span></span>

    [!code-csharp[Main](working-with-files/samples/sample10.cs)]

    <span data-ttu-id="59551-291">Zmienna `i` jest tylko tymczasowym licznikiem, który przejdzie od zera do dowolnego ustawionego górnego limitu.</span><span class="sxs-lookup"><span data-stu-id="59551-291">The variable `i` is just a temporary counter that will go from zero to whatever upper limit you set.</span></span> <span data-ttu-id="59551-292">W takim przypadku górny limit to liczba plików.</span><span class="sxs-lookup"><span data-stu-id="59551-292">In this case, the upper limit is the number of files.</span></span> <span data-ttu-id="59551-293">Ale ponieważ licznik zaczyna się od zera, jak zwykle jest to typowy dla obliczeń scenariuszy w ASP.NET, górny limit jest w rzeczywistości mniejszy niż liczba plików.</span><span class="sxs-lookup"><span data-stu-id="59551-293">But because the counter starts at zero, as is typical for counting scenarios in ASP.NET, the upper limit is actually one less than the file count.</span></span> <span data-ttu-id="59551-294">(Jeśli przekazano trzy pliki, liczba jest równa zero do 2).</span><span class="sxs-lookup"><span data-stu-id="59551-294">(If three files are uploaded, the count is zero to 2.)</span></span>

    <span data-ttu-id="59551-295">Zmienna `uploadedCount` sumuje wszystkie pliki, które zostały pomyślnie przekazane i zapisane.</span><span class="sxs-lookup"><span data-stu-id="59551-295">The `uploadedCount` variable totals all the files that are successfully uploaded and saved.</span></span> <span data-ttu-id="59551-296">Ten kod może nie być w stanie przekazywać oczekiwanego pliku.</span><span class="sxs-lookup"><span data-stu-id="59551-296">This code accounts for the possibility that an expected file may not be able to be uploaded.</span></span>
4. <span data-ttu-id="59551-297">Uruchom stronę w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="59551-297">Run the page in a browser.</span></span> <span data-ttu-id="59551-298">Przeglądarka wyświetli stronę i jej dwa pola przekazywania.</span><span class="sxs-lookup"><span data-stu-id="59551-298">The browser displays the page and its two upload boxes.</span></span>
5. <span data-ttu-id="59551-299">Wybierz dwa pliki do przekazania.</span><span class="sxs-lookup"><span data-stu-id="59551-299">Select two files to upload.</span></span>
6. <span data-ttu-id="59551-300">Kliknij przycisk **Dodaj inny plik**.</span><span class="sxs-lookup"><span data-stu-id="59551-300">Click **Add another file**.</span></span> <span data-ttu-id="59551-301">Na stronie zostanie wyświetlone nowe pole przekazywania.</span><span class="sxs-lookup"><span data-stu-id="59551-301">The page displays a new upload box.</span></span> 

    ![Image](working-with-files/_static/image12.jpg)
7. <span data-ttu-id="59551-303">Kliknij pozycję **Przekaż**.</span><span class="sxs-lookup"><span data-stu-id="59551-303">Click **Upload**.</span></span>
8. <span data-ttu-id="59551-304">W witrynie sieci Web kliknij prawym przyciskiem myszy folder projektu, a następnie kliknij polecenie **Odśwież**.</span><span class="sxs-lookup"><span data-stu-id="59551-304">In the website, right-click the project folder and then click **Refresh**.</span></span>
9. <span data-ttu-id="59551-305">Otwórz folder *UploadedFiles* , aby zobaczyć pomyślnie przekazane pliki.</span><span class="sxs-lookup"><span data-stu-id="59551-305">Open the *UploadedFiles* folder to see the successfully uploaded files.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="59551-306">Dodatkowe materiały</span><span class="sxs-lookup"><span data-stu-id="59551-306">Additional Resources</span></span>

[<span data-ttu-id="59551-307">Praca z obrazami w witrynie ASP.NET Web Pages</span><span class="sxs-lookup"><span data-stu-id="59551-307">Working with Images in an ASP.NET Web Pages Site</span></span>](https://go.microsoft.com/fwlink/?LinkId=202897)

[<span data-ttu-id="59551-308">Eksportowanie do pliku CSV</span><span class="sxs-lookup"><span data-stu-id="59551-308">Exporting to a CSV File</span></span>](https://msdn.microsoft.com/library/ms155919.aspx)
