---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/entering-data
title: Wprowadzenie do składnika ASP.NET Web Pages — wprowadzanie danych bazy danych za pomocą formularzy | Dokumentacja firmy Microsoft
author: Rick-Anderson
description: W tym samouczku pokazano, jak utworzyć formularz wprowadzania, a następnie wprowadź dane, który można pobrać z formularza do tabeli bazy danych użycia stron ASP.NET Web Pages (...)
ms.author: riande
ms.date: 05/28/2015
ms.assetid: d37c93fc-25fd-4e94-8671-0d437beef206
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/entering-data
msc.type: authoredcontent
ms.openlocfilehash: e40d2962ccac56eaaf4812819aa42168e69295bc
ms.sourcegitcommit: 24b1f6decbb17bb22a45166e5fdb0845c65af498
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/01/2019
ms.locfileid: "57067553"
---
<a name="introducing-aspnet-web-pages---entering-database-data-by-using-forms"></a><span data-ttu-id="44972-103">Wprowadzenie do składnika ASP.NET Web Pages — wprowadzanie danych bazy danych za pomocą formularzy</span><span class="sxs-lookup"><span data-stu-id="44972-103">Introducing ASP.NET Web Pages - Entering Database Data by Using Forms</span></span>
====================
<span data-ttu-id="44972-104">przez [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="44972-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="44972-105">Ten samouczek pokazuje, jak utworzyć formularz wprowadzania, a następnie wprowadź dane, który można pobrać z formularza do tabeli bazy danych użycia stron ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="44972-105">This tutorial shows you how to create an entry form and then enter the data that you get from the form into a database table when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="44972-106">Przyjęto założenie, że zostały wykonane serii za pośrednictwem [podstawy formularzy HTML strony ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=251581).</span><span class="sxs-lookup"><span data-stu-id="44972-106">It assumes you have completed the series through [Basics of HTML Forms in ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=251581).</span></span>
> 
> <span data-ttu-id="44972-107">Zawartość:</span><span class="sxs-lookup"><span data-stu-id="44972-107">What you'll learn:</span></span>
> 
> - <span data-ttu-id="44972-108">Więcej informacji o sposobie przetwarzania formularzach wprowadzania.</span><span class="sxs-lookup"><span data-stu-id="44972-108">More about how to process entry forms.</span></span>
> - <span data-ttu-id="44972-109">Jak dodawać dane (Wstaw) w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="44972-109">How to add (insert) data in a database.</span></span>
> - <span data-ttu-id="44972-110">Jak upewnić się, że użytkownicy wprowadzono wymaganej wartości w postaci (sposób sprawdzania poprawności danych wejściowych użytkownika).</span><span class="sxs-lookup"><span data-stu-id="44972-110">How to make sure that users have entered a required value in a form (how to validate user input).</span></span>
> - <span data-ttu-id="44972-111">Jak wyświetlić błędy sprawdzania poprawności.</span><span class="sxs-lookup"><span data-stu-id="44972-111">How to display validation errors.</span></span>
> - <span data-ttu-id="44972-112">Jak przejść do innej strony z bieżącej strony.</span><span class="sxs-lookup"><span data-stu-id="44972-112">How to jump to another page from the current page.</span></span>
>   
> 
> <span data-ttu-id="44972-113">Funkcje/technologie omówione:</span><span class="sxs-lookup"><span data-stu-id="44972-113">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="44972-114">`Database.Execute` Metody.</span><span class="sxs-lookup"><span data-stu-id="44972-114">The `Database.Execute` method.</span></span>
> - <span data-ttu-id="44972-115">SQL `Insert Into` — instrukcja</span><span class="sxs-lookup"><span data-stu-id="44972-115">The SQL `Insert Into` statement</span></span>
> - <span data-ttu-id="44972-116">`Validation` Pomocnika.</span><span class="sxs-lookup"><span data-stu-id="44972-116">The `Validation` helper.</span></span>
> - <span data-ttu-id="44972-117">`Response.Redirect` Metody.</span><span class="sxs-lookup"><span data-stu-id="44972-117">The `Response.Redirect` method.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="44972-118">Jakie będziesz tworzyć</span><span class="sxs-lookup"><span data-stu-id="44972-118">What You'll Build</span></span>

<span data-ttu-id="44972-119">W tym samouczku wcześniej pokazujący, jak utworzyć bazę danych, wprowadzone dane z bazy danych, edytując bazy danych bezpośrednio w programie WebMatrix, pracy w **bazy danych** obszaru roboczego.</span><span class="sxs-lookup"><span data-stu-id="44972-119">In the tutorial earlier that showed you how to create a database, you entered database data by editing the database directly in WebMatrix, working in the **Database** workspace.</span></span> <span data-ttu-id="44972-120">W przypadku większości aplikacji, która nie jest praktycznym sposobem dane umieszczane w bazie danych, mimo że.</span><span class="sxs-lookup"><span data-stu-id="44972-120">In most apps, that's not a practical way to put data into the database, though.</span></span> <span data-ttu-id="44972-121">Dlatego w tym samouczku utworzysz opartego na sieci web interfejs, umożliwiający Ty, ani wprowadzać dane i zapisz go w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="44972-121">So in this tutorial, you'll create a web-based interface that lets you or anyone enter data and save it to the database.</span></span>

<span data-ttu-id="44972-122">Utworzysz strony, w którym możesz wprowadzić nowe filmy.</span><span class="sxs-lookup"><span data-stu-id="44972-122">You'll create a page where you can enter new movies.</span></span> <span data-ttu-id="44972-123">Strona będzie zawierać formularza zgłoszenia, który ma pola (pola tekstowe), gdzie można wprowadzić tytuł filmu, gatunku i roku.</span><span class="sxs-lookup"><span data-stu-id="44972-123">The page will contain an entry form that has fields (text boxes) where you can enter a movie title, genre, and year.</span></span> <span data-ttu-id="44972-124">Strona będzie wyglądała podobna do tej strony:</span><span class="sxs-lookup"><span data-stu-id="44972-124">The page will look like this page:</span></span>

!["Dodawanie filmu" strony w przeglądarce](entering-data/_static/image1.png)

<span data-ttu-id="44972-126">Pola tekstowe będzie HTML `<input>` elementy, które będzie wyglądać jak ten kod znaczników:</span><span class="sxs-lookup"><span data-stu-id="44972-126">The text boxes will be HTML `<input>` elements that will look like this markup:</span></span>

`<input type="text" name="genre" value="" />`

## <a name="creating-the-basic-entry-form"></a><span data-ttu-id="44972-127">Tworzenie formularza podstawowego</span><span class="sxs-lookup"><span data-stu-id="44972-127">Creating the Basic Entry Form</span></span>

<span data-ttu-id="44972-128">Utwórz stronę o nazwie *AddMovie.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="44972-128">Create a page named *AddMovie.cshtml*.</span></span>

<span data-ttu-id="44972-129">Zastąp, co znajduje się w pliku następującym kodem.</span><span class="sxs-lookup"><span data-stu-id="44972-129">Replace what's in the file with the following markup.</span></span> <span data-ttu-id="44972-130">Zastąp wszystko; wkrótce zostanie dodana blok kodu u góry.</span><span class="sxs-lookup"><span data-stu-id="44972-130">Overwrite everything; you'll add a code block at the top shortly.</span></span>

[!code-cshtml[Main](entering-data/samples/sample1.cshtml)]

<span data-ttu-id="44972-131">Ten przykład przedstawia typowy HTML podczas tworzenia formularza.</span><span class="sxs-lookup"><span data-stu-id="44972-131">This example shows typical HTML for creating a form.</span></span> <span data-ttu-id="44972-132">Używa ona `<input>` elementów dla pola tekstowe i przycisk Prześlij.</span><span class="sxs-lookup"><span data-stu-id="44972-132">It uses `<input>` elements for the text boxes and for the submit button.</span></span> <span data-ttu-id="44972-133">Podpisy dla pola tekstowe są tworzone przy użyciu standardu `<label>` elementów.</span><span class="sxs-lookup"><span data-stu-id="44972-133">The captions for the text boxes are created by using standard `<label>` elements.</span></span> <span data-ttu-id="44972-134">`<fieldset>` i `<legend>` elementy umieść nieuprzywilejowany ramkę wokół formularza.</span><span class="sxs-lookup"><span data-stu-id="44972-134">The `<fieldset>` and `<legend>` elements put a nice box around the form.</span></span>

<span data-ttu-id="44972-135">Należy zauważyć, że na tej stronie `<form>` element używa `post` jako wartość pozycji `method` atrybutu.</span><span class="sxs-lookup"><span data-stu-id="44972-135">Notice that in this page, the `<form>` element uses `post` as the value for the `method` attribute.</span></span> <span data-ttu-id="44972-136">W poprzednim samouczku utworzono formularz, który jest używany `get` metody.</span><span class="sxs-lookup"><span data-stu-id="44972-136">In the previous tutorial, you created a form that used the `get` method.</span></span> <span data-ttu-id="44972-137">Było poprawne, mimo że przesłać formularza, wartości do serwera, żądanie nie wprowadzone zmiany.</span><span class="sxs-lookup"><span data-stu-id="44972-137">That was correct, because although the form submitted values to the server, the request did not make any changes.</span></span> <span data-ttu-id="44972-138">Wszystko, co tak było pobierania danych na różne sposoby.</span><span class="sxs-lookup"><span data-stu-id="44972-138">All it did was fetch data in different ways.</span></span> <span data-ttu-id="44972-139">Jednak w tym stronie *będzie* zmiany — zamierzamy dodawać nowe rekordy bazy danych.</span><span class="sxs-lookup"><span data-stu-id="44972-139">However, in this page you *will* make changes—you're going to add new database records.</span></span> <span data-ttu-id="44972-140">Dlatego należy używać tego formularza `post` metody.</span><span class="sxs-lookup"><span data-stu-id="44972-140">Therefore, this form should use the `post` method.</span></span> <span data-ttu-id="44972-141">(Aby uzyskać więcej informacji o różnicach między `GET` i `POST` operacji, zobacz[GET, POST i bezpieczeństwa zlecenie HTTP](https://go.microsoft.com/fwlink/?LinkId=251581#GET,_POST,_and_HTTP_Verb_Safety) paska bocznego w poprzednim samouczku.)</span><span class="sxs-lookup"><span data-stu-id="44972-141">(For more about the difference between `GET` and `POST` operations, see the[GET, POST, and HTTP Verb Safety](https://go.microsoft.com/fwlink/?LinkId=251581#GET,_POST,_and_HTTP_Verb_Safety) sidebar in the previous tutorial.)</span></span>

<span data-ttu-id="44972-142">Należy zauważyć, że każde pole tekstowe ma `name` — element (`title`, `genre`, `year`).</span><span class="sxs-lookup"><span data-stu-id="44972-142">Note that each text box has a `name` element (`title`, `genre`, `year`).</span></span> <span data-ttu-id="44972-143">Jak w poprzednim samouczku, te nazwy są ważne, ponieważ muszą mieć te nazwy, dzięki czemu możesz później uzyskać dane wejściowe użytkownika.</span><span class="sxs-lookup"><span data-stu-id="44972-143">As you saw in the previous tutorial, these names are important because you must have those names so you can get the user's input later.</span></span> <span data-ttu-id="44972-144">Możesz użyć dowolnej nazwy.</span><span class="sxs-lookup"><span data-stu-id="44972-144">You can use any names.</span></span> <span data-ttu-id="44972-145">Pomocne jest użycie znaczące nazwy, które ułatwiają należy pamiętać, jakie dane pracujemy z użyciem.</span><span class="sxs-lookup"><span data-stu-id="44972-145">It's helpful to use meaningful names that help you remember what data you're working with.</span></span>

<span data-ttu-id="44972-146">`value` Atrybut każdego `<input>` element zawiera ilość kodu Razor (na przykład `Request.Form["title"]`).</span><span class="sxs-lookup"><span data-stu-id="44972-146">The `value` attribute of each `<input>` element contains a bit of Razor code (for example, `Request.Form["title"]`).</span></span> <span data-ttu-id="44972-147">Pokazaliśmy ci, wersja ta procedura w poprzednim samouczku, aby zachować wartość wprowadzona w polu tekstowym (jeśli istnieją) po przesłaniu formularza.</span><span class="sxs-lookup"><span data-stu-id="44972-147">You learned a version of this trick in the previous tutorial to preserve the value entered into the text box (if any) after the form has been submitted.</span></span>

## <a name="getting-the-form-values"></a><span data-ttu-id="44972-148">Pobieranie wartości formularza</span><span class="sxs-lookup"><span data-stu-id="44972-148">Getting the Form Values</span></span>

<span data-ttu-id="44972-149">Następnie dodasz kod, który przetwarza formularza.</span><span class="sxs-lookup"><span data-stu-id="44972-149">Next, you add code that processes the form.</span></span> <span data-ttu-id="44972-150">Konspekt artykułu będzie wykonywać następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="44972-150">In outline, you'll do the following:</span></span>

1. <span data-ttu-id="44972-151">Sprawdź, czy strona jest przesyłana (przesłano).</span><span class="sxs-lookup"><span data-stu-id="44972-151">Check whether the page is being posted (was submitted).</span></span> <span data-ttu-id="44972-152">Chcesz w kodzie tylko wtedy, gdy użytkownicy kliknięty przycisk, nie wtedy, gdy strona pierwszego uruchomienia.</span><span class="sxs-lookup"><span data-stu-id="44972-152">You want to your code to run only when users have clicked the button, not when the page first runs.</span></span>
2. <span data-ttu-id="44972-153">Pobiera wartości, które użytkownik wprowadził w polach tekstowych.</span><span class="sxs-lookup"><span data-stu-id="44972-153">Get the values that the user entered into the text boxes.</span></span> <span data-ttu-id="44972-154">W tym przypadku ponieważ korzysta z formularza `POST` zlecenie, Pobierz wartości formularza z `Request.Form` kolekcji.</span><span class="sxs-lookup"><span data-stu-id="44972-154">In this case, because the form is using the `POST` verb, you get the form values from the `Request.Form` collection.</span></span>
3. <span data-ttu-id="44972-155">Wstaw wartości jako nowego rekordu w *filmy* tabeli bazy danych.</span><span class="sxs-lookup"><span data-stu-id="44972-155">Insert the values as a new record in the *Movies* database table.</span></span>

<span data-ttu-id="44972-156">W górnej części pliku Dodaj następujący kod:</span><span class="sxs-lookup"><span data-stu-id="44972-156">At the top of the file, add the following code:</span></span>

[!code-cshtml[Main](entering-data/samples/sample2.cshtml)]

<span data-ttu-id="44972-157">Pierwszych kilka wierszy tworzenia zmiennych (`title`, `genre`, i `year`) do przechowywania wartości w polach tekstowych.</span><span class="sxs-lookup"><span data-stu-id="44972-157">The first few lines create variables (`title`, `genre`, and `year`) to hold the values from the text boxes.</span></span> <span data-ttu-id="44972-158">Wiersz `if(IsPost)` upewnia się, że zmienne są ustawiane *tylko* po użytkownik kliknie **dodać film** przycisk — oznacza to, kiedy formularz została opublikowana.</span><span class="sxs-lookup"><span data-stu-id="44972-158">The line `if(IsPost)` makes sure that the variables are set *only* when users click the **Add Movie** button — that is, when the form has been posted.</span></span>

<span data-ttu-id="44972-159">Jak przedstawiono wcześniej samouczka można pobrać wartość w polu tekstowym za pomocą wyrażenia takie jak `Request.Form["name"]`, gdzie *nazwa* nazywa się `<input>` elementu.</span><span class="sxs-lookup"><span data-stu-id="44972-159">As you saw in an earlier tutorial, you get the value of a text box by using an expression like `Request.Form["name"]`, where *name* is the name of the `<input>` element.</span></span>

<span data-ttu-id="44972-160">Nazwy zmiennych (`title`, `genre`, i `year`) są dowolne.</span><span class="sxs-lookup"><span data-stu-id="44972-160">The names of the variables (`title`, `genre`, and `year`) are arbitrary.</span></span> <span data-ttu-id="44972-161">Takie jak nazwy, które można przypisać do `<input>` elementy, można je wywoływać wszystko chcesz.</span><span class="sxs-lookup"><span data-stu-id="44972-161">Like the names that you assign to `<input>` elements, you can call them anything you like.</span></span> <span data-ttu-id="44972-162">(Nazwy zmiennych nie muszą być zgodne z atrybutami name `<input>` elementów w formularzu.) Ale podobnie jak w przypadku `<input>` elementów, to dobry pomysł, aby użyć nazwy zmiennych, które odzwierciedlają dane, które zawierają.</span><span class="sxs-lookup"><span data-stu-id="44972-162">(The names of the variables don't have to match the name attributes of `<input>` elements on the form.) But as with the `<input>` elements, it's a good idea to use variable names that reflect the data that they contain.</span></span> <span data-ttu-id="44972-163">Podczas pisania kodu, spójne nazwy ułatwiają aby zapamiętać dane pracujemy z użyciem.</span><span class="sxs-lookup"><span data-stu-id="44972-163">When you write code, consistent names make it easier for you to remember what data you're working with.</span></span>

## <a name="adding-data-to-the-database"></a><span data-ttu-id="44972-164">Dodawanie danych do bazy danych</span><span class="sxs-lookup"><span data-stu-id="44972-164">Adding Data to the Database</span></span>

<span data-ttu-id="44972-165">W kodzie zablokujemy Cię właśnie dodanej, po prostu *wewnątrz* zamykającego nawiasu klamrowego ( `}` ) z `if` blokuje (nie tylko wewnątrz bloku kodu), Dodaj następujący kod:</span><span class="sxs-lookup"><span data-stu-id="44972-165">In the code block you just added, just *inside* the closing brace ( `}` ) of the `if` block (not just inside the code block), add the following code:</span></span>

[!code-csharp[Main](entering-data/samples/sample3.cs)]

<span data-ttu-id="44972-166">Ten przykład jest podobny do kodu, którego użyto w poprzednim samouczku do pobierania i wyświetlania danych.</span><span class="sxs-lookup"><span data-stu-id="44972-166">This example is similar to the code you used in a previous tutorial to fetch and display data.</span></span> <span data-ttu-id="44972-167">Wiersz, który rozpoczyna się od `db =` otwiera bazy danych, takich jak wcześniej i następny wiersz określa instrukcję SQL ponownie jako przedstawiono wcześniej.</span><span class="sxs-lookup"><span data-stu-id="44972-167">The line that starts with `db =` opens the database, like before, and the next line defines a SQL statement, again as you saw before.</span></span> <span data-ttu-id="44972-168">Jednak tym razem definiuje SQL `Insert Into` instrukcji.</span><span class="sxs-lookup"><span data-stu-id="44972-168">However, this time it defines a SQL `Insert Into` statement.</span></span> <span data-ttu-id="44972-169">Poniższy przykład pokazuje ogólna składnia `Insert Into` instrukcji:</span><span class="sxs-lookup"><span data-stu-id="44972-169">The following example shows the general syntax of the `Insert Into` statement:</span></span>

`INSERT INTO table (column1, column2, column3, ...) VALUES (value1, value2, value3, ...)`

<span data-ttu-id="44972-170">Innymi słowy należy określić tabeli, wstawianie, wyświetlona lista kolumn do wstawienia do, a następnie listę wartości do wstawienia.</span><span class="sxs-lookup"><span data-stu-id="44972-170">In other words, you specify the table to insert into, then list the columns to insert into, and then list the values to insert.</span></span> <span data-ttu-id="44972-171">(Wspomniane wcześniej SQL nie jest uwzględniana wielkość liter, ale niektórzy wielką literą słów kluczowych, aby ułatwić interpretowanie polecenie).</span><span class="sxs-lookup"><span data-stu-id="44972-171">(As noted before, SQL is not case sensitive but some people capitalize the keywords to make it easier to read the command.)</span></span>

<span data-ttu-id="44972-172">Kolumny, które w przypadku wstawiania do już są wymienione w poleceniu — `(Title, Genre, Year)`.</span><span class="sxs-lookup"><span data-stu-id="44972-172">The columns that you're inserting into are already listed in the command — `(Title, Genre, Year)`.</span></span> <span data-ttu-id="44972-173">Interesujące części jest, jak pobrać wartości z pól tekstowych do `VALUES` część polecenia.</span><span class="sxs-lookup"><span data-stu-id="44972-173">The interesting part is how you get the values from the text boxes into the `VALUES` part of the command.</span></span> <span data-ttu-id="44972-174">Zamiast wartości faktycznych można zobaczyć `@0`, `@1`, i `@2`, które z kolei są symbolami zastępczymi.</span><span class="sxs-lookup"><span data-stu-id="44972-174">Instead of actual values, you see `@0`, `@1`, and `@2`, which are of course placeholders.</span></span> <span data-ttu-id="44972-175">Po uruchomieniu polecenia (na `db.Execute` wiersza), możesz przekazać wartości, które stało się z pól tekstowych.</span><span class="sxs-lookup"><span data-stu-id="44972-175">When you run the command (on the `db.Execute` line), you pass the values that you got from the text boxes.</span></span>

<span data-ttu-id="44972-176">**Ważne!**</span><span class="sxs-lookup"><span data-stu-id="44972-176">**Important!**</span></span> <span data-ttu-id="44972-177">Należy pamiętać, że jedynie nigdy nie powinna zawierać dane wprowadzone w tryb online przez użytkownika w instrukcji SQL jest użycie symboli zastępczych, jak widać w tym miejscu (`VALUES(@0, @1, @2)`).</span><span class="sxs-lookup"><span data-stu-id="44972-177">Remember that the only way you should ever include data entered online by a user in a SQL statement is to use placeholders, as you see here (`VALUES(@0, @1, @2)`).</span></span> <span data-ttu-id="44972-178">Jeśli możesz połączyć dane wejściowe użytkownika w instrukcji SQL, należy otworzyć samodzielnie do ataku polegającego na iniekcji SQL zgodnie z objaśnieniem w [podstawy formularzy stron ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=251581) (poprzedniego samouczka).</span><span class="sxs-lookup"><span data-stu-id="44972-178">If you concatenate user input into a SQL statement, you open yourself to a SQL injection attack, as explained in [Form Basics in ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=251581) (the previous tutorial).</span></span>

<span data-ttu-id="44972-179">Nadal wewnątrz `if` zablokować, Dodaj następujący wiersz po `db.Execute` wiersza:</span><span class="sxs-lookup"><span data-stu-id="44972-179">Still inside the `if` block, add the following line after the `db.Execute` line:</span></span>

[!code-css[Main](entering-data/samples/sample4.css)]

<span data-ttu-id="44972-180">Po ten nowy film został wstawiony do bazy danych, ten wiersz skacze możesz (przekierowania) do *filmy* strony, aby było widać filmu wprowadzonej.</span><span class="sxs-lookup"><span data-stu-id="44972-180">After the new movie has been inserted into the database, this line jumps you (redirects) to the *Movies* page so you can see the movie you just entered.</span></span> <span data-ttu-id="44972-181">`~` Operator oznacza "root witryny sieci Web".</span><span class="sxs-lookup"><span data-stu-id="44972-181">The `~` operator means "root of the website."</span></span> <span data-ttu-id="44972-182">( `~` Operator działa tylko na stronach ASP.NET, nie w formacie HTML ogólnie.)</span><span class="sxs-lookup"><span data-stu-id="44972-182">(The `~` operator works only in ASP.NET pages, not in HTML generally.)</span></span>

<span data-ttu-id="44972-183">Ukończony blok kodu wygląda następująco:</span><span class="sxs-lookup"><span data-stu-id="44972-183">The complete code block looks like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample5.cshtml)]

## <a name="testing-the-insert-command-so-far"></a><span data-ttu-id="44972-184">Testowanie polecenia Insert (pory)</span><span class="sxs-lookup"><span data-stu-id="44972-184">Testing the Insert Command (So Far)</span></span>

<span data-ttu-id="44972-185">Nie masz jeszcze zrobione, ale teraz jest odpowiedni moment, aby przetestować.</span><span class="sxs-lookup"><span data-stu-id="44972-185">You're not done yet, but now is a good time to test.</span></span>

<span data-ttu-id="44972-186">W plikach w programie WebMatrix w widoku drzewa kliknij prawym przyciskiem myszy *AddMovie.cshtml* strony, a następnie kliknij przycisk **Uruchom w przeglądarce**.</span><span class="sxs-lookup"><span data-stu-id="44972-186">In the tree view of files in WebMatrix, right-click the *AddMovie.cshtml* page and then click **Launch in browser**.</span></span>

!["Dodawanie filmu" strony w przeglądarce](entering-data/_static/image2.png)

<span data-ttu-id="44972-188">(Jeśli na końcu innej strony w przeglądarce, upewnij się, że adres URL jest `http://localhost:nnnnn/AddMovie`), gdzie *nnnnn* jest numerem portu, którego używasz.)</span><span class="sxs-lookup"><span data-stu-id="44972-188">(If you end up with a different page in the browser, make sure that the URL is `http://localhost:nnnnn/AddMovie`), where *nnnnn* is the port number that you're using.)</span></span>

<span data-ttu-id="44972-189">Czy został wyświetlony strona błędu</span><span class="sxs-lookup"><span data-stu-id="44972-189">Did you get an error page?</span></span> <span data-ttu-id="44972-190">Jeśli tak, należy uważnie przeczytać i upewnij się, że kod wyglądał dokładnie co zostało wymienione wcześniej.</span><span class="sxs-lookup"><span data-stu-id="44972-190">If so, read it carefully and make sure that the code looks exactly what was listed earlier.</span></span>

<span data-ttu-id="44972-191">Wprowadź filmu w postaci &mdash; na przykład użyć "Dla obywateli Wójcik", "Dramat" i "1941".</span><span class="sxs-lookup"><span data-stu-id="44972-191">Enter a movie in the form &mdash; for example, use "Citizen Kane", "Drama", and "1941".</span></span> <span data-ttu-id="44972-192">(Lub zależności od potrzeb). Następnie kliknij przycisk **dodać film**.</span><span class="sxs-lookup"><span data-stu-id="44972-192">(Or whatever.) Then click **Add Movie**.</span></span>

<span data-ttu-id="44972-193">Jeśli wszystko przebiegnie poprawnie, użytkownik jest przekierowany do *filmy* strony.</span><span class="sxs-lookup"><span data-stu-id="44972-193">If all goes well, you're redirected to the *Movies* page.</span></span> <span data-ttu-id="44972-194">Upewnij się, że znajduje się nowy film.</span><span class="sxs-lookup"><span data-stu-id="44972-194">Make sure that your new movie is listed.</span></span>

![Strona filmy, przedstawiający nowo dodana filmu](entering-data/_static/image3.png)

## <a name="validating-user-input"></a><span data-ttu-id="44972-196">Walidacja danych wejściowych użytkownika</span><span class="sxs-lookup"><span data-stu-id="44972-196">Validating User Input</span></span>

<span data-ttu-id="44972-197">Wróć do *AddMovie* strony lub uruchomić go ponownie.</span><span class="sxs-lookup"><span data-stu-id="44972-197">Go back to the *AddMovie* page, or run it again.</span></span> <span data-ttu-id="44972-198">Wprowadź inny film, ale tym razem, wprowadź tylko tytuł &mdash; na przykład, wprowadź "Rejstrowanie rejestrowanie logowanie ' w Rain".</span><span class="sxs-lookup"><span data-stu-id="44972-198">Enter another movie, but this time, enter only the title &mdash; for example, enter "Singin' in the Rain".</span></span> <span data-ttu-id="44972-199">Następnie kliknij przycisk **dodać film**.</span><span class="sxs-lookup"><span data-stu-id="44972-199">Then click **Add Movie**.</span></span>

<span data-ttu-id="44972-200">Są przekierowywane do *filmy* strony.</span><span class="sxs-lookup"><span data-stu-id="44972-200">You're redirected to the *Movies* page again.</span></span> <span data-ttu-id="44972-201">Możesz znaleźć ten nowy film, ale jest on niepełny.</span><span class="sxs-lookup"><span data-stu-id="44972-201">You can find the new movie, but it's incomplete.</span></span>

![Filmy stronie nowy Film przedstawiający, którym brakuje niektórych wartości](entering-data/_static/image4.png)

<span data-ttu-id="44972-203">Podczas tworzenia *filmy* tabeli jawnie wspomniałeś, żadne pole może mieć wartości null.</span><span class="sxs-lookup"><span data-stu-id="44972-203">When you created the *Movies* table, you explicitly said that none of the fields could be null.</span></span> <span data-ttu-id="44972-204">W tym miejscu masz formularz wprowadzania dla nowych filmów, a teraz pozostawić puste pola.</span><span class="sxs-lookup"><span data-stu-id="44972-204">Here you have an entry form for new movies, and you're leaving fields blank.</span></span> <span data-ttu-id="44972-205">To jest błąd.</span><span class="sxs-lookup"><span data-stu-id="44972-205">That's an error.</span></span>

<span data-ttu-id="44972-206">W tym przypadku faktycznie nie zgłaszać bazy danych (lub *throw*) wystąpił błąd.</span><span class="sxs-lookup"><span data-stu-id="44972-206">In this case, the database didn't actually raise (or *throw*) an error.</span></span> <span data-ttu-id="44972-207">Nie podasz gatunku lub roku, dlatego ten kod w *AddMovie* strony traktować te wartości jako tak zwane *puste ciągi*.</span><span class="sxs-lookup"><span data-stu-id="44972-207">You didn't supply a genre or year, so the code in the *AddMovie* page treated those values as so-called *empty strings*.</span></span> <span data-ttu-id="44972-208">Gdy SQL `Insert Into` polecenie zadziałało, pola gatunku i rok w tamtym przydatnych danych w nich, ale nie zostały one wartość null.</span><span class="sxs-lookup"><span data-stu-id="44972-208">When the SQL `Insert Into` command ran, the genre and year fields didn't have useful data in them, but they weren't null.</span></span>

<span data-ttu-id="44972-209">Oczywiście nie chcesz umożliwić użytkownikom wprowadzanie informacji o filmach połowie puste do bazy danych.</span><span class="sxs-lookup"><span data-stu-id="44972-209">Obviously, you don't want to let users enter half-empty movie information into the database.</span></span> <span data-ttu-id="44972-210">Jest to rozwiązanie do sprawdzania poprawności danych wejściowych użytkownika.</span><span class="sxs-lookup"><span data-stu-id="44972-210">The solution is to validate the user's input.</span></span> <span data-ttu-id="44972-211">Początkowo sprawdzanie poprawności po prostu zostanie upewnij się, że użytkownik wpisze wartość dla wszystkich pól (oznacza to, że żaden z nich zawiera pusty ciąg znaków).</span><span class="sxs-lookup"><span data-stu-id="44972-211">Initially, the validation will simply make sure that the user has entered a value for all of the fields (that is, that none of them contains an empty string).</span></span>

> [!TIP]
> 
> <span data-ttu-id="44972-212">**Wartość null i pustych ciągów**</span><span class="sxs-lookup"><span data-stu-id="44972-212">**Null and Empty Strings**</span></span>
> 
> <span data-ttu-id="44972-213">W programowaniu istnieje różnica między różnych pojęcia "braku wartości".</span><span class="sxs-lookup"><span data-stu-id="44972-213">In programming, there's a distinction between different notions of "no value."</span></span> <span data-ttu-id="44972-214">Ogólnie rzecz biorąc, wartość jest *null* Jeśli nigdy nie została ustawiona lub zainicjowany w dowolny sposób.</span><span class="sxs-lookup"><span data-stu-id="44972-214">In general, a value is *null* if it has never been set or initialized in any way.</span></span> <span data-ttu-id="44972-215">Z kolei można ustawić zmiennej, która oczekuje, że dane znakowe (ciągi), aby *pusty ciąg*.</span><span class="sxs-lookup"><span data-stu-id="44972-215">In contrast, a variable that expects character data (strings) can be set to an *empty string*.</span></span> <span data-ttu-id="44972-216">W takim przypadku wartość nie jest równa null; po prostu zostały jawnie ustawiana jest na ciąg znaków, którego długość ma wartość zero.</span><span class="sxs-lookup"><span data-stu-id="44972-216">In that case, the value is not null; it's just been explicitly set to a string of characters whose length is zero.</span></span> <span data-ttu-id="44972-217">Te dwie instrukcje wyświetlenie różnicy:</span><span class="sxs-lookup"><span data-stu-id="44972-217">These two statements show the difference:</span></span>
> 
> [!code-csharp[Main](entering-data/samples/sample6.cs)]
> 
> <span data-ttu-id="44972-218">Nieco jest bardziej skomplikowane niż ta, ale istotną kwestią jest to, że `null` reprezentuje rodzaj nieokreślony stan.</span><span class="sxs-lookup"><span data-stu-id="44972-218">It's a little more complicated than that, but the important point is that `null` represents a sort of undetermined state.</span></span>
> 
> <span data-ttu-id="44972-219">Teraz, a następnie ważne jest zrozumieć dokładnie gdy ma wartość null, i kiedy jest tylko pustym ciągiem.</span><span class="sxs-lookup"><span data-stu-id="44972-219">Now and then it's important to understand exactly when a value is null and when it's just an empty string.</span></span> <span data-ttu-id="44972-220">W kodzie dla *AddMovie* strony, można pobrać wartości pola przy użyciu `Request.Form["title"]` i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="44972-220">In the code for the *AddMovie* page, you get the values of the text boxes by using `Request.Form["title"]` and so on.</span></span> <span data-ttu-id="44972-221">Po pierwszym uruchomieniu strony (przed kliknięciem przycisku), wartość `Request.Form["title"]` ma wartość null.</span><span class="sxs-lookup"><span data-stu-id="44972-221">When the page first runs (before you click the button), the value of `Request.Form["title"]` is null.</span></span> <span data-ttu-id="44972-222">Jednak gdy prześlesz formularz, `Request.Form["title"]` pobiera wartość `title` pola tekstowego.</span><span class="sxs-lookup"><span data-stu-id="44972-222">But when you submit the form, `Request.Form["title"]` gets the value of the `title` text box.</span></span> <span data-ttu-id="44972-223">Nie jest jasne, ale puste pole tekstowe nie jest równa null; ją po prostu zawiera pusty ciąg.</span><span class="sxs-lookup"><span data-stu-id="44972-223">It's not obvious, but an empty text box is not null; it just has an empty string in it.</span></span> <span data-ttu-id="44972-224">Gdy kod jest uruchamiany w odpowiedzi na przycisk kliknij przycisk, `Request.Form["title"]` zawiera pusty ciąg.</span><span class="sxs-lookup"><span data-stu-id="44972-224">So when the code runs in response to the button click, `Request.Form["title"]` has an empty string in it.</span></span>
> 
> <span data-ttu-id="44972-225">Ta różnica jest ważna</span><span class="sxs-lookup"><span data-stu-id="44972-225">Why is this distinction important?</span></span> <span data-ttu-id="44972-226">Podczas tworzenia *filmy* tabeli jawnie wspomniałeś, żadne pole może mieć wartości null.</span><span class="sxs-lookup"><span data-stu-id="44972-226">When you created the *Movies* table, you explicitly said that none of the fields could be null.</span></span> <span data-ttu-id="44972-227">Jednak w tym miejscu masz formularz wprowadzania dla nowych filmów i jest pozostawienie pustego pola.</span><span class="sxs-lookup"><span data-stu-id="44972-227">But here you have an entry form for new movies, and you're leaving fields blank.</span></span> <span data-ttu-id="44972-228">Można oczekiwać rozsądnie bazy danych, błąd podczas próby zapisania nowe filmy, które nie mają wartości gatunku lub roku.</span><span class="sxs-lookup"><span data-stu-id="44972-228">You would reasonably expect the database to complain when you tried to save new movies that didn't have values for genre or year.</span></span> <span data-ttu-id="44972-229">Ale to punkt &mdash; nawet jeśli te pola tekstowe jest puste, wartości nie ma wartość null; są one puste ciągi.</span><span class="sxs-lookup"><span data-stu-id="44972-229">But that's the point &mdash; even if you leave those text boxes blank, the values aren't null; they're empty strings.</span></span> <span data-ttu-id="44972-230">W rezultacie, możesz zapisać nowe filmy w bazie danych z tych kolumn pusty &mdash; , ale nie o wartości null!</span><span class="sxs-lookup"><span data-stu-id="44972-230">As a result, you're able to save new movies to the database with these columns empty &mdash; but not null!</span></span> <span data-ttu-id="44972-231">&mdash; wartości.</span><span class="sxs-lookup"><span data-stu-id="44972-231">&mdash; values.</span></span> <span data-ttu-id="44972-232">W związku z tym należy upewnić się, że użytkownicy nie przesyłaj pustym ciągiem, co można zrobić, sprawdzając poprawność danych wejściowych użytkownika.</span><span class="sxs-lookup"><span data-stu-id="44972-232">Therefore, you have to make sure that users don't submit an empty string, which you can do by validating the user's input.</span></span>


### <a name="the-validation-helper"></a><span data-ttu-id="44972-233">Pomocnik weryfikacji</span><span class="sxs-lookup"><span data-stu-id="44972-233">The Validation Helper</span></span>

<span data-ttu-id="44972-234">ASP.NET Web Pages obejmuje Pomocnika &mdash; `Validation` Pomocnika &mdash; , można użyć, aby upewnić się, że użytkownicy wprowadzają dane, które spełniają Twoje wymagania.</span><span class="sxs-lookup"><span data-stu-id="44972-234">ASP.NET Web Pages includes a helper &mdash; the `Validation` helper &mdash; that you can use to make sure that users enter data that meets your requirements.</span></span> <span data-ttu-id="44972-235">`Validation` Pomocnika jest jednym z wątków, które jest kompilowany w do stron ASP.NET Web Pages, więc nie trzeba go zainstalować jako pakiet za pomocą NuGet, sposób pomocnika Gravatar jest zainstalowany w samouczku wcześniej.</span><span class="sxs-lookup"><span data-stu-id="44972-235">The `Validation` helper is one of the helpers that's built in to ASP.NET Web Pages, so you don't have to install it as a package by using NuGet, the way you installed the Gravatar helper in an earlier tutorial.</span></span>

<span data-ttu-id="44972-236">Aby sprawdzić poprawność danych wejściowych użytkownika, wykonasz następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="44972-236">To validate the user's input, you'll do the following:</span></span>

- <span data-ttu-id="44972-237">Użyj kodu, aby określić wymaganie wartości w polach tekstowych na stronie.</span><span class="sxs-lookup"><span data-stu-id="44972-237">Use code to specify that you want to require values in the text boxes on the page.</span></span>
- <span data-ttu-id="44972-238">Umieść testu kod tak, aby informacje filmu są dodawane do bazy danych, tylko wtedy, gdy wszystko, co sprawdza poprawność prawidłowo.</span><span class="sxs-lookup"><span data-stu-id="44972-238">Put a test into the code so that the movie information is added to the database only if everything validates properly.</span></span>
- <span data-ttu-id="44972-239">Dodaj kod do znaczników, aby wyświetlić komunikaty o błędach.</span><span class="sxs-lookup"><span data-stu-id="44972-239">Add code into the markup to display error messages.</span></span>

<span data-ttu-id="44972-240">W bloku kodu w *AddMovie* strony, prawo do góry u góry przed deklaracji zmiennych, Dodaj następujący kod:</span><span class="sxs-lookup"><span data-stu-id="44972-240">In the code block in the *AddMovie* page, right up at the top before the variable declarations, add the following code:</span></span>

[!code-csharp[Main](entering-data/samples/sample7.cs)]

<span data-ttu-id="44972-241">Należy wywołać `Validation.RequireField` jeden raz dla każdego pola (`<input>` elementu) gdzie chcesz wymagać wpis.</span><span class="sxs-lookup"><span data-stu-id="44972-241">You call `Validation.RequireField` once for each field (`<input>` element) where you want to require an entry.</span></span> <span data-ttu-id="44972-242">Można również dodać niestandardowy komunikat o błędzie dla każdego wywołania, tak jak to tutaj.</span><span class="sxs-lookup"><span data-stu-id="44972-242">You can also add a custom error message for each call, like you see here.</span></span> <span data-ttu-id="44972-243">(Firma Microsoft zróżnicowane wiadomości, aby zobrazować można umieścić coś, co chcesz istnieje).</span><span class="sxs-lookup"><span data-stu-id="44972-243">(We varied the messages just to show that you can put anything you like there.)</span></span>

<span data-ttu-id="44972-244">W przypadku problemu chcesz uniemożliwić nowe informacje film z wstawiane do bazy danych.</span><span class="sxs-lookup"><span data-stu-id="44972-244">If there's a problem, you want to prevent the new movie information from being inserted into the database.</span></span> <span data-ttu-id="44972-245">W `if(IsPost)` blokowania, należy użyć `&&` (operator logiczny oraz), aby dodać inny warunek, który umożliwia sprawdzenie `Validation.IsValid()`.</span><span class="sxs-lookup"><span data-stu-id="44972-245">In the `if(IsPost)` block, use `&&` (logical AND) to add another condition that tests `Validation.IsValid()`.</span></span> <span data-ttu-id="44972-246">Gdy wszystko będzie gotowe, cały `if(IsPost)` blok będzie wyglądać podobnie do tego kodu:</span><span class="sxs-lookup"><span data-stu-id="44972-246">When you're done, the whole `if(IsPost)` block looks like this code:</span></span>

[!code-csharp[Main](entering-data/samples/sample8.cs)]

<span data-ttu-id="44972-247">Jeśli występuje błąd sprawdzania poprawności z żadnym z pól, które są zarejestrowane przy użyciu `Validation` Pomocnika `Validation.IsValid` metoda zwraca wartość false.</span><span class="sxs-lookup"><span data-stu-id="44972-247">If there's a validation error with any of the fields that you registered by using the `Validation` helper, the `Validation.IsValid` method returns false.</span></span> <span data-ttu-id="44972-248">I w takim przypadku Brak kodu, w tym bloku zostaną uruchomione, dzięki czemu żadnych wpisów nieprawidłowy film zostanie wstawiony do bazy danych.</span><span class="sxs-lookup"><span data-stu-id="44972-248">And in that case, none of the code in that block will run, so no invalid movie entries will be inserted into the database.</span></span> <span data-ttu-id="44972-249">I oczywiście możesz teraz nie przekierowana do *filmy* strony.</span><span class="sxs-lookup"><span data-stu-id="44972-249">And of course you're not redirected to the *Movies* page.</span></span>

<span data-ttu-id="44972-250">Ukończony blok kodu, w tym kod sprawdzania poprawności wygląda teraz następująco:</span><span class="sxs-lookup"><span data-stu-id="44972-250">The complete code block, including the validation code, now looks like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample9.cshtml?highlight=10)]

## <a name="displaying-validation-errors"></a><span data-ttu-id="44972-251">Wyświetlanie błędów walidacji</span><span class="sxs-lookup"><span data-stu-id="44972-251">Displaying Validation Errors</span></span>

<span data-ttu-id="44972-252">Ostatnim krokiem jest, aby wyświetlić komunikaty o błędach.</span><span class="sxs-lookup"><span data-stu-id="44972-252">The last step is to display any error messages.</span></span> <span data-ttu-id="44972-253">Można wyświetlić pojedyncze wiadomości dla każdego błędu weryfikacji, lub możesz wyświetlić podsumowanie i / lub.</span><span class="sxs-lookup"><span data-stu-id="44972-253">You can display individual messages for each validation error, or you can display a summary, or both.</span></span> <span data-ttu-id="44972-254">W tym samouczku wykonasz zarówno tak, aby zobaczyć, jak to działa.</span><span class="sxs-lookup"><span data-stu-id="44972-254">For this tutorial, you'll do both so that you can see how it works.</span></span>

<span data-ttu-id="44972-255">Obok każdego `<input>` element, który jest sprawdzanie poprawności, wywołanie `Html.ValidationMessage` metody i przekaż mu nazwę z `<input>` element one sprawdzania poprawności.</span><span class="sxs-lookup"><span data-stu-id="44972-255">Next to each `<input>` element that you're validating, call the `Html.ValidationMessage` method and pass it the name of the `<input>` element you're validating.</span></span> <span data-ttu-id="44972-256">Możesz umieścić `Html.ValidationMessage` metoda po prawej stronie, gdzie ma być wyświetlany komunikat o błędzie.</span><span class="sxs-lookup"><span data-stu-id="44972-256">You put the `Html.ValidationMessage` method right where you want the error message to appear.</span></span> <span data-ttu-id="44972-257">Po uruchomieniu strony, `Html.ValidationMessage` metoda renderuje `<span>` elementu, gdzie błąd sprawdzania poprawności.</span><span class="sxs-lookup"><span data-stu-id="44972-257">When the page runs, the `Html.ValidationMessage` method renders a `<span>` element where the validation error will go.</span></span> <span data-ttu-id="44972-258">(Jeśli nie ma błędów, `<span>` element jest renderowany, ale nie ma żadnego tekstu w nim.)</span><span class="sxs-lookup"><span data-stu-id="44972-258">(If there's no error, the `<span>` element is rendered, but there's no text in it.)</span></span>

<span data-ttu-id="44972-259">Zmień adiustację tak, na stronie, aby obejmowała `Html.ValidationMessage` metody dla każdego z trzech `<input>` elementów na stronie, takich jak w tym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="44972-259">Change the markup in the page so that it includes an `Html.ValidationMessage` method for each of the three `<input>` elements on the page, like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample10.cshtml?highlight=3,8,13)]

<span data-ttu-id="44972-260">Aby zobaczyć, jak działa podsumowania, dodać następujący kod znaczników i kodu bezpośrednio po `<h1>Add a Movie</h1>` elementu na stronie:</span><span class="sxs-lookup"><span data-stu-id="44972-260">To see how the summary works, also add the following markup and code right after the `<h1>Add a Movie</h1>` element on the page:</span></span>

[!code-cshtml[Main](entering-data/samples/sample11.cshtml)]

<span data-ttu-id="44972-261">Domyślnie `Html.ValidationSummary` metoda Wyświetla wszystkie komunikaty sprawdzania poprawności w postaci listy ( `<ul>` element, który znajduje się wewnątrz `<div>` elementu).</span><span class="sxs-lookup"><span data-stu-id="44972-261">By default, the `Html.ValidationSummary` method displays all the validation messages in a list (a `<ul>` element that's inside a `<div>` element).</span></span> <span data-ttu-id="44972-262">Podobnie jak w przypadku `Html.ValidationMessage` metody zawsze renderowania kodu znaczników dla podsumowania weryfikacji; Jeśli nie ma żadnych błędów, elementów listy są renderowane.</span><span class="sxs-lookup"><span data-stu-id="44972-262">As with the `Html.ValidationMessage` method, the markup for the validation summary is always rendered; if there are no errors, no list items are rendered.</span></span>

<span data-ttu-id="44972-263">Podsumowanie może być alternatywny sposób wyświetlania komunikatów dotyczących sprawdzania poprawności, a nie za pomocą `Html.ValidationMessage` metodę w celu wyświetlenia każdego błędu określonego pola.</span><span class="sxs-lookup"><span data-stu-id="44972-263">The summary can be an alternative way to display validation messages instead of by using the `Html.ValidationMessage` method to display each field-specific error.</span></span> <span data-ttu-id="44972-264">Możesz też podsumowanie i szczegóły.</span><span class="sxs-lookup"><span data-stu-id="44972-264">Or you can use both a summary and the details.</span></span> <span data-ttu-id="44972-265">Możesz też `Html.ValidationSummary` metodę, aby wyświetlić ogólny błąd, a następnie użyj poszczególnych `Html.ValidationMessage` wywołania w celu wyświetlenia szczegółowych informacji.</span><span class="sxs-lookup"><span data-stu-id="44972-265">Or you can use the `Html.ValidationSummary` method to display a generic error and then use individual `Html.ValidationMessage` calls to display details.</span></span>

<span data-ttu-id="44972-266">Stronie zakończenia wygląda teraz następująco:</span><span class="sxs-lookup"><span data-stu-id="44972-266">The complete page now looks like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample12.cshtml)]

<span data-ttu-id="44972-267">To wszystko.</span><span class="sxs-lookup"><span data-stu-id="44972-267">That's it.</span></span> <span data-ttu-id="44972-268">Teraz możesz przetestować stronę przez dodawanie filmu, ale pomijając jedną lub więcej pól.</span><span class="sxs-lookup"><span data-stu-id="44972-268">You can now test the page by adding a movie but leaving out one or more of the fields.</span></span> <span data-ttu-id="44972-269">Po wykonaniu, zostanie wyświetlony następujący błąd wyświetlania:</span><span class="sxs-lookup"><span data-stu-id="44972-269">When you do, you see the following error display:</span></span>

![Dodaj stronę Film przedstawiający komunikaty o błędach weryfikacji](entering-data/_static/image5.png)

## <a name="styling-the-validation-error-messages"></a><span data-ttu-id="44972-271">Ustawianie stylów komunikatów o błędach weryfikacji</span><span class="sxs-lookup"><span data-stu-id="44972-271">Styling the Validation Error Messages</span></span>

<span data-ttu-id="44972-272">Widać, że istnieją komunikaty o błędach, ale ich nie naprawdę wyróżniały się bardzo dobrze.</span><span class="sxs-lookup"><span data-stu-id="44972-272">You can see that there are error messages, but they don't really stand out very well.</span></span> <span data-ttu-id="44972-273">Jest łatwe do określania stylu komunikatów o błędach, mimo że.</span><span class="sxs-lookup"><span data-stu-id="44972-273">There's an easy way to style the error messages, though.</span></span>

<span data-ttu-id="44972-274">Do określania stylu poszczególne komunikaty o błędach, które są wyświetlane przez `Html.ValidationMessage`, Utwórz klasę style CSS, o nazwie `field-validation-error`.</span><span class="sxs-lookup"><span data-stu-id="44972-274">To style the individual error messages that are displayed by `Html.ValidationMessage`, create a CSS style class named `field-validation-error`.</span></span> <span data-ttu-id="44972-275">Aby zdefiniować wygląd podsumowania weryfikacji, należy utworzyć klasę style CSS o nazwie `validation-summary-errors`.</span><span class="sxs-lookup"><span data-stu-id="44972-275">To define the look for the validation summary, create a CSS style class named `validation-summary-errors`.</span></span>

<span data-ttu-id="44972-276">Aby zobaczyć, jak działa ta technika, Dodaj `<style>` element wewnątrz `<head>` części strony.</span><span class="sxs-lookup"><span data-stu-id="44972-276">To see how this technique works, add a `<style>` element inside the `<head>` section of the page.</span></span> <span data-ttu-id="44972-277">Następnie zdefiniuj stylu klasy o nazwie `field-validation-error` i `validation-summary-errors` zawierające następujące reguły:</span><span class="sxs-lookup"><span data-stu-id="44972-277">Then define style classes named `field-validation-error` and `validation-summary-errors` that contain the following rules:</span></span>

[!code-cshtml[Main](entering-data/samples/sample13.cshtml?highlight=4-17)]

<span data-ttu-id="44972-278">Zwykle prawdopodobnie umieścić informacje dotyczące stylu w oddzielnych *.css* pliku, ale dla uproszczenia można je umieścić w stronę teraz.</span><span class="sxs-lookup"><span data-stu-id="44972-278">Normally you'd probably put style information into a separate *.css* file, but for simplicity you can put them in the page for now.</span></span> <span data-ttu-id="44972-279">(W dalszej części tego samouczka zestawu przeniesienie reguły CSS do osobnego *.css* pliku.)</span><span class="sxs-lookup"><span data-stu-id="44972-279">(Later in this tutorial set, you'll move the CSS rules to a separate *.css* file.)</span></span>

<span data-ttu-id="44972-280">Jeśli występuje błąd sprawdzania poprawności `Html.ValidationMessage` metoda renderuje `<span>` element, który zawiera `class="field-validation-error"`.</span><span class="sxs-lookup"><span data-stu-id="44972-280">If there's a validation error, the `Html.ValidationMessage` method renders a `<span>` element that includes `class="field-validation-error"`.</span></span> <span data-ttu-id="44972-281">Dodanie definicji stylu dla tej klasy, można skonfigurować komunikat wygląda następująco.</span><span class="sxs-lookup"><span data-stu-id="44972-281">By adding a style definition for that class, you can configure what the message looks like.</span></span> <span data-ttu-id="44972-282">Jeśli występują błędy, `ValidationSummary` metoda renderuje podobnie dynamicznie atrybut `class="validation-summary-errors"`.</span><span class="sxs-lookup"><span data-stu-id="44972-282">If there are errors, the `ValidationSummary` method likewise dynamically renders the attribute `class="validation-summary-errors"`.</span></span>

<span data-ttu-id="44972-283">Ponownie uruchom strony i celowo Opuść kilka pól.</span><span class="sxs-lookup"><span data-stu-id="44972-283">Run the page again and deliberately leave out a couple of the fields.</span></span> <span data-ttu-id="44972-284">Błędy są teraz lepiej widoczne.</span><span class="sxs-lookup"><span data-stu-id="44972-284">The errors are now more noticeable.</span></span> <span data-ttu-id="44972-285">(W rzeczywistości są one overdone, ale jest, że jedynie pokazanie, co można zrobić).</span><span class="sxs-lookup"><span data-stu-id="44972-285">(In fact, they're overdone, but that's just to show what you can do.)</span></span>

![Dodaj stronę Film przedstawiający błędy sprawdzania poprawności, które zostały różne](entering-data/_static/image6.png)

## <a name="adding-a-link-to-the-movies-page"></a><span data-ttu-id="44972-287">Dodawanie Linku do strony filmy</span><span class="sxs-lookup"><span data-stu-id="44972-287">Adding a Link to the Movies Page</span></span>

<span data-ttu-id="44972-288">Jeden ostatnim krokiem jest zapewnienie wygodne przejść do *AddMovie* strony z oryginalnej listy filmów.</span><span class="sxs-lookup"><span data-stu-id="44972-288">One final step is to make it convenient to get to the *AddMovie* page from the original movie listing.</span></span>

<span data-ttu-id="44972-289">Otwórz *filmy* strony.</span><span class="sxs-lookup"><span data-stu-id="44972-289">Open the *Movies* page again.</span></span> <span data-ttu-id="44972-290">Po zamykającym `</div>` tag, który następuje po `WebGrid` pomocnika, Dodaj następujący kod:</span><span class="sxs-lookup"><span data-stu-id="44972-290">After the closing `</div>` tag that follows the `WebGrid` helper, add the following markup:</span></span>

[!code-cshtml[Main](entering-data/samples/sample14.cshtml)]

<span data-ttu-id="44972-291">Jak przedstawiono wcześniej ASP.NET interpretuje `~` operatora jako katalogu głównego witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="44972-291">As you saw before, ASP.NET interprets the `~` operator as the root of the website.</span></span> <span data-ttu-id="44972-292">Nie trzeba stosować `~` operatora; można użyć znaczników `<a href="./AddMovie">Add a movie</a>` lub w inny sposób zdefiniować ścieżkę, która rozumie HTML.</span><span class="sxs-lookup"><span data-stu-id="44972-292">You don't have to use the `~` operator; you could use the markup `<a href="./AddMovie">Add a movie</a>` or some other way to define the path that HTML understands.</span></span> <span data-ttu-id="44972-293">Ale `~` operator to dobra metoda ogólne podczas tworzenia łączy do stron Razor, ponieważ sprawia, że witryna bardziej elastyczne — Jeśli przesuniesz bieżącą stronę do podfolderu łącze nadal przejdzie *AddMovie* strony.</span><span class="sxs-lookup"><span data-stu-id="44972-293">But the `~` operator is a good general approach when you create links for Razor pages, because it makes the site more flexible — if you move the current page to a subfolder, the link will still go to the *AddMovie* page.</span></span> <span data-ttu-id="44972-294">(Należy pamiętać, że `~` operator działa tylko w *.cshtml* stron.</span><span class="sxs-lookup"><span data-stu-id="44972-294">(Remember that the `~` operator only works in *.cshtml* pages.</span></span> <span data-ttu-id="44972-295">ASP.NET rozpoznaje ją, ale nie jest to standardowy HTML).</span><span class="sxs-lookup"><span data-stu-id="44972-295">ASP.NET understands it, but it's not standard HTML.)</span></span>

<span data-ttu-id="44972-296">Gdy wszystko będzie gotowe, uruchom *filmy* strony.</span><span class="sxs-lookup"><span data-stu-id="44972-296">When you're done, run the *Movies* page.</span></span> <span data-ttu-id="44972-297">Ta strona będzie wyglądać:</span><span class="sxs-lookup"><span data-stu-id="44972-297">It will look like this page:</span></span>

![Strona filmów z linkiem do strony "Dodaj filmy"](entering-data/_static/image7.png)

<span data-ttu-id="44972-299">Kliknij przycisk **Dodawanie filmu** łącze, aby upewnić się, że przejdzie do *AddMovie* strony.</span><span class="sxs-lookup"><span data-stu-id="44972-299">Click the **Add a movie** link to make sure that it goes to the *AddMovie* page.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="44972-300">Pojawi się dalej</span><span class="sxs-lookup"><span data-stu-id="44972-300">Coming Up Next</span></span>

<span data-ttu-id="44972-301">W następnym samouczku dowiesz się, jak umożliwić użytkownikom edytowanie danych, który jest już w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="44972-301">In the next tutorial, you'll learn how to let users edit data that's already in the database.</span></span>

## <a name="complete-listing-for-addmovie-page"></a><span data-ttu-id="44972-302">Kompletna lista AddMovie strony</span><span class="sxs-lookup"><span data-stu-id="44972-302">Complete Listing for AddMovie Page</span></span>

[!code-cshtml[Main](entering-data/samples/sample15.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="44972-303">Dodatkowe zasoby</span><span class="sxs-lookup"><span data-stu-id="44972-303">Additional Resources</span></span>

- [<span data-ttu-id="44972-304">Wprowadzenie do programowania dla sieci Web platformy ASP.NET używająca składni Razor</span><span class="sxs-lookup"><span data-stu-id="44972-304">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="44972-305">[Wstaw do instrukcji SQL](http://www.w3schools.com/sql/sql_insert.asp) witrynie W3Schools</span><span class="sxs-lookup"><span data-stu-id="44972-305">[SQL INSERT INTO Statement](http://www.w3schools.com/sql/sql_insert.asp) on the W3Schools site</span></span>
- <span data-ttu-id="44972-306">[Walidacja danych wejściowych użytkownika we wzorcu ASP.NET Web Pages witryn](https://go.microsoft.com/fwlink/?LinkId=253002).</span><span class="sxs-lookup"><span data-stu-id="44972-306">[Validating User Input in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=253002).</span></span> <span data-ttu-id="44972-307">Więcej informacji na temat pracy z `Validation` pomocnika.</span><span class="sxs-lookup"><span data-stu-id="44972-307">More information about working with the `Validation` helper.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="44972-308">[Poprzednie](form-basics.md)
> [dalej](updating-data.md)</span><span class="sxs-lookup"><span data-stu-id="44972-308">[Previous](form-basics.md)
[Next](updating-data.md)</span></span>
