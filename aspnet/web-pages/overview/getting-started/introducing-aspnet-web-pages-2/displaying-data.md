---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
title: Wprowadzenie do składnika ASP.NET Web Pages — wyświetlanie danych | Dokumentacja firmy Microsoft
author: Rick-Anderson
description: W tym samouczku dowiesz się, jak utworzyć bazę danych w programie WebMatrix oraz jak wyświetlać dane z bazy danych na stronie, gdy używasz stron ASP.NET Web Pages (Razor). Przyjęto założenie, y...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: b3a006a0-3ea2-4d45-b833-e20e3a3c0a1a
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
msc.type: authoredcontent
ms.openlocfilehash: 9e665ca8dd064c23a8b8bd3593014969d0c3da48
ms.sourcegitcommit: 51b01b6ff8edde57d8243e4da28c9f1e7f1962b2
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 05/06/2019
ms.locfileid: "65128514"
---
# <a name="introducing-aspnet-web-pages---displaying-data"></a><span data-ttu-id="7491f-104">Wprowadzenie do wzorca ASP.NET Web Pages — wyświetlanie danych</span><span class="sxs-lookup"><span data-stu-id="7491f-104">Introducing ASP.NET Web Pages - Displaying Data</span></span>

<span data-ttu-id="7491f-105">przez [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="7491f-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="7491f-106">W tym samouczku dowiesz się, jak utworzyć bazę danych w programie WebMatrix oraz jak wyświetlać dane z bazy danych na stronie, gdy używasz stron ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="7491f-106">This tutorial shows you how to create a database in WebMatrix and how to display database data in a page when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="7491f-107">Przyjęto założenie, że zostały wykonane serii za pośrednictwem [wprowadzenie do programowania programu ASP.NET Web Pages](../introducing-razor-syntax-c.md).</span><span class="sxs-lookup"><span data-stu-id="7491f-107">It assumes you have completed the series through [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md).</span></span>
> 
> <span data-ttu-id="7491f-108">Zawartość:</span><span class="sxs-lookup"><span data-stu-id="7491f-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="7491f-109">Jak używać narzędzi programu WebMatrix do tworzenia bazy danych i tabel bazy danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-109">How to use WebMatrix tools to create a database and database tables.</span></span>
> - <span data-ttu-id="7491f-110">Jak używać narzędzi programu WebMatrix, aby dodać dane do bazy danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-110">How to use WebMatrix tools to add data to a database.</span></span>
> - <span data-ttu-id="7491f-111">Jak wyświetlić dane z bazy danych na stronie.</span><span class="sxs-lookup"><span data-stu-id="7491f-111">How to display data from the database on a page.</span></span>
> - <span data-ttu-id="7491f-112">Sposób uruchamiania poleceń SQL w składniku ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="7491f-112">How to run SQL commands in ASP.NET Web Pages.</span></span>
> - <span data-ttu-id="7491f-113">Jak dostosować `WebGrid` pomocnika, aby zmienić sposób wyświetlania danych i dodać stronicowanie i sortowanie.</span><span class="sxs-lookup"><span data-stu-id="7491f-113">How to customize the `WebGrid` helper to change the data display and to add paging and sorting.</span></span>
>   
> 
> <span data-ttu-id="7491f-114">Funkcje/technologie omówione:</span><span class="sxs-lookup"><span data-stu-id="7491f-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="7491f-115">Narzędzia graficzne bazy danych programu WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="7491f-115">WebMatrix database tools.</span></span>
> - <span data-ttu-id="7491f-116">`WebGrid` Pomocnik.</span><span class="sxs-lookup"><span data-stu-id="7491f-116">`WebGrid` helper.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="7491f-117">Jakie będziesz tworzyć</span><span class="sxs-lookup"><span data-stu-id="7491f-117">What You'll Build</span></span>

<span data-ttu-id="7491f-118">W poprzednim samouczku zostały wprowadzone do stron ASP.NET Web Pages (*.cshtml* plików), podstawowe informacje o składni Razor i Pomocnicy.</span><span class="sxs-lookup"><span data-stu-id="7491f-118">In the previous tutorial, you were introduced to ASP.NET Web Pages (*.cshtml* files), to the basics of Razor syntax, and to helpers.</span></span> <span data-ttu-id="7491f-119">W tym samouczku będziesz Rozpocznij tworzenie aplikacji rzeczywisty sieci web, którego będziesz używać w pozostałej części tej serii.</span><span class="sxs-lookup"><span data-stu-id="7491f-119">In this tutorial, you'll begin creating the actual web application that you'll use for the rest of the series.</span></span> <span data-ttu-id="7491f-120">Aplikacja jest aplikacji proste filmów, która umożliwia wyświetlanie, dodawanie, zmienianie i usuwanie informacje na temat filmów.</span><span class="sxs-lookup"><span data-stu-id="7491f-120">The app is a simple movie application that lets you view, add, change, and delete information about movies.</span></span>

<span data-ttu-id="7491f-121">Po zakończeniu korzystania z tego samouczka, będzie można wyświetlić listę filmu, która wygląda podobnie do tej strony:</span><span class="sxs-lookup"><span data-stu-id="7491f-121">When you're done with this tutorial, you'll be able to view a movie listing that looks like this page:</span></span>

![Wyświetlanie WebGrid, która obejmuje parametry równa nazwy klas CSS](displaying-data/_static/image1.png)

<span data-ttu-id="7491f-123">Jednak aby rozpocząć, należy utworzyć bazę danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-123">But to begin, you have to create a database.</span></span>

## <a name="a-very-brief-introduction-to-databases"></a><span data-ttu-id="7491f-124">Bardzo krótkie wprowadzenie do bazy danych</span><span class="sxs-lookup"><span data-stu-id="7491f-124">A Very Brief Introduction to Databases</span></span>

<span data-ttu-id="7491f-125">W tym samouczku będzie udostępniać tylko briefest wprowadzenie do bazy danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-125">This tutorial will provide only the briefest introduction to databases.</span></span> <span data-ttu-id="7491f-126">Jeśli masz doświadczenie w bazie danych, możesz pominąć tę sekcję krótki.</span><span class="sxs-lookup"><span data-stu-id="7491f-126">If you have database experience, you can skip this short section.</span></span>

<span data-ttu-id="7491f-127">Baza danych zawiera co najmniej jednej tabeli, które zawierają informacje &mdash; na przykład tabele dla klientów, zamówień i dostawców lub dla studentów, nauczycieli, klasy i klas.</span><span class="sxs-lookup"><span data-stu-id="7491f-127">A database contains one or more tables that contain information &mdash; for example, tables for customers, orders, and vendors, or for students, teachers, classes, and grades.</span></span> <span data-ttu-id="7491f-128">Strukturalnie tabeli bazy danych jest jak arkusz kalkulacyjny.</span><span class="sxs-lookup"><span data-stu-id="7491f-128">Structurally, a database table is like a spreadsheet.</span></span> <span data-ttu-id="7491f-129">Wyobraź sobie książki adresowej typowy.</span><span class="sxs-lookup"><span data-stu-id="7491f-129">Imagine a typical address book.</span></span> <span data-ttu-id="7491f-130">Dla każdego wpisu w książce adresowej (oznacza to, że dla każdej osoby nawiązującej) ma kilka rodzajów informacji, takich jak imię, nazwisko, adres, adres e-mail i numer telefonu.</span><span class="sxs-lookup"><span data-stu-id="7491f-130">For each entry in the address book (that is, for each person) you have several pieces of information such as first name, last name, address, email address, and phone number.</span></span>

![Przykładowa tabela bazy danych jako prosta siatka](displaying-data/_static/image2.png)

<span data-ttu-id="7491f-132">(Wiersze są czasami określane jako *rekordów*, i kolumny są czasami określane jako *pola*.)</span><span class="sxs-lookup"><span data-stu-id="7491f-132">(Rows are sometimes referred to as *records*, and columns are sometimes referred to as *fields*.)</span></span>

<span data-ttu-id="7491f-133">W przypadku większości tabel bazy danych Tabela musi mieć kolumnę zawierającą unikatową wartość, na przykład numer klienta, numer konta i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="7491f-133">For most database tables, the table has to have a column that contains a unique value, like a customer number, account number, and so on.</span></span> <span data-ttu-id="7491f-134">Ta wartość jest znana jako tabela *klucz podstawowy*, i używa ich do identyfikacji każdego wiersza w tabeli.</span><span class="sxs-lookup"><span data-stu-id="7491f-134">This value is known as the table's *primary key*, and you use it to identify each row in the table.</span></span> <span data-ttu-id="7491f-135">W tym przykładzie kolumna Identyfikator jest kluczem podstawowym książki adresowej w poprzednim przykładzie.</span><span class="sxs-lookup"><span data-stu-id="7491f-135">In the example, the ID column is the primary key for the address book shown in the previous example.</span></span>

<span data-ttu-id="7491f-136">Większość zadań, co zrobić w aplikacji sieci web składa się z odczytywania informacji z bazy danych i wyświetlanie go na stronie.</span><span class="sxs-lookup"><span data-stu-id="7491f-136">Much of the work you do in web applications consists of reading information out of the database and displaying it on a page.</span></span> <span data-ttu-id="7491f-137">Zostanie również często zbierać dane od użytkowników i dodać go do bazy danych lub zmodyfikujesz rekordy, które znajdują się już w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-137">You'll also often gather information from users and add it to a database, or you'll modify records that are already in the database.</span></span> <span data-ttu-id="7491f-138">(Omówimy wszystkie te operacje, w tym zestawie samouczków.)</span><span class="sxs-lookup"><span data-stu-id="7491f-138">(We'll cover all of these operations in the course of this tutorial set.)</span></span>

<span data-ttu-id="7491f-139">Praca bazy danych może być teraz znacznie skomplikowane i wymagać specjalistyczną wiedzę.</span><span class="sxs-lookup"><span data-stu-id="7491f-139">Database work can be enormously complex and can require specialized knowledge.</span></span> <span data-ttu-id="7491f-140">Dla tego zestawu samouczków masz jednak zapoznać się tylko podstawowymi pojęciami, które zostaną wszystkie wyjaśnione zgodnie z rzeczywistym.</span><span class="sxs-lookup"><span data-stu-id="7491f-140">For this tutorial set, though, you have to understand only basic concepts, which will all be explained as you go.</span></span>

## <a name="creating-a-database"></a><span data-ttu-id="7491f-141">Tworzenie bazy danych</span><span class="sxs-lookup"><span data-stu-id="7491f-141">Creating a Database</span></span>

<span data-ttu-id="7491f-142">Program WebMatrix zawiera narzędzia, które ułatwiają tworzenie bazy danych i tworzenie tabel w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-142">WebMatrix includes tools that make it easy to create a database and to create tables in the database.</span></span> <span data-ttu-id="7491f-143">(Struktury bazy danych jest określany jako bazy danych *schematu*.) Dla tego zestawu samouczków, utworzysz bazę danych, która zawiera tylko jedną tabelę &mdash; filmów.</span><span class="sxs-lookup"><span data-stu-id="7491f-143">(The structure of a database is referred to as the database's *schema*.) For this tutorial set, you'll create a database that has only one table in it &mdash; Movies.</span></span>

<span data-ttu-id="7491f-144">Otwórz program WebMatrix, jeśli jeszcze tego nie zrobiłeś, a następnie otwórz witrynę WebPagesMovies, który został utworzony w poprzednim samouczku.</span><span class="sxs-lookup"><span data-stu-id="7491f-144">Open WebMatrix if you haven't already done so, and open the WebPagesMovies site that you created in the previous tutorial.</span></span>

<span data-ttu-id="7491f-145">W okienku po lewej stronie kliknij **bazy danych** obszaru roboczego.</span><span class="sxs-lookup"><span data-stu-id="7491f-145">In the left pane, click the **Database** workspace.</span></span>

![Karty Obszar roboczy bazy danych programu WebMatrix](displaying-data/_static/image3.png)

<span data-ttu-id="7491f-147">Zmiany wstążki, aby wyświetlić zadania związane z bazy danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-147">The ribbon changes to show database-related tasks.</span></span> <span data-ttu-id="7491f-148">Na wstążce kliknij **nową bazę danych**.</span><span class="sxs-lookup"><span data-stu-id="7491f-148">In the ribbon, click **New Database**.</span></span>

![Nowa baza danych przycisk na wstążce programu WebMatrix](displaying-data/_static/image4.png)

<span data-ttu-id="7491f-150">Program WebMatrix tworzy bazę danych programu SQL Server CE ( *.sdf* pliku), ma taką samą nazwę jako lokacji &mdash; *WebPagesMovies.sdf*.</span><span class="sxs-lookup"><span data-stu-id="7491f-150">WebMatrix creates a SQL Server CE database (an *.sdf* file) that has the same name as your site &mdash; *WebPagesMovies.sdf*.</span></span> <span data-ttu-id="7491f-151">(Nie zrobi to w tym miejscu, ale można zmienić nazwę pliku do żadnego elementu lubisz, tak długo, jak przedstawiono w nim *.sdf* rozszerzenia.)</span><span class="sxs-lookup"><span data-stu-id="7491f-151">(You won't do this here, but you can rename the file to anything you like, as long as it has an *.sdf* extension.)</span></span>

## <a name="creating-a-table"></a><span data-ttu-id="7491f-152">Tworzenie tabeli</span><span class="sxs-lookup"><span data-stu-id="7491f-152">Creating a Table</span></span>

<span data-ttu-id="7491f-153">Na wstążce kliknij **nową tabelę**.</span><span class="sxs-lookup"><span data-stu-id="7491f-153">In the ribbon, click **New Table**.</span></span> <span data-ttu-id="7491f-154">Program WebMatrix zostanie otwarty projektant tabel w nowej karcie. (Jeśli opcja Nowa tabela jest niedostępna, upewnij się, że nowa baza danych jest wybrane w widoku drzewa po lewej stronie.)</span><span class="sxs-lookup"><span data-stu-id="7491f-154">WebMatrix opens the table designer in a new tab. (If the New Table option isn't available, make sure that the new database is selected in the tree view on the left.)</span></span>

!["Nowa tabela" przycisk na wstążce programu WebMatrix](displaying-data/_static/image5.png)

<span data-ttu-id="7491f-156">W polu tekstowym u góry (gdzie znak wodny mówi "Wprowadź nazwę tabeli") wprowadź "Filmy".</span><span class="sxs-lookup"><span data-stu-id="7491f-156">In the text box at the top (where the watermark says "Enter table name"), enter "Movies".</span></span>

![Wprowadź nazwę tabeli w projektancie bazy danych programu WebMatrix](displaying-data/_static/image6.png)

<span data-ttu-id="7491f-158">Określa poszczególnych kolumn, które się w okienku poniżej nazwy tabeli.</span><span class="sxs-lookup"><span data-stu-id="7491f-158">The pane underneath the table name is where you define individual columns.</span></span> <span data-ttu-id="7491f-159">Aby uzyskać *filmy* tabeli w tym samouczku utworzysz tylko kilka kolumn: *Identyfikator*, *tytuł*, *gatunku*, i *roku*.</span><span class="sxs-lookup"><span data-stu-id="7491f-159">For the *Movies* table in this tutorial, you'll create only a few columns: *ID*, *Title*, *Genre*, and *Year*.</span></span>

<span data-ttu-id="7491f-160">W **nazwa** wprowadź "ID".</span><span class="sxs-lookup"><span data-stu-id="7491f-160">In the **Name** box, enter "ID".</span></span> <span data-ttu-id="7491f-161">Wprowadź wartość w tym miejscu aktywuje wszystkich kontrolek dla nowej kolumny.</span><span class="sxs-lookup"><span data-stu-id="7491f-161">Entering a value here activates all the controls for the new column.</span></span>

<span data-ttu-id="7491f-162">Kartę **— typ danych** i wybierz polecenie **int**. Ta wartość określa, że kolumna Identyfikatora będzie zawierać danych liczb całkowitych (liczba).</span><span class="sxs-lookup"><span data-stu-id="7491f-162">Tab to the **Data Type** list and choose **int**. This value specifies that the ID column will contain integer (number) data.</span></span>

> [!NOTE]
> <span data-ttu-id="7491f-163">Nie będzie nazywamy je jakąkolwiek więcej informacji znajdziesz tutaj (znacznie), ale standardowa gestów klawiatury Windows można użyć do nawigacji w tej siatce.</span><span class="sxs-lookup"><span data-stu-id="7491f-163">We won't call it out any more here (much), but you can use standard Windows keyboard gestures to navigate in this grid.</span></span> <span data-ttu-id="7491f-164">Na przykład ustawić tabulator między polami, można po prostu zacznij pisać, aby wybrać element na liście i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="7491f-164">For example, you can tab between fields, you can just start typing in order to select an item in a list, and so on.</span></span>

<span data-ttu-id="7491f-165">Karta ostatnie **wartości domyślnej** pole (oznacza to, pozostaw to pole puste).</span><span class="sxs-lookup"><span data-stu-id="7491f-165">Tab past the **Default Value** box (that is, leave it blank).</span></span> <span data-ttu-id="7491f-166">Kartę **jest kluczem podstawowym** pole wyboru, a następnie wybierz ją.</span><span class="sxs-lookup"><span data-stu-id="7491f-166">Tab to the **Is Primary Key** check box and select it.</span></span> <span data-ttu-id="7491f-167">Ta opcja nakazuje bazy danych *identyfikator* kolumna będzie zawierać dane, który identyfikuje poszczególne wiersze.</span><span class="sxs-lookup"><span data-stu-id="7491f-167">This option tells the database that the *ID* column will contain the data that identifies individual rows.</span></span> <span data-ttu-id="7491f-168">(Czyli każdy wiersz będą mieć unikatową wartość w kolumnie identyfikator, która umożliwia znalezienie tego wiersza.)</span><span class="sxs-lookup"><span data-stu-id="7491f-168">(That is, each row will have a unique value in the ID column that you can use to find that row.)</span></span>

<span data-ttu-id="7491f-169">Wybierz **tożsamości jest** opcji.</span><span class="sxs-lookup"><span data-stu-id="7491f-169">Choose the **Is Identity** option.</span></span> <span data-ttu-id="7491f-170">Ta opcja nakazuje bazy danych, czy należy automatycznie wygenerować kolejny numer kolejny dla każdego nowego wiersza.</span><span class="sxs-lookup"><span data-stu-id="7491f-170">This option tells the database that it should automatically generate the next sequential number for each new row.</span></span> <span data-ttu-id="7491f-171">( **Tożsamości jest** opcji działa tylko wtedy, gdy wybrano również **jest kluczem podstawowym** opcję.)</span><span class="sxs-lookup"><span data-stu-id="7491f-171">(The **Is Identity** option works only if you've also selected the **Is Primary Key** option.)</span></span>

<span data-ttu-id="7491f-172">Kliknij następny wiersz siatki, lub naciśnij klawisz Tab dwa razy, aby zakończyć bieżący wiersz.</span><span class="sxs-lookup"><span data-stu-id="7491f-172">Click in the next grid row, or press Tab twice to finish the current row.</span></span> <span data-ttu-id="7491f-173">Albo gestu zapisuje bieżący wiersz i rozpoczyna się kolejny.</span><span class="sxs-lookup"><span data-stu-id="7491f-173">Either gesture saves the current row and starts the next one.</span></span> <span data-ttu-id="7491f-174">Należy zauważyć, że **wartości domyślnej** kolumny jest teraz wyświetlany komunikat **Null**.</span><span class="sxs-lookup"><span data-stu-id="7491f-174">Notice that the **Default Value** column now says **Null**.</span></span> <span data-ttu-id="7491f-175">(Wartość null jest wartością domyślną dla wartości domyślnej, więc do speak).</span><span class="sxs-lookup"><span data-stu-id="7491f-175">(Null is the default value for the default value, so to speak.)</span></span>

<span data-ttu-id="7491f-176">Po zakończeniu, definiując nowe **identyfikator** kolumny, Projektant będzie wyglądać na tej ilustracji:</span><span class="sxs-lookup"><span data-stu-id="7491f-176">When you've finished defining the new **ID** column, the designer will look like this illustration:</span></span>

![Projektant baz danych programu WebMatrix po zdefiniowaniu kolumny Identyfikatora dla tabeli filmy](displaying-data/_static/image7.png)

<span data-ttu-id="7491f-178">Aby utworzyć w następnej kolumnie, kliknij pole w **nazwa** kolumny.</span><span class="sxs-lookup"><span data-stu-id="7491f-178">To create the next column, click in the box in the **Name** column.</span></span> <span data-ttu-id="7491f-179">Wprowadź "Title" dla kolumny, a następnie wybierz **nvarchar** dla **— typ danych** wartość.</span><span class="sxs-lookup"><span data-stu-id="7491f-179">Enter "Title" for the column and then select **nvarchar** for the **Data Type** value.</span></span> <span data-ttu-id="7491f-180">Część "var" **nvarchar** bazy danych informuje, że dane dla tej kolumny będzie ciąg, którego rozmiar może się różnić między rekordami.</span><span class="sxs-lookup"><span data-stu-id="7491f-180">The "var" part of **nvarchar** tells the database that the data for this column will be a string whose size might vary from record to record.</span></span> <span data-ttu-id="7491f-181">(Prefiks "n" reprezentuje "national", co oznacza, że pole może zawierać dane znaków dla dowolnego alfabetu lub systemie pisma — oznacza to pole zawiera dane Unicode.)</span><span class="sxs-lookup"><span data-stu-id="7491f-181">(The "n" prefix represents "national," which indicates that the field can hold character data for any alphabet or writing system — that is, the field holds Unicode data.)</span></span>

<span data-ttu-id="7491f-182">Po wybraniu **nvarchar**, pojawi się okno innego, za pomocą której można wprowadzić maksymalną długość pola.</span><span class="sxs-lookup"><span data-stu-id="7491f-182">When you choose **nvarchar**, another box appears where you can enter the maximum length for the field.</span></span> <span data-ttu-id="7491f-183">Wprowadź 50, przy założeniu, że bez tytułu film, w tym samouczku będziesz pracować będzie więcej niż 50 znaków.</span><span class="sxs-lookup"><span data-stu-id="7491f-183">Enter 50, on the assumption that no movie title that you'll work with in this tutorial will be longer than 50 characters.</span></span>

<span data-ttu-id="7491f-184">Pomiń **wartości domyślnej** i wyczyść **Zezwalaj na wartości null** opcji.</span><span class="sxs-lookup"><span data-stu-id="7491f-184">Skip **Default Value** and clear the **Allow Nulls** option.</span></span> <span data-ttu-id="7491f-185">Nie ma bazy danych do Zezwalaj na filmy wprowadzane do bazy danych, które nie mają tytuł.</span><span class="sxs-lookup"><span data-stu-id="7491f-185">You don't want the database to allow any movies to be entered into the database that don't have a title.</span></span>

<span data-ttu-id="7491f-186">Gdy ukończysz pracę i przejdź do następnego wiersza projektanta wygląda na tej ilustracji:</span><span class="sxs-lookup"><span data-stu-id="7491f-186">When you're done and move to the next row, the designer looks like this illustration:</span></span>

![Projektant baz danych programu WebMatrix po zdefiniowaniu kolumny Title dla tabeli filmy](displaying-data/_static/image8.png)

<span data-ttu-id="7491f-188">Powtórz te kroki, aby utworzyć kolumnę o nazwie "Rodzaju", z wyjątkiem długości, ustaw ją po prostu 30.</span><span class="sxs-lookup"><span data-stu-id="7491f-188">Repeat these steps to create a column named "Genre", except for the length, set it to just 30.</span></span>

<span data-ttu-id="7491f-189">Utwórz inną kolumnę o nazwie "Roku."</span><span class="sxs-lookup"><span data-stu-id="7491f-189">Create another column named "Year."</span></span> <span data-ttu-id="7491f-190">Typ danych wybierz **nchar** (nie **nvarchar**) i ustawić długość na 4.</span><span class="sxs-lookup"><span data-stu-id="7491f-190">For the data type, choose **nchar** (not **nvarchar**) and set the length to 4.</span></span> <span data-ttu-id="7491f-191">W roku możesz zacząć korzystać 4-cyfrowy numer, takich jak "1995" lub "2010", dzięki czemu nie wymagają kolumny o zmiennym rozmiarze.</span><span class="sxs-lookup"><span data-stu-id="7491f-191">For the year, you're going to use a 4-digit number like "1995" or "2010", so you don't require a variable-sized column.</span></span>

<span data-ttu-id="7491f-192">Oto jak wygląda gotowego projektu:</span><span class="sxs-lookup"><span data-stu-id="7491f-192">Here's what the finished design looks like:</span></span>

![Projektant baz danych programu WebMatrix po wszystkich pól zdefiniowanych dla tabeli filmy](displaying-data/_static/image9.png)

<span data-ttu-id="7491f-194">Naciśnij klawisze Ctrl + S, lub kliknij przycisk **Zapisz** przycisku paska narzędzi Szybki dostęp.</span><span class="sxs-lookup"><span data-stu-id="7491f-194">Press Ctrl+S or click the **Save** button in the Quick Access toolbar.</span></span> <span data-ttu-id="7491f-195">Zamknięcie karty, aby zamknąć projektanta bazy danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-195">Close the database designer by closing the tab.</span></span>

## <a name="adding-some-example-data"></a><span data-ttu-id="7491f-196">Dodawanie niektóre przykładowe dane</span><span class="sxs-lookup"><span data-stu-id="7491f-196">Adding Some Example Data</span></span>

<span data-ttu-id="7491f-197">W dalszej części tej serii samouczków utworzysz strony, w którym możesz wprowadzić nowe filmy w formularzu.</span><span class="sxs-lookup"><span data-stu-id="7491f-197">Later in this tutorial series you'll create a page where you can enter new movies in a form.</span></span> <span data-ttu-id="7491f-198">Teraz jednak możesz dodać niektóre przykładowe dane, które następnie można wyświetlić na stronie.</span><span class="sxs-lookup"><span data-stu-id="7491f-198">For now, however, you can add some example data that you can then display on a page.</span></span>

<span data-ttu-id="7491f-199">W **bazy danych** obszaru roboczego w programie WebMatrix, zwróć uwagę, że istnieje drzewa, która pokazuje *.sdf* pliku została utworzona wcześniej.</span><span class="sxs-lookup"><span data-stu-id="7491f-199">In the **Database** workspace in WebMatrix, notice that there's a tree that shows you the *.sdf* file you created earlier.</span></span> <span data-ttu-id="7491f-200">Otwórz węzeł nowej *.sdf* pliku, a następnie otwórz **tabel** węzła.</span><span class="sxs-lookup"><span data-stu-id="7491f-200">Open the node for your new *.sdf* file, and then open the **Tables** node.</span></span>

![Obszar roboczy bazy danych programu WebMatrix, przy użyciu drzewa, otwarty do tabeli filmy](displaying-data/_static/image10.png)

<span data-ttu-id="7491f-202">Kliknij prawym przyciskiem myszy **filmy** węzeł, a następnie wybierz **danych**.</span><span class="sxs-lookup"><span data-stu-id="7491f-202">Right-click the **Movies** node and then choose **Data**.</span></span> <span data-ttu-id="7491f-203">Program WebMatrix otwiera siatki, w którym możesz wprowadzić dane dla *filmy* tabeli:</span><span class="sxs-lookup"><span data-stu-id="7491f-203">WebMatrix opens a grid where you can enter data for the *Movies* table:</span></span>

![Siatka zapisu bazy danych w programie WebMatrix (pusty)](displaying-data/_static/image11.png)

<span data-ttu-id="7491f-205">Kliknij przycisk **tytuł** kolumny i wpisz "Podczas Harry spełnione Zosi".</span><span class="sxs-lookup"><span data-stu-id="7491f-205">Click the **Title** column and enter "When Harry Met Sally".</span></span> <span data-ttu-id="7491f-206">Przenieś do **gatunku** kolumny (możesz użyć klawisza Tab) i wprowadź "Sceny miłosne Komedia".</span><span class="sxs-lookup"><span data-stu-id="7491f-206">Move to the **Genre** column (you can use the Tab key) and enter "Romantic Comedy".</span></span> <span data-ttu-id="7491f-207">Przenieś do **roku** kolumny i wpisz "1989":</span><span class="sxs-lookup"><span data-stu-id="7491f-207">Move to the **Year** column and enter "1989":</span></span>

![Siatka zapisu bazy danych w programie WebMatrix z jednego rekordu](displaying-data/_static/image12.png)

<span data-ttu-id="7491f-209">Naciśnij klawisz Enter i programu WebMatrix zapisuje ten nowy film.</span><span class="sxs-lookup"><span data-stu-id="7491f-209">Press Enter, and WebMatrix saves the new movie.</span></span> <span data-ttu-id="7491f-210">Należy zauważyć, że **identyfikator** kolumna została wypełniona.</span><span class="sxs-lookup"><span data-stu-id="7491f-210">Notice that the **ID** column has been filled in.</span></span>

![Siatka wpis bazy danych w programie WebMatrix z jednego rekordu i automatycznie wygenerowany identyfikator](displaying-data/_static/image13.png)

<span data-ttu-id="7491f-212">Wprowadź inny film (na przykład "stała się za pomocą wiatru", "Dramat", "1939").</span><span class="sxs-lookup"><span data-stu-id="7491f-212">Enter another movie (for example, "Gone with the Wind", "Drama", "1939").</span></span> <span data-ttu-id="7491f-213">W kolumnie identyfikator jest wprowadzone ponownie:</span><span class="sxs-lookup"><span data-stu-id="7491f-213">The ID column is filled in again:</span></span>

![Siatka zapisu bazy danych w programie WebMatrix z dwa rekordy i identyfikatorów generowanych automatycznie](displaying-data/_static/image14.png)

<span data-ttu-id="7491f-215">Wprowadź trzeci filmu (na przykład "Ghostbusters", "Dokument").</span><span class="sxs-lookup"><span data-stu-id="7491f-215">Enter a third movie (for example, "Ghostbusters", "Comedy").</span></span> <span data-ttu-id="7491f-216">Jako eksperyment, pozostaw **roku** kolumny blank, a następnie naciśnij klawisz Enter.</span><span class="sxs-lookup"><span data-stu-id="7491f-216">As an experiment, leave the **Year** column blank and then press Enter.</span></span> <span data-ttu-id="7491f-217">Ponieważ użytkownik niezaznaczoną **Zezwalaj na wartości null** opcji i baza danych zawiera błąd:</span><span class="sxs-lookup"><span data-stu-id="7491f-217">Because you unselected the **Allow Nulls** option, the database shows an error:</span></span>

![Błąd 'Nieprawidłowe dane' wyświetlane, gdy wartości wymaganej kolumny jest puste](displaying-data/_static/image15.png)

<span data-ttu-id="7491f-219">Kliknij przycisk **OK** Przejdź wstecz i naprawić zapisu (jest 1984 roku "Ghostbusters".) i naciśnij klawisz Enter.</span><span class="sxs-lookup"><span data-stu-id="7491f-219">Click **OK** to go back and fix the entry (the year for "Ghostbusters" is 1984), and then press Enter.</span></span>

<span data-ttu-id="7491f-220">Wypełnij kilka filmów, dopóki nie uzyskasz 8, lub tak.</span><span class="sxs-lookup"><span data-stu-id="7491f-220">Fill in several movies until you have 8 or so.</span></span> <span data-ttu-id="7491f-221">(Wprowadzanie 8 sprawia, że łatwiej pracować z stronicowania później.</span><span class="sxs-lookup"><span data-stu-id="7491f-221">(Entering 8 makes it easier to work with paging later.</span></span> <span data-ttu-id="7491f-222">Ale jeśli jest zbyt wiele, należy wprowadzić kilka teraz). Rzeczywiste dane, nie ma znaczenia.</span><span class="sxs-lookup"><span data-stu-id="7491f-222">But if that's too many, enter just a few for now.) The actual data doesn't matter.</span></span>

![Siatka zapisu bazy danych w programie WebMatrix z albo rekordów](displaying-data/_static/image16.png)

<span data-ttu-id="7491f-224">Po wprowadzeniu wszystkich filmów bez żadnych błędów, identyfikator wartości są sekwencyjne.</span><span class="sxs-lookup"><span data-stu-id="7491f-224">If you entered all the movies without any errors, the ID values are sequential.</span></span> <span data-ttu-id="7491f-225">Podjęto próbę zapisania rekordu niekompletne filmu, numery identyfikatorów może być sekwencyjne.</span><span class="sxs-lookup"><span data-stu-id="7491f-225">If you tried to save an incomplete movie record, the ID numbers might not be sequential.</span></span> <span data-ttu-id="7491f-226">Jeśli tak, to zignorować.</span><span class="sxs-lookup"><span data-stu-id="7491f-226">If so, that's okay.</span></span> <span data-ttu-id="7491f-227">Liczby nie mają żadnego znaczenia nieprzerwaną pracę, a jedyną czynnością, które są ważne jest, że są one unikatowe w *filmy* tabeli.</span><span class="sxs-lookup"><span data-stu-id="7491f-227">The numbers don't have any inherent meaning, and the only thing that's important is that they're unique in the *Movies* table.</span></span>

<span data-ttu-id="7491f-228">Zamknij kartę zawierającą projektanta bazy danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-228">Close the tab that contains the database designer.</span></span>

<span data-ttu-id="7491f-229">Teraz możesz wyłączyć wyświetlanie tych danych na stronie sieci web.</span><span class="sxs-lookup"><span data-stu-id="7491f-229">Now you can turn to displaying this data on a web page.</span></span>

## <a name="displaying-data-in-a-page-by-using-the-webgrid-helper"></a><span data-ttu-id="7491f-230">Wyświetlanie danych na stronie sieci przy użyciu Pomocnika WebGrid</span><span class="sxs-lookup"><span data-stu-id="7491f-230">Displaying Data in a Page by Using the WebGrid Helper</span></span>

<span data-ttu-id="7491f-231">Aby wyświetlić dane na stronie sieci, możesz zacząć korzystać `WebGrid` pomocnika.</span><span class="sxs-lookup"><span data-stu-id="7491f-231">To display data in a page, you're going to use the `WebGrid` helper.</span></span> <span data-ttu-id="7491f-232">Tego pomocnika generuje wyświetlaną w siatce lub na tabeli (wiersze i kolumny).</span><span class="sxs-lookup"><span data-stu-id="7491f-232">This helper produces a display in a grid or table (rows and columns).</span></span> <span data-ttu-id="7491f-233">Jak można zauważyć, będzie możliwe dopracować siatka z formatowania i inne funkcje.</span><span class="sxs-lookup"><span data-stu-id="7491f-233">As you'll see, you'll be able refine the grid with formatting and other features.</span></span>

<span data-ttu-id="7491f-234">Aby uruchomić siatki, musisz napisać kilka wierszy kodu.</span><span class="sxs-lookup"><span data-stu-id="7491f-234">To run the grid, you'll have to write a few lines of code.</span></span> <span data-ttu-id="7491f-235">Te wiersze kilka będzie służyć jako rodzaju wzorzec prawie cały dostęp do danych, jak w tym samouczku.</span><span class="sxs-lookup"><span data-stu-id="7491f-235">These few lines will serve as a kind of pattern for almost all of the data access that you do in this tutorial.</span></span>

> [!NOTE]
> <span data-ttu-id="7491f-236">Masz naprawdę wiele opcji do wyświetlania danych na stronie; `WebGrid` pomocy jest tylko jeden.</span><span class="sxs-lookup"><span data-stu-id="7491f-236">You actually have many options for displaying data on a page; the `WebGrid` helper is just one.</span></span> <span data-ttu-id="7491f-237">Wybraliśmy ją na potrzeby tego samouczka, ponieważ jest najprostszym sposobem wyświetlania danych i jest jest elastyczny.</span><span class="sxs-lookup"><span data-stu-id="7491f-237">We chose it for this tutorial because it's the easiest way to display data and because it's reasonably flexible.</span></span> <span data-ttu-id="7491f-238">W zestawie następnego samouczka pojawi się, jak używać więcej "manual" nowy sposób pracy z danymi na stronie, co daje więcej bezpośrednią kontrolę nad sposób wyświetlania danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-238">In the next tutorial set, you'll see how to use a more "manual" way to work with data in the page, which gives you more direct control over how to display the data.</span></span>

<span data-ttu-id="7491f-239">W okienku po lewej stronie w programie WebMatrix, kliknij przycisk **pliki** obszaru roboczego.</span><span class="sxs-lookup"><span data-stu-id="7491f-239">In the left pane in WebMatrix, click the **Files** workspace.</span></span>

<span data-ttu-id="7491f-240">Nowe utworzona baza danych znajduje się w *aplikacji\_danych* folderu.</span><span class="sxs-lookup"><span data-stu-id="7491f-240">The new database you created is in the *App\_Data* folder.</span></span> <span data-ttu-id="7491f-241">Jeśli folder już nie istnieje, program WebMatrix utworzony dla nowej bazy danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-241">If the folder didn't already exist, WebMatrix created it for your new database.</span></span> <span data-ttu-id="7491f-242">(Folder może występować Jeśli wcześniej została zainstalowana pomocników.)</span><span class="sxs-lookup"><span data-stu-id="7491f-242">(The folder might have existed if you'd previously installed helpers.)</span></span>

<span data-ttu-id="7491f-243">W widoku drzewa wybierz katalog główny witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="7491f-243">In the tree view, select the root of the website.</span></span> <span data-ttu-id="7491f-244">Musisz wybrać katalog główny witryny sieci Web; w przeciwnym razie nowy plik może być dodana do aplikacji\_folderu danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-244">You must select the root of the website; otherwise, the new file might be added to the App\_Data folder.</span></span>

<span data-ttu-id="7491f-245">Na wstążce kliknij **New**.</span><span class="sxs-lookup"><span data-stu-id="7491f-245">In the ribbon, click **New**.</span></span> <span data-ttu-id="7491f-246">W **wybierz typ pliku** wybierz **CSHTML**.</span><span class="sxs-lookup"><span data-stu-id="7491f-246">In the **Choose a File Type** box, choose **CSHTML**.</span></span>

<span data-ttu-id="7491f-247">W **nazwa** pola, określ nazwę nowej strony "Movies.cshtml":</span><span class="sxs-lookup"><span data-stu-id="7491f-247">In the **Name** box, name the new page "Movies.cshtml":</span></span>

![Okno dialogowe "Wybierz typ pliku" przedstawiający stronę "Filmy"](displaying-data/_static/image17.png)

<span data-ttu-id="7491f-249">Kliknij przycisk **OK** przycisku.</span><span class="sxs-lookup"><span data-stu-id="7491f-249">Click the **OK** button.</span></span> <span data-ttu-id="7491f-250">Program WebMatrix zostanie otwarty nowy plik niektóre szkielet zawartych.</span><span class="sxs-lookup"><span data-stu-id="7491f-250">WebMatrix opens a new file with some skeleton elements in it.</span></span> <span data-ttu-id="7491f-251">Najpierw przedstawiono tworzenie kodu, aby pobrać dane z bazy danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-251">First you'll write some code to go get the data from the database.</span></span> <span data-ttu-id="7491f-252">Następnie dodasz kod znaczników do strony, aby faktycznie wyświetlić dane.</span><span class="sxs-lookup"><span data-stu-id="7491f-252">Then you'll add markup to the page to actually display the data.</span></span>

### <a name="writing-the-data-query-code"></a><span data-ttu-id="7491f-253">Pisanie kodu zapytania danych</span><span class="sxs-lookup"><span data-stu-id="7491f-253">Writing the Data Query Code</span></span>

<span data-ttu-id="7491f-254">W górnej części strony między `@{` i `}` znaków, wprowadź następujący kod.</span><span class="sxs-lookup"><span data-stu-id="7491f-254">At the top of the page, between the `@{` and `}` characters, enter the following code.</span></span> <span data-ttu-id="7491f-255">(Upewnij się, wprowadź ten kod między otwierające i zamykające nawiasy klamrowe).</span><span class="sxs-lookup"><span data-stu-id="7491f-255">(Make sure that you enter this code between the opening and closing braces.)</span></span>

[!code-csharp[Main](displaying-data/samples/sample1.cs)]

<span data-ttu-id="7491f-256">Pierwszy wiersz spowoduje otwarcie bazy danych, który został utworzony wcześniej, który jest zawsze pierwszym krokiem przed wykonaniem coś z bazą danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-256">The first line opens the database that you created earlier, which is always the first step before doing something with the database.</span></span> <span data-ttu-id="7491f-257">Poinformuj `Database.Open` nazwę metody bazy danych, aby otworzyć.</span><span class="sxs-lookup"><span data-stu-id="7491f-257">You tell the `Database.Open` method name of the database to open.</span></span> <span data-ttu-id="7491f-258">Należy zauważyć, że nie uwzględniają *.sdf* w nazwie.</span><span class="sxs-lookup"><span data-stu-id="7491f-258">Notice that you don't include *.sdf* in the name.</span></span> <span data-ttu-id="7491f-259">`Open` Metoda zakłada, że szuka *.sdf* pliku (oznacza to, że *WebPagesMovies.sdf*) oraz że *.sdf* plik znajduje się w *aplikacji\_ Dane* folderu.</span><span class="sxs-lookup"><span data-stu-id="7491f-259">The `Open` method assumes that it's looking for an *.sdf* file (that is, *WebPagesMovies.sdf*) and that the *.sdf* file is in the *App\_Data* folder.</span></span> <span data-ttu-id="7491f-260">(Wcześniej mogliśmy zauważyć że *aplikacji\_danych* folder jest zarezerwowany; ten scenariusz jest jednym z miejsc, w którym ASP.NET sprawia, że założeń o tej nazwie.)</span><span class="sxs-lookup"><span data-stu-id="7491f-260">(Earlier we noted that the *App\_Data* folder is reserved; this scenario is one of the places where ASP.NET makes assumptions about that name.)</span></span>

<span data-ttu-id="7491f-261">Gdy baza danych jest otwarta, odwołanie do niej są umieszczane w zmiennej o nazwie `db`.</span><span class="sxs-lookup"><span data-stu-id="7491f-261">When the database is opened, a reference to it is put into the variable named `db`.</span></span> <span data-ttu-id="7491f-262">(Które mogą być nazwane niczego.) `db` Zmienna jest jak użytkownik będzie pozostać interakcji z bazą danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-262">(Which could be named anything.) The `db` variable is how you'll end up interacting with the database.</span></span>

<span data-ttu-id="7491f-263">Drugi wiersz faktycznie pobiera dane bazy danych przy użyciu `Query` metody.</span><span class="sxs-lookup"><span data-stu-id="7491f-263">The second line actually fetches the database data by using the `Query` method.</span></span> <span data-ttu-id="7491f-264">Zwróć uwagę, jak działa ten kod: `db` zmienna odwołuje się do otwartej bazy danych, a następnie wywołuje `Query` metody, używając `db` zmiennej (`db.Query`).</span><span class="sxs-lookup"><span data-stu-id="7491f-264">Notice how this code works: the `db` variable has a reference to the opened database, and you invoke the `Query` method by using the `db` variable (`db.Query`).</span></span>

<span data-ttu-id="7491f-265">Samo zapytanie jest SQL `Select` instrukcji.</span><span class="sxs-lookup"><span data-stu-id="7491f-265">The query itself is a SQL `Select` statement.</span></span> <span data-ttu-id="7491f-266">(Aby trochę informacji uzupełniających na temat programu SQL, zobacz objaśnienia w dalszej części). W instrukcji `Movies` Określa tabelę, aby wykonać zapytanie.</span><span class="sxs-lookup"><span data-stu-id="7491f-266">(For a little background about SQL, see the explanation later.) In the statement, `Movies` identifies the table to query.</span></span> <span data-ttu-id="7491f-267">`*` Znak Określa, że zapytanie powinno zwrócić wszystkie kolumny z tabeli.</span><span class="sxs-lookup"><span data-stu-id="7491f-267">The `*` character specifies that the query should return all the columns from the table.</span></span> <span data-ttu-id="7491f-268">(Można podać również listę kolumn pojedynczo, oddzielonych przecinkami.)</span><span class="sxs-lookup"><span data-stu-id="7491f-268">(You could also list columns individually, separated by commas.)</span></span>

<span data-ttu-id="7491f-269">Wyniki zapytania, ewentualnie są zwracane i udostępniona w `selectedData` zmiennej.</span><span class="sxs-lookup"><span data-stu-id="7491f-269">The results of the query, if any, are returned and made available in the `selectedData` variable.</span></span> <span data-ttu-id="7491f-270">Ponownie zmiennej może być nazwane niczego.</span><span class="sxs-lookup"><span data-stu-id="7491f-270">Again, the variable could be named anything.</span></span>

<span data-ttu-id="7491f-271">Na koniec trzeci wiersz informuje ASP.NET, czy chcesz użyć instancji `WebGrid` pomocnika.</span><span class="sxs-lookup"><span data-stu-id="7491f-271">Finally, the third line tells ASP.NET that you want to use an instance of the `WebGrid` helper.</span></span> <span data-ttu-id="7491f-272">Możesz utworzyć (*wystąpienia*) obiekt pomocnika za pomocą `new` — słowo kluczowe i przekaż go wyników zapytania za pomocą `selectedData` zmiennej.</span><span class="sxs-lookup"><span data-stu-id="7491f-272">You create (*instantiate*) the helper object by using the `new` keyword and pass it the query results via the `selectedData` variable.</span></span> <span data-ttu-id="7491f-273">Nowy `WebGrid` obiektu wraz z wynikami zapytania bazy danych są udostępniane w `grid` zmiennej.</span><span class="sxs-lookup"><span data-stu-id="7491f-273">The new `WebGrid` object, along with the results of the database query, are made available in the `grid` variable.</span></span> <span data-ttu-id="7491f-274">Należy wynik za chwilę, aby faktycznie wyświetlić dane na stronie.</span><span class="sxs-lookup"><span data-stu-id="7491f-274">You'll need that result in a moment to actually display the data in the page.</span></span>

<span data-ttu-id="7491f-275">Na tym etapie bazy danych został otwarty, trafiła do Ciebie dane chcesz i przygotowaniu `WebGrid` pomocnika z tymi danymi.</span><span class="sxs-lookup"><span data-stu-id="7491f-275">At this stage, the database has been opened, you've gotten the data you want, and you've prepared the `WebGrid` helper with that data.</span></span> <span data-ttu-id="7491f-276">Następnie jest utworzenie znaczników na stronie.</span><span class="sxs-lookup"><span data-stu-id="7491f-276">Next is to create the markup in the page.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="7491f-277">**Język Structured Query Language (SQL)**</span><span class="sxs-lookup"><span data-stu-id="7491f-277">**Structured Query Language (SQL)**</span></span>
> 
> <span data-ttu-id="7491f-278">SQL jest językiem, który jest używany w większości relacyjnych baz danych do zarządzania danymi w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-278">SQL is a language that's used in most relational databases for managing data in a database.</span></span> <span data-ttu-id="7491f-279">Zawiera polecenia umożliwiające pobieranie danych i zaktualizować go i umożliwiające tworzenie, modyfikowanie i zarządzanie danymi w tabelach bazy danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-279">It includes commands that let you retrieve data and update it, and that let you create, modify, and manage data in database tables.</span></span> <span data-ttu-id="7491f-280">SQL różni się od języka programowania (takich jak C#).</span><span class="sxs-lookup"><span data-stu-id="7491f-280">SQL is different than a programming language (like C#).</span></span> <span data-ttu-id="7491f-281">Przy użyciu języka SQL Poinformuj bazy danych należy, i jest zadanie bazy danych, aby dowiedzieć się, jak można pobrać dane lub wykonać zadanie.</span><span class="sxs-lookup"><span data-stu-id="7491f-281">With SQL, you tell the database what you want, and it's the database's job to figure out how to get the data or perform the task.</span></span> <span data-ttu-id="7491f-282">Poniżej przedstawiono przykłady niektórych poleceń SQL oraz ich działania:</span><span class="sxs-lookup"><span data-stu-id="7491f-282">Here are examples of some SQL commands and what they do:</span></span>
> 
> `Select * From Movies`
> 
> `SELECT ID, Name, Price FROM Product WHERE Price > 10.00 ORDER BY Name`
> 
> <span data-ttu-id="7491f-283">Pierwszy `Select` instrukcji pobiera wszystkie kolumny (określony przez `*`) z *filmy* tabeli.</span><span class="sxs-lookup"><span data-stu-id="7491f-283">The first `Select` statement gets all the columns (specified by `*`) from the *Movies* table.</span></span>
> 
> <span data-ttu-id="7491f-284">Drugi `Select` instrukcji pobiera kolumny Identyfikatora, nazwy i ceny z rekordów *produktu* tabeli, w której wartość kolumny Cena jest więcej niż 10.</span><span class="sxs-lookup"><span data-stu-id="7491f-284">The second `Select` statement fetches the ID, Name, and Price columns from records in the *Product* table whose Price column value is more than 10.</span></span> <span data-ttu-id="7491f-285">Polecenie zwraca wyniki w kolejności alfabetycznej na podstawie wartości w kolumnie Nazwa.</span><span class="sxs-lookup"><span data-stu-id="7491f-285">The command returns the results in alphabetical order based on the values of the Name column.</span></span> <span data-ttu-id="7491f-286">Jeśli żadne rekordy nie odpowiadają kryteria ceny, polecenie zwraca pusty zestaw.</span><span class="sxs-lookup"><span data-stu-id="7491f-286">If no records match the price criteria, the command returns an empty set.</span></span>
> 
> `INSERT INTO Product (Name, Description, Price) VALUES ('Croissant', 'A flaky delight', 1.99)`
> 
> <span data-ttu-id="7491f-287">To polecenie Wstawia nowy rekord do *produktu* tabeli, ustawienie nazwy kolumny "Croissant" Opis kolumny do "A niestabilne wzbudzanie" oraz ceny 1,99.</span><span class="sxs-lookup"><span data-stu-id="7491f-287">This command inserts a new record into the *Product* table, setting the Name column to "Croissant", the Description column to "A flaky delight", and the price to 1.99.</span></span>
> 
> <span data-ttu-id="7491f-288">Należy zauważyć, że określając one wartości nieliczbowe wartość jest ujęta w znaki pojedynczego cudzysłowu (nie znaki cudzysłowu, tak jak w języku C#).</span><span class="sxs-lookup"><span data-stu-id="7491f-288">Notice that when you're specifying a non-numeric value, the value is enclosed in single quotation marks (not double quotation marks, as in C#).</span></span> <span data-ttu-id="7491f-289">Użyjesz tych znaków cudzysłowu wokół wartości tekstowe lub daty, ale nie w całym liczb.</span><span class="sxs-lookup"><span data-stu-id="7491f-289">You use these quotation marks around text or date values, but not around numbers.</span></span>
> 
> `DELETE FROM Product WHERE ExpirationDate < '01/01/2008'`
> 
> <span data-ttu-id="7491f-290">To polecenie usuwa rekordy w *produktu* tabeli, którego kolumna Data wygaśnięcia jest wcześniejsza niż 1 stycznia 2008.</span><span class="sxs-lookup"><span data-stu-id="7491f-290">This command deletes records in the *Product* table whose expiration date column is earlier than January 1, 2008.</span></span> <span data-ttu-id="7491f-291">(Polecenia założono, że *produktu* tabela ma kolumnę, oczywiście.) W tym miejscu podano daty w formacie MM/DD/RRRR, ale powinny być wprowadzane w formacie, który jest używany dla ustawień regionalnych.</span><span class="sxs-lookup"><span data-stu-id="7491f-291">(The command assumes that the *Product* table has such a column, of course.) The date is entered here in MM/DD/YYYY format, but it should be entered in the format that's used for your locale.</span></span>
> 
> <span data-ttu-id="7491f-292">`Insert` i `Delete` polecenia nie zwracają zestaw wyników.</span><span class="sxs-lookup"><span data-stu-id="7491f-292">The `Insert` and `Delete` commands don't return result sets.</span></span> <span data-ttu-id="7491f-293">Zamiast tego zwracają liczbę, która informuje, jak wiele rekordów wpłynęła na polecenie.</span><span class="sxs-lookup"><span data-stu-id="7491f-293">Instead, they return a number that tells you how many records were affected by the command.</span></span>
> 
> <span data-ttu-id="7491f-294">W przypadku niektórych z tych operacji (takich jak wstawianie i usuwanie rekordów) proces, który żąda operacji musi mieć odpowiednie uprawnienia w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-294">For some of these operations (like inserting and deleting records), the process that's requesting the operation has to have appropriate permissions in the database.</span></span> <span data-ttu-id="7491f-295">To dlatego produkcyjnych baz danych, często musisz podać nazwę użytkownika i hasło, po nawiązaniu połączenia z bazą danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-295">That's why for production databases you often have to supply a user name and password when you connect to the database.</span></span>
> 
> <span data-ttu-id="7491f-296">Istnieją dziesiątek poleceń SQL, ale wszystkie one wykonać wzorca, takich jak polecenia widocznej w tym miejscu.</span><span class="sxs-lookup"><span data-stu-id="7491f-296">There are dozens of SQL commands, but they all follow a pattern like the commands you see here.</span></span> <span data-ttu-id="7491f-297">Można użyć poleceń SQL do tworzenia tabel bazy danych, liczby rekordów w tabeli, Oblicz ceny i wykonuje wiele więcej operacji.</span><span class="sxs-lookup"><span data-stu-id="7491f-297">You can use SQL commands to create database tables, count the number of records in a table, calculate prices, and perform many more operations.</span></span>

### <a name="adding-markup-to-display-the-data"></a><span data-ttu-id="7491f-298">Dodawanie znaczników do wyświetlania danych</span><span class="sxs-lookup"><span data-stu-id="7491f-298">Adding Markup to Display the Data</span></span>

<span data-ttu-id="7491f-299">Wewnątrz `<head>` elementu, zawartość zestawu `<title>` elementu "Filmy":</span><span class="sxs-lookup"><span data-stu-id="7491f-299">Inside the `<head>` element, set contents of the `<title>` element to "Movies":</span></span>

[!code-html[Main](displaying-data/samples/sample2.html?highlight=3)]

<span data-ttu-id="7491f-300">Wewnątrz `<body>` elementu strony, Dodaj następujący kod:</span><span class="sxs-lookup"><span data-stu-id="7491f-300">Inside the `<body>` element of the page, add the following:</span></span>

[!code-html[Main](displaying-data/samples/sample3.html)]

<span data-ttu-id="7491f-301">To wszystko.</span><span class="sxs-lookup"><span data-stu-id="7491f-301">That's it.</span></span> <span data-ttu-id="7491f-302">`grid` Zmienna jest wartość utworzona podczas tworzenia `WebGrid` obiektu w kodzie wcześniej.</span><span class="sxs-lookup"><span data-stu-id="7491f-302">The `grid` variable is the value you created when you created the `WebGrid` object in code earlier.</span></span>

<span data-ttu-id="7491f-303">W widoku drzewa w programie WebMatrix, kliknij prawym przyciskiem myszy strony, a następnie wybierz **Uruchom w przeglądarce**.</span><span class="sxs-lookup"><span data-stu-id="7491f-303">In the WebMatrix tree view, right-click the page and select **Launch in browser**.</span></span> <span data-ttu-id="7491f-304">Zostanie wyświetlony podobny do tej strony:</span><span class="sxs-lookup"><span data-stu-id="7491f-304">You'll see something like this page:</span></span>

![Domyślne dane wyjściowe pomocnika WebGrid z tabeli filmy](displaying-data/_static/image18.png)

<span data-ttu-id="7491f-306">Kliknij link do nagłówka kolumny, aby sortować według tej kolumny.</span><span class="sxs-lookup"><span data-stu-id="7491f-306">Click a column heading link to sort by that column.</span></span> <span data-ttu-id="7491f-307">Możliwość sortować, klikając nagłówek jest funkcją, która jest wbudowana w **WebGrid** pomocnika.</span><span class="sxs-lookup"><span data-stu-id="7491f-307">Being able to sort by clicking a heading is a feature that's built into the **WebGrid** helper.</span></span>

<span data-ttu-id="7491f-308">`GetHtml` Metody, jak sugeruje nazwa, generuje kod znaczników, który wyświetla dane.</span><span class="sxs-lookup"><span data-stu-id="7491f-308">The `GetHtml` method, as its name suggests, generates markup that displays the data.</span></span> <span data-ttu-id="7491f-309">Domyślnie `GetHtml` metoda generuje HTML `<table>` elementu.</span><span class="sxs-lookup"><span data-stu-id="7491f-309">By default, the `GetHtml` method generates an HTML `<table>` element.</span></span> <span data-ttu-id="7491f-310">(Jeśli chcesz, możesz sprawdzić renderowania, analizując źródło strony w przeglądarce.)</span><span class="sxs-lookup"><span data-stu-id="7491f-310">(If you want, you can verify the rendering by looking at the source of the page in the browser.)</span></span>

## <a name="modifying-the-look-of-the-grid"></a><span data-ttu-id="7491f-311">Modyfikowanie wyglądu siatki</span><span class="sxs-lookup"><span data-stu-id="7491f-311">Modifying the Look of the Grid</span></span>

<span data-ttu-id="7491f-312">Za pomocą `WebGrid` pomocnika, takie jak właśnie zrobił jest łatwe, ale wynikowy ekranu to zwykły.</span><span class="sxs-lookup"><span data-stu-id="7491f-312">Using the `WebGrid` helper like you just did is easy, but the resulting display is plain.</span></span> <span data-ttu-id="7491f-313">`WebGrid` Pomocnika udostępnia szeroką gamę opcji umożliwiające możesz kontrolować sposób wyświetlania danych.</span><span class="sxs-lookup"><span data-stu-id="7491f-313">The `WebGrid` helper has all sorts of options that let you control how the data is displayed.</span></span> <span data-ttu-id="7491f-314">Istnieją zbyt dużo, aby zapoznać się w tym samouczku, ale w tej sekcji zostanie dają pogląd, niektórych z tych opcji.</span><span class="sxs-lookup"><span data-stu-id="7491f-314">There are far too many to explore in this tutorial, but this section will give you an idea of some of those options.</span></span> <span data-ttu-id="7491f-315">Kilka dodatkowych opcji zostały omówione w kolejnych samouczkach w tej serii.</span><span class="sxs-lookup"><span data-stu-id="7491f-315">A few additional options will be covered in later tutorials in this series.</span></span>

### <a name="specifying-individual-columns-to-display"></a><span data-ttu-id="7491f-316">Określenie poszczególnych kolumn do wyświetlenia</span><span class="sxs-lookup"><span data-stu-id="7491f-316">Specifying Individual Columns to Display</span></span>

<span data-ttu-id="7491f-317">Aby rozpocząć, można określić, czy chcesz wyświetlić tylko określone kolumny.</span><span class="sxs-lookup"><span data-stu-id="7491f-317">To start, you can specify that you want to display only certain columns.</span></span> <span data-ttu-id="7491f-318">Domyślnie, zgodnie z wyświetlonymi wcześniej na siatce są wyświetlane wszystkie cztery kolumny z *filmy* tabeli.</span><span class="sxs-lookup"><span data-stu-id="7491f-318">By default, as you've seen, the grid shows all four of the columns from the *Movies* table.</span></span>

<span data-ttu-id="7491f-319">W *Movies.cshtml* pliku, Zastąp `@grid.GetHtml()` kod znaczników, który właśnie został dodany następującym kodem:</span><span class="sxs-lookup"><span data-stu-id="7491f-319">In the *Movies.cshtml* file, replace the `@grid.GetHtml()` markup that you just added with the following:</span></span>

[!code-css[Main](displaying-data/samples/sample4.css)]

<span data-ttu-id="7491f-320">Sprawdzić pomocnika, które kolumny mają być wyświetlane, obejmują `columns` parametr `GetHtml` metody i przekazać w zbiorze kolumn.</span><span class="sxs-lookup"><span data-stu-id="7491f-320">To tell the helper which columns to display, you include a `columns` parameter for the `GetHtml` method and pass in a collection of columns.</span></span> <span data-ttu-id="7491f-321">W tej kolekcji należy określić każdej kolumny do uwzględnienia.</span><span class="sxs-lookup"><span data-stu-id="7491f-321">In the collection, you specify each column to include.</span></span> <span data-ttu-id="7491f-322">Należy określić pojedynczej kolumny do wyświetlenia, umieszczając `grid.Column` obiektu, a następnie przekazać nazwę kolumny danych, które chcesz.</span><span class="sxs-lookup"><span data-stu-id="7491f-322">You specify an individual column to display by including a `grid.Column` object, and pass in the name of the data column you want.</span></span> <span data-ttu-id="7491f-323">(Te kolumny muszą być zawarte w wynikach kwerendy SQL — `WebGrid` pomocnika nie może wyświetlić kolumn, które nie zostały zwrócone przez zapytanie.)</span><span class="sxs-lookup"><span data-stu-id="7491f-323">(These columns must be included in the SQL query results — the `WebGrid` helper cannot display columns that were not returned by the query.)</span></span>

<span data-ttu-id="7491f-324">Uruchom *Movies.cshtml* strony w przeglądarce, ponownie i tym razem pojawi się ekran podobny do następującego (należy zauważyć, że żadna kolumna ID jest wyświetlany):</span><span class="sxs-lookup"><span data-stu-id="7491f-324">Launch the *Movies.cshtml* page in the browser again, and this time you get a display like the following one (notice that no ID column is displayed):</span></span>

![Wyświetlanie WebGrid przedstawiający tylko zaznaczonych kolumn](displaying-data/_static/image19.png)

### <a name="changing-the-look-of-the-grid"></a><span data-ttu-id="7491f-326">Zmienianie wyglądu siatki</span><span class="sxs-lookup"><span data-stu-id="7491f-326">Changing the Look of the Grid</span></span>

<span data-ttu-id="7491f-327">Istnieje kilka więcej opcji wyświetlania kolumn, niektóre z nich zostaną przedstawione w kolejnych samouczkach w tym zestawie.</span><span class="sxs-lookup"><span data-stu-id="7491f-327">There are quite a few more options for displaying columns, some of which will be explored in later tutorials in this set.</span></span> <span data-ttu-id="7491f-328">Obecnie w tej sekcji przedstawiono sposoby stylizować siatki jako całości.</span><span class="sxs-lookup"><span data-stu-id="7491f-328">For now, this section will introduce you to ways in which you can style the grid as a whole.</span></span>

<span data-ttu-id="7491f-329">Wewnątrz `<head>` części strony, bezpośrednio przed zamykającym `</head>` tag, Dodaj następujący kod `<style>` elementu:</span><span class="sxs-lookup"><span data-stu-id="7491f-329">Inside the `<head>` section of the page, just before the closing `</head>` tag, add the following `<style>` element:</span></span>

[!code-css[Main](displaying-data/samples/sample5.css)]

<span data-ttu-id="7491f-330">Ten kod znaczników CSS definiuje klasy o nazwie `grid`, `head`i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="7491f-330">This CSS markup defines classes named `grid`, `head`, and so on.</span></span> <span data-ttu-id="7491f-331">Możesz również umieścić te definicje styl w osobnym *.css* pliku i łączem do strony.</span><span class="sxs-lookup"><span data-stu-id="7491f-331">You could also put these style definitions in a separate *.css* file and link that to the page.</span></span> <span data-ttu-id="7491f-332">(W rzeczywistości, należy to zrobić później w tym zestawie samouczków.) Jednak aby umożliwić ich łatwe w tym samouczku, są one w tej samej stronie, który wyświetla dane.</span><span class="sxs-lookup"><span data-stu-id="7491f-332">(In fact, you'll do that later in this tutorial set.) But to make things easy for this tutorial, they're inside the same page that displays the data.</span></span>

<span data-ttu-id="7491f-333">Teraz możesz dostawać `WebGrid` pomocnika do użycia w ramach tych zajęć stylu.</span><span class="sxs-lookup"><span data-stu-id="7491f-333">Now you can get the `WebGrid` helper to use these style classes.</span></span> <span data-ttu-id="7491f-334">Pomocnik ma wiele właściwości (na przykład `tableStyle`) tylko w tym celu — przypisać nazwy klasy stylów CSS do nich i że nazwa klasy jest renderowana jako część znaczników, który jest renderowany przez pomocnika.</span><span class="sxs-lookup"><span data-stu-id="7491f-334">The helper has a number of properties (for example, `tableStyle`) for just this purpose — you assign a CSS style class name to them, and that class name is rendered as part of the markup that's rendered by the helper.</span></span>

<span data-ttu-id="7491f-335">Zmiana `grid.GetHtml` znaczników, tak że wygląda podobnie do tego kodu:</span><span class="sxs-lookup"><span data-stu-id="7491f-335">Change the `grid.GetHtml` markup so that it now looks like this code:</span></span>

[!code-css[Main](displaying-data/samples/sample6.css)]

<span data-ttu-id="7491f-336">Co nowego w tym miejscu jest, że dodano `tableStyle`, `headerStyle`, i `alternatingRowStyle` parametry `GetHtml` metody.</span><span class="sxs-lookup"><span data-stu-id="7491f-336">What's new here is that you've added `tableStyle`, `headerStyle`, and `alternatingRowStyle` parameters to the `GetHtml` method.</span></span> <span data-ttu-id="7491f-337">Te parametry zostały ustawione do nazw stylów CSS, które dodałeś przed chwilą.</span><span class="sxs-lookup"><span data-stu-id="7491f-337">These parameters have been set to the names of the CSS styles that you added a moment ago.</span></span>

<span data-ttu-id="7491f-338">Uruchom stronę i tym razem zostanie wyświetlony siatki, która wygląda znacznie mniej zwykły niż przed:</span><span class="sxs-lookup"><span data-stu-id="7491f-338">Run the page, and this time you see a grid that looks much less plain than before:</span></span>

![Wyświetlanie WebGrid, która obejmuje parametry równa nazwy klas CSS](displaying-data/_static/image20.png)

<span data-ttu-id="7491f-340">Aby zobaczyć, co `GetHtml` generowane metody, można sprawdzić źródło strony w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="7491f-340">To see what the `GetHtml` method generated, you can look at the source of the page in the browser.</span></span> <span data-ttu-id="7491f-341">Firma Microsoft nie będzie przejdź do szczegółów w tym miejscu, ale istotną kwestią jest to, że, określając parametry, takie jak `tableStyle`, spowodowane siatki, które mają być Generowanie tagi HTML, jak pokazano poniżej:</span><span class="sxs-lookup"><span data-stu-id="7491f-341">We won't go into detail here, but the important point is that by specifying parameters like `tableStyle`, you caused the grid to generate HTML tags like the following:</span></span>

`<table class="grid">`

<span data-ttu-id="7491f-342">`<table>` Miał znacznik `class` atrybut dodawanych do niego, który odwołuje się do jednej reguły CSS, które wcześniej dodałeś.</span><span class="sxs-lookup"><span data-stu-id="7491f-342">The `<table>` tag has had a `class` attribute added to it that references one of the CSS rules that you added earlier.</span></span> <span data-ttu-id="7491f-343">Ten kod przedstawia podstawowy wzorzec &mdash; różne parametry dla `GetHtml` metody pozwalają możesz odwołanie CSS klasy, aby z nich metoda generuje następnie wraz z kodu znaczników.</span><span class="sxs-lookup"><span data-stu-id="7491f-343">This code shows you the basic pattern &mdash; different parameters for the `GetHtml` method let you reference CSS classes that the method then generates along with the markup.</span></span> <span data-ttu-id="7491f-344">Co zrobisz z klas CSS zależy od użytkownika.</span><span class="sxs-lookup"><span data-stu-id="7491f-344">What you do with the CSS classes is up to you.</span></span>

## <a name="adding-paging"></a><span data-ttu-id="7491f-345">Dodawanie stronicowania</span><span class="sxs-lookup"><span data-stu-id="7491f-345">Adding Paging</span></span>

<span data-ttu-id="7491f-346">Jako ostatnie zadanie w ramach tego samouczka dodasz stronicowania do siatki.</span><span class="sxs-lookup"><span data-stu-id="7491f-346">As the last task for this tutorial, you'll add paging to the grid.</span></span> <span data-ttu-id="7491f-347">W tej chwili, to żaden problem do wyświetlenia wszystkich filmów na raz.</span><span class="sxs-lookup"><span data-stu-id="7491f-347">Right now it's no problem to display all your movies at once.</span></span> <span data-ttu-id="7491f-348">Ale jeśli dodano setki filmów wyświetlania strony będzie długo.</span><span class="sxs-lookup"><span data-stu-id="7491f-348">But if you added hundreds of movies, the page display would get long.</span></span>

<span data-ttu-id="7491f-349">W kodzie strony Zmień wiersz, który tworzy `WebGrid` obiektu z następującym kodem:</span><span class="sxs-lookup"><span data-stu-id="7491f-349">In the page code, change the line that creates the `WebGrid` object to the following code:</span></span>

[!code-csharp[Main](displaying-data/samples/sample7.cs)]

<span data-ttu-id="7491f-350">Jedyną różnicą między przed jest, że dodano `rowsPerPage` parametr, który jest ustawiony na 3.</span><span class="sxs-lookup"><span data-stu-id="7491f-350">The only difference from before is that you've added a `rowsPerPage` parameter that's set to 3.</span></span>

<span data-ttu-id="7491f-351">Uruchom stronę.</span><span class="sxs-lookup"><span data-stu-id="7491f-351">Run the page.</span></span> <span data-ttu-id="7491f-352">Siatka wyświetla 3 wiersze w czasie oraz łączy nawigacji, które umożliwiają przeglądanie filmów w bazie danych:</span><span class="sxs-lookup"><span data-stu-id="7491f-352">The grid displays 3 rows at a time, plus navigation links that let you page through the movies in your database:</span></span>

![Wyświetlanie WebGrid za pomocą stronicowania](displaying-data/_static/image21.png)

## <a name="coming-up-next"></a><span data-ttu-id="7491f-354">Pojawi się dalej</span><span class="sxs-lookup"><span data-stu-id="7491f-354">Coming Up Next</span></span>

<span data-ttu-id="7491f-355">W następnym samouczku dowiesz się, jak pobrać dane wejściowe użytkownika w postaci za pomocą kodu Razor i C#.</span><span class="sxs-lookup"><span data-stu-id="7491f-355">In the next tutorial, you'll learn how to use Razor and C# code to get user input in a form.</span></span> <span data-ttu-id="7491f-356">Dodasz pole wyszukiwania do strony filmy, aby mógł znaleźć filmów według tytułu lub gatunku.</span><span class="sxs-lookup"><span data-stu-id="7491f-356">You'll add a search box to the Movies page so that you can find movies by title or genre.</span></span>

## <a name="complete-listing-for-movies-page"></a><span data-ttu-id="7491f-357">Kompletna lista strony filmy</span><span class="sxs-lookup"><span data-stu-id="7491f-357">Complete Listing for Movies Page</span></span>

[!code-cshtml[Main](displaying-data/samples/sample8.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="7491f-358">Dodatkowe zasoby</span><span class="sxs-lookup"><span data-stu-id="7491f-358">Additional Resources</span></span>

- [<span data-ttu-id="7491f-359">Wprowadzenie do programowania dla sieci Web platformy ASP.NET używająca składni Razor</span><span class="sxs-lookup"><span data-stu-id="7491f-359">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)

> [!div class="step-by-step"]
> <span data-ttu-id="7491f-360">[Poprzednie](intro-to-web-pages-programming.md)
> [dalej](form-basics.md)</span><span class="sxs-lookup"><span data-stu-id="7491f-360">[Previous](intro-to-web-pages-programming.md)
[Next](form-basics.md)</span></span>
