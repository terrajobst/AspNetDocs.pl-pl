---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
title: Wprowadzenie do stron sieci Web ASP.NET — wyświetlanie danych | Microsoft Docs
author: Rick-Anderson
description: W tym samouczku pokazano, jak utworzyć bazę danych w programie WebMatrix oraz jak wyświetlać dane bazy danych na stronie w przypadku używania stron sieci Web ASP.NET (Razor). Zakłada, że y...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: b3a006a0-3ea2-4d45-b833-e20e3a3c0a1a
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
msc.type: authoredcontent
ms.openlocfilehash: 9e665ca8dd064c23a8b8bd3593014969d0c3da48
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/06/2020
ms.locfileid: "78525222"
---
# <a name="introducing-aspnet-web-pages---displaying-data"></a><span data-ttu-id="e18a5-104">Wprowadzenie do stron sieci Web ASP.NET — wyświetlanie danych</span><span class="sxs-lookup"><span data-stu-id="e18a5-104">Introducing ASP.NET Web Pages - Displaying Data</span></span>

<span data-ttu-id="e18a5-105">Autor [FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="e18a5-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="e18a5-106">W tym samouczku pokazano, jak utworzyć bazę danych w programie WebMatrix oraz jak wyświetlać dane bazy danych na stronie w przypadku używania stron sieci Web ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="e18a5-106">This tutorial shows you how to create a database in WebMatrix and how to display database data in a page when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="e18a5-107">Przyjęto założenie, że seria została zakończona przez [wprowadzenie do programowania stron sieci Web ASP.NET](../introducing-razor-syntax-c.md).</span><span class="sxs-lookup"><span data-stu-id="e18a5-107">It assumes you have completed the series through [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md).</span></span>
> 
> <span data-ttu-id="e18a5-108">Zawartość:</span><span class="sxs-lookup"><span data-stu-id="e18a5-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="e18a5-109">Jak używać narzędzi WebMatrix do tworzenia bazy danych i tabel bazy danych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-109">How to use WebMatrix tools to create a database and database tables.</span></span>
> - <span data-ttu-id="e18a5-110">Jak dodać dane do bazy danych przy użyciu narzędzi WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="e18a5-110">How to use WebMatrix tools to add data to a database.</span></span>
> - <span data-ttu-id="e18a5-111">Jak wyświetlać dane z bazy danych na stronie.</span><span class="sxs-lookup"><span data-stu-id="e18a5-111">How to display data from the database on a page.</span></span>
> - <span data-ttu-id="e18a5-112">Jak uruchamiać polecenia SQL na stronach sieci Web ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="e18a5-112">How to run SQL commands in ASP.NET Web Pages.</span></span>
> - <span data-ttu-id="e18a5-113">Jak dostosować pomocnika `WebGrid`, aby zmienić wyświetlanie danych i dodać stronicowanie i sortowanie.</span><span class="sxs-lookup"><span data-stu-id="e18a5-113">How to customize the `WebGrid` helper to change the data display and to add paging and sorting.</span></span>
>   
> 
> <span data-ttu-id="e18a5-114">Omówione funkcje/technologie:</span><span class="sxs-lookup"><span data-stu-id="e18a5-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="e18a5-115">Narzędzia WebMatrix Database.</span><span class="sxs-lookup"><span data-stu-id="e18a5-115">WebMatrix database tools.</span></span>
> - <span data-ttu-id="e18a5-116">pomocnik `WebGrid`.</span><span class="sxs-lookup"><span data-stu-id="e18a5-116">`WebGrid` helper.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="e18a5-117">Co będziesz kompilować</span><span class="sxs-lookup"><span data-stu-id="e18a5-117">What You'll Build</span></span>

<span data-ttu-id="e18a5-118">W poprzednim samouczku wprowadzono do ASP.NET stron sieci Web (pliki *. cshtml* ), do podstaw składnia Razor i do pomocników.</span><span class="sxs-lookup"><span data-stu-id="e18a5-118">In the previous tutorial, you were introduced to ASP.NET Web Pages (*.cshtml* files), to the basics of Razor syntax, and to helpers.</span></span> <span data-ttu-id="e18a5-119">W tym samouczku rozpocznie się tworzenie rzeczywistej aplikacji sieci Web, która będzie używana w pozostałej części serii.</span><span class="sxs-lookup"><span data-stu-id="e18a5-119">In this tutorial, you'll begin creating the actual web application that you'll use for the rest of the series.</span></span> <span data-ttu-id="e18a5-120">Aplikacja to prosta aplikacja filmowa, która umożliwia wyświetlanie, Dodawanie, zmienianie i usuwanie informacji o filmach.</span><span class="sxs-lookup"><span data-stu-id="e18a5-120">The app is a simple movie application that lets you view, add, change, and delete information about movies.</span></span>

<span data-ttu-id="e18a5-121">Gdy skończysz korzystać z tego samouczka, zobaczysz listę filmów, która wygląda podobnie do tej strony:</span><span class="sxs-lookup"><span data-stu-id="e18a5-121">When you're done with this tutorial, you'll be able to view a movie listing that looks like this page:</span></span>

![Wyświetlacz WebGrid zawierający parametry ustawione na nazwy klas CSS](displaying-data/_static/image1.png)

<span data-ttu-id="e18a5-123">Aby rozpocząć, musisz utworzyć bazę danych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-123">But to begin, you have to create a database.</span></span>

## <a name="a-very-brief-introduction-to-databases"></a><span data-ttu-id="e18a5-124">Krótkie wprowadzenie do baz danych</span><span class="sxs-lookup"><span data-stu-id="e18a5-124">A Very Brief Introduction to Databases</span></span>

<span data-ttu-id="e18a5-125">W tym samouczku przedstawiono tylko krótkie wprowadzenie do baz danych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-125">This tutorial will provide only the briefest introduction to databases.</span></span> <span data-ttu-id="e18a5-126">W przypadku korzystania z bazy danych można pominąć tę krótką sekcję.</span><span class="sxs-lookup"><span data-stu-id="e18a5-126">If you have database experience, you can skip this short section.</span></span>

<span data-ttu-id="e18a5-127">Baza danych zawiera co najmniej jedną tabelę zawierającą informacje &mdash; na przykład tabele dla klientów, zamówień i dostawców lub dla studentów, nauczycieli, klas i kategorii.</span><span class="sxs-lookup"><span data-stu-id="e18a5-127">A database contains one or more tables that contain information &mdash; for example, tables for customers, orders, and vendors, or for students, teachers, classes, and grades.</span></span> <span data-ttu-id="e18a5-128">Strukturalnie tabela bazy danych jest jak arkusz kalkulacyjny.</span><span class="sxs-lookup"><span data-stu-id="e18a5-128">Structurally, a database table is like a spreadsheet.</span></span> <span data-ttu-id="e18a5-129">Wyobraź sobie typową książkę adresową.</span><span class="sxs-lookup"><span data-stu-id="e18a5-129">Imagine a typical address book.</span></span> <span data-ttu-id="e18a5-130">Dla każdego wpisu w książce adresowej (czyli dla każdej osoby) masz kilka informacji, takich jak imię, nazwisko, adres, adres e-mail i numer telefonu.</span><span class="sxs-lookup"><span data-stu-id="e18a5-130">For each entry in the address book (that is, for each person) you have several pieces of information such as first name, last name, address, email address, and phone number.</span></span>

![Przykładowa tabela bazy danych jako prosta siatka](displaying-data/_static/image2.png)

<span data-ttu-id="e18a5-132">(Wiersze są czasami określane jako *rekordy*, a kolumny są czasami określane jako *pola*).</span><span class="sxs-lookup"><span data-stu-id="e18a5-132">(Rows are sometimes referred to as *records*, and columns are sometimes referred to as *fields*.)</span></span>

<span data-ttu-id="e18a5-133">W przypadku większości tabel bazy danych tabela musi mieć kolumnę, która zawiera unikatową wartość, na przykład numer klienta, numer konta itd.</span><span class="sxs-lookup"><span data-stu-id="e18a5-133">For most database tables, the table has to have a column that contains a unique value, like a customer number, account number, and so on.</span></span> <span data-ttu-id="e18a5-134">Ta wartość jest określana jako *klucz podstawowy*tabeli i służy do identyfikowania każdego wiersza w tabeli.</span><span class="sxs-lookup"><span data-stu-id="e18a5-134">This value is known as the table's *primary key*, and you use it to identify each row in the table.</span></span> <span data-ttu-id="e18a5-135">W przykładzie kolumna ID jest kluczem podstawowym dla książki adresowej pokazanej w poprzednim przykładzie.</span><span class="sxs-lookup"><span data-stu-id="e18a5-135">In the example, the ID column is the primary key for the address book shown in the previous example.</span></span>

<span data-ttu-id="e18a5-136">Większość pracy wykonywanych w aplikacjach sieci Web polega na odczytywaniu informacji z bazy danych i wyświetlaniu jej na stronie.</span><span class="sxs-lookup"><span data-stu-id="e18a5-136">Much of the work you do in web applications consists of reading information out of the database and displaying it on a page.</span></span> <span data-ttu-id="e18a5-137">Często są również zbierane informacje od użytkowników i dodawane do bazy danych programu. można też modyfikować rekordy, które znajdują się już w bazie danych programu.</span><span class="sxs-lookup"><span data-stu-id="e18a5-137">You'll also often gather information from users and add it to a database, or you'll modify records that are already in the database.</span></span> <span data-ttu-id="e18a5-138">(Wszystkie te operacje zostaną omówione w ramach tego zestawu samouczków).</span><span class="sxs-lookup"><span data-stu-id="e18a5-138">(We'll cover all of these operations in the course of this tutorial set.)</span></span>

<span data-ttu-id="e18a5-139">Baza danych może być kompleksowo złożona i może wymagać specjalistycznej wiedzy.</span><span class="sxs-lookup"><span data-stu-id="e18a5-139">Database work can be enormously complex and can require specialized knowledge.</span></span> <span data-ttu-id="e18a5-140">Na potrzeby tego samouczka należy zrozumieć tylko podstawowe koncepcje, które zostaną wyjaśnione po przejściu.</span><span class="sxs-lookup"><span data-stu-id="e18a5-140">For this tutorial set, though, you have to understand only basic concepts, which will all be explained as you go.</span></span>

## <a name="creating-a-database"></a><span data-ttu-id="e18a5-141">Tworzenie bazy danych</span><span class="sxs-lookup"><span data-stu-id="e18a5-141">Creating a Database</span></span>

<span data-ttu-id="e18a5-142">Program WebMatrix zawiera narzędzia, które ułatwiają tworzenie bazy danych i tworzenie tabel w bazie danych programu.</span><span class="sxs-lookup"><span data-stu-id="e18a5-142">WebMatrix includes tools that make it easy to create a database and to create tables in the database.</span></span> <span data-ttu-id="e18a5-143">(Struktura bazy danych jest określana jako *schemat*bazy danych). Dla tego zestawu samouczków utworzysz bazę danych zawierającą tylko jedną tabelę w &mdash; filmach.</span><span class="sxs-lookup"><span data-stu-id="e18a5-143">(The structure of a database is referred to as the database's *schema*.) For this tutorial set, you'll create a database that has only one table in it &mdash; Movies.</span></span>

<span data-ttu-id="e18a5-144">Otwórz program WebMatrix, jeśli jeszcze tego nie zrobiono, a następnie otwórz witrynę WebPagesMovies utworzoną w poprzednim samouczku.</span><span class="sxs-lookup"><span data-stu-id="e18a5-144">Open WebMatrix if you haven't already done so, and open the WebPagesMovies site that you created in the previous tutorial.</span></span>

<span data-ttu-id="e18a5-145">W lewym okienku kliknij obszar roboczy **baza danych** .</span><span class="sxs-lookup"><span data-stu-id="e18a5-145">In the left pane, click the **Database** workspace.</span></span>

![Karta obszaru roboczego bazy danych WebMatrix](displaying-data/_static/image3.png)

<span data-ttu-id="e18a5-147">Wstążka zmienia się w celu wyświetlenia zadań związanych z bazą danych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-147">The ribbon changes to show database-related tasks.</span></span> <span data-ttu-id="e18a5-148">Na wstążce kliknij pozycję **Nowa baza danych**.</span><span class="sxs-lookup"><span data-stu-id="e18a5-148">In the ribbon, click **New Database**.</span></span>

![Przycisk "Nowa baza danych" na Wstążce WebMatrix](displaying-data/_static/image4.png)

<span data-ttu-id="e18a5-150">WebMatrix tworzy bazę danych SQL Server CE (plik *. sdf* ) o takiej samej nazwie, co witryna &mdash; *WebPagesMovies. sdf*.</span><span class="sxs-lookup"><span data-stu-id="e18a5-150">WebMatrix creates a SQL Server CE database (an *.sdf* file) that has the same name as your site &mdash; *WebPagesMovies.sdf*.</span></span> <span data-ttu-id="e18a5-151">(W tym miejscu nie można wykonać tej czynności, ale można zmienić nazwę pliku na dowolną zawartość, o ile ma rozszerzenie *. sdf* ).</span><span class="sxs-lookup"><span data-stu-id="e18a5-151">(You won't do this here, but you can rename the file to anything you like, as long as it has an *.sdf* extension.)</span></span>

## <a name="creating-a-table"></a><span data-ttu-id="e18a5-152">Tworzenie tabeli</span><span class="sxs-lookup"><span data-stu-id="e18a5-152">Creating a Table</span></span>

<span data-ttu-id="e18a5-153">Na wstążce kliknij pozycję **Nowa tabela**.</span><span class="sxs-lookup"><span data-stu-id="e18a5-153">In the ribbon, click **New Table**.</span></span> <span data-ttu-id="e18a5-154">WebMatrix otwiera projektanta tabel na nowej karcie. (Jeśli opcja Nowa tabela jest niedostępna, upewnij się, że nowa baza danych została wybrana w widoku drzewa po lewej stronie).</span><span class="sxs-lookup"><span data-stu-id="e18a5-154">WebMatrix opens the table designer in a new tab. (If the New Table option isn't available, make sure that the new database is selected in the tree view on the left.)</span></span>

![Przycisk "Nowa tabela" na Wstążce WebMatrix](displaying-data/_static/image5.png)

<span data-ttu-id="e18a5-156">W polu tekstowym u góry (w którym znajduje się znak wodny "Wprowadź nazwę tabeli"), wprowadź "filmy".</span><span class="sxs-lookup"><span data-stu-id="e18a5-156">In the text box at the top (where the watermark says "Enter table name"), enter "Movies".</span></span>

![Wprowadzanie nazwy tabeli w projektancie bazy danych WebMatrix](displaying-data/_static/image6.png)

<span data-ttu-id="e18a5-158">W okienku pod nazwą tabeli można definiować poszczególne kolumny.</span><span class="sxs-lookup"><span data-stu-id="e18a5-158">The pane underneath the table name is where you define individual columns.</span></span> <span data-ttu-id="e18a5-159">W przypadku tabeli *filmów* w tym samouczku utworzysz tylko kilka kolumn: *Identyfikator*, *tytuł*, *gatunek*i *rok*.</span><span class="sxs-lookup"><span data-stu-id="e18a5-159">For the *Movies* table in this tutorial, you'll create only a few columns: *ID*, *Title*, *Genre*, and *Year*.</span></span>

<span data-ttu-id="e18a5-160">W polu **Nazwa** wprowadź wartość "ID".</span><span class="sxs-lookup"><span data-stu-id="e18a5-160">In the **Name** box, enter "ID".</span></span> <span data-ttu-id="e18a5-161">Wprowadzenie wartości w tym miejscu aktywuje wszystkie kontrolki dla nowej kolumny.</span><span class="sxs-lookup"><span data-stu-id="e18a5-161">Entering a value here activates all the controls for the new column.</span></span>

<span data-ttu-id="e18a5-162">Tab na listę **Typ danych** i wybierz **int**. Ta wartość określa, że kolumna identyfikatora będzie zawierać dane całkowite (liczbowe).</span><span class="sxs-lookup"><span data-stu-id="e18a5-162">Tab to the **Data Type** list and choose **int**. This value specifies that the ID column will contain integer (number) data.</span></span>

> [!NOTE]
> <span data-ttu-id="e18a5-163">Nie wywołajemy go więcej (wiele), ale możesz użyć standardowych gestów klawiatury systemu Windows, aby przejść do tej siatki.</span><span class="sxs-lookup"><span data-stu-id="e18a5-163">We won't call it out any more here (much), but you can use standard Windows keyboard gestures to navigate in this grid.</span></span> <span data-ttu-id="e18a5-164">Na przykład można nawiązać tabulację między polami, po prostu zacznij pisać, aby wybrać element na liście i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="e18a5-164">For example, you can tab between fields, you can just start typing in order to select an item in a list, and so on.</span></span>

<span data-ttu-id="e18a5-165">Tab poza polem **wartości domyślnej** (to oznacza, pozostaw puste).</span><span class="sxs-lookup"><span data-stu-id="e18a5-165">Tab past the **Default Value** box (that is, leave it blank).</span></span> <span data-ttu-id="e18a5-166">Na karcie **klucz podstawowy** pole wyboru i wybierz ją.</span><span class="sxs-lookup"><span data-stu-id="e18a5-166">Tab to the **Is Primary Key** check box and select it.</span></span> <span data-ttu-id="e18a5-167">Ta opcja instruuje bazę danych, że kolumna *Identyfikator* będzie zawierać dane, które identyfikują poszczególne wiersze.</span><span class="sxs-lookup"><span data-stu-id="e18a5-167">This option tells the database that the *ID* column will contain the data that identifies individual rows.</span></span> <span data-ttu-id="e18a5-168">(Oznacza to, że każdy wiersz będzie miał unikatową wartość w kolumnie ID, która może być używana do znajdowania tego wiersza).</span><span class="sxs-lookup"><span data-stu-id="e18a5-168">(That is, each row will have a unique value in the ID column that you can use to find that row.)</span></span>

<span data-ttu-id="e18a5-169">Wybierz opcję **tożsamość** .</span><span class="sxs-lookup"><span data-stu-id="e18a5-169">Choose the **Is Identity** option.</span></span> <span data-ttu-id="e18a5-170">Ta opcja instruuje bazę danych, że powinna automatycznie generować następny numer sekwencyjny dla każdego nowego wiersza.</span><span class="sxs-lookup"><span data-stu-id="e18a5-170">This option tells the database that it should automatically generate the next sequential number for each new row.</span></span> <span data-ttu-id="e18a5-171">(Opcja **jest tożsamość** działa tylko wtedy, gdy wybrano również opcję " **jest kluczem podstawowym** ").</span><span class="sxs-lookup"><span data-stu-id="e18a5-171">(The **Is Identity** option works only if you've also selected the **Is Primary Key** option.)</span></span>

<span data-ttu-id="e18a5-172">Kliknij w następnym wierszu siatki lub dwukrotnie naciśnij klawisz Tab, aby zakończyć bieżący wiersz.</span><span class="sxs-lookup"><span data-stu-id="e18a5-172">Click in the next grid row, or press Tab twice to finish the current row.</span></span> <span data-ttu-id="e18a5-173">Każdy gest zapisuje bieżący wiersz i uruchamia kolejny.</span><span class="sxs-lookup"><span data-stu-id="e18a5-173">Either gesture saves the current row and starts the next one.</span></span> <span data-ttu-id="e18a5-174">Zwróć uwagę, że kolumna **wartość domyślna** ma teraz **wartość null**.</span><span class="sxs-lookup"><span data-stu-id="e18a5-174">Notice that the **Default Value** column now says **Null**.</span></span> <span data-ttu-id="e18a5-175">(Wartość domyślna to wartość domyślna, dlatego należy mówić).</span><span class="sxs-lookup"><span data-stu-id="e18a5-175">(Null is the default value for the default value, so to speak.)</span></span>

<span data-ttu-id="e18a5-176">Po zakończeniu definiowania nowej kolumny **identyfikatora** Projektant będzie wyglądać podobnie do poniższej ilustracji:</span><span class="sxs-lookup"><span data-stu-id="e18a5-176">When you've finished defining the new **ID** column, the designer will look like this illustration:</span></span>

![Projektant baz danych WebMatrix po zdefiniowaniu kolumny ID dla tabeli filmów](displaying-data/_static/image7.png)

<span data-ttu-id="e18a5-178">Aby utworzyć następną kolumnę, kliknij w polu w kolumnie **Nazwa** .</span><span class="sxs-lookup"><span data-stu-id="e18a5-178">To create the next column, click in the box in the **Name** column.</span></span> <span data-ttu-id="e18a5-179">Wprowadź "title" dla kolumny, a następnie wybierz pozycję **nvarchar** dla wartości **Typ danych** .</span><span class="sxs-lookup"><span data-stu-id="e18a5-179">Enter "Title" for the column and then select **nvarchar** for the **Data Type** value.</span></span> <span data-ttu-id="e18a5-180">Część "var" elementu **nvarchar** instruuje bazę danych, że dane dla tej kolumny będą ciągiem, którego rozmiar może się różnić od rekordu do rekordu.</span><span class="sxs-lookup"><span data-stu-id="e18a5-180">The "var" part of **nvarchar** tells the database that the data for this column will be a string whose size might vary from record to record.</span></span> <span data-ttu-id="e18a5-181">(Prefiks "n" reprezentuje "National", co oznacza, że pole może zawierać dane znakowe dla każdego systemu alfabetu lub pisania — to znaczy, że pole przechowuje dane Unicode).</span><span class="sxs-lookup"><span data-stu-id="e18a5-181">(The "n" prefix represents "national," which indicates that the field can hold character data for any alphabet or writing system — that is, the field holds Unicode data.)</span></span>

<span data-ttu-id="e18a5-182">Po wybraniu opcji **nvarchar**, pojawi się inne pole, w którym można wprowadzić maksymalną długość pola.</span><span class="sxs-lookup"><span data-stu-id="e18a5-182">When you choose **nvarchar**, another box appears where you can enter the maximum length for the field.</span></span> <span data-ttu-id="e18a5-183">Wprowadź 50, w założeniu, że żaden tytuł filmu, z którym będziesz korzystać z tego samouczka, będzie dłuższy niż 50 znaków.</span><span class="sxs-lookup"><span data-stu-id="e18a5-183">Enter 50, on the assumption that no movie title that you'll work with in this tutorial will be longer than 50 characters.</span></span>

<span data-ttu-id="e18a5-184">Pomiń **wartość domyślną** i usuń zaznaczenie opcji **Zezwalaj na wartości null** .</span><span class="sxs-lookup"><span data-stu-id="e18a5-184">Skip **Default Value** and clear the **Allow Nulls** option.</span></span> <span data-ttu-id="e18a5-185">Nie chcesz, aby baza danych zezwalała na wprowadzanie jakichkolwiek filmów do bazy danych, która nie ma tytułu.</span><span class="sxs-lookup"><span data-stu-id="e18a5-185">You don't want the database to allow any movies to be entered into the database that don't have a title.</span></span>

<span data-ttu-id="e18a5-186">Gdy skończysz i przejdziesz do następnego wiersza, Projektant wygląda jak na poniższej ilustracji:</span><span class="sxs-lookup"><span data-stu-id="e18a5-186">When you're done and move to the next row, the designer looks like this illustration:</span></span>

![Projektant baz danych WebMatrix po zdefiniowaniu kolumny title dla tabeli filmów](displaying-data/_static/image8.png)

<span data-ttu-id="e18a5-188">Powtórz te kroki, aby utworzyć kolumnę o nazwie "gatunek" z wyjątkiem długości, ustaw ją na wartość zaledwie 30.</span><span class="sxs-lookup"><span data-stu-id="e18a5-188">Repeat these steps to create a column named "Genre", except for the length, set it to just 30.</span></span>

<span data-ttu-id="e18a5-189">Utwórz kolejną kolumnę o nazwie "Year".</span><span class="sxs-lookup"><span data-stu-id="e18a5-189">Create another column named "Year."</span></span> <span data-ttu-id="e18a5-190">Dla typu danych wybierz opcję **nchar** (nie **nvarchar**) i Ustaw długość na 4.</span><span class="sxs-lookup"><span data-stu-id="e18a5-190">For the data type, choose **nchar** (not **nvarchar**) and set the length to 4.</span></span> <span data-ttu-id="e18a5-191">Na rok jest używana 4-cyfrowa liczba, taka jak "1995" lub "2010", więc nie jest wymagana kolumna o zmiennym rozmiarze.</span><span class="sxs-lookup"><span data-stu-id="e18a5-191">For the year, you're going to use a 4-digit number like "1995" or "2010", so you don't require a variable-sized column.</span></span>

<span data-ttu-id="e18a5-192">Oto, jak wygląda gotowy projekt:</span><span class="sxs-lookup"><span data-stu-id="e18a5-192">Here's what the finished design looks like:</span></span>

![Projektant baz danych WebMatrix po zdefiniowaniu wszystkich pól dla tabeli filmów](displaying-data/_static/image9.png)

<span data-ttu-id="e18a5-194">Naciśnij klawisze CTRL + S lub kliknij przycisk **Zapisz** na pasku narzędzi Szybki dostęp.</span><span class="sxs-lookup"><span data-stu-id="e18a5-194">Press Ctrl+S or click the **Save** button in the Quick Access toolbar.</span></span> <span data-ttu-id="e18a5-195">Zamknij projektanta baz danych, zamykając kartę.</span><span class="sxs-lookup"><span data-stu-id="e18a5-195">Close the database designer by closing the tab.</span></span>

## <a name="adding-some-example-data"></a><span data-ttu-id="e18a5-196">Dodawanie przykładowych danych</span><span class="sxs-lookup"><span data-stu-id="e18a5-196">Adding Some Example Data</span></span>

<span data-ttu-id="e18a5-197">W dalszej części tej serii samouczków utworzysz stronę umożliwiającą wprowadzanie nowych filmów w formularzu.</span><span class="sxs-lookup"><span data-stu-id="e18a5-197">Later in this tutorial series you'll create a page where you can enter new movies in a form.</span></span> <span data-ttu-id="e18a5-198">Na razie można jednak dodać przykładowe dane, które można następnie wyświetlić na stronie.</span><span class="sxs-lookup"><span data-stu-id="e18a5-198">For now, however, you can add some example data that you can then display on a page.</span></span>

<span data-ttu-id="e18a5-199">W obszarze roboczym **baza danych** w programie WebMatrix Zwróć uwagę, że istnieje drzewo pokazujące utworzony wcześniej plik *. sdf* .</span><span class="sxs-lookup"><span data-stu-id="e18a5-199">In the **Database** workspace in WebMatrix, notice that there's a tree that shows you the *.sdf* file you created earlier.</span></span> <span data-ttu-id="e18a5-200">Otwórz węzeł dla nowego pliku *. sdf* , a następnie otwórz węzeł **tabele** .</span><span class="sxs-lookup"><span data-stu-id="e18a5-200">Open the node for your new *.sdf* file, and then open the **Tables** node.</span></span>

![Obszar roboczy bazy danych WebMatrix z otwartym drzewem w tabeli filmów](displaying-data/_static/image10.png)

<span data-ttu-id="e18a5-202">Kliknij prawym przyciskiem myszy węzeł **filmy** , a następnie wybierz polecenie **dane**.</span><span class="sxs-lookup"><span data-stu-id="e18a5-202">Right-click the **Movies** node and then choose **Data**.</span></span> <span data-ttu-id="e18a5-203">WebMatrix otwiera siatkę, w której można wprowadzać dane dla tabeli *filmów* :</span><span class="sxs-lookup"><span data-stu-id="e18a5-203">WebMatrix opens a grid where you can enter data for the *Movies* table:</span></span>

![Siatka wejścia bazy danych w programie WebMatrix (pusta)](displaying-data/_static/image11.png)

<span data-ttu-id="e18a5-205">Kliknij kolumnę **tytuł** i wprowadź wartość "Kiedy Harry MET Sally".</span><span class="sxs-lookup"><span data-stu-id="e18a5-205">Click the **Title** column and enter "When Harry Met Sally".</span></span> <span data-ttu-id="e18a5-206">Przejdź do kolumny **gatunek** (możesz użyć klawisza Tab) i wprowadź ciąg "Romantic komedia".</span><span class="sxs-lookup"><span data-stu-id="e18a5-206">Move to the **Genre** column (you can use the Tab key) and enter "Romantic Comedy".</span></span> <span data-ttu-id="e18a5-207">Przejdź do kolumny **Year** i wprowadź wartość "1989":</span><span class="sxs-lookup"><span data-stu-id="e18a5-207">Move to the **Year** column and enter "1989":</span></span>

![Siatka wejścia bazy danych w programie WebMatrix z jednym rekordem](displaying-data/_static/image12.png)

<span data-ttu-id="e18a5-209">Naciśnij klawisz ENTER, a Program WebMatrix zapisuje nowy film.</span><span class="sxs-lookup"><span data-stu-id="e18a5-209">Press Enter, and WebMatrix saves the new movie.</span></span> <span data-ttu-id="e18a5-210">Zwróć uwagę, że kolumna **ID** została wypełniona.</span><span class="sxs-lookup"><span data-stu-id="e18a5-210">Notice that the **ID** column has been filled in.</span></span>

![Siatka wejścia bazy danych w programie WebMatrix z jednym rekordem i automatycznie generowanym IDENTYFIKATORem](displaying-data/_static/image13.png)

<span data-ttu-id="e18a5-212">Wprowadź inny film (na przykład "znikł z wiatrem", "dramat", "1939").</span><span class="sxs-lookup"><span data-stu-id="e18a5-212">Enter another movie (for example, "Gone with the Wind", "Drama", "1939").</span></span> <span data-ttu-id="e18a5-213">Kolumna Identyfikator jest wypełniana ponownie:</span><span class="sxs-lookup"><span data-stu-id="e18a5-213">The ID column is filled in again:</span></span>

![Siatka wejścia bazy danych w programie WebMatrix z dwoma rekordami i automatycznie generowanymi identyfikatorami](displaying-data/_static/image14.png)

<span data-ttu-id="e18a5-215">Wprowadź trzeci film (na przykład "Ghostbusters", "komedia").</span><span class="sxs-lookup"><span data-stu-id="e18a5-215">Enter a third movie (for example, "Ghostbusters", "Comedy").</span></span> <span data-ttu-id="e18a5-216">W ramach eksperymentu pozostaw pustą kolumnę **Year** , a następnie naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="e18a5-216">As an experiment, leave the **Year** column blank and then press Enter.</span></span> <span data-ttu-id="e18a5-217">Ponieważ wyznaczono opcję **Zezwalaj na wartości null** , w bazie danych zostanie wyświetlony komunikat o błędzie:</span><span class="sxs-lookup"><span data-stu-id="e18a5-217">Because you unselected the **Allow Nulls** option, the database shows an error:</span></span>

![Wyświetlany jest błąd "nieprawidłowe dane", jeśli wymagana wartość kolumny pozostaje pusta](displaying-data/_static/image15.png)

<span data-ttu-id="e18a5-219">Kliknij przycisk **OK** , aby wrócić i naprawić wpis (rok dla "Ghostbusters" to 1984), a następnie naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="e18a5-219">Click **OK** to go back and fix the entry (the year for "Ghostbusters" is 1984), and then press Enter.</span></span>

<span data-ttu-id="e18a5-220">Wypełnij kilka filmów do momentu, gdy nie masz 8 lub tak.</span><span class="sxs-lookup"><span data-stu-id="e18a5-220">Fill in several movies until you have 8 or so.</span></span> <span data-ttu-id="e18a5-221">(Wprowadzenie 8 ułatwia pracę z stronicowaniem później.</span><span class="sxs-lookup"><span data-stu-id="e18a5-221">(Entering 8 makes it easier to work with paging later.</span></span> <span data-ttu-id="e18a5-222">Ale jeśli jest to zbyt wiele, wprowadź tylko kilka. Rzeczywiste dane nie są istotne.</span><span class="sxs-lookup"><span data-stu-id="e18a5-222">But if that's too many, enter just a few for now.) The actual data doesn't matter.</span></span>

![Siatka wejścia bazy danych w programie WebMatrix z dowolnego rekordu](displaying-data/_static/image16.png)

<span data-ttu-id="e18a5-224">Jeśli wprowadzono wszystkie filmy bez błędów, wartości identyfikatorów są sekwencyjne.</span><span class="sxs-lookup"><span data-stu-id="e18a5-224">If you entered all the movies without any errors, the ID values are sequential.</span></span> <span data-ttu-id="e18a5-225">Jeśli podjęto próbę zapisania niekompletnego rekordu filmu, numery IDENTYFIKACYJNe mogą nie być sekwencyjne.</span><span class="sxs-lookup"><span data-stu-id="e18a5-225">If you tried to save an incomplete movie record, the ID numbers might not be sequential.</span></span> <span data-ttu-id="e18a5-226">Jeśli tak, to jest dobry.</span><span class="sxs-lookup"><span data-stu-id="e18a5-226">If so, that's okay.</span></span> <span data-ttu-id="e18a5-227">Liczby nie mają własnych znaczenia i jedyną istotną kwestią jest to, że są one unikatowe w tabeli *filmów* .</span><span class="sxs-lookup"><span data-stu-id="e18a5-227">The numbers don't have any inherent meaning, and the only thing that's important is that they're unique in the *Movies* table.</span></span>

<span data-ttu-id="e18a5-228">Zamknij kartę, która zawiera projektanta baz danych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-228">Close the tab that contains the database designer.</span></span>

<span data-ttu-id="e18a5-229">Teraz można włączyć wyświetlanie tych danych na stronie sieci Web.</span><span class="sxs-lookup"><span data-stu-id="e18a5-229">Now you can turn to displaying this data on a web page.</span></span>

## <a name="displaying-data-in-a-page-by-using-the-webgrid-helper"></a><span data-ttu-id="e18a5-230">Wyświetlanie danych na stronie przy użyciu pomocnika WebGrid</span><span class="sxs-lookup"><span data-stu-id="e18a5-230">Displaying Data in a Page by Using the WebGrid Helper</span></span>

<span data-ttu-id="e18a5-231">Aby wyświetlić dane na stronie, użyj pomocnika `WebGrid`.</span><span class="sxs-lookup"><span data-stu-id="e18a5-231">To display data in a page, you're going to use the `WebGrid` helper.</span></span> <span data-ttu-id="e18a5-232">Ten pomocnik tworzy ekran w siatce lub tabeli (wiersze i kolumny).</span><span class="sxs-lookup"><span data-stu-id="e18a5-232">This helper produces a display in a grid or table (rows and columns).</span></span> <span data-ttu-id="e18a5-233">Jak widać, będziesz mieć możliwość dostosowania siatki z formatowaniem i innymi funkcjami.</span><span class="sxs-lookup"><span data-stu-id="e18a5-233">As you'll see, you'll be able refine the grid with formatting and other features.</span></span>

<span data-ttu-id="e18a5-234">Aby uruchomić siatkę, należy napisać kilka wierszy kodu.</span><span class="sxs-lookup"><span data-stu-id="e18a5-234">To run the grid, you'll have to write a few lines of code.</span></span> <span data-ttu-id="e18a5-235">Te kilka wierszy będzie działać jako rodzaj wzorca dla niemal wszystkich danych, które można wykonać w tym samouczku.</span><span class="sxs-lookup"><span data-stu-id="e18a5-235">These few lines will serve as a kind of pattern for almost all of the data access that you do in this tutorial.</span></span>

> [!NOTE]
> <span data-ttu-id="e18a5-236">Istnieje wiele opcji wyświetlania danych na stronie; pomocnik `WebGrid` jest tylko jeden.</span><span class="sxs-lookup"><span data-stu-id="e18a5-236">You actually have many options for displaying data on a page; the `WebGrid` helper is just one.</span></span> <span data-ttu-id="e18a5-237">Wybieramy go dla tego samouczka, ponieważ jest to najprostszy sposób na wyświetlanie danych i ponieważ jest on rozsądnie elastyczny.</span><span class="sxs-lookup"><span data-stu-id="e18a5-237">We chose it for this tutorial because it's the easiest way to display data and because it's reasonably flexible.</span></span> <span data-ttu-id="e18a5-238">W następnym zestawie samouczków zobaczysz, jak używać bardziej "ręcznego" sposobu pracy z danymi na stronie, co zapewnia większą kontrolę nad sposobem wyświetlania danych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-238">In the next tutorial set, you'll see how to use a more "manual" way to work with data in the page, which gives you more direct control over how to display the data.</span></span>

<span data-ttu-id="e18a5-239">W lewym okienku w programie WebMatrix kliknij obszar roboczy **pliki** .</span><span class="sxs-lookup"><span data-stu-id="e18a5-239">In the left pane in WebMatrix, click the **Files** workspace.</span></span>

<span data-ttu-id="e18a5-240">Utworzona nowa baza danych znajduje się w folderze *danych\_aplikacji* .</span><span class="sxs-lookup"><span data-stu-id="e18a5-240">The new database you created is in the *App\_Data* folder.</span></span> <span data-ttu-id="e18a5-241">Jeśli folder nie istnieje, WebMatrix utworzył go dla nowej bazy danych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-241">If the folder didn't already exist, WebMatrix created it for your new database.</span></span> <span data-ttu-id="e18a5-242">(Folder mógł istnieć, jeśli wcześniej zainstalowano pomocników).</span><span class="sxs-lookup"><span data-stu-id="e18a5-242">(The folder might have existed if you'd previously installed helpers.)</span></span>

<span data-ttu-id="e18a5-243">W widoku drzewa wybierz katalog główny witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="e18a5-243">In the tree view, select the root of the website.</span></span> <span data-ttu-id="e18a5-244">Musisz wybrać katalog główny witryny sieci Web; w przeciwnym razie nowy plik może zostać dodany do folderu danych\_aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e18a5-244">You must select the root of the website; otherwise, the new file might be added to the App\_Data folder.</span></span>

<span data-ttu-id="e18a5-245">Na wstążce kliknij pozycję **Nowy**.</span><span class="sxs-lookup"><span data-stu-id="e18a5-245">In the ribbon, click **New**.</span></span> <span data-ttu-id="e18a5-246">W polu **Wybierz typ pliku** wybierz pozycję **cshtml**.</span><span class="sxs-lookup"><span data-stu-id="e18a5-246">In the **Choose a File Type** box, choose **CSHTML**.</span></span>

<span data-ttu-id="e18a5-247">W polu **Nazwa** wpisz nową stronę "filmy. cshtml":</span><span class="sxs-lookup"><span data-stu-id="e18a5-247">In the **Name** box, name the new page "Movies.cshtml":</span></span>

![Okno dialogowe "Wybieranie typu pliku" pokazujące stronę "filmy"](displaying-data/_static/image17.png)

<span data-ttu-id="e18a5-249">Kliknij przycisk **OK**.</span><span class="sxs-lookup"><span data-stu-id="e18a5-249">Click the **OK** button.</span></span> <span data-ttu-id="e18a5-250">W programie WebMatrix zostanie otwarty nowy plik z niektórymi elementami szkieletu.</span><span class="sxs-lookup"><span data-stu-id="e18a5-250">WebMatrix opens a new file with some skeleton elements in it.</span></span> <span data-ttu-id="e18a5-251">Najpierw napiszesz kod, aby pobrać dane z bazy danych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-251">First you'll write some code to go get the data from the database.</span></span> <span data-ttu-id="e18a5-252">Następnie dodasz znaczniki do strony, aby faktycznie wyświetlały dane.</span><span class="sxs-lookup"><span data-stu-id="e18a5-252">Then you'll add markup to the page to actually display the data.</span></span>

### <a name="writing-the-data-query-code"></a><span data-ttu-id="e18a5-253">Pisanie kodu zapytania o dane</span><span class="sxs-lookup"><span data-stu-id="e18a5-253">Writing the Data Query Code</span></span>

<span data-ttu-id="e18a5-254">W górnej części strony między znakami `@{` i `}` wprowadź następujący kod.</span><span class="sxs-lookup"><span data-stu-id="e18a5-254">At the top of the page, between the `@{` and `}` characters, enter the following code.</span></span> <span data-ttu-id="e18a5-255">(Upewnij się, że wprowadzasz ten kod między otwierającym i zamykającym nawiasem klamrowym).</span><span class="sxs-lookup"><span data-stu-id="e18a5-255">(Make sure that you enter this code between the opening and closing braces.)</span></span>

[!code-csharp[Main](displaying-data/samples/sample1.cs)]

<span data-ttu-id="e18a5-256">Pierwszy wiersz otwiera utworzoną wcześniej bazę danych, która jest zawsze pierwszym krokiem przed wykonaniem operacji w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-256">The first line opens the database that you created earlier, which is always the first step before doing something with the database.</span></span> <span data-ttu-id="e18a5-257">Poznasz nazwę metody `Database.Open` bazy danych do otwarcia.</span><span class="sxs-lookup"><span data-stu-id="e18a5-257">You tell the `Database.Open` method name of the database to open.</span></span> <span data-ttu-id="e18a5-258">Zwróć uwagę, że w nazwie nie jest uwzględniony *. sdf.*</span><span class="sxs-lookup"><span data-stu-id="e18a5-258">Notice that you don't include *.sdf* in the name.</span></span> <span data-ttu-id="e18a5-259">Metoda `Open` zakłada, że szuka pliku *. sdf* (czyli *WebPagesMovies. sdf*) i że plik *. sdf* znajduje się w folderze *\_danych aplikacji* .</span><span class="sxs-lookup"><span data-stu-id="e18a5-259">The `Open` method assumes that it's looking for an *.sdf* file (that is, *WebPagesMovies.sdf*) and that the *.sdf* file is in the *App\_Data* folder.</span></span> <span data-ttu-id="e18a5-260">(Wcześniej zauważono, że folder *danych\_aplikacji* jest zarezerwowany. ten scenariusz jest jednym z miejsc, w których ASP.NET tworzy założeń o tej nazwie.)</span><span class="sxs-lookup"><span data-stu-id="e18a5-260">(Earlier we noted that the *App\_Data* folder is reserved; this scenario is one of the places where ASP.NET makes assumptions about that name.)</span></span>

<span data-ttu-id="e18a5-261">Gdy baza danych zostanie otwarta, odwołanie do niej jest umieszczane w zmiennej o nazwie `db`.</span><span class="sxs-lookup"><span data-stu-id="e18a5-261">When the database is opened, a reference to it is put into the variable named `db`.</span></span> <span data-ttu-id="e18a5-262">(Może to być nazwane wszystko). Zmienna `db` to sposób, w jaki będziesz kończyć pracę z bazą danych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-262">(Which could be named anything.) The `db` variable is how you'll end up interacting with the database.</span></span>

<span data-ttu-id="e18a5-263">Drugi wiersz w rzeczywistości pobiera dane bazy danych za pomocą metody `Query`.</span><span class="sxs-lookup"><span data-stu-id="e18a5-263">The second line actually fetches the database data by using the `Query` method.</span></span> <span data-ttu-id="e18a5-264">Zwróć uwagę, jak działa ten kod: zmienna `db` ma odwołanie do otwartej bazy danych i wywołujemy metodę `Query` przy użyciu zmiennej `db` (`db.Query`).</span><span class="sxs-lookup"><span data-stu-id="e18a5-264">Notice how this code works: the `db` variable has a reference to the opened database, and you invoke the `Query` method by using the `db` variable (`db.Query`).</span></span>

<span data-ttu-id="e18a5-265">Sama kwerenda jest instrukcją SQL `Select`.</span><span class="sxs-lookup"><span data-stu-id="e18a5-265">The query itself is a SQL `Select` statement.</span></span> <span data-ttu-id="e18a5-266">(Aby uzyskać informacje na temat programu SQL Server w niewielkim stopniu, zobacz wyjaśnienie później). W instrukcji `Movies` identyfikuje tabelę do zapytania.</span><span class="sxs-lookup"><span data-stu-id="e18a5-266">(For a little background about SQL, see the explanation later.) In the statement, `Movies` identifies the table to query.</span></span> <span data-ttu-id="e18a5-267">Znak `*` określa, że zapytanie powinno zwrócić wszystkie kolumny z tabeli.</span><span class="sxs-lookup"><span data-stu-id="e18a5-267">The `*` character specifies that the query should return all the columns from the table.</span></span> <span data-ttu-id="e18a5-268">(Można również wystawić pojedyncze kolumny, oddzielone przecinkami).</span><span class="sxs-lookup"><span data-stu-id="e18a5-268">(You could also list columns individually, separated by commas.)</span></span>

<span data-ttu-id="e18a5-269">Wyniki zapytania, jeśli istnieją, są zwracane i udostępniane w zmiennej `selectedData`.</span><span class="sxs-lookup"><span data-stu-id="e18a5-269">The results of the query, if any, are returned and made available in the `selectedData` variable.</span></span> <span data-ttu-id="e18a5-270">Ponownie zmienna może mieć nazwę wszystko.</span><span class="sxs-lookup"><span data-stu-id="e18a5-270">Again, the variable could be named anything.</span></span>

<span data-ttu-id="e18a5-271">Na koniec trzeci wiersz informuje ASP.NET o konieczności użycia wystąpienia pomocnika `WebGrid`.</span><span class="sxs-lookup"><span data-stu-id="e18a5-271">Finally, the third line tells ASP.NET that you want to use an instance of the `WebGrid` helper.</span></span> <span data-ttu-id="e18a5-272">Można utworzyć (utworzyć*wystąpienie*) obiekt pomocnika za pomocą słowa kluczowego `new` i przekazać go do wyników zapytania za pomocą zmiennej `selectedData`.</span><span class="sxs-lookup"><span data-stu-id="e18a5-272">You create (*instantiate*) the helper object by using the `new` keyword and pass it the query results via the `selectedData` variable.</span></span> <span data-ttu-id="e18a5-273">Nowy obiekt `WebGrid`, wraz z wynikami zapytania bazy danych, jest udostępniany w zmiennej `grid`.</span><span class="sxs-lookup"><span data-stu-id="e18a5-273">The new `WebGrid` object, along with the results of the database query, are made available in the `grid` variable.</span></span> <span data-ttu-id="e18a5-274">Konieczne będzie chwilowe wyświetlenie danych na stronie.</span><span class="sxs-lookup"><span data-stu-id="e18a5-274">You'll need that result in a moment to actually display the data in the page.</span></span>

<span data-ttu-id="e18a5-275">Na tym etapie baza danych została otwarta i masz wybrane dane i przygotowano pomocnika `WebGrid` z tymi danymi.</span><span class="sxs-lookup"><span data-stu-id="e18a5-275">At this stage, the database has been opened, you've gotten the data you want, and you've prepared the `WebGrid` helper with that data.</span></span> <span data-ttu-id="e18a5-276">Następnie należy utworzyć adiustację na stronie.</span><span class="sxs-lookup"><span data-stu-id="e18a5-276">Next is to create the markup in the page.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="e18a5-277">**Structured Query Language (SQL)**</span><span class="sxs-lookup"><span data-stu-id="e18a5-277">**Structured Query Language (SQL)**</span></span>
> 
> <span data-ttu-id="e18a5-278">SQL to język, który jest używany w większości relacyjnych baz danych do zarządzania danymi w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-278">SQL is a language that's used in most relational databases for managing data in a database.</span></span> <span data-ttu-id="e18a5-279">Zawiera polecenia, które umożliwiają pobieranie danych i aktualizowanie ich oraz pozwalające tworzyć, modyfikować i zarządzać danymi w tabelach bazy danych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-279">It includes commands that let you retrieve data and update it, and that let you create, modify, and manage data in database tables.</span></span> <span data-ttu-id="e18a5-280">Język SQL różni się od języka programowania (na C#przykład).</span><span class="sxs-lookup"><span data-stu-id="e18a5-280">SQL is different than a programming language (like C#).</span></span> <span data-ttu-id="e18a5-281">Dzięki programowi SQL możesz poinformowanie bazy danych o tym, co chcesz zrobić, i jest to zadanie bazy danych, aby ustalić, jak pobrać dane lub wykonać zadanie.</span><span class="sxs-lookup"><span data-stu-id="e18a5-281">With SQL, you tell the database what you want, and it's the database's job to figure out how to get the data or perform the task.</span></span> <span data-ttu-id="e18a5-282">Oto przykłady niektórych poleceń SQL i ich działania:</span><span class="sxs-lookup"><span data-stu-id="e18a5-282">Here are examples of some SQL commands and what they do:</span></span>
> 
> `Select * From Movies`
> 
> `SELECT ID, Name, Price FROM Product WHERE Price > 10.00 ORDER BY Name`
> 
> <span data-ttu-id="e18a5-283">Pierwsza instrukcja `Select` pobiera wszystkie kolumny (określone przez `*`) z tabeli *filmów* .</span><span class="sxs-lookup"><span data-stu-id="e18a5-283">The first `Select` statement gets all the columns (specified by `*`) from the *Movies* table.</span></span>
> 
> <span data-ttu-id="e18a5-284">Druga instrukcja `Select` Pobiera kolumny ID, Name i Price z rekordów w tabeli *Product* , których wartość kolumny cena jest większa niż 10.</span><span class="sxs-lookup"><span data-stu-id="e18a5-284">The second `Select` statement fetches the ID, Name, and Price columns from records in the *Product* table whose Price column value is more than 10.</span></span> <span data-ttu-id="e18a5-285">Polecenie zwraca wyniki w kolejności alfabetycznej na podstawie wartości kolumny Name.</span><span class="sxs-lookup"><span data-stu-id="e18a5-285">The command returns the results in alphabetical order based on the values of the Name column.</span></span> <span data-ttu-id="e18a5-286">Jeśli żadne rekordy nie pasują do kryteriów cen, polecenie zwraca pusty zestaw.</span><span class="sxs-lookup"><span data-stu-id="e18a5-286">If no records match the price criteria, the command returns an empty set.</span></span>
> 
> `INSERT INTO Product (Name, Description, Price) VALUES ('Croissant', 'A flaky delight', 1.99)`
> 
> <span data-ttu-id="e18a5-287">To polecenie wstawia nowy rekord do tabeli *Product* , ustawiając dla kolumny Name wartość "croissant", kolumnę Description na wartość "płatne" i cenę do 1,99.</span><span class="sxs-lookup"><span data-stu-id="e18a5-287">This command inserts a new record into the *Product* table, setting the Name column to "Croissant", the Description column to "A flaky delight", and the price to 1.99.</span></span>
> 
> <span data-ttu-id="e18a5-288">Należy zauważyć, że podczas określania wartości innej niż numeryczna wartość jest ujęta w znaki pojedynczego cudzysłowu (nie podwójne cudzysłowy, jak w C#przypadku).</span><span class="sxs-lookup"><span data-stu-id="e18a5-288">Notice that when you're specifying a non-numeric value, the value is enclosed in single quotation marks (not double quotation marks, as in C#).</span></span> <span data-ttu-id="e18a5-289">Te znaki cudzysłowu są używane wokół wartości tekstowych lub dat, ale nie liczby.</span><span class="sxs-lookup"><span data-stu-id="e18a5-289">You use these quotation marks around text or date values, but not around numbers.</span></span>
> 
> `DELETE FROM Product WHERE ExpirationDate < '01/01/2008'`
> 
> <span data-ttu-id="e18a5-290">To polecenie usuwa rekordy z tabeli *Product* , której kolumna daty wygaśnięcia jest wcześniejsza niż 1 stycznia 2008.</span><span class="sxs-lookup"><span data-stu-id="e18a5-290">This command deletes records in the *Product* table whose expiration date column is earlier than January 1, 2008.</span></span> <span data-ttu-id="e18a5-291">(Polecenie zakłada, że tabela *produktów* ma taką kolumnę, oczywiście). Data jest wprowadzana w formacie MM/DD/RRRR, ale należy ją wprowadzić w formacie używanym dla ustawień regionalnych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-291">(The command assumes that the *Product* table has such a column, of course.) The date is entered here in MM/DD/YYYY format, but it should be entered in the format that's used for your locale.</span></span>
> 
> <span data-ttu-id="e18a5-292">Polecenia `Insert` i `Delete` nie zwracają zestawów wyników.</span><span class="sxs-lookup"><span data-stu-id="e18a5-292">The `Insert` and `Delete` commands don't return result sets.</span></span> <span data-ttu-id="e18a5-293">Zamiast tego zwraca liczbę, która informuje o liczbie rekordów, na które miało wpływ polecenie.</span><span class="sxs-lookup"><span data-stu-id="e18a5-293">Instead, they return a number that tells you how many records were affected by the command.</span></span>
> 
> <span data-ttu-id="e18a5-294">W przypadku niektórych z tych operacji (takich jak wstawianie i usuwanie rekordów) proces żądający operacji musi mieć odpowiednie uprawnienia w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-294">For some of these operations (like inserting and deleting records), the process that's requesting the operation has to have appropriate permissions in the database.</span></span> <span data-ttu-id="e18a5-295">Dlatego w przypadku produkcyjnych baz danych często konieczne jest podanie nazwy użytkownika i hasła podczas łączenia się z bazą danych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-295">That's why for production databases you often have to supply a user name and password when you connect to the database.</span></span>
> 
> <span data-ttu-id="e18a5-296">Istnieją dziesiątki poleceń SQL, ale wszystkie są zgodne ze wzorcem, takim jak polecenia widoczne w tym miejscu.</span><span class="sxs-lookup"><span data-stu-id="e18a5-296">There are dozens of SQL commands, but they all follow a pattern like the commands you see here.</span></span> <span data-ttu-id="e18a5-297">Za pomocą poleceń SQL można tworzyć tabele baz danych, liczyć liczbę rekordów w tabeli, obliczać ceny i wykonywać wiele innych operacji.</span><span class="sxs-lookup"><span data-stu-id="e18a5-297">You can use SQL commands to create database tables, count the number of records in a table, calculate prices, and perform many more operations.</span></span>

### <a name="adding-markup-to-display-the-data"></a><span data-ttu-id="e18a5-298">Dodawanie znaczników w celu wyświetlenia danych</span><span class="sxs-lookup"><span data-stu-id="e18a5-298">Adding Markup to Display the Data</span></span>

<span data-ttu-id="e18a5-299">Wewnątrz elementu `<head>` Ustaw zawartość elementu `<title>` na "filmy":</span><span class="sxs-lookup"><span data-stu-id="e18a5-299">Inside the `<head>` element, set contents of the `<title>` element to "Movies":</span></span>

[!code-html[Main](displaying-data/samples/sample2.html?highlight=3)]

<span data-ttu-id="e18a5-300">Wewnątrz elementu `<body>` strony Dodaj następujące elementy:</span><span class="sxs-lookup"><span data-stu-id="e18a5-300">Inside the `<body>` element of the page, add the following:</span></span>

[!code-html[Main](displaying-data/samples/sample3.html)]

<span data-ttu-id="e18a5-301">Gotowe.</span><span class="sxs-lookup"><span data-stu-id="e18a5-301">That's it.</span></span> <span data-ttu-id="e18a5-302">Zmienna `grid` jest wartością utworzoną podczas tworzenia obiektu `WebGrid` w kodzie wcześniejszym.</span><span class="sxs-lookup"><span data-stu-id="e18a5-302">The `grid` variable is the value you created when you created the `WebGrid` object in code earlier.</span></span>

<span data-ttu-id="e18a5-303">W widoku drzewa WebMatrix kliknij prawym przyciskiem myszy stronę i wybierz polecenie **Uruchom w przeglądarce**.</span><span class="sxs-lookup"><span data-stu-id="e18a5-303">In the WebMatrix tree view, right-click the page and select **Launch in browser**.</span></span> <span data-ttu-id="e18a5-304">Zobaczysz coś podobnej do tej strony:</span><span class="sxs-lookup"><span data-stu-id="e18a5-304">You'll see something like this page:</span></span>

![Domyślne dane wyjściowe pomocnika WebGrid z tabeli filmów](displaying-data/_static/image18.png)

<span data-ttu-id="e18a5-306">Kliknij link nagłówka kolumny, aby posortować według tej kolumny.</span><span class="sxs-lookup"><span data-stu-id="e18a5-306">Click a column heading link to sort by that column.</span></span> <span data-ttu-id="e18a5-307">Można sortować według kliknięcia nagłówka jest funkcją wbudowaną pomocnika **WebGrid** .</span><span class="sxs-lookup"><span data-stu-id="e18a5-307">Being able to sort by clicking a heading is a feature that's built into the **WebGrid** helper.</span></span>

<span data-ttu-id="e18a5-308">Metoda `GetHtml`, która sugeruje jego nazwę, generuje znacznik, który wyświetla dane.</span><span class="sxs-lookup"><span data-stu-id="e18a5-308">The `GetHtml` method, as its name suggests, generates markup that displays the data.</span></span> <span data-ttu-id="e18a5-309">Domyślnie metoda `GetHtml` generuje element HTML `<table>`.</span><span class="sxs-lookup"><span data-stu-id="e18a5-309">By default, the `GetHtml` method generates an HTML `<table>` element.</span></span> <span data-ttu-id="e18a5-310">(Jeśli chcesz, możesz sprawdzić renderowanie, przeglądając Źródło strony w przeglądarce).</span><span class="sxs-lookup"><span data-stu-id="e18a5-310">(If you want, you can verify the rendering by looking at the source of the page in the browser.)</span></span>

## <a name="modifying-the-look-of-the-grid"></a><span data-ttu-id="e18a5-311">Modyfikowanie wyglądu siatki</span><span class="sxs-lookup"><span data-stu-id="e18a5-311">Modifying the Look of the Grid</span></span>

<span data-ttu-id="e18a5-312">Korzystanie z pomocnika `WebGrid`, podobnie jak wiesz, jest proste, ale wyświetlane wyniki są zwykłe.</span><span class="sxs-lookup"><span data-stu-id="e18a5-312">Using the `WebGrid` helper like you just did is easy, but the resulting display is plain.</span></span> <span data-ttu-id="e18a5-313">Pomocnik `WebGrid` ma wszystkie opcje, które umożliwiają kontrolowanie sposobu wyświetlania danych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-313">The `WebGrid` helper has all sorts of options that let you control how the data is displayed.</span></span> <span data-ttu-id="e18a5-314">W tym samouczku jest zbyt wiele, aby eksplorować ten samouczek, ale ta sekcja zawiera pomysł dotyczący niektórych z tych opcji.</span><span class="sxs-lookup"><span data-stu-id="e18a5-314">There are far too many to explore in this tutorial, but this section will give you an idea of some of those options.</span></span> <span data-ttu-id="e18a5-315">Kilka dodatkowych opcji zostanie uwzględnionych w kolejnych samouczkach w tej serii.</span><span class="sxs-lookup"><span data-stu-id="e18a5-315">A few additional options will be covered in later tutorials in this series.</span></span>

### <a name="specifying-individual-columns-to-display"></a><span data-ttu-id="e18a5-316">Określanie pojedynczych kolumn do wyświetlenia</span><span class="sxs-lookup"><span data-stu-id="e18a5-316">Specifying Individual Columns to Display</span></span>

<span data-ttu-id="e18a5-317">Aby rozpocząć, można określić, że mają być wyświetlane tylko niektóre kolumny.</span><span class="sxs-lookup"><span data-stu-id="e18a5-317">To start, you can specify that you want to display only certain columns.</span></span> <span data-ttu-id="e18a5-318">Domyślnie, jak widać, Siatka pokazuje wszystkie cztery kolumny z tabeli *filmów* .</span><span class="sxs-lookup"><span data-stu-id="e18a5-318">By default, as you've seen, the grid shows all four of the columns from the *Movies* table.</span></span>

<span data-ttu-id="e18a5-319">W pliku *Films. cshtml* Zastąp znacznik `@grid.GetHtml()`, który właśnie został dodany:</span><span class="sxs-lookup"><span data-stu-id="e18a5-319">In the *Movies.cshtml* file, replace the `@grid.GetHtml()` markup that you just added with the following:</span></span>

[!code-css[Main](displaying-data/samples/sample4.css)]

<span data-ttu-id="e18a5-320">Aby poinformować pomocnika, które kolumny mają być wyświetlane, należy podać parametr `columns` dla metody `GetHtml` i przekazać kolekcję kolumn.</span><span class="sxs-lookup"><span data-stu-id="e18a5-320">To tell the helper which columns to display, you include a `columns` parameter for the `GetHtml` method and pass in a collection of columns.</span></span> <span data-ttu-id="e18a5-321">W kolekcji należy określić każdą kolumnę do uwzględnienia.</span><span class="sxs-lookup"><span data-stu-id="e18a5-321">In the collection, you specify each column to include.</span></span> <span data-ttu-id="e18a5-322">Należy określić pojedynczą kolumnę, która ma być wyświetlana przez uwzględnienie obiektu `grid.Column` i przekazać nazwę wybranej kolumny danych.</span><span class="sxs-lookup"><span data-stu-id="e18a5-322">You specify an individual column to display by including a `grid.Column` object, and pass in the name of the data column you want.</span></span> <span data-ttu-id="e18a5-323">(Te kolumny muszą być zawarte w wynikach zapytania SQL — pomocnik `WebGrid` nie może wyświetlić kolumn, które nie zostały zwrócone przez zapytanie.)</span><span class="sxs-lookup"><span data-stu-id="e18a5-323">(These columns must be included in the SQL query results — the `WebGrid` helper cannot display columns that were not returned by the query.)</span></span>

<span data-ttu-id="e18a5-324">Uruchom ponownie stronę *filmy. cshtml* w przeglądarce, a tym razem wyświetli ekran podobny do następującego (Zauważ, że nie jest wyświetlana żadna kolumna identyfikatora):</span><span class="sxs-lookup"><span data-stu-id="e18a5-324">Launch the *Movies.cshtml* page in the browser again, and this time you get a display like the following one (notice that no ID column is displayed):</span></span>

![Wyświetlanie siatki w sieci WebGrid pokazujące tylko wybrane kolumny](displaying-data/_static/image19.png)

### <a name="changing-the-look-of-the-grid"></a><span data-ttu-id="e18a5-326">Zmiana wyglądu siatki</span><span class="sxs-lookup"><span data-stu-id="e18a5-326">Changing the Look of the Grid</span></span>

<span data-ttu-id="e18a5-327">Istnieje całkiem kilka dodatkowych opcji wyświetlania kolumn, a niektóre z nich zostaną omówione w kolejnych samouczkach w tym zestawie.</span><span class="sxs-lookup"><span data-stu-id="e18a5-327">There are quite a few more options for displaying columns, some of which will be explored in later tutorials in this set.</span></span> <span data-ttu-id="e18a5-328">W tej sekcji opisano sposób, w jaki można stylować siatkę jako całość.</span><span class="sxs-lookup"><span data-stu-id="e18a5-328">For now, this section will introduce you to ways in which you can style the grid as a whole.</span></span>

<span data-ttu-id="e18a5-329">Wewnątrz sekcji `<head>` strony tuż przed tagiem zamykającym `</head>` Dodaj następujący element `<style>`:</span><span class="sxs-lookup"><span data-stu-id="e18a5-329">Inside the `<head>` section of the page, just before the closing `</head>` tag, add the following `<style>` element:</span></span>

[!code-css[Main](displaying-data/samples/sample5.css)]

<span data-ttu-id="e18a5-330">Ten znacznik CSS definiuje klasy o nazwie `grid`, `head`i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="e18a5-330">This CSS markup defines classes named `grid`, `head`, and so on.</span></span> <span data-ttu-id="e18a5-331">Można również umieścić te definicje stylów w osobnym pliku *CSS* i połączyć go ze stroną.</span><span class="sxs-lookup"><span data-stu-id="e18a5-331">You could also put these style definitions in a separate *.css* file and link that to the page.</span></span> <span data-ttu-id="e18a5-332">(W rzeczywistości wykonasz tę czynność w dalszej części tego samouczka). Jednak aby ułatwić Ci ten samouczek, znajdują się one na tej samej stronie, na której są wyświetlane dane.</span><span class="sxs-lookup"><span data-stu-id="e18a5-332">(In fact, you'll do that later in this tutorial set.) But to make things easy for this tutorial, they're inside the same page that displays the data.</span></span>

<span data-ttu-id="e18a5-333">Teraz możesz uzyskać pomocnika `WebGrid`, aby używać tych klas stylów.</span><span class="sxs-lookup"><span data-stu-id="e18a5-333">Now you can get the `WebGrid` helper to use these style classes.</span></span> <span data-ttu-id="e18a5-334">Pomocnik ma wiele właściwości (na przykład `tableStyle`) tylko w tym celu — przypisujesz do nich nazwę klasy stylu CSS, a nazwa klasy jest renderowana jako część znaczników, które są renderowane przez pomocnika.</span><span class="sxs-lookup"><span data-stu-id="e18a5-334">The helper has a number of properties (for example, `tableStyle`) for just this purpose — you assign a CSS style class name to them, and that class name is rendered as part of the markup that's rendered by the helper.</span></span>

<span data-ttu-id="e18a5-335">Zmień znacznik `grid.GetHtml` tak, aby wyglądał teraz tak, jak w tym kodzie:</span><span class="sxs-lookup"><span data-stu-id="e18a5-335">Change the `grid.GetHtml` markup so that it now looks like this code:</span></span>

[!code-css[Main](displaying-data/samples/sample6.css)]

<span data-ttu-id="e18a5-336">Nowością jest dodanie `tableStyle`, `headerStyle`i `alternatingRowStyle` parametrów do metody `GetHtml`.</span><span class="sxs-lookup"><span data-stu-id="e18a5-336">What's new here is that you've added `tableStyle`, `headerStyle`, and `alternatingRowStyle` parameters to the `GetHtml` method.</span></span> <span data-ttu-id="e18a5-337">Te parametry zostały ustawione na nazwy stylów CSS, które zostały dodane do momentu temu.</span><span class="sxs-lookup"><span data-stu-id="e18a5-337">These parameters have been set to the names of the CSS styles that you added a moment ago.</span></span>

<span data-ttu-id="e18a5-338">Uruchom stronę, a w tym momencie zobaczysz siatkę, która wygląda mniej więcej tak, jak wcześniej:</span><span class="sxs-lookup"><span data-stu-id="e18a5-338">Run the page, and this time you see a grid that looks much less plain than before:</span></span>

![Wyświetlacz WebGrid zawierający parametry ustawione na nazwy klas CSS](displaying-data/_static/image20.png)

<span data-ttu-id="e18a5-340">Aby zobaczyć, jak Wygenerowano metodę `GetHtml`, możesz przyjrzeć się źródłu strony w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="e18a5-340">To see what the `GetHtml` method generated, you can look at the source of the page in the browser.</span></span> <span data-ttu-id="e18a5-341">Nie będziemy tutaj podawać szczegółów, ale ważnym punktem jest to, że przez określenie parametrów takich jak `tableStyle`, Siatka generuje Tagi HTML w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="e18a5-341">We won't go into detail here, but the important point is that by specifying parameters like `tableStyle`, you caused the grid to generate HTML tags like the following:</span></span>

`<table class="grid">`

<span data-ttu-id="e18a5-342">Tag `<table>` ma dodany atrybut `class`, który odwołuje się do jednej z wcześniej dodanych reguł CSS.</span><span class="sxs-lookup"><span data-stu-id="e18a5-342">The `<table>` tag has had a `class` attribute added to it that references one of the CSS rules that you added earlier.</span></span> <span data-ttu-id="e18a5-343">Ten kod przedstawia podstawowy wzorzec &mdash; różne parametry metody `GetHtml` umożliwiają odwoływanie się do klas CSS, które następnie generują metody wraz ze znacznikiem.</span><span class="sxs-lookup"><span data-stu-id="e18a5-343">This code shows you the basic pattern &mdash; different parameters for the `GetHtml` method let you reference CSS classes that the method then generates along with the markup.</span></span> <span data-ttu-id="e18a5-344">Co robisz z klasami CSS.</span><span class="sxs-lookup"><span data-stu-id="e18a5-344">What you do with the CSS classes is up to you.</span></span>

## <a name="adding-paging"></a><span data-ttu-id="e18a5-345">Dodawanie stronicowania</span><span class="sxs-lookup"><span data-stu-id="e18a5-345">Adding Paging</span></span>

<span data-ttu-id="e18a5-346">Jako ostatnie zadanie dla tego samouczka dodasz stronicowanie do siatki.</span><span class="sxs-lookup"><span data-stu-id="e18a5-346">As the last task for this tutorial, you'll add paging to the grid.</span></span> <span data-ttu-id="e18a5-347">Teraz nie ma problemu, aby wyświetlić wszystkie filmy jednocześnie.</span><span class="sxs-lookup"><span data-stu-id="e18a5-347">Right now it's no problem to display all your movies at once.</span></span> <span data-ttu-id="e18a5-348">Ale jeśli dodano setki filmów, wyświetlenie strony będzie długie.</span><span class="sxs-lookup"><span data-stu-id="e18a5-348">But if you added hundreds of movies, the page display would get long.</span></span>

<span data-ttu-id="e18a5-349">W kodzie strony Zmień wiersz, który tworzy obiekt `WebGrid`, na następujący kod:</span><span class="sxs-lookup"><span data-stu-id="e18a5-349">In the page code, change the line that creates the `WebGrid` object to the following code:</span></span>

[!code-csharp[Main](displaying-data/samples/sample7.cs)]

<span data-ttu-id="e18a5-350">Jedyną różnicą od przed jest dodanie parametru `rowsPerPage`, który jest ustawiony na 3.</span><span class="sxs-lookup"><span data-stu-id="e18a5-350">The only difference from before is that you've added a `rowsPerPage` parameter that's set to 3.</span></span>

<span data-ttu-id="e18a5-351">Uruchom stronę.</span><span class="sxs-lookup"><span data-stu-id="e18a5-351">Run the page.</span></span> <span data-ttu-id="e18a5-352">Siatka wyświetla 3 wiersze jednocześnie oraz linki nawigacyjne, które umożliwiają przechodzenie do stron w bazie danych:</span><span class="sxs-lookup"><span data-stu-id="e18a5-352">The grid displays 3 rows at a time, plus navigation links that let you page through the movies in your database:</span></span>

![Wyświetlanie siatki WebGrid z stronicowaniem](displaying-data/_static/image21.png)

## <a name="coming-up-next"></a><span data-ttu-id="e18a5-354">Przyszłe przejście</span><span class="sxs-lookup"><span data-stu-id="e18a5-354">Coming Up Next</span></span>

<span data-ttu-id="e18a5-355">W następnym samouczku dowiesz się, jak używać składni Razor i C# Code do uzyskiwania danych wejściowych użytkownika w formularzu.</span><span class="sxs-lookup"><span data-stu-id="e18a5-355">In the next tutorial, you'll learn how to use Razor and C# code to get user input in a form.</span></span> <span data-ttu-id="e18a5-356">Dodasz pole wyszukiwania do strony filmy, aby można było znaleźć filmy według tytułu lub gatunku.</span><span class="sxs-lookup"><span data-stu-id="e18a5-356">You'll add a search box to the Movies page so that you can find movies by title or genre.</span></span>

## <a name="complete-listing-for-movies-page"></a><span data-ttu-id="e18a5-357">Pełna lista dla filmów</span><span class="sxs-lookup"><span data-stu-id="e18a5-357">Complete Listing for Movies Page</span></span>

[!code-cshtml[Main](displaying-data/samples/sample8.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="e18a5-358">Dodatkowe materiały</span><span class="sxs-lookup"><span data-stu-id="e18a5-358">Additional Resources</span></span>

- [<span data-ttu-id="e18a5-359">Wprowadzenie do programowania w ASP.NET sieci Web przy użyciu składni Razor</span><span class="sxs-lookup"><span data-stu-id="e18a5-359">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)

> [!div class="step-by-step"]
> <span data-ttu-id="e18a5-360">[Poprzednie](intro-to-web-pages-programming.md)
> [dalej](form-basics.md)</span><span class="sxs-lookup"><span data-stu-id="e18a5-360">[Previous](intro-to-web-pages-programming.md)
[Next](form-basics.md)</span></span>
