---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: Wprowadzenie do stron sieci Web ASP.NET — podstawowe informacje o formularzach HTML | Microsoft Docs
author: Rick-Anderson
description: W tym samouczku przedstawiono podstawowe informacje dotyczące sposobu tworzenia formularza wejściowego i sposobu obsługi danych wejściowych użytkownika podczas korzystania z ASP.NET stron sieci Web (Razor). A teraz...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: f57661077ec3bb13f3d4ec41b130bda4d2fb9070
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/06/2020
ms.locfileid: "78574285"
---
# <a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="adf26-104">Wprowadzenie do stron sieci Web ASP.NET — podstawy formularzy HTML</span><span class="sxs-lookup"><span data-stu-id="adf26-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>

<span data-ttu-id="adf26-105">Autor [FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="adf26-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="adf26-106">W tym samouczku przedstawiono podstawowe informacje dotyczące sposobu tworzenia formularza wejściowego i sposobu obsługi danych wejściowych użytkownika podczas korzystania z ASP.NET stron sieci Web (Razor).</span><span class="sxs-lookup"><span data-stu-id="adf26-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="adf26-107">Teraz, gdy już masz bazę danych, będziesz używać swoich umiejętności, aby umożliwić użytkownikom znalezienie określonych filmów w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="adf26-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="adf26-108">Przyjęto założenie, że została ukończona seria przez [wprowadzenie do wyświetlania danych przy użyciu stron sieci Web ASP.NET](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="adf26-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="adf26-109">Zawartość:</span><span class="sxs-lookup"><span data-stu-id="adf26-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="adf26-110">Jak utworzyć formularz przy użyciu standardowych elementów HTML.</span><span class="sxs-lookup"><span data-stu-id="adf26-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="adf26-111">Jak odczytać dane wejściowe użytkownika w formularzu.</span><span class="sxs-lookup"><span data-stu-id="adf26-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="adf26-112">Jak utworzyć zapytanie SQL, które selektywnie pobiera dane przy użyciu wyszukiwanego terminu przez użytkownika.</span><span class="sxs-lookup"><span data-stu-id="adf26-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="adf26-113">Jak mieć pola na stronie "Zapamiętaj" wprowadzone przez użytkownika.</span><span class="sxs-lookup"><span data-stu-id="adf26-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="adf26-114">Omówione funkcje/technologie:</span><span class="sxs-lookup"><span data-stu-id="adf26-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="adf26-115">Obiekt `Request`.</span><span class="sxs-lookup"><span data-stu-id="adf26-115">The `Request` object.</span></span>
> - <span data-ttu-id="adf26-116">Klauzula `Where` SQL.</span><span class="sxs-lookup"><span data-stu-id="adf26-116">The SQL `Where` clause.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="adf26-117">Co będziesz kompilować</span><span class="sxs-lookup"><span data-stu-id="adf26-117">What You'll Build</span></span>

<span data-ttu-id="adf26-118">W poprzednim samouczku została utworzona baza danych, do której dodano dane, a następnie użyto pomocnika `WebGrid`, aby wyświetlić dane.</span><span class="sxs-lookup"><span data-stu-id="adf26-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="adf26-119">W tym samouczku dodasz pole wyszukiwania, które umożliwia znalezienie filmów określonego gatunku lub tytuł zawiera wszelkie wprowadzane słowa.</span><span class="sxs-lookup"><span data-stu-id="adf26-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="adf26-120">(Na przykład można znaleźć wszystkie filmy, których gatunek jest "Action" lub którego tytuł zawiera "Harry" lub "Adventure").</span><span class="sxs-lookup"><span data-stu-id="adf26-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="adf26-121">Gdy skończysz korzystać z tego samouczka, zobaczysz stronę podobną do tej:</span><span class="sxs-lookup"><span data-stu-id="adf26-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Strona filmów z gatunkiem i wyszukiwaniem tytułu](form-basics/_static/image1.png)

<span data-ttu-id="adf26-123">Część wystawiania strony jest taka sama jak w ostatnim samouczku &mdash; siatki.</span><span class="sxs-lookup"><span data-stu-id="adf26-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="adf26-124">Różnica polega na tym, że siatka będzie zawierać tylko te filmy, które zostały wyszukane.</span><span class="sxs-lookup"><span data-stu-id="adf26-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="adf26-125">Informacje o formularzach HTML</span><span class="sxs-lookup"><span data-stu-id="adf26-125">About HTML Forms</span></span>

<span data-ttu-id="adf26-126">(Jeśli masz doświadczenie w tworzeniu formularzy HTML i z różnicą między `GET` i `POST`, możesz pominąć tę sekcję).</span><span class="sxs-lookup"><span data-stu-id="adf26-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="adf26-127">Formularz zawiera elementy danych wejściowych użytkownika &mdash; pola tekstowe, przyciski, przyciski radiowe, pola wyboru, listy rozwijane itd.</span><span class="sxs-lookup"><span data-stu-id="adf26-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="adf26-128">Użytkownicy wypełniają te kontrolki lub dokonają wyboru, a następnie przesyłają formularz, klikając przycisk.</span><span class="sxs-lookup"><span data-stu-id="adf26-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="adf26-129">Podstawowa składnia HTML formularza ilustruje następujący przykład:</span><span class="sxs-lookup"><span data-stu-id="adf26-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="adf26-130">Po uruchomieniu tego znacznika na stronie tworzy prosty formularz, który wygląda podobnie do poniższej ilustracji:</span><span class="sxs-lookup"><span data-stu-id="adf26-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Podstawowa postać HTML zgodna z renderowaniem w przeglądarce](form-basics/_static/image2.png)

<span data-ttu-id="adf26-132">Element `<form>` obejmuje elementy HTML do przesłania.</span><span class="sxs-lookup"><span data-stu-id="adf26-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="adf26-133">(Łatwa do zaniechania należy dodać elementy do strony, a następnie zapomnieć, aby umieścić je wewnątrz elementu `<form>`.</span><span class="sxs-lookup"><span data-stu-id="adf26-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="adf26-134">W takim przypadku nic nie zostanie przesłane. Atrybut `method` informuje przeglądarkę o sposobie przesyłania danych wejściowych użytkownika.</span><span class="sxs-lookup"><span data-stu-id="adf26-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="adf26-135">Ta wartość jest ustawiana na `post` w przypadku przeprowadzania aktualizacji na serwerze lub `get`, jeśli właśnie pobierasz dane z serwera.</span><span class="sxs-lookup"><span data-stu-id="adf26-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="adf26-136">**Bezpieczeństwo zlecenia GET, POST i HTTP**</span><span class="sxs-lookup"><span data-stu-id="adf26-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="adf26-137">HTTP, protokół używany przez przeglądarki i serwery do wymiany informacji, jest niezwykle prosty w swoich podstawowych operacjach.</span><span class="sxs-lookup"><span data-stu-id="adf26-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="adf26-138">Przeglądarki używają tylko kilku czasowników do żądania serwerów.</span><span class="sxs-lookup"><span data-stu-id="adf26-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="adf26-139">Podczas pisania kodu dla sieci Web warto zrozumieć te czasowniki oraz sposób ich używania przez przeglądarkę i serwer.</span><span class="sxs-lookup"><span data-stu-id="adf26-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="adf26-140">Najczęściej używane czasowniki są następujące:</span><span class="sxs-lookup"><span data-stu-id="adf26-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="adf26-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="adf26-141">`GET`.</span></span> <span data-ttu-id="adf26-142">Przeglądarka używa tego zlecenia do pobrania elementu z serwera.</span><span class="sxs-lookup"><span data-stu-id="adf26-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="adf26-143">Na przykład po wpisaniu adresu URL w przeglądarce przeglądarka wykonuje `GET` operacji, aby zażądać żądanej strony.</span><span class="sxs-lookup"><span data-stu-id="adf26-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="adf26-144">Jeśli strona zawiera grafikę, przeglądarka wykonuje dodatkowe operacje `GET`, aby pobrać obrazy.</span><span class="sxs-lookup"><span data-stu-id="adf26-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="adf26-145">Jeśli operacja `GET` musi przekazać informacje do serwera, informacje są przekazywane jako część adresu URL w ciągu zapytania.</span><span class="sxs-lookup"><span data-stu-id="adf26-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="adf26-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="adf26-146">`POST`.</span></span> <span data-ttu-id="adf26-147">Przeglądarka wysyła żądanie `POST` w celu przesłania danych do dodania lub zmiany na serwerze.</span><span class="sxs-lookup"><span data-stu-id="adf26-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="adf26-148">Na przykład zlecenie `POST` służy do tworzenia rekordów w bazie danych lub zmieniania istniejących.</span><span class="sxs-lookup"><span data-stu-id="adf26-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="adf26-149">W większości przypadków, gdy wypełnisz formularz i klikniesz przycisk Prześlij, przeglądarka wykonuje operację `POST`.</span><span class="sxs-lookup"><span data-stu-id="adf26-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="adf26-150">W operacji `POST` dane przesyłane do serwera są w treści strony.</span><span class="sxs-lookup"><span data-stu-id="adf26-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="adf26-151">Istotną różnicą między tymi zleceniami jest to, że operacja `GET` nie powinna być zmieniana na serwerze, czy też w nieco bardziej abstrakcyjny sposób operacja `GET` nie spowoduje zmiany stanu na serwerze.</span><span class="sxs-lookup"><span data-stu-id="adf26-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="adf26-152">Możesz wykonać `GET` operacji na tych samych zasobach tyle razy, ile chcesz, i te zasoby nie zmienią się.</span><span class="sxs-lookup"><span data-stu-id="adf26-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="adf26-153">(`GET` operacja jest często określana jako "bezpieczna" lub do korzystania z warunków technicznych jest *idempotentne*). Z kolei oczywiście żądanie `POST` zmienia coś na serwerze za każdym razem, gdy wykonujesz operację.</span><span class="sxs-lookup"><span data-stu-id="adf26-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="adf26-154">Dwa przykłady ułatwią zilustrowanie tego rozróżnienia.</span><span class="sxs-lookup"><span data-stu-id="adf26-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="adf26-155">Podczas wyszukiwania przy użyciu aparatu, takiego jak Bing lub Google, należy wypełnić formularz, który składa się z jednego pola tekstowego, a następnie kliknąć przycisk wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="adf26-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="adf26-156">Przeglądarka wykonuje `GET` operacji z wartością wprowadzoną w polu przekazaną w ramach adresu URL.</span><span class="sxs-lookup"><span data-stu-id="adf26-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="adf26-157">Używanie `GET` operacji dla tego typu formularza jest prawidłowy, ponieważ operacja wyszukiwania nie zmienia żadnych zasobów na serwerze, po prostu pobiera informacje.</span><span class="sxs-lookup"><span data-stu-id="adf26-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="adf26-158">Teraz Rozważmy proces porządkowania elementu w trybie online.</span><span class="sxs-lookup"><span data-stu-id="adf26-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="adf26-159">Wypełnij pola szczegóły zamówienia, a następnie kliknij przycisk Prześlij.</span><span class="sxs-lookup"><span data-stu-id="adf26-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="adf26-160">Ta operacja będzie `POST` żądanie, ponieważ operacja spowoduje zmiany na serwerze, takie jak nowy rekord zamówienia, zmiana informacji o koncie i prawdopodobnie wiele innych zmian.</span><span class="sxs-lookup"><span data-stu-id="adf26-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="adf26-161">W przeciwieństwie do operacji `GET` nie można powtórzyć żądania `POST` — w przypadku, gdy żądanie zostało wysłane ponownie, zostanie wygenerowane nowe zamówienie na serwerze.</span><span class="sxs-lookup"><span data-stu-id="adf26-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="adf26-162">(W takich przypadkach witryny sieci Web często ostrzegają, aby nie klikać przycisku przesyłania więcej niż jeden raz lub wyłączyli przycisk Prześlij, aby nie ponownie przesłać formularza przypadkowo).</span><span class="sxs-lookup"><span data-stu-id="adf26-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="adf26-163">W ramach tego samouczka użyjesz operacji `GET` i `POST` operacji do pracy z formularzami HTML.</span><span class="sxs-lookup"><span data-stu-id="adf26-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="adf26-164">W każdym przypadku wyjaśnimy, dlaczego używane zlecenie są odpowiednie.</span><span class="sxs-lookup"><span data-stu-id="adf26-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="adf26-165">(Aby dowiedzieć się więcej na temat czasowników HTTP, zobacz artykuł [definicje metod](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) w witrynie W3C).</span><span class="sxs-lookup"><span data-stu-id="adf26-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>

<span data-ttu-id="adf26-166">Większość elementów wejściowych użytkownika jest `<input>` elementów HTML.</span><span class="sxs-lookup"><span data-stu-id="adf26-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="adf26-167">Wyglądają tak jak `<input type="type" name="name">,` gdzie *Type* wskazuje rodzaj kontroli danych wejściowych użytkownika.</span><span class="sxs-lookup"><span data-stu-id="adf26-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="adf26-168">Te elementy są typowymi:</span><span class="sxs-lookup"><span data-stu-id="adf26-168">These elements are the common ones:</span></span>

- <span data-ttu-id="adf26-169">Pole tekstowe: `<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="adf26-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="adf26-170">Pole wyboru: `<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="adf26-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="adf26-171">Przycisk radiowy: `<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="adf26-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="adf26-172">Przycisk: `<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="adf26-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="adf26-173">Przycisk Prześlij: `<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="adf26-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="adf26-174">Można również użyć elementu `<textarea>` do utworzenia wielowierszowego pola tekstowego i elementu `<select>`, aby utworzyć listę rozwijaną lub przewijaną listę.</span><span class="sxs-lookup"><span data-stu-id="adf26-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="adf26-175">(Aby uzyskać więcej informacji na temat elementów formularza HTML, zobacz [formularze HTML i dane wejściowe](http://www.w3schools.com/html/html_forms.asp) w witrynie w3schools).</span><span class="sxs-lookup"><span data-stu-id="adf26-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="adf26-176">Atrybut `name` jest bardzo istotny, ponieważ jest to sposób, w jaki otrzymasz wartość elementu później, jak zobaczysz szybko.</span><span class="sxs-lookup"><span data-stu-id="adf26-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="adf26-177">Interesująca część to to, co ty, programista strony, na dane wejściowe użytkownika.</span><span class="sxs-lookup"><span data-stu-id="adf26-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="adf26-178">Nie ma wbudowanego zachowania skojarzonego z tymi elementami.</span><span class="sxs-lookup"><span data-stu-id="adf26-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="adf26-179">Zamiast tego należy pobrać wartości wprowadzone lub wybrane przez użytkownika i zrobić coś z nimi.</span><span class="sxs-lookup"><span data-stu-id="adf26-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="adf26-180">To już poznasz w tym samouczku.</span><span class="sxs-lookup"><span data-stu-id="adf26-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="adf26-181">**Formularze HTML5 i dane wejściowe**</span><span class="sxs-lookup"><span data-stu-id="adf26-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="adf26-182">Jak wiadomo, kod HTML jest w stanie przejścia, a Najnowsza wersja (HTML5) obejmuje obsługę bardziej intuicyjnych sposobów wprowadzania informacji przez użytkowników.</span><span class="sxs-lookup"><span data-stu-id="adf26-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="adf26-183">Na przykład w języku HTML5 użytkownik (deweloper strony) może powiedzieć stronie, że użytkownik ma wprowadzić datę.</span><span class="sxs-lookup"><span data-stu-id="adf26-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="adf26-184">Przeglądarka może następnie automatycznie wyświetlać kalendarz, a nie wymagać od użytkownika ręcznego wprowadzania daty.</span><span class="sxs-lookup"><span data-stu-id="adf26-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="adf26-185">Jednak program HTML5 jest nowy i nie jest jeszcze obsługiwany we wszystkich przeglądarkach.</span><span class="sxs-lookup"><span data-stu-id="adf26-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="adf26-186">Strony sieci Web ASP.NET obsługują dane wejściowe HTML5 w tym zakresie, w jakim działa przeglądarka użytkownika.</span><span class="sxs-lookup"><span data-stu-id="adf26-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="adf26-187">Aby uzyskać pomysł dotyczący nowych atrybutów elementu `<input>` w języku HTML5, zobacz [atrybut typu danych wejściowych&gt; HTML &lt;](http://www.w3schools.com/html/html_form_input_types.asp) w witrynie w3schools.</span><span class="sxs-lookup"><span data-stu-id="adf26-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>

## <a name="creating-the-form"></a><span data-ttu-id="adf26-188">Tworzenie formularza</span><span class="sxs-lookup"><span data-stu-id="adf26-188">Creating the Form</span></span>

<span data-ttu-id="adf26-189">W programie WebMatrix w obszarze roboczym **pliki** Otwórz stronę *filmy. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="adf26-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="adf26-190">Po tagu `</h1>` zamykającym i przed tagiem otwierającym `<div>` wywołania `grid.GetHtml` Dodaj następujący znacznik:</span><span class="sxs-lookup"><span data-stu-id="adf26-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="adf26-191">Ten znacznik tworzy formularz, który ma pole tekstowe o nazwie `searchGenre` i przycisk Prześlij.</span><span class="sxs-lookup"><span data-stu-id="adf26-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="adf26-192">Pole tekstowe i przycisk Prześlij są ujęte w `<form>` elementu, którego atrybut `method` jest ustawiony na `get`.</span><span class="sxs-lookup"><span data-stu-id="adf26-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="adf26-193">(Pamiętaj, że jeśli nie umieścisz pola tekstowego i przycisk Prześlij wewnątrz elementu `<form>`, nic nie zostanie przesłane po kliknięciu przycisku). Tutaj możesz użyć zlecenia `GET`, ponieważ tworzysz formularz, który nie wprowadza żadnych zmian na serwerze — tylko spowoduje to wyszukanie.</span><span class="sxs-lookup"><span data-stu-id="adf26-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="adf26-194">(W poprzednim samouczku użyto metody `post`, która umożliwia przesyłanie zmian na serwerze.</span><span class="sxs-lookup"><span data-stu-id="adf26-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="adf26-195">Zobaczysz to w następnym samouczku ponownie.</span><span class="sxs-lookup"><span data-stu-id="adf26-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="adf26-196">Uruchom stronę.</span><span class="sxs-lookup"><span data-stu-id="adf26-196">Run the page.</span></span> <span data-ttu-id="adf26-197">Mimo że nie zdefiniowano żadnego zachowania dla formularza, można zobaczyć, co wygląda następująco:</span><span class="sxs-lookup"><span data-stu-id="adf26-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Strona filmów z polem wyszukiwania dla gatunku](form-basics/_static/image3.png)

<span data-ttu-id="adf26-199">Wprowadź wartość w polu tekstowym, na przykład "komedia".</span><span class="sxs-lookup"><span data-stu-id="adf26-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="adf26-200">Następnie kliknij pozycję **kryterium wyszukiwania**.</span><span class="sxs-lookup"><span data-stu-id="adf26-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="adf26-201">Zwróć uwagę na adres URL strony.</span><span class="sxs-lookup"><span data-stu-id="adf26-201">Take note of the URL of the page.</span></span> <span data-ttu-id="adf26-202">Ponieważ atrybut `method` elementu `<form>` jest ustawiany na `get`, wprowadzona wartość jest teraz częścią ciągu zapytania w adresie URL, na przykład:</span><span class="sxs-lookup"><span data-stu-id="adf26-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="adf26-203">Odczytywanie wartości formularza</span><span class="sxs-lookup"><span data-stu-id="adf26-203">Reading Form Values</span></span>

<span data-ttu-id="adf26-204">Strona zawiera już kod, który pobiera dane bazy danych i wyświetla wyniki w postaci siatki.</span><span class="sxs-lookup"><span data-stu-id="adf26-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="adf26-205">Teraz musisz dodać kod, który odczytuje wartość pola tekstowego, aby można było uruchomić zapytanie SQL zawierające termin wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="adf26-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="adf26-206">Ponieważ ustawiamy metodę `get`, można odczytać wartość wprowadzoną w polu tekstowym przy użyciu kodu w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="adf26-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="adf26-207">Obiekt `Request.QueryString` (Właściwość `QueryString` obiektu `Request`) zawiera wartości elementów, które zostały przesłane jako część operacji `GET`.</span><span class="sxs-lookup"><span data-stu-id="adf26-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="adf26-208">Właściwość `Request.QueryString` zawiera *kolekcję* (listę) wartości, które są przesyłane w formularzu.</span><span class="sxs-lookup"><span data-stu-id="adf26-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="adf26-209">Aby uzyskać indywidualną wartość, należy określić nazwę żądanego elementu.</span><span class="sxs-lookup"><span data-stu-id="adf26-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="adf26-210">Dlatego należy mieć atrybut `name` w elemencie `<input>` (`searchTerm`), który tworzy pole tekstowe.</span><span class="sxs-lookup"><span data-stu-id="adf26-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="adf26-211">(Aby uzyskać więcej informacji na temat obiektu `Request`, zobacz [pasek boczny](#BKMK_TheRequestObject) później).</span><span class="sxs-lookup"><span data-stu-id="adf26-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="adf26-212">Jest to bardzo proste, aby odczytać wartość pola tekstowego.</span><span class="sxs-lookup"><span data-stu-id="adf26-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="adf26-213">Ale jeśli użytkownik nie wprowadził niczego w polu tekstowym, ale zamiast tego kliknął opcję **Wyszukaj** , możesz zignorować to kliknięcie, ponieważ nie ma niczego do wyszukania.</span><span class="sxs-lookup"><span data-stu-id="adf26-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="adf26-214">Poniższy kod jest przykładem, który pokazuje, jak zaimplementować te warunki.</span><span class="sxs-lookup"><span data-stu-id="adf26-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="adf26-215">(Nie musisz jeszcze dodawać tego kodu; możesz to zrobić w chwilę).</span><span class="sxs-lookup"><span data-stu-id="adf26-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="adf26-216">Test jest podzielony w dół w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="adf26-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="adf26-217">Pobierz wartość `Request.QueryString["searchGenre"]`, a mianowicie wartość wprowadzoną w elemencie `<input>` o nazwie `searchGenre`.</span><span class="sxs-lookup"><span data-stu-id="adf26-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="adf26-218">Sprawdź, czy jest on pusty przy użyciu metody `IsEmpty`.</span><span class="sxs-lookup"><span data-stu-id="adf26-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="adf26-219">Ta metoda jest standardowym sposobem określenia, czy element (na przykład element formularza) zawiera wartość.</span><span class="sxs-lookup"><span data-stu-id="adf26-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="adf26-220">Jednak naprawdę należy zadbać o to, aby *nie* były puste...</span><span class="sxs-lookup"><span data-stu-id="adf26-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="adf26-221">Dodaj operator `!` przed testem `IsEmpty`.</span><span class="sxs-lookup"><span data-stu-id="adf26-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="adf26-222">(Operator `!` oznacza wartość logiczną NOT).</span><span class="sxs-lookup"><span data-stu-id="adf26-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="adf26-223">W zwykłym języku angielskim cały warunek `if` jest tłumaczony na następujące: *Jeśli element searchGenre formularza nie jest pusty, a następnie...*</span><span class="sxs-lookup"><span data-stu-id="adf26-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="adf26-224">Ten blok ustawia etap tworzenia zapytania, które korzysta z wyszukiwanego terminu.</span><span class="sxs-lookup"><span data-stu-id="adf26-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="adf26-225">Należy to zrobić w następnej sekcji.</span><span class="sxs-lookup"><span data-stu-id="adf26-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="adf26-226">**Obiekt żądania**</span><span class="sxs-lookup"><span data-stu-id="adf26-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="adf26-227">Obiekt `Request` zawiera wszystkie informacje wysyłane przez przeglądarkę do aplikacji po zażądaniu lub przesłaniu strony.</span><span class="sxs-lookup"><span data-stu-id="adf26-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="adf26-228">Ten obiekt zawiera wszystkie informacje udostępniane przez użytkownika, takie jak wartości pól tekstowych lub pliki do przekazania.</span><span class="sxs-lookup"><span data-stu-id="adf26-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="adf26-229">Obejmuje ona również wszystkie informacje dodatkowe, takie jak pliki cookie, wartości w ciągu zapytania adresu URL (jeśli istnieją), ścieżka pliku strony, która jest uruchomiona, typ przeglądarki używanej przez użytkownika, listę języków ustawionych w przeglądarce i wiele innych.</span><span class="sxs-lookup"><span data-stu-id="adf26-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="adf26-230">Obiekt `Request` jest *kolekcją* (listą) wartości.</span><span class="sxs-lookup"><span data-stu-id="adf26-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="adf26-231">Otrzymasz pojedynczą wartość z kolekcji, określając jej nazwę:</span><span class="sxs-lookup"><span data-stu-id="adf26-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="adf26-232">Obiekt `Request` rzeczywiście uwidacznia kilka podzestawów.</span><span class="sxs-lookup"><span data-stu-id="adf26-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="adf26-233">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="adf26-233">For example:</span></span>
> 
> - <span data-ttu-id="adf26-234">`Request.Form` daje wartości z elementów wewnątrz przesłanego `<form>` elementu, jeśli żądanie jest żądaniem `POST`.</span><span class="sxs-lookup"><span data-stu-id="adf26-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="adf26-235">`Request.QueryString` daje tylko wartości w ciągu zapytania adresu URL.</span><span class="sxs-lookup"><span data-stu-id="adf26-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="adf26-236">(W adresie URL, na przykład `http://mysite/myapp/page?searchGenre=action&page=2`, sekcja `?searchGenre=action&page=2` adresu URL jest ciągiem zapytania).</span><span class="sxs-lookup"><span data-stu-id="adf26-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="adf26-237">Kolekcja `Request.Cookies` zapewnia dostęp do plików cookie wysłanych przez przeglądarkę.</span><span class="sxs-lookup"><span data-stu-id="adf26-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="adf26-238">Aby uzyskać wartość, którą znasz, możesz użyć `Request["name"]`.</span><span class="sxs-lookup"><span data-stu-id="adf26-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="adf26-239">Alternatywnie można użyć bardziej konkretnych wersji `Request.Form["name"]` (dla żądań `POST`) lub `Request.QueryString["name"]` (dla żądań `GET`).</span><span class="sxs-lookup"><span data-stu-id="adf26-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="adf26-240">Oczywiście *Nazwa* to nazwa elementu, który ma zostać pobrany.</span><span class="sxs-lookup"><span data-stu-id="adf26-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="adf26-241">Nazwa elementu, który ma zostać pobrany, musi być unikatowa w obrębie używanej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="adf26-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="adf26-242">Dlatego, że obiekt `Request` dostarcza podzestawy, takich jak `Request.Form` i `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="adf26-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="adf26-243">Załóżmy, że strona zawiera element formularza o nazwie `userName` i zawiera *również* plik cookie o nazwie `userName`.</span><span class="sxs-lookup"><span data-stu-id="adf26-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="adf26-244">Jeśli uzyskasz `Request["userName"]`, jest to niejednoznaczne, czy chcesz, aby wartość formularza lub plik cookie.</span><span class="sxs-lookup"><span data-stu-id="adf26-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="adf26-245">Jeśli jednak uzyskasz `Request.Form["userName"]` lub `Request.Cookie["userName"]`, masz wyraźne informacje o tym, którą wartość uzyskać.</span><span class="sxs-lookup"><span data-stu-id="adf26-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="adf26-246">Dobrym sposobem jest zastosowanie i użycie podzbioru interesujących Cię `Request`, takich jak `Request.Form` lub `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="adf26-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="adf26-247">W przypadku prostych stron, które tworzysz w tym samouczku, prawdopodobnie nie spowoduje to w praktyce żadnej różnicy.</span><span class="sxs-lookup"><span data-stu-id="adf26-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="adf26-248">Jednak podczas tworzenia bardziej skomplikowanych stron przy użyciu jawnej wersji `Request.Form` lub `Request.QueryString` mogą pomóc uniknąć problemów, które mogą wystąpić, gdy strona zawiera formularz (lub wiele formularzy), pliki cookie, wartości ciągów zapytań itd.</span><span class="sxs-lookup"><span data-stu-id="adf26-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>

## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="adf26-249">Tworzenie zapytania przy użyciu terminu wyszukiwania</span><span class="sxs-lookup"><span data-stu-id="adf26-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="adf26-250">Teraz, gdy wiesz, jak uzyskać termin wyszukiwania wprowadzony przez użytkownika, możesz utworzyć zapytanie, które go używa.</span><span class="sxs-lookup"><span data-stu-id="adf26-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="adf26-251">Należy pamiętać, że aby pobrać wszystkie elementy filmu z bazy danych, używasz zapytania SQL, które wygląda następująco:</span><span class="sxs-lookup"><span data-stu-id="adf26-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="adf26-252">Aby uzyskać tylko niektóre filmy, należy użyć zapytania zawierającego klauzulę `Where`.</span><span class="sxs-lookup"><span data-stu-id="adf26-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="adf26-253">Ta klauzula umożliwia ustawienie warunku, w którym wiersze są zwracane przez zapytanie.</span><span class="sxs-lookup"><span data-stu-id="adf26-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="adf26-254">Oto przykład:</span><span class="sxs-lookup"><span data-stu-id="adf26-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="adf26-255">Format podstawowy jest `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="adf26-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="adf26-256">Możesz użyć różnych operatorów oprócz tylko `=`, takich jak `>` (większe niż), `<` (mniejsze niż), `<>` (nie równe), `<=` (mniejsze niż lub równe) itp., w zależności od tego, czego szukasz.</span><span class="sxs-lookup"><span data-stu-id="adf26-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="adf26-257">W przypadku, gdy zastanawiasz się, w instrukcji SQL nie jest rozróżniana wielkość liter &mdash; `SELECT` jest taka sama jak `Select` (lub nawet `select`).</span><span class="sxs-lookup"><span data-stu-id="adf26-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="adf26-258">Jednak osoby często używają słów kluczowych w instrukcji SQL, takich jak `SELECT` i `WHERE`, aby ułatwić ich odczytywanie.</span><span class="sxs-lookup"><span data-stu-id="adf26-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="adf26-259">Przekazywanie terminu wyszukiwania jako parametru</span><span class="sxs-lookup"><span data-stu-id="adf26-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="adf26-260">Wyszukiwanie określonego gatunku jest wystarczająco proste (`WHERE Genre = 'Action'`), ale chcesz mieć możliwość wyszukiwania dowolnego gatunku wprowadzonego przez użytkownika.</span><span class="sxs-lookup"><span data-stu-id="adf26-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="adf26-261">W tym celu utworzysz jako kwerendę SQL, która zawiera symbol zastępczy dla wartości do wyszukania.</span><span class="sxs-lookup"><span data-stu-id="adf26-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="adf26-262">To polecenie będzie wyglądać następująco:</span><span class="sxs-lookup"><span data-stu-id="adf26-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="adf26-263">Symbol zastępczy jest znakiem `@`, po którym następuje zero.</span><span class="sxs-lookup"><span data-stu-id="adf26-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="adf26-264">Jak można odgadnąć, zapytanie może zawierać wiele symboli zastępczych i mieć nazwę `@0`, `@1`, `@2`itd.</span><span class="sxs-lookup"><span data-stu-id="adf26-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="adf26-265">Aby skonfigurować zapytanie i faktycznie przekazać je do wartości, należy użyć następującego kodu:</span><span class="sxs-lookup"><span data-stu-id="adf26-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="adf26-266">Ten kod jest podobny do tego, co zostało już zrobione w celu wyświetlenia danych w siatce.</span><span class="sxs-lookup"><span data-stu-id="adf26-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="adf26-267">Jedyne różnice:</span><span class="sxs-lookup"><span data-stu-id="adf26-267">The only differences are:</span></span>

- <span data-ttu-id="adf26-268">Zapytanie zawiera symbol zastępczy (`WHERE Genre = @0"`).</span><span class="sxs-lookup"><span data-stu-id="adf26-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="adf26-269">Zapytanie jest umieszczane w zmiennej (`selectCommand`); wcześniej zapytanie zostało przesłane bezpośrednio do metody `db.Query`.</span><span class="sxs-lookup"><span data-stu-id="adf26-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="adf26-270">Po wywołaniu metody `db.Query` należy przekazać zarówno zapytanie, jak i wartość do użycia dla symbolu zastępczego.</span><span class="sxs-lookup"><span data-stu-id="adf26-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="adf26-271">(Jeśli zapytanie miało wiele symboli zastępczych, należy przekazać je wszystkie jako oddzielne wartości do metody).</span><span class="sxs-lookup"><span data-stu-id="adf26-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="adf26-272">Jeśli umieścisz wszystkie te elementy razem, uzyskasz następujący kod:</span><span class="sxs-lookup"><span data-stu-id="adf26-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="adf26-273">**Ważne!**</span><span class="sxs-lookup"><span data-stu-id="adf26-273">**Important!**</span></span> <span data-ttu-id="adf26-274">Używanie symboli zastępczych (takich jak `@0`) do przekazywania wartości do polecenia SQL jest *niezwykle ważne* dla bezpieczeństwa.</span><span class="sxs-lookup"><span data-stu-id="adf26-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="adf26-275">Sposób, w jaki zobaczysz, przy użyciu symboli zastępczych dla danych zmiennych, jest jedynym sposobem konstruowania poleceń SQL.</span><span class="sxs-lookup"><span data-stu-id="adf26-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="adf26-276">Nie należy tworzyć instrukcji języka SQL przez umieszczenie razem (łączenie) tekstu literału i wartości pobieranych od użytkownika.</span><span class="sxs-lookup"><span data-stu-id="adf26-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="adf26-277">Połączenie danych wprowadzonych przez użytkownika do instrukcji SQL powoduje otwarcie witryny programu w celu uzyskania ataku polegającego na *iniekcji kodu SQL* , gdy złośliwy użytkownik przesyła wartości do strony, która nadaje się do złamania bazy danych.</span><span class="sxs-lookup"><span data-stu-id="adf26-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="adf26-278">(Więcej informacji można znaleźć w artykule [iniekcja kodu SQL](https://msdn.microsoft.com/library/ms161953.aspx) w witrynie MSDN).</span><span class="sxs-lookup"><span data-stu-id="adf26-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>

## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="adf26-279">Aktualizowanie strony filmów przy użyciu kodu wyszukiwania</span><span class="sxs-lookup"><span data-stu-id="adf26-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="adf26-280">Teraz można zaktualizować kod w pliku *Films. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="adf26-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="adf26-281">Aby rozpocząć, Zastąp kod w bloku kodu w górnej części strony tym kodem:</span><span class="sxs-lookup"><span data-stu-id="adf26-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="adf26-282">Różnica polega na tym, że kwerenda została umieszczona w zmiennej `selectCommand`, co zostanie przekazane do `db.Query` później.</span><span class="sxs-lookup"><span data-stu-id="adf26-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="adf26-283">Umieszczenie instrukcji SQL w zmiennej umożliwia zmianę instrukcji, co jest wykonywane w celu przeprowadzenia wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="adf26-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="adf26-284">Zostały również usunięte te dwa wiersze, które zostaną umieszczone później:</span><span class="sxs-lookup"><span data-stu-id="adf26-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="adf26-285">Nie chcesz jeszcze uruchamiać zapytania (oznacza to, że Wywołaj `db.Query`) i nie chcesz już inicjować pomocnika `WebGrid`.</span><span class="sxs-lookup"><span data-stu-id="adf26-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="adf26-286">Te czynności należy wykonać po rozpoczęciu wykonywania instrukcji języka SQL.</span><span class="sxs-lookup"><span data-stu-id="adf26-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="adf26-287">Po tym zapisaniu można dodać nową logikę do obsługi wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="adf26-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="adf26-288">Ukończony kod będzie wyglądać podobnie do poniższego.</span><span class="sxs-lookup"><span data-stu-id="adf26-288">The completed code will look like the following.</span></span> <span data-ttu-id="adf26-289">Zaktualizuj kod na stronie, aby pasował do tego przykładu:</span><span class="sxs-lookup"><span data-stu-id="adf26-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="adf26-290">Ta strona działa teraz tak.</span><span class="sxs-lookup"><span data-stu-id="adf26-290">The page now works like this.</span></span> <span data-ttu-id="adf26-291">Za każdym razem, gdy strona jest uruchamiana, kod otwiera bazę danych, a zmienna `selectCommand` jest ustawiona na instrukcję SQL, która pobiera wszystkie rekordy z tabeli `Movies`.</span><span class="sxs-lookup"><span data-stu-id="adf26-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="adf26-292">Kod inicjuje również zmienną `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="adf26-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="adf26-293">Jeśli jednak bieżące żądanie zawiera wartość dla elementu `searchGenre`, kod ustawia `selectCommand` na inne zapytanie — a mianowicie, na taki, który zawiera klauzulę `Where`, aby wyszukać gatunek.</span><span class="sxs-lookup"><span data-stu-id="adf26-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="adf26-294">Ustawia również `searchTerm` do dowolnego elementu, który został przekazano do pola wyszukiwania (co może nie mieć wartości Nothing).</span><span class="sxs-lookup"><span data-stu-id="adf26-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="adf26-295">Bez względu na to, która instrukcja SQL znajduje się w `selectCommand`, kod następnie wywołuje `db.Query`, aby uruchomić zapytanie, przekazując je w `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="adf26-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="adf26-296">Jeśli nie ma nic w `searchTerm`, nie ma znaczenia, ponieważ w takim przypadku nie istnieje parametr do przekazania wartości do `selectCommand` mimo to.</span><span class="sxs-lookup"><span data-stu-id="adf26-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="adf26-297">Na koniec kod inicjuje pomocnika `WebGrid` przy użyciu wyników zapytania, podobnie jak przed.</span><span class="sxs-lookup"><span data-stu-id="adf26-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="adf26-298">Możesz zobaczyć, że przez umieszczenie instrukcji SQL i wyszukiwanego terminu w zmiennych, dodaliśmy elastyczność do kodu.</span><span class="sxs-lookup"><span data-stu-id="adf26-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="adf26-299">Jak widać w dalszej części tego samouczka, możesz użyć tej podstawowej platformy i kontynuować dodawanie logiki dla różnych typów wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="adf26-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="adf26-300">Testowanie funkcji wyszukiwania przez gatunek</span><span class="sxs-lookup"><span data-stu-id="adf26-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="adf26-301">W programie WebMatrix Uruchom stronę *filmy. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="adf26-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="adf26-302">Zostanie wyświetlona strona z polem tekstowym dla gatunku.</span><span class="sxs-lookup"><span data-stu-id="adf26-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="adf26-303">Wprowadź gatunek, który został wprowadzony dla jednego z rekordów testu, a następnie kliknij przycisk **Wyszukaj**.</span><span class="sxs-lookup"><span data-stu-id="adf26-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="adf26-304">Tym razem zobaczysz listę tylko tych filmów, które pasują do tego gatunku:</span><span class="sxs-lookup"><span data-stu-id="adf26-304">This time you see a listing of just the movies that match that genre:</span></span>

![Strona filmów po wyszukiwaniu gatunku "Comedies"](form-basics/_static/image4.png)

<span data-ttu-id="adf26-306">Wprowadź inny gatunek i wyszukaj go ponownie.</span><span class="sxs-lookup"><span data-stu-id="adf26-306">Enter a different genre and search again.</span></span> <span data-ttu-id="adf26-307">Spróbuj wprowadzić gatunek, używając wielkich liter lub wersalików, aby zobaczyć, że wyszukiwanie nie uwzględnia wielkości liter.</span><span class="sxs-lookup"><span data-stu-id="adf26-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="adf26-308">"Zapamiętywanie" zawartości wprowadzonej przez użytkownika</span><span class="sxs-lookup"><span data-stu-id="adf26-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="adf26-309">Być może zauważono, że po wprowadzeniu gatunku i kliknięciu **gatunku wyszukiwania**zobaczysz listę dla tego gatunku.</span><span class="sxs-lookup"><span data-stu-id="adf26-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="adf26-310">Jednak pole tekstowe wyszukiwania było puste &mdash; innymi słowy, nie pamiętasz, co zostało wprowadzone.</span><span class="sxs-lookup"><span data-stu-id="adf26-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="adf26-311">Ważne jest, aby zrozumieć, dlaczego takie zachowanie występuje.</span><span class="sxs-lookup"><span data-stu-id="adf26-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="adf26-312">Po przesłaniu strony przeglądarka wysyła żądanie do serwera sieci Web.</span><span class="sxs-lookup"><span data-stu-id="adf26-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="adf26-313">Gdy ASP.NET pobiera żądanie, tworzy nowe wystąpienie strony, uruchamia w nim kod, a następnie ponownie renderuje stronę do przeglądarki.</span><span class="sxs-lookup"><span data-stu-id="adf26-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="adf26-314">W efekcie Strona nie wie, że pracujesz tylko z poprzednią wersją.</span><span class="sxs-lookup"><span data-stu-id="adf26-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="adf26-315">Wszystkie informacje wiedzą, że otrzymały żądanie, które zawierało pewne dane formularza.</span><span class="sxs-lookup"><span data-stu-id="adf26-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="adf26-316">Za każdym razem, gdy zażądasz strony &mdash;, czy po raz pierwszy, czy przez przesłanie &mdash; otrzymujesz nową stronę.</span><span class="sxs-lookup"><span data-stu-id="adf26-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="adf26-317">Serwer sieci Web nie ma pamięci ostatniego żądania.</span><span class="sxs-lookup"><span data-stu-id="adf26-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="adf26-318">Ani nie jest ASP.NET, ani nie obsługuje przeglądarki.</span><span class="sxs-lookup"><span data-stu-id="adf26-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="adf26-319">Jedyne połączenie między tymi osobnymi wystąpieniami strony to wszystkie dane przesyłane między nimi.</span><span class="sxs-lookup"><span data-stu-id="adf26-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="adf26-320">Jeśli zostanie przesłana Strona, na przykład nowe wystąpienie strony może pobrać dane formularza, które zostały wysłane przez poprzednie wystąpienie.</span><span class="sxs-lookup"><span data-stu-id="adf26-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="adf26-321">(Inny sposób przekazywania danych między stronami polega na użyciu plików cookie).</span><span class="sxs-lookup"><span data-stu-id="adf26-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="adf26-322">Formalna Metoda opisywania tej sytuacji polega na tym, że strony sieci Web są *bezstanowe*.</span><span class="sxs-lookup"><span data-stu-id="adf26-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="adf26-323">Serwery sieci Web i same strony i elementy na stronie nie zachowują żadnych informacji o poprzednim stanie strony.</span><span class="sxs-lookup"><span data-stu-id="adf26-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="adf26-324">Sieć Web została zaprojektowana w ten sposób, ponieważ utrzymywanie stanu poszczególnych żądań szybko wyczerpuje zasoby serwerów sieci Web, które często obsługują tysiące, nawet setki tysięcy z żądań na sekundę.</span><span class="sxs-lookup"><span data-stu-id="adf26-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="adf26-325">Dlatego też pole tekstowe było puste.</span><span class="sxs-lookup"><span data-stu-id="adf26-325">So that's why the text box was empty.</span></span> <span data-ttu-id="adf26-326">Po przesłaniu strony ASP.NET utworzone nowe wystąpienie strony i uruchomiono je za pomocą kodu i znaczników.</span><span class="sxs-lookup"><span data-stu-id="adf26-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="adf26-327">W tym kodzie nie wprowadzono żadnych informacji, które poinformowały ASP.NET o wprowadzeniu wartości do pola tekstowego.</span><span class="sxs-lookup"><span data-stu-id="adf26-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="adf26-328">Tak więc ASP.NET nie zrobił niczego i pole tekstowe zostało renderowane bez wartości.</span><span class="sxs-lookup"><span data-stu-id="adf26-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="adf26-329">Istnieje już prosty sposób na obejście tego problemu.</span><span class="sxs-lookup"><span data-stu-id="adf26-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="adf26-330">Gatunek wprowadzony w polu tekstowym *jest* dostępny w kodzie, &mdash; jest `Request.QueryString["searchGenre"]`.</span><span class="sxs-lookup"><span data-stu-id="adf26-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="adf26-331">Zaktualizuj znaczniki dla pola tekstowego, aby atrybut `value` pobiera swoją wartość z `searchTerm`, jak w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="adf26-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="adf26-332">Na tej stronie można również ustawić atrybut `value` na zmienną `searchTerm`, ponieważ ta zmienna również zawiera wprowadzony gatunek.</span><span class="sxs-lookup"><span data-stu-id="adf26-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="adf26-333">Jednak użycie obiektu `Request` do ustawienia atrybutu `value`, jak pokazano poniżej, jest standardowym sposobem wykonania tego zadania.</span><span class="sxs-lookup"><span data-stu-id="adf26-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="adf26-334">(Przy założeniu, że nawet chcesz zrobić to &mdash; w niektórych sytuacjach, możesz chcieć renderować stronę *bez* wartości w polach.</span><span class="sxs-lookup"><span data-stu-id="adf26-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="adf26-335">Wszystko to zależy od tego, co się dzieje z Twoją aplikacją.</span><span class="sxs-lookup"><span data-stu-id="adf26-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="adf26-336">Nie możesz "zapamiętać" wartości pola tekstowego, które jest używane na potrzeby haseł.</span><span class="sxs-lookup"><span data-stu-id="adf26-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="adf26-337">Może to być Luka w zabezpieczeniach umożliwiająca użytkownikom Wypełnienie pola hasła przy użyciu kodu.</span><span class="sxs-lookup"><span data-stu-id="adf26-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>

<span data-ttu-id="adf26-338">Uruchom ponownie stronę, wprowadź gatunek i kliknij pozycję **Szukaj gatunku**.</span><span class="sxs-lookup"><span data-stu-id="adf26-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="adf26-339">Tym razem nie tylko zobaczysz wyniki wyszukiwania, ale pole tekstowe zapamiętuje dane wprowadzone w ostatnim momencie:</span><span class="sxs-lookup"><span data-stu-id="adf26-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Strona wskazująca, że w polu tekstowym zapamiętano Poprzedni wpis](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="adf26-341">Wyszukiwanie dowolnego wyrazu w tytule</span><span class="sxs-lookup"><span data-stu-id="adf26-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="adf26-342">Możesz teraz wyszukać dowolny gatunek, ale możesz również wyszukać tytuł.</span><span class="sxs-lookup"><span data-stu-id="adf26-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="adf26-343">Przed rozpoczęciem wyszukiwania trudno uzyskać tytuł, dlatego możesz wyszukiwać słowo, które pojawia się w dowolnym miejscu w tytule.</span><span class="sxs-lookup"><span data-stu-id="adf26-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="adf26-344">Aby to zrobić w programie SQL, należy użyć operatora `LIKE` i składni podobnej do następujących:</span><span class="sxs-lookup"><span data-stu-id="adf26-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="adf26-345">To polecenie pobiera wszystkie filmy, których tytuły zawierają "Adventure".</span><span class="sxs-lookup"><span data-stu-id="adf26-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="adf26-346">Korzystając z operatora `LIKE`, należy uwzględnić symbol wieloznaczny `%` w ramach wyszukiwanego terminu.</span><span class="sxs-lookup"><span data-stu-id="adf26-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="adf26-347">`LIKE 'adventure%'` wyszukiwania to "zaczynające się od" Adventure "".</span><span class="sxs-lookup"><span data-stu-id="adf26-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="adf26-348">(Technicznie oznacza "ciąg" Adventure ", po którym następuje wszystko") ". Podobnie termin wyszukiwania `LIKE '%adventure'` oznacza "coś, a po nim ciąg" Adventure "", który jest innym sposobem na "zakończenie" Adventure "".</span><span class="sxs-lookup"><span data-stu-id="adf26-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="adf26-349">Termin wyszukiwania `LIKE '%adventure%'` oznacza "" "z" Adventure "w dowolnym miejscu w tytule".</span><span class="sxs-lookup"><span data-stu-id="adf26-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="adf26-350">(Technicznie, "wszystkie elementy w tytule, po którym następuje" Adventure ", po którym następuje" ").</span><span class="sxs-lookup"><span data-stu-id="adf26-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="adf26-351">Wewnątrz elementu `<form>` Dodaj następujące znaczniki bezpośrednio pod tagiem zamykającym `</div>` dla wyszukiwania gatunku (tuż przed zamykającym elementem `</form>`):</span><span class="sxs-lookup"><span data-stu-id="adf26-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="adf26-352">Kod do obsługi tego wyszukiwania jest podobny do kodu dla wyszukiwania gatunku, z tą różnicą, że musisz złożyć wyszukiwanie `LIKE`.</span><span class="sxs-lookup"><span data-stu-id="adf26-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="adf26-353">W bloku kodu w górnej części strony Dodaj następujący blok `if` tuż po bloku `if` dla wyszukiwania gatunku:</span><span class="sxs-lookup"><span data-stu-id="adf26-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="adf26-354">Ten kod używa tej samej logiki, która była wcześniej, z tą różnicą, że wyszukiwanie używa operatora `LIKE`, a kod umieszcza "`%`" przed upływem terminu i po nim.</span><span class="sxs-lookup"><span data-stu-id="adf26-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="adf26-355">Zwróć uwagę, jak łatwo dodać kolejne wyszukiwanie do strony.</span><span class="sxs-lookup"><span data-stu-id="adf26-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="adf26-356">Wszystkie musiały zostać:</span><span class="sxs-lookup"><span data-stu-id="adf26-356">All you had to do was:</span></span>

- <span data-ttu-id="adf26-357">Utwórz blok `if` przetestowany w celu sprawdzenia, czy odpowiednie pole wyszukiwania ma wartość.</span><span class="sxs-lookup"><span data-stu-id="adf26-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="adf26-358">Ustaw zmienną `selectCommand` na nową instrukcję języka SQL.</span><span class="sxs-lookup"><span data-stu-id="adf26-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="adf26-359">Ustaw zmienną `searchTerm` na wartość, która ma zostać przekazana do zapytania.</span><span class="sxs-lookup"><span data-stu-id="adf26-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="adf26-360">Oto kompletny blok kodu, który zawiera nową logikę wyszukiwania tytułu:</span><span class="sxs-lookup"><span data-stu-id="adf26-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="adf26-361">Oto podsumowanie informacji o tym kodzie:</span><span class="sxs-lookup"><span data-stu-id="adf26-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="adf26-362">Zmienne `searchTerm` i `selectCommand` są inicjowane u góry.</span><span class="sxs-lookup"><span data-stu-id="adf26-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="adf26-363">Te zmienne są ustawiane na odpowiedni termin wyszukiwania (jeśli istnieje) i odpowiednie polecenie SQL na podstawie tego, co użytkownik wykonuje na stronie.</span><span class="sxs-lookup"><span data-stu-id="adf26-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="adf26-364">Domyślne wyszukiwanie to prosty przypadek uzyskiwania wszystkich filmów z bazy danych.</span><span class="sxs-lookup"><span data-stu-id="adf26-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="adf26-365">W testach dla `searchGenre` i `searchTitle`kod ustawia `searchTerm` na wartość, którą chcesz wyszukać.</span><span class="sxs-lookup"><span data-stu-id="adf26-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="adf26-366">Te bloki kodu są również ustawiane `selectCommand` na odpowiednie polecenie SQL dla tego wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="adf26-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="adf26-367">Metoda `db.Query` jest wywoływana tylko raz, przy użyciu dowolnego polecenia SQL znajduje się w `selectedCommand` i niezależnie od wartości w `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="adf26-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="adf26-368">Jeśli nie ma wyszukiwanego terminu (bez gatunku i bez tytułu), wartość `searchTerm` jest pustym ciągiem.</span><span class="sxs-lookup"><span data-stu-id="adf26-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="adf26-369">Jednak nie ma znaczenia, ponieważ w takim przypadku zapytanie nie wymaga parametru.</span><span class="sxs-lookup"><span data-stu-id="adf26-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="adf26-370">Testowanie funkcji wyszukiwania tytułów</span><span class="sxs-lookup"><span data-stu-id="adf26-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="adf26-371">Teraz możesz przetestować ukończoną stronę wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="adf26-371">Now you can test your completed search page.</span></span> <span data-ttu-id="adf26-372">Uruchom *filmy. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="adf26-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="adf26-373">Wprowadź gatunek i kliknij pozycję **kryterium wyszukiwania**.</span><span class="sxs-lookup"><span data-stu-id="adf26-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="adf26-374">Siatka wyświetla filmy tego gatunku, takie jak przed.</span><span class="sxs-lookup"><span data-stu-id="adf26-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="adf26-375">Wprowadź wyraz tytułu i kliknij przycisk **Wyszukaj tytuł**.</span><span class="sxs-lookup"><span data-stu-id="adf26-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="adf26-376">Siatka wyświetla filmy, które mają ten wyraz w tytule.</span><span class="sxs-lookup"><span data-stu-id="adf26-376">The grid displays movies that have that word in the title.</span></span>

![Strona filmów po wyszukiwaniu wyrazu "The" w tytule](form-basics/_static/image6.png)

<span data-ttu-id="adf26-378">Pozostaw puste pola tekstowe i kliknij jeden z przycisków.</span><span class="sxs-lookup"><span data-stu-id="adf26-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="adf26-379">Siatka wyświetla wszystkie filmy.</span><span class="sxs-lookup"><span data-stu-id="adf26-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="adf26-380">Łączenie zapytań</span><span class="sxs-lookup"><span data-stu-id="adf26-380">Combining the Queries</span></span>

<span data-ttu-id="adf26-381">Możesz zauważyć, że wyszukiwania, które można wykonywać, są wyłącznie na wyłączność.</span><span class="sxs-lookup"><span data-stu-id="adf26-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="adf26-382">Nie można wyszukać tytułu i gatunku w tym samym czasie, nawet jeśli oba pola wyszukiwania zawierają wartości.</span><span class="sxs-lookup"><span data-stu-id="adf26-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="adf26-383">Na przykład nie można wyszukać wszystkich filmów akcji, których tytuł zawiera ciąg "Adventure".</span><span class="sxs-lookup"><span data-stu-id="adf26-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="adf26-384">(Jako że strona jest teraz zakodowana, jeśli wprowadzisz wartości dla obu gatunków i tytułów, wyszukiwanie tytułu będzie miało pierwszeństwo). Aby utworzyć wyszukiwanie, które łączy warunki, należy utworzyć zapytanie SQL, które ma składnię podobną do następującej:</span><span class="sxs-lookup"><span data-stu-id="adf26-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="adf26-385">I musisz uruchomić zapytanie przy użyciu instrukcji podobnej do następującej (w przybliżeniu mówiąc):</span><span class="sxs-lookup"><span data-stu-id="adf26-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="adf26-386">Tworzenie logiki zezwalającej na wiele permutacji kryteriów wyszukiwania może uzyskać nieco, jak widać.</span><span class="sxs-lookup"><span data-stu-id="adf26-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="adf26-387">W związku z tym zatrzymamy się tutaj.</span><span class="sxs-lookup"><span data-stu-id="adf26-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="adf26-388">Przyszłe przejście</span><span class="sxs-lookup"><span data-stu-id="adf26-388">Coming Up Next</span></span>

<span data-ttu-id="adf26-389">W następnym samouczku utworzysz stronę korzystającą z formularza, aby umożliwić użytkownikom dodawanie filmów do bazy danych.</span><span class="sxs-lookup"><span data-stu-id="adf26-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="adf26-390">Ukończ listę dla strony filmu (Zaktualizowano przy użyciu wyszukiwania)</span><span class="sxs-lookup"><span data-stu-id="adf26-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="adf26-391">Dodatkowe materiały</span><span class="sxs-lookup"><span data-stu-id="adf26-391">Additional Resources</span></span>

- [<span data-ttu-id="adf26-392">Wprowadzenie do programowania w ASP.NET sieci Web przy użyciu składni Razor</span><span class="sxs-lookup"><span data-stu-id="adf26-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="adf26-393">[Klauzula WHERE języka SQL](http://www.w3schools.com/sql/sql_where.asp) w witrynie w3schools</span><span class="sxs-lookup"><span data-stu-id="adf26-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="adf26-394">Artykuł [definicji metod](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) w witrynie W3C</span><span class="sxs-lookup"><span data-stu-id="adf26-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="adf26-395">[Poprzednie](displaying-data.md)
> [dalej](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="adf26-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
