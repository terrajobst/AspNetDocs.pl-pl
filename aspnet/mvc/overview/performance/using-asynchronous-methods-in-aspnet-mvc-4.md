---
uid: mvc/overview/performance/using-asynchronous-methods-in-aspnet-mvc-4
title: Używanie metod asynchronicznych we wzorcu ASP.NET MVC 4 | Dokumentacja firmy Microsoft
author: Rick-Anderson
description: Ta seria samouczków obejmuje podstawy tworzenia asynchronicznych aplikacji sieci Web platformy ASP.NET MVC przy użyciu programu Visual Studio Express 2012 for Web, która jest bezpłatna ve...
ms.author: riande
ms.date: 06/06/2012
ms.assetid: a56572ba-81c3-47af-826d-941e9c4775ec
msc.legacyurl: /mvc/overview/performance/using-asynchronous-methods-in-aspnet-mvc-4
msc.type: authoredcontent
ms.openlocfilehash: 8292fd43ffa2bc66b4daa8f0fc09569226d90bff
ms.sourcegitcommit: 0f1119340e4464720cfd16d0ff15764746ea1fea
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/17/2019
ms.locfileid: "59379563"
---
# <a name="using-asynchronous-methods-in-aspnet-mvc-4"></a><span data-ttu-id="a8182-103">Korzystanie z metod asynchronicznych we wzorcu ASP.NET MVC 4</span><span class="sxs-lookup"><span data-stu-id="a8182-103">Using Asynchronous Methods in ASP.NET MVC 4</span></span>

<span data-ttu-id="a8182-104">Przez [Rick Anderson]((https://twitter.com/RickAndMSFT))</span><span class="sxs-lookup"><span data-stu-id="a8182-104">by [Rick Anderson]((https://twitter.com/RickAndMSFT))</span></span>

> <span data-ttu-id="a8182-105">Ta seria samouczków obejmuje podstawy tworzenia asynchronicznych sieci Web platformy ASP.NET MVC aplikacji za pomocą [Visual Studio Express 2012 for Web](https://www.microsoft.com/visualstudio/11), która jest bezpłatna wersja programu Microsoft Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="a8182-105">This tutorial will teach you the basics of building an asynchronous ASP.NET MVC Web application using [Visual Studio Express 2012 for Web](https://www.microsoft.com/visualstudio/11), which is a free version of Microsoft Visual Studio.</span></span> <span data-ttu-id="a8182-106">Można również użyć [programu Visual Studio 2012](https://www.microsoft.com/visualstudio/11).</span><span class="sxs-lookup"><span data-stu-id="a8182-106">You can also use [Visual Studio 2012](https://www.microsoft.com/visualstudio/11).</span></span>
> 
> <span data-ttu-id="a8182-107">Pełny przykład znajduje się w tym samouczku w witrynie github [https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET/)</span><span class="sxs-lookup"><span data-stu-id="a8182-107">A complete sample is provided for this tutorial on github [https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET/)</span></span>


<span data-ttu-id="a8182-108">ASP.NET MVC 4 [kontrolera](https://msdn.microsoft.com/library/system.web.mvc.controller(VS.108).aspx) klasy w połączeniu [.NET 4.5](https://msdn.microsoft.com/library/w0x726c2(VS.110).aspx) umożliwia pisanie metod asynchronicznych akcji, które zwracają obiekt typu [zadań&lt;ActionResult&gt; ](https://msdn.microsoft.com/library/dd321424(VS.110).aspx).</span><span class="sxs-lookup"><span data-stu-id="a8182-108">The ASP.NET MVC 4 [Controller](https://msdn.microsoft.com/library/system.web.mvc.controller(VS.108).aspx) class in combination [.NET 4.5](https://msdn.microsoft.com/library/w0x726c2(VS.110).aspx) enables you to write asynchronous action methods that return an object of type [Task&lt;ActionResult&gt;](https://msdn.microsoft.com/library/dd321424(VS.110).aspx).</span></span> <span data-ttu-id="a8182-109">.NET Framework 4 wprowadzono asynchronicznego koncepcji programowania, nazywane [zadań](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) i obsługuje platformy ASP.NET MVC 4 [zadań](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx).</span><span class="sxs-lookup"><span data-stu-id="a8182-109">The .NET Framework 4 introduced an asynchronous programming concept referred to as a [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) and ASP.NET MVC 4 supports [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx).</span></span> <span data-ttu-id="a8182-110">Zadania są reprezentowane przez **zadań** typu i powiązanych typów [System.Threading.Tasks](https://msdn.microsoft.com/library/system.threading.tasks.aspx) przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="a8182-110">Tasks are represented by the **Task** type and related types in the [System.Threading.Tasks](https://msdn.microsoft.com/library/system.threading.tasks.aspx) namespace.</span></span> <span data-ttu-id="a8182-111">.NET Framework 4.5 jest oparta na tej asynchronicznej obsługi za pomocą [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) i [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) słów kluczowych, które ułatwić pracę z [zadań](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) obiektów znacznie mniej złożone niż wcześniejsza metody asynchroniczne.</span><span class="sxs-lookup"><span data-stu-id="a8182-111">The .NET Framework 4.5 builds on this asynchronous support with the [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords that make working with [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) objects much less complex than previous asynchronous approaches.</span></span> <span data-ttu-id="a8182-112">[Await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) — słowo kluczowe jest syntaktycznych skrótem wskazująca, że fragment kodu asynchronicznego powinien zaczekać na inne części kodu.</span><span class="sxs-lookup"><span data-stu-id="a8182-112">The [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keyword is syntactical shorthand for indicating that a piece of code should asynchronously wait on some other piece of code.</span></span> <span data-ttu-id="a8182-113">[Async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) — słowo kluczowe reprezentuje wskazówkę, która służy do oznaczania metod jako opartego na zadaniach asynchronicznej metody.</span><span class="sxs-lookup"><span data-stu-id="a8182-113">The [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keyword represents a hint that you can use to mark methods as task-based asynchronous methods.</span></span> <span data-ttu-id="a8182-114">Kombinacja **await**, **async**i **zadań** obiektu ułatwia pisanie kodu asynchronicznego w .NET 4.5.</span><span class="sxs-lookup"><span data-stu-id="a8182-114">The combination of **await**, **async**, and the **Task** object makes it much easier for you to write asynchronous code in .NET 4.5.</span></span> <span data-ttu-id="a8182-115">Nowy model, w przypadku metod asynchronicznych jest nazywany *wzorca asynchronicznego opartego na zadaniach* (**NACIŚNIJ**).</span><span class="sxs-lookup"><span data-stu-id="a8182-115">The new model for asynchronous methods is called the *Task-based Asynchronous Pattern* (**TAP**).</span></span> <span data-ttu-id="a8182-116">W tym samouczku założono, masz pewną znajomość asynchroniczne przy użyciu programistyczne [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) i [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) słów kluczowych i [zadań](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="a8182-116">This tutorial assumes you have some familiarity with asynchronous programing using [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords and the [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) namespace.</span></span>

<span data-ttu-id="a8182-117">Aby uzyskać więcej informacji na temat korzystania z [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) i [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) słów kluczowych i [zadań](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) przestrzeni nazw, zobacz następujące informacje.</span><span class="sxs-lookup"><span data-stu-id="a8182-117">For more information on the using [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords and the [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) namespace, see the following references.</span></span>

- [<span data-ttu-id="a8182-118">Oficjalny dokument: Asynchroniczność na platformie .NET</span><span class="sxs-lookup"><span data-stu-id="a8182-118">Whitepaper: Asynchrony in .NET</span></span>](https://go.microsoft.com/fwlink/?LinkId=204844)
- [<span data-ttu-id="a8182-119">Async/Await — często zadawane pytania</span><span class="sxs-lookup"><span data-stu-id="a8182-119">Async/Await FAQ</span></span>](https://blogs.msdn.com/b/pfxteam/archive/2012/04/12/10293335.aspx)
- [<span data-ttu-id="a8182-120">Visual Studio Asynchronous Programming</span><span class="sxs-lookup"><span data-stu-id="a8182-120">Visual Studio Asynchronous Programming</span></span>](https://msdn.microsoft.com/vstudio/gg316360)

## <a id="HowRequestsProcessedByTP"></a>  <span data-ttu-id="a8182-121">Sposób przetwarzania żądań w puli wątków</span><span class="sxs-lookup"><span data-stu-id="a8182-121">How Requests Are Processed by the Thread Pool</span></span>

<span data-ttu-id="a8182-122">Na serwerze sieci web programu .NET Framework obsługuje puli wątków, które są używane do obsługi żądań ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="a8182-122">On the web server, the .NET Framework maintains a pool of threads that are used to service ASP.NET requests.</span></span> <span data-ttu-id="a8182-123">Po odebraniu żądania wątków z puli jest wysyłane do przetworzenia tego żądania.</span><span class="sxs-lookup"><span data-stu-id="a8182-123">When a request arrives, a thread from the pool is dispatched to process that request.</span></span> <span data-ttu-id="a8182-124">Jeśli żądanie jest przetwarzane synchronicznie, wątek, który przetwarza żądanie jest zajęty podczas żądania jest przetwarzany, że wątek nie może obsłużyć inne żądanie.</span><span class="sxs-lookup"><span data-stu-id="a8182-124">If the request is processed synchronously, the thread that processes the request is busy while the request is being processed, and that thread cannot service another request.</span></span>   
  
<span data-ttu-id="a8182-125">Może to nie być problemem, ponieważ w puli wątków może pracować z wystarczająco dużą do obsługi wielu zajęte wątki.</span><span class="sxs-lookup"><span data-stu-id="a8182-125">This might not be a problem, because the thread pool can be made large enough to accommodate many busy threads.</span></span> <span data-ttu-id="a8182-126">Jednakże, jest ograniczona liczba wątków w puli wątków (wartość domyślna dla platformy .NET 4.5 to 5000).</span><span class="sxs-lookup"><span data-stu-id="a8182-126">However, the number of threads in the thread pool is limited (the default maximum for .NET 4.5 is 5,000).</span></span> <span data-ttu-id="a8182-127">W dużych aplikacji z wysoką współbieżności długotrwałych żądań wszystkie dostępne wątki może być zajęta.</span><span class="sxs-lookup"><span data-stu-id="a8182-127">In large applications with high concurrency of long-running requests, all available threads might be busy.</span></span> <span data-ttu-id="a8182-128">Ten stan jest nazywany zablokowania wątku.</span><span class="sxs-lookup"><span data-stu-id="a8182-128">This condition is known as thread starvation.</span></span> <span data-ttu-id="a8182-129">Po osiągnięciu tego warunku, serwer sieci web kolejki żądań.</span><span class="sxs-lookup"><span data-stu-id="a8182-129">When this condition is reached, the web server queues requests.</span></span> <span data-ttu-id="a8182-130">Jeśli zapełnieniu kolejki żądań serwera sieci web odrzucanie żądań ze stanem HTTP 503 (serwer jest zbyt zajęty).</span><span class="sxs-lookup"><span data-stu-id="a8182-130">If the request queue becomes full, the web server rejects requests with an HTTP 503 status (Server Too Busy).</span></span> <span data-ttu-id="a8182-131">Pula wątków CLR ma ograniczenia na nowy wątek wstrzyknięć kodu.</span><span class="sxs-lookup"><span data-stu-id="a8182-131">The CLR thread pool has limitations on new thread injections.</span></span> <span data-ttu-id="a8182-132">Jeśli intensywnego współbieżności (oznacza to, że witryny sieci web można nagle pobierać dużą liczbę żądań) i wszystkie wątki żądanie dostępne są zajęte ze względu na wywołania zaplecza dużymi opóźnieniami, szybkość iniekcji ograniczone wątku można uczynić aplikację reagować bardzo źle.</span><span class="sxs-lookup"><span data-stu-id="a8182-132">If concurrency is bursty (that is, your web site can suddenly get a large number of requests) and all available request threads are busy because of backend calls with high latency, the limited thread injection rate can make your application respond very poorly.</span></span> <span data-ttu-id="a8182-133">Ponadto każdy nowy wątek dodane do puli wątków ma obciążenie (np. 1 MB pamięci stosu).</span><span class="sxs-lookup"><span data-stu-id="a8182-133">Additionally, each new thread added to the thread pool has overhead (such as 1 MB of stack memory).</span></span> <span data-ttu-id="a8182-134">W przypadku aplikacji sieci web przy użyciu synchronicznej metody do wywołania duże opóźnienie usługi, którym puli wątków rozwoju domyślne .NET 4.5 maksymalnie 5 000 wątków będzie korzystać około 5 GB więcej pamięci niż aplikacja może takie same żądania obsługi przy użyciu metody asynchroniczne i tylko 50 wątków.</span><span class="sxs-lookup"><span data-stu-id="a8182-134">A web application using synchronous methods to service high latency calls where the thread pool grows to the .NET 4.5 default maximum of 5, 000 threads would consume approximately 5 GB more memory than an application able the service the same requests using asynchronous methods and only 50 threads.</span></span> <span data-ttu-id="a8182-135">Podczas prowadzenia prac asynchronicznych, nie zawsze jest używany wątek.</span><span class="sxs-lookup"><span data-stu-id="a8182-135">When you're doing asynchronous work, you're not always using a thread.</span></span> <span data-ttu-id="a8182-136">Na przykład, gdy żądanie usługi sieci web w asynchronicznej, ASP.NET nie będzie korzystać z żadnych wątków między **async** wywołania metody oraz **await**.</span><span class="sxs-lookup"><span data-stu-id="a8182-136">For example, when you make an asynchronous web service request, ASP.NET will not be using any threads between the **async** method call and the **await**.</span></span> <span data-ttu-id="a8182-137">Przy użyciu puli wątków do obsługi żądań dużymi opóźnieniami może prowadzić do zużycia dużej ilości pamięci i niską wykorzystanie sprzętu serwera.</span><span class="sxs-lookup"><span data-stu-id="a8182-137">Using the thread pool to service requests with high latency can lead to a large memory footprint and poor utilization of the server hardware.</span></span>

## <a name="processing-asynchronous-requests"></a><span data-ttu-id="a8182-138">Przetwarzanie żądań asynchronicznych</span><span class="sxs-lookup"><span data-stu-id="a8182-138">Processing Asynchronous Requests</span></span>

<span data-ttu-id="a8182-139">W aplikacji sieci web, która będzie widział dużą liczbą jednoczesnych żądań przy rozruchu lub ma obciążenia intensywnego (gdzie współbieżności zwiększa nagle) wywołań usługi sieci web jest asynchroniczne zwiększa szybkość reakcji aplikacji.</span><span class="sxs-lookup"><span data-stu-id="a8182-139">In a web app that sees a large number of concurrent requests at start-up or has a bursty load (where concurrency increases suddenly), making web service calls asynchronous increases the responsiveness of the app.</span></span> <span data-ttu-id="a8182-140">Asynchroniczne żądanie trwa tyle samo czasu na przetworzenie jako żądań synchronicznych.</span><span class="sxs-lookup"><span data-stu-id="a8182-140">An asynchronous request takes the same amount of time to process as a synchronous request.</span></span> <span data-ttu-id="a8182-141">Jeśli żądanie sprawia, że usługi sieci web, w wywołaniu, które wymaga dwóch sekund, przyjmuje żądania dwóch sekund czy jest wykonywane synchronicznie lub asynchronicznie.</span><span class="sxs-lookup"><span data-stu-id="a8182-141">If a request makes a web service call that requires two seconds to complete, the request takes two seconds whether it's performed synchronously or asynchronously.</span></span> <span data-ttu-id="a8182-142">Jednak podczas wywołania asynchronicznego wątku nie jest zablokowany odpowiada na inne żądania podczas oczekiwania na pierwsze żądanie zakończyć.</span><span class="sxs-lookup"><span data-stu-id="a8182-142">However during an asynchronous call, a thread isn't blocked from responding to other requests while it waits for the first request to complete.</span></span> <span data-ttu-id="a8182-143">W związku z tym żądań asynchronicznych uniknięcia wzrostu puli żądania usługi kolejkowania i wątku, gdy są dużo współbieżnych żądań, które wywołują długotrwałych operacji.</span><span class="sxs-lookup"><span data-stu-id="a8182-143">Therefore, asynchronous requests prevent request queuing and thread pool growth when there are many concurrent requests that invoke long-running operations.</span></span>

## <a id="ChoosingSyncVasync"></a>  <span data-ttu-id="a8182-144">Wybór metody akcji synchroniczna lub asynchroniczna</span><span class="sxs-lookup"><span data-stu-id="a8182-144">Choosing Synchronous or Asynchronous Action Methods</span></span>

<span data-ttu-id="a8182-145">Ta sekcja zawiera wskazówki dotyczące kiedy należy używać metod akcji synchroniczna lub asynchroniczna.</span><span class="sxs-lookup"><span data-stu-id="a8182-145">This section lists guidelines for when to use synchronous or asynchronous action methods.</span></span> <span data-ttu-id="a8182-146">Są to po prostu wytycznych; zbadać każdą aplikację pojedynczo, aby określić, czy metod asynchronicznych poprawić wydajność.</span><span class="sxs-lookup"><span data-stu-id="a8182-146">These are just guidelines; examine each application individually to determine whether asynchronous methods help with performance.</span></span>

<span data-ttu-id="a8182-147">Ogólnie rzecz biorąc należy użyć metod synchronicznych następujące warunki:</span><span class="sxs-lookup"><span data-stu-id="a8182-147">In general, use synchronous methods for the following conditions:</span></span>

- <span data-ttu-id="a8182-148">Operacje są proste lub krótko działających.</span><span class="sxs-lookup"><span data-stu-id="a8182-148">The operations are simple or short-running.</span></span>
- <span data-ttu-id="a8182-149">Prostota jest ważniejsza niż wydajność.</span><span class="sxs-lookup"><span data-stu-id="a8182-149">Simplicity is more important than efficiency.</span></span>
- <span data-ttu-id="a8182-150">Operacje są głównie operacje Procesora zamiast operacje obejmujące rozbudowane dysków lub obciążenie sieci.</span><span class="sxs-lookup"><span data-stu-id="a8182-150">The operations are primarily CPU operations instead of operations that involve extensive disk or network overhead.</span></span> <span data-ttu-id="a8182-151">Za pomocą metod asynchronicznych akcji na operacje zależne od Procesora CPU zapewnia nie korzyści i wyniki w większej liczbie obciążenie.</span><span class="sxs-lookup"><span data-stu-id="a8182-151">Using asynchronous action methods on CPU-bound operations provides no benefits and results in more overhead.</span></span>

  <span data-ttu-id="a8182-152">Ogólnie rzecz biorąc należy używać metod asynchronicznych następujące warunki:</span><span class="sxs-lookup"><span data-stu-id="a8182-152">In general, use asynchronous methods for the following conditions:</span></span>

- <span data-ttu-id="a8182-153">W przypadku wywoływania usługi, które mogą być wykorzystywane za pomocą metod asynchronicznych i korzystania z platformy .NET 4.5 lub nowszej.</span><span class="sxs-lookup"><span data-stu-id="a8182-153">You're calling services that can be consumed through asynchronous methods, and you're using .NET 4.5 or higher.</span></span>
- <span data-ttu-id="a8182-154">Operacje są powiązane z siecią lub I/O-granicę zamiast zależne od Procesora CPU.</span><span class="sxs-lookup"><span data-stu-id="a8182-154">The operations are network-bound or I/O-bound instead of CPU-bound.</span></span>
- <span data-ttu-id="a8182-155">Równoległość jest ważniejsza niż uproszczenia kodu.</span><span class="sxs-lookup"><span data-stu-id="a8182-155">Parallelism is more important than simplicity of code.</span></span>
- <span data-ttu-id="a8182-156">Chcesz zapewnić mechanizm, który pozwala użytkownikom na anulowanie długotrwałe żądania.</span><span class="sxs-lookup"><span data-stu-id="a8182-156">You want to provide a mechanism that lets users cancel a long-running request.</span></span>
- <span data-ttu-id="a8182-157">Kiedy korzyść przełączania wątków przewyższa koszt przełączania kontekstu.</span><span class="sxs-lookup"><span data-stu-id="a8182-157">When the benefit of switching threads outweighs the cost of the context switch.</span></span> <span data-ttu-id="a8182-158">Ogólnie rzecz biorąc należy upewnić metody asynchronicznej Jeśli metoda synchroniczna czeka na wątku żądania ASP.NET podczas wykonywania żadnej pracy.</span><span class="sxs-lookup"><span data-stu-id="a8182-158">In general, you should make a method asynchronous if the synchronous method waits on the ASP.NET request thread while doing no work.</span></span> <span data-ttu-id="a8182-159">Za pomocą wywołania asynchronicznego, wątek żądania ASP.NET jest zainstalowany, wykonując żadnej pracy podczas oczekiwania na żądanie usługi sieci web zakończyć.</span><span class="sxs-lookup"><span data-stu-id="a8182-159">By making the call asynchronous, the ASP.NET request thread is not stalled doing no work while it waits for the web service request to complete.</span></span>
- <span data-ttu-id="a8182-160">Testowanie pokazuje, że blokowania operacji jest wąskim gardłem wydajności witryn i usług IIS może obsługiwać więcej żądań przy użyciu metod asynchronicznych na te wywołania blokowania.</span><span class="sxs-lookup"><span data-stu-id="a8182-160">Testing shows that the blocking operations are a bottleneck in site performance and that IIS can service more requests by using asynchronous methods for these blocking calls.</span></span>

  <span data-ttu-id="a8182-161">Do pobrania przykładowych pokazano, jak skutecznie używać metod asynchronicznych akcji.</span><span class="sxs-lookup"><span data-stu-id="a8182-161">The downloadable sample shows how to use asynchronous action methods effectively.</span></span> <span data-ttu-id="a8182-162">Przykładowym został zaprojektowany w celu zapewnienia prosty pokaz programowania asynchronicznego w programie ASP.NET MVC 4 przy użyciu platformy .NET 4.5.</span><span class="sxs-lookup"><span data-stu-id="a8182-162">The sample provided was designed to provide a simple demonstration of asynchronous programming in ASP.NET MVC 4 using .NET 4.5.</span></span> <span data-ttu-id="a8182-163">Plik nie ma być architektury referencyjnej dla programowania asynchronicznego we wzorcu ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="a8182-163">The sample is not intended to be a reference architecture for asynchronous programming in ASP.NET MVC.</span></span> <span data-ttu-id="a8182-164">Wywołuje program przykładowy [ASP.NET Web API](../../../web-api/index.md) metod, które z kolei wywołać [Task.Delay](https://msdn.microsoft.com/library/hh139096(VS.110).aspx) do symulacji wywołania usługi sieci web długotrwałych.</span><span class="sxs-lookup"><span data-stu-id="a8182-164">The sample program calls [ASP.NET Web API](../../../web-api/index.md) methods which in turn call [Task.Delay](https://msdn.microsoft.com/library/hh139096(VS.110).aspx) to simulate long-running web service calls.</span></span> <span data-ttu-id="a8182-165">Większości aplikacji produkcyjnych, nie będą widoczne takie oczywiste korzyści wynikające z używania metod asynchronicznych akcji.</span><span class="sxs-lookup"><span data-stu-id="a8182-165">Most production applications will not show such obvious benefits to using asynchronous action methods.</span></span>   
  
<span data-ttu-id="a8182-166">Kilka aplikacji wymaga wszystkie metody akcji za asynchronicznego.</span><span class="sxs-lookup"><span data-stu-id="a8182-166">Few applications require all action methods to be asynchronous.</span></span> <span data-ttu-id="a8182-167">Często konwertowanie kilka metod synchronicznych akcji do metod asynchronicznych zapewnia najlepsze zwiększenie wydajności, ilości pracy wymaganej.</span><span class="sxs-lookup"><span data-stu-id="a8182-167">Often, converting a few synchronous action methods to asynchronous methods provides the best efficiency increase for the amount of work required.</span></span>

## <a id="SampleApp"></a>  <span data-ttu-id="a8182-168">Przykładowa aplikacja</span><span class="sxs-lookup"><span data-stu-id="a8182-168">The Sample Application</span></span>

<span data-ttu-id="a8182-169">Możesz pobrać przykładową aplikację z [ https://github.com/RickAndMSFT/Async-ASP.NET/ ](https://github.com/RickAndMSFT/Async-ASP.NET) na [GitHub](https://github.com/) lokacji.</span><span class="sxs-lookup"><span data-stu-id="a8182-169">You can download the sample application from [https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET) on the [GitHub](https://github.com/) site.</span></span> <span data-ttu-id="a8182-170">Repozytorium zawiera trzy projekty:</span><span class="sxs-lookup"><span data-stu-id="a8182-170">The repository consists of three projects:</span></span>

- <span data-ttu-id="a8182-171">*Mvc4Async*: Projekt platformy ASP.NET MVC 4, który zawiera kod używany w ramach tego samouczka.</span><span class="sxs-lookup"><span data-stu-id="a8182-171">*Mvc4Async*: The ASP.NET MVC 4 project that contains the code used in this tutorial.</span></span> <span data-ttu-id="a8182-172">To sprawia, że wywołania interfejsu API sieci Web **WebAPIpgw** usługi.</span><span class="sxs-lookup"><span data-stu-id="a8182-172">It makes Web API calls to the **WebAPIpgw** service.</span></span>
- <span data-ttu-id="a8182-173">*WebAPIpgw*: Projekt interfejsu API platformy ASP.NET MVC 4 w sieci Web, który implementuje `Products, Gizmos and Widgets` kontrolerów.</span><span class="sxs-lookup"><span data-stu-id="a8182-173">*WebAPIpgw*: The ASP.NET MVC 4 Web API project that implements the `Products, Gizmos and Widgets` controllers.</span></span> <span data-ttu-id="a8182-174">Zapewnia dane dotyczące *WebAppAsync* projektu i *Mvc4Async* projektu.</span><span class="sxs-lookup"><span data-stu-id="a8182-174">It provides the data for the *WebAppAsync* project and the *Mvc4Async* project.</span></span>
- <span data-ttu-id="a8182-175">*WebAppAsync*: Projekt formularzy sieci Web ASP.NET, używane w innym samouczku.</span><span class="sxs-lookup"><span data-stu-id="a8182-175">*WebAppAsync*: The ASP.NET Web Forms project used in another tutorial.</span></span>

## <a id="GizmosSynch"></a>  <span data-ttu-id="a8182-176">Metoda synchroniczna akcji Gizmo</span><span class="sxs-lookup"><span data-stu-id="a8182-176">The Gizmos Synchronous Action Method</span></span>

 <span data-ttu-id="a8182-177">Poniższy kod przedstawia `Gizmos` metody synchronicznej akcji, która jest używana do wyświetlania listy elementy gizmo.</span><span class="sxs-lookup"><span data-stu-id="a8182-177">The following code shows the `Gizmos` synchronous action method that is used to display a list of gizmos.</span></span> <span data-ttu-id="a8182-178">(W tym artykule gizmo jest urządzenie mechaniczne fikcyjnej).</span><span class="sxs-lookup"><span data-stu-id="a8182-178">(For this article, a gizmo is a fictional mechanical device.)</span></span> 

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample1.cs)]

<span data-ttu-id="a8182-179">Poniższy kod przedstawia `GetGizmos` metoda gizmo usługi.</span><span class="sxs-lookup"><span data-stu-id="a8182-179">The following code shows the `GetGizmos` method of the gizmo service.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample2.cs)]

<span data-ttu-id="a8182-180">`GizmoService GetGizmos` Metoda przekazuje identyfikatora URI z usługą HTTP interfejsu API sieci Web programu ASP.NET, która zwraca listę gizmo danych.</span><span class="sxs-lookup"><span data-stu-id="a8182-180">The `GizmoService GetGizmos` method passes a URI to an ASP.NET Web API HTTP service which returns a list of gizmos data.</span></span> <span data-ttu-id="a8182-181">*WebAPIpgw* projekt zawiera implementację interfejsu API sieci Web `gizmos, widget` i `product` kontrolerów.</span><span class="sxs-lookup"><span data-stu-id="a8182-181">The *WebAPIpgw* project contains the implementation of the Web API `gizmos, widget` and `product` controllers.</span></span>  
<span data-ttu-id="a8182-182">Na poniższej ilustracji przedstawiono widok gizmo z przykładowego projektu.</span><span class="sxs-lookup"><span data-stu-id="a8182-182">The following image shows the gizmos view from the sample project.</span></span>

![Elementy gizmo](using-asynchronous-methods-in-aspnet-mvc-4/_static/image1.png)

## <a id="CreatingAsynchGizmos"></a>  <span data-ttu-id="a8182-184">Tworzenie Gizmo asynchronicznej metody akcji</span><span class="sxs-lookup"><span data-stu-id="a8182-184">Creating an Asynchronous Gizmos Action Method</span></span>

<span data-ttu-id="a8182-185">W przykładzie użyto nowy [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) i [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) słowa kluczowe (dostępne w .NET 4.5 i programu Visual Studio 2012) aby pozwolić kompilatorowi odpowiada za utrzymywanie niezbędne do przekształcenia skomplikowane Programowanie asynchroniczne.</span><span class="sxs-lookup"><span data-stu-id="a8182-185">The sample uses the new [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) and [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keywords (available in .NET 4.5 and Visual Studio 2012) to let the compiler be responsible for maintaining the complicated transformations necessary for asynchronous programming.</span></span> <span data-ttu-id="a8182-186">Kompilator pozwala napisać kod, używając konstrukcji języka C# firmy synchroniczne sterowanie przepływem i kompilator automatycznie stosuje przekształcenia, które są niezbędne do korzystania z wywołań zwrotnych w celu unikania blokowania wątków.</span><span class="sxs-lookup"><span data-stu-id="a8182-186">The compiler lets you write code using the C#'s synchronous control flow constructs and the compiler automatically applies the transformations necessary to use callbacks in order to avoid blocking threads.</span></span>

<span data-ttu-id="a8182-187">Poniższy kod przedstawia `Gizmos` metody synchronicznej i `GizmosAsync` metody asynchronicznej.</span><span class="sxs-lookup"><span data-stu-id="a8182-187">The following code shows the `Gizmos` synchronous method and the `GizmosAsync` asynchronous method.</span></span> <span data-ttu-id="a8182-188">Jeśli przeglądarka obsługuje [HTML 5 `<mark>` elementu](http://www.w3.org/wiki/HTML/Elements/mark), zobaczysz zmiany w `GizmosAsync` w Wyróżnij żółty.</span><span class="sxs-lookup"><span data-stu-id="a8182-188">If your browser supports the [HTML 5 `<mark>` element](http://www.w3.org/wiki/HTML/Elements/mark), you'll see the changes in `GizmosAsync` in yellow highlight.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample3.cs)]

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample4.cs?highlight=1,3,5)]

 <span data-ttu-id="a8182-189">Następujące zmiany zostały zastosowane, aby umożliwić `GizmosAsync` być asynchroniczne.</span><span class="sxs-lookup"><span data-stu-id="a8182-189">The following changes were applied to allow the `GizmosAsync` to be asynchronous.</span></span>

- <span data-ttu-id="a8182-190">Metoda jest oznaczona atrybutem [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) słowo kluczowe, które informuje kompilator, aby wygenerować wywołania zwrotne dla części treści, a także automatyczne tworzenie `Task<ActionResult>` zwracanym.</span><span class="sxs-lookup"><span data-stu-id="a8182-190">The method is marked with the [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keyword, which tells the compiler to generate callbacks for parts of the body and to automatically create a `Task<ActionResult>` that is returned.</span></span>
- <span data-ttu-id="a8182-191">&quot;Asynchroniczne&quot; został dołączony do nazwy metody.</span><span class="sxs-lookup"><span data-stu-id="a8182-191">&quot;Async&quot; was appended to the method name.</span></span> <span data-ttu-id="a8182-192">Dołączanie "Async" nie jest wymagana, ale jest do Konwencja, podczas zapisywania metod asynchronicznych.</span><span class="sxs-lookup"><span data-stu-id="a8182-192">Appending "Async" is not required but is the convention when writing asynchronous methods.</span></span>
- <span data-ttu-id="a8182-193">Zwracany typ został zmieniony z `ActionResult` do `Task<ActionResult>`.</span><span class="sxs-lookup"><span data-stu-id="a8182-193">The return type was changed from `ActionResult` to `Task<ActionResult>`.</span></span> <span data-ttu-id="a8182-194">Zwracany typ `Task<ActionResult>` reprezentuje pracę w toku i zapewnia obiektów wywołujących metodę z dojściem za pomocą którego można czekać na zakończenie operacji asynchronicznej.</span><span class="sxs-lookup"><span data-stu-id="a8182-194">The return type of `Task<ActionResult>` represents ongoing work and provides callers of the method with a handle through which to wait for the asynchronous operation's completion.</span></span> <span data-ttu-id="a8182-195">W tym przypadku obiekt wywołujący jest usługą sieci web.</span><span class="sxs-lookup"><span data-stu-id="a8182-195">In this case, the caller is the web service.</span></span> <span data-ttu-id="a8182-196">`Task<ActionResult>` trwającą reprezentuje pracować w wyniku `ActionResult.`</span><span class="sxs-lookup"><span data-stu-id="a8182-196">`Task<ActionResult>` represents ongoing work with a result of `ActionResult.`</span></span>
- <span data-ttu-id="a8182-197">[Await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) — słowo kluczowe została zastosowana do wywołania usługi sieci web.</span><span class="sxs-lookup"><span data-stu-id="a8182-197">The [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keyword was applied to the web service call.</span></span>
- <span data-ttu-id="a8182-198">Wywołano asynchroniczną internetowy interfejs API usługi (`GetGizmosAsync`).</span><span class="sxs-lookup"><span data-stu-id="a8182-198">The asynchronous web service API was called (`GetGizmosAsync`).</span></span>

<span data-ttu-id="a8182-199">Wewnątrz `GetGizmosAsync` metoda treści innej metody asynchronicznej, `GetGizmosAsync` jest wywoływana.</span><span class="sxs-lookup"><span data-stu-id="a8182-199">Inside of the `GetGizmosAsync` method body another asynchronous method, `GetGizmosAsync` is called.</span></span> <span data-ttu-id="a8182-200">`GetGizmosAsync` natychmiast zwraca `Task<List<Gizmo>>` , zostanie ostatecznie zakończona, gdy dane są dostępne.</span><span class="sxs-lookup"><span data-stu-id="a8182-200">`GetGizmosAsync` immediately returns a `Task<List<Gizmo>>` that will eventually complete when the data is available.</span></span> <span data-ttu-id="a8182-201">Ponieważ nie chcesz wykonywanie dodatkowych czynności, aż do uzyskania danych gizmo, kod czeka na zadanie (przy użyciu **await** — słowo kluczowe).</span><span class="sxs-lookup"><span data-stu-id="a8182-201">Because you don't want to do anything else until you have the gizmo data, the code awaits the task (using the **await** keyword).</span></span> <span data-ttu-id="a8182-202">Możesz użyć **await** — słowo kluczowe tylko w metodach oznaczony za pomocą **async** — słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="a8182-202">You can use the **await** keyword only in methods annotated with the **async** keyword.</span></span>

<span data-ttu-id="a8182-203">**Await** — słowo kluczowe nie blokuje wątku, do czasu ukończenia zadania.</span><span class="sxs-lookup"><span data-stu-id="a8182-203">The **await** keyword does not block the thread until the task is complete.</span></span> <span data-ttu-id="a8182-204">Rejestruje pozostałą część metody jako wywołania zwrotnego dla zadania i natychmiast powraca.</span><span class="sxs-lookup"><span data-stu-id="a8182-204">It signs up the rest of the method as a callback on the task, and immediately returns.</span></span> <span data-ttu-id="a8182-205">Gdy ostatecznie zakończy się oczekiwane zadanie, spowoduje to wywołanie wywołania zwrotnego i dlatego wznowić wykonywanie metody po prawej stronie, tam, gdzie ją przerwaliśmy.</span><span class="sxs-lookup"><span data-stu-id="a8182-205">When the awaited task eventually completes, it will invoke that callback and thus resume the execution of the method right where it left off.</span></span> <span data-ttu-id="a8182-206">Aby uzyskać więcej informacji na temat korzystania z [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) i [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) słów kluczowych i [zadań](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) przestrzeni nazw, zobacz [odwołania async](https://docs.microsoft.com/dotnet/csharp/language-reference/keywords/async).</span><span class="sxs-lookup"><span data-stu-id="a8182-206">For more information on using the [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords and the [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) namespace, see the [async references](https://docs.microsoft.com/dotnet/csharp/language-reference/keywords/async).</span></span>

<span data-ttu-id="a8182-207">Poniższy kod przedstawia `GetGizmos` i `GetGizmosAsync` metody.</span><span class="sxs-lookup"><span data-stu-id="a8182-207">The following code shows the `GetGizmos` and `GetGizmosAsync` methods.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample5.cs)]

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample6.cs?highlight=1,4-8)]

 <span data-ttu-id="a8182-208">Asynchroniczne zmiany są podobne do wprowadzonych **GizmosAsync** powyżej.</span><span class="sxs-lookup"><span data-stu-id="a8182-208">The asynchronous changes are similar to those made to the **GizmosAsync** above.</span></span> 

- <span data-ttu-id="a8182-209">Podpis metody został oznaczony za pomocą [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) — słowo kluczowe, zwracany typ została zmieniona na `Task<List<Gizmo>>`, i *Async* został dołączony do nazwy metody.</span><span class="sxs-lookup"><span data-stu-id="a8182-209">The method signature was annotated with the [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keyword, the return type was changed to `Task<List<Gizmo>>`, and *Async* was appended to the method name.</span></span>
- <span data-ttu-id="a8182-210">Asynchroniczną [HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx) klasa jest używana zamiast [WebClient](https://msdn.microsoft.com/library/system.net.webclient.aspx) klasy.</span><span class="sxs-lookup"><span data-stu-id="a8182-210">The asynchronous [HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx) class is used instead of the [WebClient](https://msdn.microsoft.com/library/system.net.webclient.aspx) class.</span></span>
- <span data-ttu-id="a8182-211">[Await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) — słowo kluczowe została zastosowana do [HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx) metod asynchronicznych.</span><span class="sxs-lookup"><span data-stu-id="a8182-211">The [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keyword was applied to the [HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx) asynchronous methods.</span></span>

<span data-ttu-id="a8182-212">Na poniższej ilustracji przedstawiono widok gizmo asynchronicznego.</span><span class="sxs-lookup"><span data-stu-id="a8182-212">The following image shows the asynchronous gizmo view.</span></span>

![async](using-asynchronous-methods-in-aspnet-mvc-4/_static/image2.png)

<span data-ttu-id="a8182-214">Prezentacji przeglądarek danych gizmo jest identyczna z widoku utworzonego przez wywołanie synchroniczne.</span><span class="sxs-lookup"><span data-stu-id="a8182-214">The browsers presentation of the gizmos data is identical to the view created by the synchronous call.</span></span> <span data-ttu-id="a8182-215">Jedyną różnicą jest to wersja asynchroniczna mogą działać wydajniej pod dużym obciążeniem.</span><span class="sxs-lookup"><span data-stu-id="a8182-215">The only difference is the asynchronous version may be more performant under heavy loads.</span></span>

## <a id="Parallel"></a>  <span data-ttu-id="a8182-216">Wykonywanie operacji na wielu równolegle</span><span class="sxs-lookup"><span data-stu-id="a8182-216">Performing Multiple Operations in Parallel</span></span>

<span data-ttu-id="a8182-217">Metod asynchronicznych akcji mieć znaczącą korzyścią za pośrednictwem metod synchronicznych, gdy akcję, należy wykonać kilka operacji niezależnych.</span><span class="sxs-lookup"><span data-stu-id="a8182-217">Asynchronous action methods have a significant advantage over synchronous methods when an action must perform several independent operations.</span></span> <span data-ttu-id="a8182-218">W przykładowym, metoda synchroniczna `PWG`(w przypadku produktów, elementów widget i elementy Gizmo) służy do wyświetlania wyników trzy wywołania usługi sieci web, aby uzyskać listę produktów, elementów widget i elementy gizmo.</span><span class="sxs-lookup"><span data-stu-id="a8182-218">In the sample provided, the synchronous method `PWG`(for Products, Widgets and Gizmos) displays the results of three web service calls to get a list of products, widgets, and gizmos.</span></span> <span data-ttu-id="a8182-219">[ASP.NET Web API](../../../web-api/index.md) projektu, który udostępnia te usługi używa [Task.Delay](https://msdn.microsoft.com/library/hh139096(VS.110).aspx) do symulowania opóźnienia lub wolno działającą sieć wywołań.</span><span class="sxs-lookup"><span data-stu-id="a8182-219">The [ASP.NET Web API](../../../web-api/index.md) project that provides these services uses [Task.Delay](https://msdn.microsoft.com/library/hh139096(VS.110).aspx) to simulate latency or slow network calls.</span></span> <span data-ttu-id="a8182-220">Gdy opóźnienie ma wartość 500 milisekund asynchroniczną `PWGasync` metoda zajmuje to nieco ponad 500 milisekund do wykonania podczas synchronicznej `PWG` wersji przejmuje 1500 MS.</span><span class="sxs-lookup"><span data-stu-id="a8182-220">When the delay is set to 500 milliseconds, the asynchronous `PWGasync` method takes a little over 500 milliseconds to complete while the synchronous `PWG` version takes over 1,500 milliseconds.</span></span> <span data-ttu-id="a8182-221">Synchronicznej `PWG` metoda przedstawiono w poniższym kodzie.</span><span class="sxs-lookup"><span data-stu-id="a8182-221">The synchronous `PWG` method is shown in the following code.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample7.cs)]

<span data-ttu-id="a8182-222">Asynchroniczną `PWGasync` metoda przedstawiono w poniższym kodzie.</span><span class="sxs-lookup"><span data-stu-id="a8182-222">The asynchronous `PWGasync` method is shown in the following code.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample8.cs?highlight=1,3,12)]

<span data-ttu-id="a8182-223">Na poniższej ilustracji przedstawiono widok zwróciło **PWGasync** metody.</span><span class="sxs-lookup"><span data-stu-id="a8182-223">The following image shows the view returned from the **PWGasync** method.</span></span>

![pwgAsync](using-asynchronous-methods-in-aspnet-mvc-4/_static/image3.png)

## <a id="CancelToken"></a>  <span data-ttu-id="a8182-225">Korzystając z tokena odwołania</span><span class="sxs-lookup"><span data-stu-id="a8182-225">Using a Cancellation Token</span></span>

<span data-ttu-id="a8182-226">Metody akcji asynchronicznych, zwracając `Task<ActionResult>`czy można anulować, który jest przyjmują [CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx) parametru, jeśli została dostarczona z [Wartość AsyncTimeout](https://msdn.microsoft.com/library/system.web.mvc.asynctimeoutattribute(VS.108).aspx) atrybutu.</span><span class="sxs-lookup"><span data-stu-id="a8182-226">Asynchronous action methods returning `Task<ActionResult>`are cancelable, that is they take a [CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx) parameter when one is provided with the [AsyncTimeout](https://msdn.microsoft.com/library/system.web.mvc.asynctimeoutattribute(VS.108).aspx) attribute.</span></span> <span data-ttu-id="a8182-227">Poniższy kod przedstawia `GizmosCancelAsync` metody z limitem czasu równym 150 milisekund.</span><span class="sxs-lookup"><span data-stu-id="a8182-227">The following code shows the `GizmosCancelAsync` method with a timeout of 150 milliseconds.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample9.cs?highlight=1-3,5,10)]

<span data-ttu-id="a8182-228">Poniższy kod pokazuje przeciążenie GetGizmosAsync, która przyjmuje [CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx) parametru.</span><span class="sxs-lookup"><span data-stu-id="a8182-228">The following code shows the GetGizmosAsync overload, which takes a [CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx) parameter.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample10.cs)]

<span data-ttu-id="a8182-229">W przykładowej aplikacji pod warunkiem, wybierając *pokaz tokenu anulowania* link wywołania `GizmosCancelAsync` metody i demonstruje anulowanie wywołania asynchronicznego.</span><span class="sxs-lookup"><span data-stu-id="a8182-229">In the sample application provided, selecting the *Cancellation Token Demo* link calls the `GizmosCancelAsync` method and demonstrates the cancellation of the asynchronous call.</span></span>

## <a id="ServerConfig"></a>  <span data-ttu-id="a8182-230">Konfiguracja serwera dla wywołania usługi sieci Web opóźnienie współbieżności wysoki/wysoka</span><span class="sxs-lookup"><span data-stu-id="a8182-230">Server Configuration for High Concurrency/High Latency Web Service Calls</span></span>

<span data-ttu-id="a8182-231">Aby wykorzystać zalety aplikacji sieci web asynchronicznego, konieczne może być pewnych zmian w domyślnej konfiguracji serwera.</span><span class="sxs-lookup"><span data-stu-id="a8182-231">To realize the benefits of an asynchronous web application, you might need to make some changes to the default server configuration.</span></span> <span data-ttu-id="a8182-232">Pamiętaj o następujących ducha podczas konfigurowania i testowanie aplikacji sieci web asynchronicznego obciążeniowe.</span><span class="sxs-lookup"><span data-stu-id="a8182-232">Keep the following in mind when configuring and stress testing your asynchronous web application.</span></span>

- <span data-ttu-id="a8182-233">Windows 7, Windows Vista i wszystkie systemy operacyjne klienta Windows mieć co najwyżej 10 równoczesnych żądań.</span><span class="sxs-lookup"><span data-stu-id="a8182-233">Windows 7, Windows Vista and all Windows client operating systems have a maximum of 10 concurrent requests.</span></span> <span data-ttu-id="a8182-234">Potrzebna będzie systemu operacyjnego Windows Server, aby zobaczyć korzyści wynikające z metod asynchronicznych pod dużym obciążeniem.</span><span class="sxs-lookup"><span data-stu-id="a8182-234">You'll need a Windows Server operating system to see the benefits of asynchronous methods under high load.</span></span>
- <span data-ttu-id="a8182-235">Zarejestruj .NET 4.5 za pomocą programu IIS z wiersza polecenia z podwyższonym poziomem uprawnień:</span><span class="sxs-lookup"><span data-stu-id="a8182-235">Register .NET 4.5 with IIS from an elevated command prompt:</span></span>  
  <span data-ttu-id="a8182-236">%windir%\Microsoft.NET\Framework64\v4.0.30319\aspnet\_regiis -i</span><span class="sxs-lookup"><span data-stu-id="a8182-236">%windir%\Microsoft.NET\Framework64\v4.0.30319\aspnet\_regiis -i</span></span>  
  <span data-ttu-id="a8182-237">Zobacz [narzędzie rejestracji usług IIS (Aspnet\_regiis.exe)](https://msdn.microsoft.com/library/k6h9cz8h.aspx)</span><span class="sxs-lookup"><span data-stu-id="a8182-237">See    [ASP.NET IIS Registration Tool (Aspnet\_regiis.exe)](https://msdn.microsoft.com/library/k6h9cz8h.aspx)</span></span>
- <span data-ttu-id="a8182-238">Może być konieczne zwiększenie [HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture) limit kolejki z domyślnej wartości 1000 i 5000.</span><span class="sxs-lookup"><span data-stu-id="a8182-238">You might need to increase the [HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture) queue limit from the default value of 1,000 to 5,000.</span></span> <span data-ttu-id="a8182-239">Jeśli ustawienie jest zbyt niska, może zostać wyświetlony [HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture) odrzucanie żądań ze stanem HTTP 503.</span><span class="sxs-lookup"><span data-stu-id="a8182-239">If the setting is too low, you may see [HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture) reject requests with a HTTP 503 status.</span></span> <span data-ttu-id="a8182-240">Aby zmienić limit kolejki sterownik HTTP.sys:</span><span class="sxs-lookup"><span data-stu-id="a8182-240">To change the HTTP.sys queue limit:</span></span>

    - <span data-ttu-id="a8182-241">Otwórz Menedżera usług IIS i przejdź do okienka pul aplikacji.</span><span class="sxs-lookup"><span data-stu-id="a8182-241">Open IIS manager and navigate to the Application Pools pane.</span></span>
    - <span data-ttu-id="a8182-242">Kliknij prawym przyciskiem myszy docelową pulę aplikacji, a następnie wybierz pozycję **Zaawansowane ustawienia**.</span><span class="sxs-lookup"><span data-stu-id="a8182-242">Right click on the target application pool and select **Advanced Settings**.</span></span>  
        <span data-ttu-id="a8182-243">![Zaawansowane](using-asynchronous-methods-in-aspnet-mvc-4/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="a8182-243">![advanced](using-asynchronous-methods-in-aspnet-mvc-4/_static/image4.png)</span></span>
    - <span data-ttu-id="a8182-244">W **Zaawansowane ustawienia** okno dialogowe, zmiana *długość kolejki* od 1000 do 5000.</span><span class="sxs-lookup"><span data-stu-id="a8182-244">In the **Advanced Settings** dialog box, change *Queue Length* from 1,000 to 5,000.</span></span>  
        <span data-ttu-id="a8182-245">![Długość kolejki](using-asynchronous-methods-in-aspnet-mvc-4/_static/image5.png)</span><span class="sxs-lookup"><span data-stu-id="a8182-245">![Queue length](using-asynchronous-methods-in-aspnet-mvc-4/_static/image5.png)</span></span>  
  
  <span data-ttu-id="a8182-246">Należy zwrócić uwagę na obrazach powyżej, .NET framework znajduje się w wersji 4.0, mimo że pula aplikacji jest przy użyciu platformy .NET 4.5.</span><span class="sxs-lookup"><span data-stu-id="a8182-246">Note in the images above, the .NET framework is listed as v4.0, even though the application pool is using .NET 4.5.</span></span> <span data-ttu-id="a8182-247">Aby poznać tę rozbieżność, zobacz następujące tematy:</span><span class="sxs-lookup"><span data-stu-id="a8182-247">To understand this discrepancy, see the following:</span></span>

    - [<span data-ttu-id="a8182-248">Przechowywanie wersji platformy .NET i .NET 4.5 Multi-Targeting — jest uaktualnienie w miejscu do programu .NET 4.0</span><span class="sxs-lookup"><span data-stu-id="a8182-248">.NET Versioning and Multi-Targeting - .NET 4.5 is an in-place upgrade to .NET 4.0</span></span>](http://www.hanselman.com/blog/NETVersioningAndMultiTargetingNET45IsAnInplaceUpgradeToNET40.aspx)
    - [<span data-ttu-id="a8182-249">Jak skonfigurować aplikację usług IIS lub pulę aplikacji do użycia w programie ASP.NET 3.5, a nie w wersji 2.0</span><span class="sxs-lookup"><span data-stu-id="a8182-249">How to set an IIS Application or AppPool to use ASP.NET 3.5 rather than 2.0</span></span>](http://www.hanselman.com/blog/HowToSetAnIISApplicationOrAppPoolToUseASPNET35RatherThan20.aspx)
    - <span data-ttu-id="a8182-250">[Wersje i zależności platformy .NET Framework](https://msdn.microsoft.com/library/bb822049(VS.110).aspx)</span><span class="sxs-lookup"><span data-stu-id="a8182-250">[.NET Framework Versions and Dependencies](https://msdn.microsoft.com/library/bb822049(VS.110).aspx)</span></span>
- <span data-ttu-id="a8182-251">Jeśli aplikacja korzysta z usług sieci web lub może być konieczne zwiększenie przestrzeni nazw System.NET nawiązać połączenia z usługą zaplecza za pośrednictwem protokołu HTTP należy [connectionManagement/maxconnection](https://msdn.microsoft.com/library/fb6y0fyc(VS.110).aspx) elementu.</span><span class="sxs-lookup"><span data-stu-id="a8182-251">If your application is using web services or System.NET to communicate with a backend over HTTP you may need to increase the [connectionManagement/maxconnection](https://msdn.microsoft.com/library/fb6y0fyc(VS.110).aspx) element.</span></span> <span data-ttu-id="a8182-252">Dla aplikacji platformy ASP.NET to jest ograniczona przez funkcję automatycznej konfiguracji do 12 razy liczba procesorów CPU.</span><span class="sxs-lookup"><span data-stu-id="a8182-252">For ASP.NET applications, this is limited by the autoConfig feature to 12 times the number of CPUs.</span></span> <span data-ttu-id="a8182-253">Oznacza to, że na cztery procesory mogą istnieć maksymalnie 12 \* 4 = 48 jednoczesnych połączeń do punktu końcowego adresu IP.</span><span class="sxs-lookup"><span data-stu-id="a8182-253">That means that on a quad-proc, you can have at most 12 \* 4 = 48 concurrent connections to an IP end point.</span></span> <span data-ttu-id="a8182-254">Ponieważ to jest powiązany z [autokonfiguracji](https://msdn.microsoft.com/library/7w2sway1(VS.110).aspx), najłatwiejszy sposób na zwiększenie `maxconnection` na platformie ASP.NET aplikacja ma ustawiony [System.Net.ServicePointManager.DefaultConnectionLimit](https://msdn.microsoft.com/library/system.net.servicepointmanager.defaultconnectionlimit(VS.110).aspx) programowo w z `Application_Start` method in Class metoda *global.asax* pliku.</span><span class="sxs-lookup"><span data-stu-id="a8182-254">Because this is tied to [autoConfig](https://msdn.microsoft.com/library/7w2sway1(VS.110).aspx), the easiest way to increase `maxconnection` in an ASP.NET application is to set [System.Net.ServicePointManager.DefaultConnectionLimit](https://msdn.microsoft.com/library/system.net.servicepointmanager.defaultconnectionlimit(VS.110).aspx) programmatically in the from `Application_Start` method in the *global.asax* file.</span></span> <span data-ttu-id="a8182-255">Zobacz przykład pobrać przykład.</span><span class="sxs-lookup"><span data-stu-id="a8182-255">See the sample download for an example.</span></span>
- <span data-ttu-id="a8182-256">W .NET 4.5, wartość domyślna 5000 dla [MaxConcurrentRequestsPerCPU](https://blogs.msdn.com/tmarq/archive/2007/07/21/asp-net-thread-usage-on-iis-7-0-and-6-0.aspx) powinny być dobrym stanie.</span><span class="sxs-lookup"><span data-stu-id="a8182-256">In .NET 4.5, the default of 5000 for [MaxConcurrentRequestsPerCPU](https://blogs.msdn.com/tmarq/archive/2007/07/21/asp-net-thread-usage-on-iis-7-0-and-6-0.aspx) should be fine.</span></span>
