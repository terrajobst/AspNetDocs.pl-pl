---
uid: mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-cs
title: Omówienie filtrów akcji (C#) | Microsoft Docs
author: microsoft
description: Celem tego samouczka jest wyjaśnienie filtrów akcji. Filtr akcji jest atrybutem, który można zastosować do akcji kontrolera — lub całego kontrolera...
ms.author: riande
ms.date: 10/16/2008
ms.assetid: a94e4e81-40c1-47b7-8613-126a1a6cc93d
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-cs
msc.type: authoredcontent
ms.openlocfilehash: d1c72c2355c6122f851351a8c1e8f04fa63ae04e
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/06/2020
ms.locfileid: "78581824"
---
# <a name="understanding-action-filters-c"></a><span data-ttu-id="f062e-104">Objaśnienie filtrów akcji (C#)</span><span class="sxs-lookup"><span data-stu-id="f062e-104">Understanding Action Filters (C#)</span></span>

<span data-ttu-id="f062e-105">przez [firmę Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="f062e-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="f062e-106">Pobierz plik PDF</span><span class="sxs-lookup"><span data-stu-id="f062e-106">Download PDF</span></span>](https://download.microsoft.com/download/e/f/3/ef3f2ff6-7424-48f7-bdaa-180ef64c3490/ASPNET_MVC_Tutorial_14_CS.pdf)

> <span data-ttu-id="f062e-107">Celem tego samouczka jest wyjaśnienie filtrów akcji.</span><span class="sxs-lookup"><span data-stu-id="f062e-107">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="f062e-108">Filtr akcji jest atrybutem, który można zastosować do akcji kontrolera--lub całego kontrolera — który modyfikuje sposób wykonywania akcji.</span><span class="sxs-lookup"><span data-stu-id="f062e-108">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span>

## <a name="understanding-action-filters"></a><span data-ttu-id="f062e-109">Omówienie filtrów akcji</span><span class="sxs-lookup"><span data-stu-id="f062e-109">Understanding Action Filters</span></span>

<span data-ttu-id="f062e-110">Celem tego samouczka jest wyjaśnienie filtrów akcji.</span><span class="sxs-lookup"><span data-stu-id="f062e-110">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="f062e-111">Filtr akcji jest atrybutem, który można zastosować do akcji kontrolera--lub całego kontrolera — który modyfikuje sposób wykonywania akcji.</span><span class="sxs-lookup"><span data-stu-id="f062e-111">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span> <span data-ttu-id="f062e-112">Struktura ASP.NET MVC zawiera kilka filtrów akcji:</span><span class="sxs-lookup"><span data-stu-id="f062e-112">The ASP.NET MVC framework includes several action filters:</span></span>

- <span data-ttu-id="f062e-113">OutputCache — ta akcja filtr umożliwia buforowanie danych wyjściowych akcji kontrolera przez określony czas.</span><span class="sxs-lookup"><span data-stu-id="f062e-113">OutputCache – This action filter caches the output of a controller action for a specified amount of time.</span></span>
- <span data-ttu-id="f062e-114">HandleError — ten filtr akcji obsługuje błędy wywoływane, gdy akcja kontrolera zostanie wykonana.</span><span class="sxs-lookup"><span data-stu-id="f062e-114">HandleError – This action filter handles errors raised when a controller action executes.</span></span>
- <span data-ttu-id="f062e-115">Autoryzuj — ten filtr akcji umożliwia ograniczenie dostępu do określonego użytkownika lub roli.</span><span class="sxs-lookup"><span data-stu-id="f062e-115">Authorize – This action filter enables you to restrict access to a particular user or role.</span></span>

<span data-ttu-id="f062e-116">Możesz również utworzyć własne niestandardowe filtry akcji.</span><span class="sxs-lookup"><span data-stu-id="f062e-116">You also can create your own custom action filters.</span></span> <span data-ttu-id="f062e-117">Na przykład można utworzyć niestandardowy filtr akcji w celu zaimplementowania niestandardowego systemu uwierzytelniania.</span><span class="sxs-lookup"><span data-stu-id="f062e-117">For example, you might want to create a custom action filter in order to implement a custom authentication system.</span></span> <span data-ttu-id="f062e-118">Można też utworzyć filtr akcji, który modyfikuje dane widoku zwrócone przez akcję kontrolera.</span><span class="sxs-lookup"><span data-stu-id="f062e-118">Or, you might want to create an action filter that modifies the view data returned by a controller action.</span></span>

<span data-ttu-id="f062e-119">W tym samouczku dowiesz się, jak skompilować filtr akcji od podstaw.</span><span class="sxs-lookup"><span data-stu-id="f062e-119">In this tutorial, you learn how to build an action filter from the ground up.</span></span> <span data-ttu-id="f062e-120">Tworzymy filtr akcji dziennika, który rejestruje różne etapy przetwarzania akcji w oknie danych wyjściowych programu Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="f062e-120">We create a Log action filter that logs different stages of the processing of an action to the Visual Studio Output window.</span></span>

### <a name="using-an-action-filter"></a><span data-ttu-id="f062e-121">Korzystanie z filtru akcji</span><span class="sxs-lookup"><span data-stu-id="f062e-121">Using an Action Filter</span></span>

<span data-ttu-id="f062e-122">Filtr akcji jest atrybutem.</span><span class="sxs-lookup"><span data-stu-id="f062e-122">An action filter is an attribute.</span></span> <span data-ttu-id="f062e-123">Większość filtrów akcji można zastosować do poszczególnych akcji kontrolera lub całego kontrolera.</span><span class="sxs-lookup"><span data-stu-id="f062e-123">You can apply most action filters to either an individual controller action or an entire controller.</span></span>

<span data-ttu-id="f062e-124">Na przykład kontroler danych na liście 1 uwidacznia akcję o nazwie `Index()`, która zwraca bieżący czas.</span><span class="sxs-lookup"><span data-stu-id="f062e-124">For example, the Data controller in Listing 1 exposes an action named `Index()` that returns the current time.</span></span> <span data-ttu-id="f062e-125">Ta akcja ma `OutputCache` filtr akcji.</span><span class="sxs-lookup"><span data-stu-id="f062e-125">This action is decorated with the `OutputCache` action filter.</span></span> <span data-ttu-id="f062e-126">Ten filtr powoduje, że wartość zwracana przez akcję jest buforowana przez 10 sekund.</span><span class="sxs-lookup"><span data-stu-id="f062e-126">This filter causes the value returned by the action to be cached for 10 seconds.</span></span>

<span data-ttu-id="f062e-127">**Lista 1 — `Controllers\DataController.cs`**</span><span class="sxs-lookup"><span data-stu-id="f062e-127">**Listing 1 – `Controllers\DataController.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample1.cs)]

<span data-ttu-id="f062e-128">Jeśli akcja `Index()` zostanie wielokrotnie wywołana przez wprowadzenie adresu URL/Data/Index na pasku adresu przeglądarki i naciśnięcie przycisku odświeżania wiele razy, zobaczysz ten sam czas przez 10 sekund.</span><span class="sxs-lookup"><span data-stu-id="f062e-128">If you repeatedly invoke the `Index()` action by entering the URL /Data/Index into the address bar of your browser and hitting the Refresh button multiple times, then you will see the same time for 10 seconds.</span></span> <span data-ttu-id="f062e-129">Dane wyjściowe akcji `Index()` są buforowane przez 10 sekund (patrz rysunek 1).</span><span class="sxs-lookup"><span data-stu-id="f062e-129">The output of the `Index()` action is cached for 10 seconds (see Figure 1).</span></span>

<span data-ttu-id="f062e-130">[![pamięci podręcznej](understanding-action-filters-cs/_static/image2.png)](understanding-action-filters-cs/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="f062e-130">[![Cached time](understanding-action-filters-cs/_static/image2.png)](understanding-action-filters-cs/_static/image1.png)</span></span>

<span data-ttu-id="f062e-131">**Ilustracja 01**: buforowany czas ([kliknij, aby wyświetlić obraz w pełnym rozmiarze](understanding-action-filters-cs/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="f062e-131">**Figure 01**: Cached time ([Click to view full-size image](understanding-action-filters-cs/_static/image3.png))</span></span>

<span data-ttu-id="f062e-132">W przypadku wybrania jednego filtru akcji w ramach listy 1 do metody `Index()` jest stosowany filtr akcji `OutputCache`.</span><span class="sxs-lookup"><span data-stu-id="f062e-132">In Listing 1, a single action filter – the `OutputCache` action filter – is applied to the `Index()` method.</span></span> <span data-ttu-id="f062e-133">W razie potrzeby można zastosować wiele filtrów akcji do tej samej akcji.</span><span class="sxs-lookup"><span data-stu-id="f062e-133">If you need, you can apply multiple action filters to the same action.</span></span> <span data-ttu-id="f062e-134">Na przykład możesz chcieć zastosować filtry akcji `OutputCache` i `HandleError` do tej samej akcji.</span><span class="sxs-lookup"><span data-stu-id="f062e-134">For example, you might want to apply both the `OutputCache` and `HandleError` action filters to the same action.</span></span>

<span data-ttu-id="f062e-135">Na liście 1 Filtr akcji `OutputCache` jest stosowany do akcji `Index()`.</span><span class="sxs-lookup"><span data-stu-id="f062e-135">In Listing 1, the `OutputCache` action filter is applied to the `Index()` action.</span></span> <span data-ttu-id="f062e-136">Ten atrybut można również zastosować do samej klasy `DataController`.</span><span class="sxs-lookup"><span data-stu-id="f062e-136">You also could apply this attribute to the `DataController` class itself.</span></span> <span data-ttu-id="f062e-137">W takim przypadku wynik zwrócony przez wszystkie akcje uwidocznione przez kontroler jest buforowany przez 10 sekund.</span><span class="sxs-lookup"><span data-stu-id="f062e-137">In that case, the result returned by any action exposed by the controller would be cached for 10 seconds.</span></span>

### <a name="the-different-types-of-filters"></a><span data-ttu-id="f062e-138">Różne typy filtrów</span><span class="sxs-lookup"><span data-stu-id="f062e-138">The Different Types of Filters</span></span>

<span data-ttu-id="f062e-139">Platforma ASP.NET MVC obsługuje cztery różne typy filtrów:</span><span class="sxs-lookup"><span data-stu-id="f062e-139">The ASP.NET MVC framework supports four different types of filters:</span></span>

1. <span data-ttu-id="f062e-140">Filtry autoryzacji — implementuje atrybut `IAuthorizationFilter`.</span><span class="sxs-lookup"><span data-stu-id="f062e-140">Authorization filters – Implements the `IAuthorizationFilter` attribute.</span></span>
2. <span data-ttu-id="f062e-141">Filtry akcji — implementuje atrybut `IActionFilter`.</span><span class="sxs-lookup"><span data-stu-id="f062e-141">Action filters – Implements the `IActionFilter` attribute.</span></span>
3. <span data-ttu-id="f062e-142">Filtry wyników — implementuje atrybut `IResultFilter`.</span><span class="sxs-lookup"><span data-stu-id="f062e-142">Result filters – Implements the `IResultFilter` attribute.</span></span>
4. <span data-ttu-id="f062e-143">Filtry wyjątków — implementuje atrybut `IExceptionFilter`.</span><span class="sxs-lookup"><span data-stu-id="f062e-143">Exception filters – Implements the `IExceptionFilter` attribute.</span></span>

<span data-ttu-id="f062e-144">Filtry są wykonywane w podanej kolejności.</span><span class="sxs-lookup"><span data-stu-id="f062e-144">Filters are executed in the order listed above.</span></span> <span data-ttu-id="f062e-145">Na przykład filtry autoryzacji są zawsze wykonywane, zanim filtry akcji i filtry wyjątków są zawsze wykonywane po każdym innym typie filtru.</span><span class="sxs-lookup"><span data-stu-id="f062e-145">For example, authorization filters are always executed before action filters and exception filters are always executed after every other type of filter.</span></span>

<span data-ttu-id="f062e-146">Filtry autoryzacji służą do implementowania uwierzytelniania i autoryzacji dla akcji kontrolera.</span><span class="sxs-lookup"><span data-stu-id="f062e-146">Authorization filters are used to implement authentication and authorization for controller actions.</span></span> <span data-ttu-id="f062e-147">Na przykład filtr Autoryzuj jest przykładem filtru autoryzacji.</span><span class="sxs-lookup"><span data-stu-id="f062e-147">For example, the Authorize filter is an example of an Authorization filter.</span></span>

<span data-ttu-id="f062e-148">Filtry akcji zawierają logikę wykonywaną przed wykonaniem akcji kontrolera i po niej.</span><span class="sxs-lookup"><span data-stu-id="f062e-148">Action filters contain logic that is executed before and after a controller action executes.</span></span> <span data-ttu-id="f062e-149">Możesz użyć filtru akcji, na przykład, aby zmodyfikować dane widoku, które zwraca akcja kontrolera.</span><span class="sxs-lookup"><span data-stu-id="f062e-149">You can use an action filter, for instance, to modify the view data that a controller action returns.</span></span>

<span data-ttu-id="f062e-150">Filtry wyników zawierają logikę wykonywaną przed i po wykonaniu wyniku widoku.</span><span class="sxs-lookup"><span data-stu-id="f062e-150">Result filters contain logic that is executed before and after a view result is executed.</span></span> <span data-ttu-id="f062e-151">Na przykład możesz chcieć zmodyfikować wynik widoku bezpośrednio przed renderowaniem widoku w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="f062e-151">For example, you might want to modify a view result right before the view is rendered to the browser.</span></span>

<span data-ttu-id="f062e-152">Filtry wyjątków to ostatni typ filtru do uruchomienia.</span><span class="sxs-lookup"><span data-stu-id="f062e-152">Exception filters are the last type of filter to run.</span></span> <span data-ttu-id="f062e-153">Możesz użyć filtru wyjątków, aby obsłużyć błędy wywoływane przez akcje kontrolera lub wyniki akcji kontrolera.</span><span class="sxs-lookup"><span data-stu-id="f062e-153">You can use an exception filter to handle errors raised by either your controller actions or controller action results.</span></span> <span data-ttu-id="f062e-154">Można także użyć filtrów wyjątków, aby rejestrować błędy.</span><span class="sxs-lookup"><span data-stu-id="f062e-154">You also can use exception filters to log errors.</span></span>

<span data-ttu-id="f062e-155">Każdy inny typ filtru jest wykonywany w określonej kolejności.</span><span class="sxs-lookup"><span data-stu-id="f062e-155">Each different type of filter is executed in a particular order.</span></span> <span data-ttu-id="f062e-156">Jeśli chcesz kontrolować kolejność, w której są wykonywane filtry tego samego typu, można ustawić właściwość kolejność filtru.</span><span class="sxs-lookup"><span data-stu-id="f062e-156">If you want to control the order in which filters of the same type are executed then you can set a filter's Order property.</span></span>

<span data-ttu-id="f062e-157">Klasa bazowa dla wszystkich filtrów akcji jest klasą `System.Web.Mvc.FilterAttribute`.</span><span class="sxs-lookup"><span data-stu-id="f062e-157">The base class for all action filters is the `System.Web.Mvc.FilterAttribute` class.</span></span> <span data-ttu-id="f062e-158">Aby zaimplementować określony typ filtru, należy utworzyć klasę, która dziedziczy z klasy bazowego filtru i implementuje co najmniej jeden `IAuthorizationFilter`, `IActionFilter`, `IResultFilter`lub `IExceptionFilter`.</span><span class="sxs-lookup"><span data-stu-id="f062e-158">If you want to implement a particular type of filter, then you need to create a class that inherits from the base Filter class and implements one or more of the `IAuthorizationFilter`, `IActionFilter`, `IResultFilter`, or `IExceptionFilter` interfaces.</span></span>

### <a name="the-base-actionfilterattribute-class"></a><span data-ttu-id="f062e-159">Podstawowa klasa ActionFilterAttribute</span><span class="sxs-lookup"><span data-stu-id="f062e-159">The Base ActionFilterAttribute Class</span></span>

<span data-ttu-id="f062e-160">Aby ułatwić implementowanie niestandardowego filtru akcji, struktura ASP.NET MVC zawiera podstawową klasę `ActionFilterAttribute`.</span><span class="sxs-lookup"><span data-stu-id="f062e-160">In order to make it easier for you to implement a custom action filter, the ASP.NET MVC framework includes a base `ActionFilterAttribute` class.</span></span> <span data-ttu-id="f062e-161">Ta klasa implementuje zarówno interfejsy `IActionFilter`, jak i `IResultFilter` i dziedziczy z klasy `Filter`.</span><span class="sxs-lookup"><span data-stu-id="f062e-161">This class implements both the `IActionFilter` and `IResultFilter` interfaces and inherits from the `Filter` class.</span></span>

<span data-ttu-id="f062e-162">Terminologia nie jest całkowicie spójna.</span><span class="sxs-lookup"><span data-stu-id="f062e-162">The terminology here is not entirely consistent.</span></span> <span data-ttu-id="f062e-163">Technicznie Klasa, która dziedziczy z klasy ActionFilterAttribute, jest zarówno filtrem akcji, jak i filtrem wynikowym.</span><span class="sxs-lookup"><span data-stu-id="f062e-163">Technically, a class that inherits from the ActionFilterAttribute class is both an action filter and a result filter.</span></span> <span data-ttu-id="f062e-164">Jednak w tym sensie filtr akcji programu Word służy do odwoływania się do dowolnego typu filtru w strukturze ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="f062e-164">However, in the loose sense, the word action filter is used to refer to any type of filter in the ASP.NET MVC framework.</span></span>

<span data-ttu-id="f062e-165">Klasa bazowa `ActionFilterAttribute` ma następujące metody, które można przesłonić:</span><span class="sxs-lookup"><span data-stu-id="f062e-165">The base `ActionFilterAttribute` class has the following methods that you can override:</span></span>

- <span data-ttu-id="f062e-166">OnActionExecuting — ta metoda jest wywoływana przed wykonaniem akcji kontrolera.</span><span class="sxs-lookup"><span data-stu-id="f062e-166">OnActionExecuting – This method is called before a controller action is executed.</span></span>
- <span data-ttu-id="f062e-167">OnActionExecuted — ta metoda jest wywoływana po wykonaniu akcji kontrolera.</span><span class="sxs-lookup"><span data-stu-id="f062e-167">OnActionExecuted – This method is called after a controller action is executed.</span></span>
- <span data-ttu-id="f062e-168">OnResultExecuting — ta metoda jest wywoływana przed wykonaniem wyniku akcji kontrolera.</span><span class="sxs-lookup"><span data-stu-id="f062e-168">OnResultExecuting – This method is called before a controller action result is executed.</span></span>
- <span data-ttu-id="f062e-169">OnResultExecuted — ta metoda jest wywoływana po wykonaniu wyniku akcji kontrolera.</span><span class="sxs-lookup"><span data-stu-id="f062e-169">OnResultExecuted – This method is called after a controller action result is executed.</span></span>

<span data-ttu-id="f062e-170">W następnej sekcji zobaczymy, jak można zaimplementować każdą z tych różnych metod.</span><span class="sxs-lookup"><span data-stu-id="f062e-170">In the next section, we'll see how you can implement each of these different methods.</span></span>

### <a name="creating-a-log-action-filter"></a><span data-ttu-id="f062e-171">Tworzenie filtru akcji dziennika</span><span class="sxs-lookup"><span data-stu-id="f062e-171">Creating a Log Action Filter</span></span>

<span data-ttu-id="f062e-172">Aby zilustrować, jak można skompilować filtr akcji niestandardowych, utworzymy niestandardowy filtr akcji, który rejestruje etapy przetwarzania akcji kontrolera w oknie danych wyjściowych programu Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="f062e-172">In order to illustrate how you can build a custom action filter, we'll create a custom action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span> <span data-ttu-id="f062e-173">Nasze `LogActionFilter` znajdują się na liście 2.</span><span class="sxs-lookup"><span data-stu-id="f062e-173">Our `LogActionFilter` is contained in Listing 2.</span></span>

<span data-ttu-id="f062e-174">**Lista 2 — `ActionFilters\LogActionFilter.cs`**</span><span class="sxs-lookup"><span data-stu-id="f062e-174">**Listing 2 – `ActionFilters\LogActionFilter.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample2.cs)]

<span data-ttu-id="f062e-175">Na liście 2 metody `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`i `OnResultExecuted()` wywołują metodę `Log()`.</span><span class="sxs-lookup"><span data-stu-id="f062e-175">In Listing 2, the `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, and `OnResultExecuted()` methods all call the `Log()` method.</span></span> <span data-ttu-id="f062e-176">Nazwa metody i dane bieżącej trasy są przesyłane do metody `Log()`.</span><span class="sxs-lookup"><span data-stu-id="f062e-176">The name of the method and the current route data is passed to the `Log()` method.</span></span> <span data-ttu-id="f062e-177">Metoda `Log()` zapisuje komunikat do okna danych wyjściowych programu Visual Studio (patrz rysunek 2).</span><span class="sxs-lookup"><span data-stu-id="f062e-177">The `Log()` method writes a message to the Visual Studio Output window (see Figure 2).</span></span>

<span data-ttu-id="f062e-178">[![zapisywania do okna danych wyjściowych programu Visual Studio](understanding-action-filters-cs/_static/image5.png)](understanding-action-filters-cs/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="f062e-178">[![Writing to the Visual Studio Output window](understanding-action-filters-cs/_static/image5.png)](understanding-action-filters-cs/_static/image4.png)</span></span>

<span data-ttu-id="f062e-179">**Ilustracja 02**. zapisywanie do okna danych wyjściowych programu Visual Studio ([kliknij, aby wyświetlić obraz o pełnym rozmiarze](understanding-action-filters-cs/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="f062e-179">**Figure 02**: Writing to the Visual Studio Output window ([Click to view full-size image](understanding-action-filters-cs/_static/image6.png))</span></span>

<span data-ttu-id="f062e-180">Kontroler Home na liście 3 ilustruje, jak można zastosować filtr akcji dziennika do całej klasy kontrolera.</span><span class="sxs-lookup"><span data-stu-id="f062e-180">The Home controller in Listing 3 illustrates how you can apply the Log action filter to an entire controller class.</span></span> <span data-ttu-id="f062e-181">Za każdym razem, gdy wszystkie akcje uwidocznione przez kontroler macierzysty są wywoływane — Metoda `Index()` lub metoda `About()` — etapy przetwarzania akcji są rejestrowane w oknie danych wyjściowych programu Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="f062e-181">Whenever any of the actions exposed by the Home controller are invoked – either the `Index()` method or the `About()` method – the stages of processing the action are logged to the Visual Studio Output window.</span></span>

<span data-ttu-id="f062e-182">**Lista 3 — `Controllers\HomeController.cs`**</span><span class="sxs-lookup"><span data-stu-id="f062e-182">**Listing 3 – `Controllers\HomeController.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample3.cs)]

### <a name="summary"></a><span data-ttu-id="f062e-183">Podsumowanie</span><span class="sxs-lookup"><span data-stu-id="f062e-183">Summary</span></span>

<span data-ttu-id="f062e-184">W tym samouczku wprowadzono do ASP.NET filtrów akcji MVC.</span><span class="sxs-lookup"><span data-stu-id="f062e-184">In this tutorial, you were introduced to ASP.NET MVC action filters.</span></span> <span data-ttu-id="f062e-185">Zapoznaj się z czterema różnymi typami filtrów: filtry autoryzacji, filtry akcji, filtry wynikowe i filtry wyjątków.</span><span class="sxs-lookup"><span data-stu-id="f062e-185">You learned about the four different types of filters: authorization filters, action filters, result filters, and exception filters.</span></span> <span data-ttu-id="f062e-186">Zapoznaj się również z klasą `ActionFilterAttribute` podstawowej.</span><span class="sxs-lookup"><span data-stu-id="f062e-186">You also learned about the base `ActionFilterAttribute` class.</span></span>

<span data-ttu-id="f062e-187">Wreszcie zawiesz się, jak zaimplementować prosty filtr akcji.</span><span class="sxs-lookup"><span data-stu-id="f062e-187">Finally, you learned how to implement a simple action filter.</span></span> <span data-ttu-id="f062e-188">Utworzyliśmy filtr akcji dziennika, który rejestruje etapy przetwarzania akcji kontrolera w oknie danych wyjściowych programu Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="f062e-188">We created a Log action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="f062e-189">[Poprzednie](asp-net-mvc-routing-overview-cs.md)
> [dalej](improving-performance-with-output-caching-cs.md)</span><span class="sxs-lookup"><span data-stu-id="f062e-189">[Previous](asp-net-mvc-routing-overview-cs.md)
[Next](improving-performance-with-output-caching-cs.md)</span></span>
