---
uid: mvc/overview/older-versions-1/getting-started-with-mvc/getting-started-with-mvc-part3
title: Dodawanie widoku | Microsoft Docs
author: shanselman
description: Jest to samouczek początkującego, który wprowadza podstawy ASP.NET MVC. Utwórz prostą aplikację sieci Web, która odczytuje i zapisuje dane w bazie danych.
ms.author: riande
ms.date: 08/14/2010
ms.assetid: e8f1515c-c277-47ff-a23e-224118f13f02
msc.legacyurl: /mvc/overview/older-versions-1/getting-started-with-mvc/getting-started-with-mvc-part3
msc.type: authoredcontent
ms.openlocfilehash: 462b1210c45da67058899193afcea973f3daf122
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/06/2020
ms.locfileid: "78581558"
---
# <a name="adding-a-view"></a><span data-ttu-id="ddeaa-104">Dodawanie widoku</span><span class="sxs-lookup"><span data-stu-id="ddeaa-104">Adding a View</span></span>

<span data-ttu-id="ddeaa-105">przez [Scott Hanselman](https://github.com/shanselman)</span><span class="sxs-lookup"><span data-stu-id="ddeaa-105">by [Scott Hanselman](https://github.com/shanselman)</span></span>

> <span data-ttu-id="ddeaa-106">Jest to samouczek początkującego, który wprowadza podstawy ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-106">This is a beginner tutorial that introduces the basics of ASP.NET MVC.</span></span> <span data-ttu-id="ddeaa-107">Utworzysz prostą aplikację sieci Web, która odczytuje i zapisuje dane w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-107">You'll create a simple web application that reads and writes from a database.</span></span> <span data-ttu-id="ddeaa-108">Odwiedź [centrum learning ASP.NET MVC](../../../index.md) , aby znaleźć inne samouczki i przykłady MVC ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-108">Visit the [ASP.NET MVC learning center](../../../index.md) to find other ASP.NET MVC tutorials and samples.</span></span>

<span data-ttu-id="ddeaa-109">W tej sekcji zamierzamy zajrzeć się, w jaki sposób możemy użyć naszego klasy HelloWorldController, aby wyczyścić generowanie odpowiedzi HTML z powrotem do klienta.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-109">In this section we are going to look at how we can have our HelloWorldController class use a View template file to cleanly encapsulate generating HTML responses back to a client.</span></span>

<span data-ttu-id="ddeaa-110">Zacznijmy od użycia szablonu widoku z naszymi metodami indeksu.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-110">Let's start by using a View template with our Index method.</span></span> <span data-ttu-id="ddeaa-111">Nasza metoda jest nazywana indeksem i znajduje się w HelloWorldController.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-111">Our method is called Index and it's in the HelloWorldController.</span></span> <span data-ttu-id="ddeaa-112">Obecnie nasza metoda index () zwraca ciąg z komunikatem, który jest stałe w ramach klasy Controller.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-112">Currently our Index() method returns a string with a message that is hardcoded within the Controller class.</span></span>

[!code-csharp[Main](getting-started-with-mvc-part3/samples/sample1.cs)]

<span data-ttu-id="ddeaa-113">Zmieńmy teraz metodę index, aby zamiast tego wyglądać następująco:</span><span class="sxs-lookup"><span data-stu-id="ddeaa-113">Let's now change the Index method to instead look like this:</span></span>

[!code-csharp[Main](getting-started-with-mvc-part3/samples/sample2.cs)]

<span data-ttu-id="ddeaa-114">Teraz dodamy szablon widoku do naszego projektu, którego możemy użyć dla naszego metody index ().</span><span class="sxs-lookup"><span data-stu-id="ddeaa-114">Let's now add a View template to our project that we can use for our Index() method.</span></span> <span data-ttu-id="ddeaa-115">Aby to zrobić, kliknij prawym przyciskiem myszy myszą w środku metody indeksu i kliknij polecenie Dodaj widok...</span><span class="sxs-lookup"><span data-stu-id="ddeaa-115">To do this, right-click with your mouse somewhere in the middle of the Index method and click Add View...</span></span>

![image](getting-started-with-mvc-part3/_static/image1.png)

<span data-ttu-id="ddeaa-117">Spowoduje to wyświetlenie okna dialogowego "Dodawanie widoku", które zapewnia nam kilka opcji tworzenia szablonu widoku, który może być używany przez naszą metodę index.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-117">This will bring up the "Add View" dialog which provides us some options for how we want to create a view template that can be used by our Index method.</span></span> <span data-ttu-id="ddeaa-118">Na razie nie zmieniaj niczego i po prostu kliknij przycisk Dodaj.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-118">For now, don't change anything, and just click the Add button.</span></span>

<span data-ttu-id="ddeaa-119">[Okno dialogowe dodawania widoku ![](getting-started-with-mvc-part3/_static/image3.png)](getting-started-with-mvc-part3/_static/image2.png)</span><span class="sxs-lookup"><span data-stu-id="ddeaa-119">[![Add View Dialog](getting-started-with-mvc-part3/_static/image3.png)](getting-started-with-mvc-part3/_static/image2.png)</span></span>

<span data-ttu-id="ddeaa-120">Po kliknięciu przycisku Dodaj nowy folder i nowy plik pojawi się w folderze rozwiązania, jak pokazano tutaj.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-120">After you click Add, a new folder and a new file will appear in the Solution Folder, as seen here.</span></span> <span data-ttu-id="ddeaa-121">Teraz mam folder HelloWorld w obszarze widoki i plik index. aspx znajdujący się w tym folderze.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-121">I now have a HelloWorld folder under Views, and an Index.aspx file inside that folder.</span></span>

<span data-ttu-id="ddeaa-122">[![solutionexplorerwithindex](getting-started-with-mvc-part3/_static/image5.png)](getting-started-with-mvc-part3/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="ddeaa-122">[![solutionexplorerwithindex](getting-started-with-mvc-part3/_static/image5.png)](getting-started-with-mvc-part3/_static/image4.png)</span></span>

<span data-ttu-id="ddeaa-123">Nowy plik indeksu jest również otwarty i gotowy do edycji.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-123">The new Index file is also already opened and ready for editing.</span></span> <span data-ttu-id="ddeaa-124">Dodaj tekst poniżej pierwszej &lt;H2&gt;index&lt;/H2&gt; na przykład "Hello world".</span><span class="sxs-lookup"><span data-stu-id="ddeaa-124">Add some text under the first &lt;h2&gt;Index&lt;/h2&gt; like "Hello World."</span></span>

[!code-html[Main](getting-started-with-mvc-part3/samples/sample3.html)]

<span data-ttu-id="ddeaa-125">Uruchom aplikację i ponownie odwiedź [`http://localhost:xx/HelloWorld`](http://localhostxx) w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-125">Run your application and visit [`http://localhost:xx/HelloWorld`](http://localhostxx) again in your browser.</span></span> <span data-ttu-id="ddeaa-126">Metoda index w naszym kontrolerze w tym przykładzie nie wykonała żadnej pracy, ale wywołała "zwracany widok ()", który wskazywał, że chciałem użyć pliku szablonu widoku w celu renderowania odpowiedzi z powrotem do klienta.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-126">The Index method in our controller in this example didn't do any work, but did call "return View()" which indicated that we wanted to use a view template file to render a response back to the client.</span></span> <span data-ttu-id="ddeaa-127">Ponieważ nie określono jawnie nazwy pliku szablonu widoku, ASP.NET MVC domyślnie przy użyciu pliku widoku index. aspx znajdującego się w folderze \Views\HelloWorld.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-127">Because we did not explicitly specify the name of the view template file to use, ASP.NET MVC defaulted to using the Index.aspx view file within the \Views\HelloWorld folder.</span></span> <span data-ttu-id="ddeaa-128">Teraz zobaczymy ciąg, który został zakodowany w naszym widoku.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-128">Now we see the string we hard-coded in our View.</span></span>

<span data-ttu-id="ddeaa-129">[Indeks ![— Windows Internet Explorer](getting-started-with-mvc-part3/_static/image7.png)](getting-started-with-mvc-part3/_static/image6.png)</span><span class="sxs-lookup"><span data-stu-id="ddeaa-129">[![Index - Windows Internet Explorer](getting-started-with-mvc-part3/_static/image7.png)](getting-started-with-mvc-part3/_static/image6.png)</span></span>

<span data-ttu-id="ddeaa-130">Wyglądają dość dobre.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-130">Looks pretty good.</span></span> <span data-ttu-id="ddeaa-131">Należy jednak zauważyć, że tytuł przeglądarki mówi "index", a duży tytuł na stronie ma wartość "moja aplikacja MVC".</span><span class="sxs-lookup"><span data-stu-id="ddeaa-131">However, notice that the Browser's title says "Index" and the big title on the page says "My MVC Application."</span></span> <span data-ttu-id="ddeaa-132">Zmieńmy te.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-132">Let's change those.</span></span>

### <a name="changing-views-and-master-pages"></a><span data-ttu-id="ddeaa-133">Zmiana widoków i stron wzorcowych</span><span class="sxs-lookup"><span data-stu-id="ddeaa-133">Changing Views and Master Pages</span></span>

<span data-ttu-id="ddeaa-134">Najpierw Zmieńmy tekst "moja aplikacja MVC".</span><span class="sxs-lookup"><span data-stu-id="ddeaa-134">First, let's change the text "My MVC Application."</span></span> <span data-ttu-id="ddeaa-135">Ten tekst jest współużytkowany i pojawia się na każdej stronie.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-135">That text is shared and appears on every page.</span></span> <span data-ttu-id="ddeaa-136">Faktycznie występuje tylko w jednym miejscu w naszym kodzie, nawet jeśli znajduje się na każdej stronie w naszej aplikacji.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-136">It actually appears in only one place in our code, even though it's on every page in our app.</span></span> <span data-ttu-id="ddeaa-137">Przejdź do folderu/Views/Shared w Eksplorator rozwiązań i Otwórz plik site. Master.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-137">Go to the /Views/Shared folder in the Solution Explorer and open the Site.Master file.</span></span> <span data-ttu-id="ddeaa-138">Ten plik jest nazywany stroną wzorcową i jest udostępnianą "powłoką", którą używają wszystkie inne strony.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-138">This file is called a Master Page and it's the shared "shell" that all our other pages use.</span></span>

<span data-ttu-id="ddeaa-139">Zauważ, że w tym pliku jest wyświetlany tekst "kontrolka mainContent".</span><span class="sxs-lookup"><span data-stu-id="ddeaa-139">Notice some text that says ContentPlaceholder "MainContent" in this file.</span></span>

[!code-aspx[Main](getting-started-with-mvc-part3/samples/sample4.aspx)]

<span data-ttu-id="ddeaa-140">Ten symbol zastępczy to miejsce, w którym wszystkie utworzone strony zostaną wyświetlone na stronie wzorcowej.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-140">That placeholder is where all the pages you create will show up, "wrapped" in the master page.</span></span> <span data-ttu-id="ddeaa-141">Spróbuj zmienić tytuł, a następnie uruchom aplikację i odwiedź kilka stron.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-141">Try changing the title, then run your app and visit multiple pages.</span></span> <span data-ttu-id="ddeaa-142">Zobaczysz, że jedna zmiana zostanie wyświetlona na wielu stronach.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-142">You'll notice that your one change appears on multiple pages.</span></span>

[!code-html[Main](getting-started-with-mvc-part3/samples/sample5.html)]

<span data-ttu-id="ddeaa-143">Teraz każda strona będzie miała nagłówek podstawowy — czyli H1-of "moja aplikacja filmów MVC".</span><span class="sxs-lookup"><span data-stu-id="ddeaa-143">Now every page will have the Primary Heading - that's H1 - of "My MVC Movie Application."</span></span> <span data-ttu-id="ddeaa-144">Obsługuje on biały tekst na górze, który jest udostępniany na wszystkich stronach.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-144">That handles the white text at the top there that's shared across all the pages.</span></span>

<span data-ttu-id="ddeaa-145">Oto witryna. Master w całości z naszym zmienionym tytułem:</span><span class="sxs-lookup"><span data-stu-id="ddeaa-145">Here is the Site.Master in its entirety with our changed title:</span></span>

[!code-aspx[Main](getting-started-with-mvc-part3/samples/sample6.aspx)]

<span data-ttu-id="ddeaa-146">Teraz Zmieńmy tytuł strony indeksu.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-146">Now, let's change the title of the Index page.</span></span>

<span data-ttu-id="ddeaa-147">Open /HelloWorld/Index.aspx.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-147">Open /HelloWorld/Index.aspx.</span></span> <span data-ttu-id="ddeaa-148">Istnieją dwa miejsca do zmiany.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-148">There's two places to change.</span></span> <span data-ttu-id="ddeaa-149">Po pierwsze tytuł, który pojawia się w tytule przeglądarki, a następnie pomocniczy nagłówek — to również H2.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-149">First, the Title that appears in the title of the browser, then the secondary header - that's H2 - as well.</span></span> <span data-ttu-id="ddeaa-150">Zrobię je nieco inaczej, aby zobaczyć, który bit kodu zmienia się w ramach aplikacji.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-150">I'll make them each slightly different so you can see which bit of code changes which part of the app.</span></span>

[!code-aspx[Main](getting-started-with-mvc-part3/samples/sample7.aspx)]

<span data-ttu-id="ddeaa-151">Uruchom aplikację i odwiedź witrynę/Movies.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-151">Run your app and visit /Movies.</span></span> <span data-ttu-id="ddeaa-152">Zwróć uwagę, że tytuł przeglądarki, nagłówek podstawowy i nagłówki pomocnicze uległy zmianie.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-152">Notice that the browser title, the primary heading and the secondary headings have changed.</span></span> <span data-ttu-id="ddeaa-153">W aplikacji można łatwo wprowadzać duże zmiany, korzystając z niewielkich zmian w Twoim widoku.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-153">It's easy to make big changes in your app with small changes to your view.</span></span>

<span data-ttu-id="ddeaa-154">[Lista filmów ![— Windows Internet Explorer](getting-started-with-mvc-part3/_static/image9.png)](getting-started-with-mvc-part3/_static/image8.png)</span><span class="sxs-lookup"><span data-stu-id="ddeaa-154">[![Movie List - Windows Internet Explorer](getting-started-with-mvc-part3/_static/image9.png)](getting-started-with-mvc-part3/_static/image8.png)</span></span>

<span data-ttu-id="ddeaa-155">Nasz mały bit "Data" (w tym przypadku "Hello world!"</span><span class="sxs-lookup"><span data-stu-id="ddeaa-155">Our little bit of "data" (in this case the "Hello World!"</span></span> <span data-ttu-id="ddeaa-156">komunikat) został sztywno zakodowany.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-156">message) was hard coded though.</span></span> <span data-ttu-id="ddeaa-157">Mamy (przeglądający), a mamy C (kontrolery), ale nie ma jeszcze M (model).</span><span class="sxs-lookup"><span data-stu-id="ddeaa-157">We've got V (Views) and we've got C (Controllers), but no M (Model) yet.</span></span> <span data-ttu-id="ddeaa-158">Wkrótce przeprowadzimy Cię przez proces tworzenia bazy danych i pobierania z niej danych modelu.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-158">We'll shortly walk through how create a database and retrieve model data from it.</span></span>

## <a name="passing-a-viewmodel"></a><span data-ttu-id="ddeaa-159">Przekazywanie elementu ViewModel</span><span class="sxs-lookup"><span data-stu-id="ddeaa-159">Passing a ViewModel</span></span>

<span data-ttu-id="ddeaa-160">Zanim przejdziemy do bazy danych i porozmawiamy z modelami, najpierw porozmawiamy o "modele widoków".</span><span class="sxs-lookup"><span data-stu-id="ddeaa-160">Before we go to a database and talk about Models, though, let's first talk about "ViewModels."</span></span> <span data-ttu-id="ddeaa-161">Są to obiekty, które przedstawiają elementy wymagane przez szablon widoku do renderowania odpowiedzi HTML z powrotem do klienta.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-161">These are objects that represent what a View template requires to render an HTML response back to a client.</span></span> <span data-ttu-id="ddeaa-162">Są one zazwyczaj tworzone i przesyłane przez klasę kontrolera do szablonu widoku i powinny zawierać tylko te dane, które są wymagane przez szablon widoku — i nie tylko.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-162">They are typically created and passed by a Controller class to a View template, and should only contain the data that the View template requires - and no more.</span></span>

<span data-ttu-id="ddeaa-163">Wcześniej przy użyciu naszego przykładu HelloWorld, nasza metoda akcji powitalnej () przyjęła nazwę i parametr numTimes i wyprowadza go do przeglądarki.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-163">Previously with our HelloWorld sample, our Welcome() action method took a name and a numTimes parameter and output it to the browser.</span></span> <span data-ttu-id="ddeaa-164">Zamiast korzystać z kontrolera w dalszym ciągu renderować tę odpowiedź bezpośrednio, zamiast tego należy przystąpić do przechowywania tych danych, a następnie przekazać je do szablonu widoku w celu renderowania odpowiedzi HTML z użyciem.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-164">Rather than have the Controller continue to render this response directly,let's instead make a small class to hold that data and then pass it over to a View template to render back the HTML response using it.</span></span> <span data-ttu-id="ddeaa-165">W ten sposób kontroler jest objęty jednym elementem i szablonem widoku, który umożliwia nam zachowanie czystego "separacji problemów" w naszej aplikacji.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-165">This way the Controller is concerned with one thing and the View template another – enabling us to maintain clean "separation of concerns" within our application.</span></span>

<span data-ttu-id="ddeaa-166">Wróć do pliku HelloWorldController.cs i Dodaj nową klasę "WelcomeViewModel" i Zmień metodę Welcome na kontrolerze.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-166">Return to the HelloWorldController.cs file and add a new "WelcomeViewModel" class and change the Welcome method inside your controller.</span></span> <span data-ttu-id="ddeaa-167">Oto kompletny HelloWorldController.cs z nową klasą w tym samym pliku.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-167">Here is the complete HelloWorldController.cs with the new class in the same file.</span></span>

[!code-csharp[Main](getting-started-with-mvc-part3/samples/sample8.cs)]

<span data-ttu-id="ddeaa-168">Mimo że znajduje się on w wielu wierszach, nasze metody powitalnej są naprawdę tylko dwoma instrukcjami w kodzie.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-168">Even though it's on multiple lines, our Welcome method is really only two code statements.</span></span> <span data-ttu-id="ddeaa-169">Pierwsza instrukcja pakuje nasze dwa parametry do obiektu ViewModel, a drugi przekazuje obiekt wyniku do widoku.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-169">The first statement packages up our two parameters into a ViewModel object, and the second passes the resulting object onto the View.</span></span>

<span data-ttu-id="ddeaa-170">Teraz potrzebujemy szablonu widoku powitalnego.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-170">Now we need a Welcome View template!</span></span> <span data-ttu-id="ddeaa-171">Kliknij prawym przyciskiem myszy metodę Welcome i wybierz polecenie Dodaj widok.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-171">Right click in the Welcome method and select Add View.</span></span> <span data-ttu-id="ddeaa-172">Tym razem sprawdzimy "Tworzenie widoku o jednoznacznie określonym typie" i wybieramy nasze klasy WelcomeViewModel z listy rozwijanej.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-172">This time, we'll check "Create a strongly-typed view" and select our WelcomeViewModel class from the drop down list.</span></span> <span data-ttu-id="ddeaa-173">Ten nowy widok będzie wiedzieć tylko o WelcomeViewModels i nie ma żadnych innych typów obiektów.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-173">This new view will only know about WelcomeViewModels and no other types of objects.</span></span>

> <span data-ttu-id="ddeaa-174">*Uwaga: po dodaniu WelcomeViewModel do wyświetlania na liście rozwijanej należy wykonać kompilację.*</span><span class="sxs-lookup"><span data-stu-id="ddeaa-174">*NOTE: You'll need to have compiled once after adding your WelcomeViewModel for to show up in the drop down list.*</span></span>

<span data-ttu-id="ddeaa-175">Oto, co powinien wyglądać okno dialogowe Dodaj widok.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-175">Here's what your Add View dialog should look like.</span></span> <span data-ttu-id="ddeaa-176">Kliknij przycisk Dodaj.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-176">Click the Add button.</span></span> ![Dodaj widok w kółku](getting-started-with-mvc-part3/_static/image10.png)

<span data-ttu-id="ddeaa-178">Dodaj ten kod do &lt;H2&gt; w nowym elemencie Welcome. aspx.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-178">Add this code under the &lt;h2&gt; in your new Welcome.aspx.</span></span> <span data-ttu-id="ddeaa-179">Utworzymy pętlę i będziemy mówić do powitania tyle razy, ile powinien!</span><span class="sxs-lookup"><span data-stu-id="ddeaa-179">We'll make a loop and say Hello as many times as the user says we should!</span></span>

[!code-aspx[Main](getting-started-with-mvc-part3/samples/sample9.aspx)]

<span data-ttu-id="ddeaa-180">Zwróć uwagę na to, że podczas wpisywania otrzymujesz ten widok dotyczący WelcomeViewModel (z ślubem, pamiętaj?), aby uzyskać pomoc IntelliSense za każdym razem, gdy odwołujemy się do naszego obiektu modelu, jak pokazano na poniższym zrzucie ekranu:</span><span class="sxs-lookup"><span data-stu-id="ddeaa-180">Also, notice while you're typing that because we told this View about the WelcomeViewModel (they are married, remember?) that we get helpful Intellisense each time we reference our Model object as seen in the screenshot below:</span></span>

<span data-ttu-id="ddeaa-181">[![kod źródłowy NumTime](getting-started-with-mvc-part3/_static/image12.png)](getting-started-with-mvc-part3/_static/image11.png)</span><span class="sxs-lookup"><span data-stu-id="ddeaa-181">[![NumTime Source Code](getting-started-with-mvc-part3/_static/image12.png)](getting-started-with-mvc-part3/_static/image11.png)</span></span>

<span data-ttu-id="ddeaa-182">Uruchom aplikację i ponownie odwiedź `http://localhost:xx/HelloWorld/Welcome?name=Scott&numtimes=4`.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-182">Run your application and visit `http://localhost:xx/HelloWorld/Welcome?name=Scott&numtimes=4` again.</span></span> <span data-ttu-id="ddeaa-183">Teraz, gdy pobieramy dane z adresu URL, zostanie on automatycznie przekazana do naszego kontrolera, nasz kontroler przypakuje dane do ViewModel i przekaże ten obiekt do naszego widoku.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-183">Now we're taking data from the URL, it's passed into our Controller automatically, our Controller packages up the data into a ViewModel and passes that object onto our View.</span></span> <span data-ttu-id="ddeaa-184">Widok niż wyświetla dane w formacie HTML dla użytkownika.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-184">The View than displays the data as HTML to the user.</span></span>

<span data-ttu-id="ddeaa-185">[![— Zapraszamy — Windows Internet Explorer](getting-started-with-mvc-part3/_static/image14.png)](getting-started-with-mvc-part3/_static/image13.png)</span><span class="sxs-lookup"><span data-stu-id="ddeaa-185">[![Welcome - Windows Internet Explorer](getting-started-with-mvc-part3/_static/image14.png)](getting-started-with-mvc-part3/_static/image13.png)</span></span>

<span data-ttu-id="ddeaa-186">Jest to również rodzaj "M" dla modelu, ale nie jako rodzaj bazy danych.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-186">Well, that was a kind of an "M" for Model, but not the database kind.</span></span> <span data-ttu-id="ddeaa-187">Zapoznaj się z informacjami i Utwórz bazę danych dla filmów.</span><span class="sxs-lookup"><span data-stu-id="ddeaa-187">Let's take what we've learned and create a database of Movies.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="ddeaa-188">[Poprzednie](getting-started-with-mvc-part2.md)
> [dalej](getting-started-with-mvc-part4.md)</span><span class="sxs-lookup"><span data-stu-id="ddeaa-188">[Previous](getting-started-with-mvc-part2.md)
[Next](getting-started-with-mvc-part4.md)</span></span>
