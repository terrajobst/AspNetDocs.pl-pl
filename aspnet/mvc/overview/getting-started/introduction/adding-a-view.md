---
title: Dodawanie widoku do aplikacji MVC
author: Rick-Anderson
description: Dodawanie widoku do aplikacji MVC
ms.author: riande
ms.date: 01/23/2019
uid: mvc/overview/getting-started/introduction/adding-a-view
ms.openlocfilehash: 0bc6ac06d12aaee4b2a11c1bf246f9f20f0be017
ms.sourcegitcommit: 7709c0a091b8d55b7b33bad8849f7b66b23c3d72
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 02/19/2020
ms.locfileid: "77456650"
---
# <a name="adding-a-view"></a><span data-ttu-id="fbed8-103">Dodawanie widoku</span><span class="sxs-lookup"><span data-stu-id="fbed8-103">Adding a View</span></span>

<span data-ttu-id="fbed8-104">Autor [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="fbed8-104">by [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

[!INCLUDE [Tutorial Note](index.md)]

<span data-ttu-id="fbed8-105">W tej sekcji zamierzasz zmodyfikować klasę `HelloWorldController`, aby używać plików szablonów widoku do czystego hermetyzowania procesu generowania odpowiedzi HTML na klienta.</span><span class="sxs-lookup"><span data-stu-id="fbed8-105">In this section you're going to modify the `HelloWorldController` class to use view template files to cleanly encapsulate the process of generating HTML responses to a client.</span></span> 

<span data-ttu-id="fbed8-106">Utworzysz plik szablonu widoku przy użyciu [aparatu widoku Razor](../../../../web-pages/overview/getting-started/introducing-razor-syntax-c.md).</span><span class="sxs-lookup"><span data-stu-id="fbed8-106">You'll create a view template file using the [Razor view engine](../../../../web-pages/overview/getting-started/introducing-razor-syntax-c.md).</span></span> <span data-ttu-id="fbed8-107">Szablony widoków oparte na składni Razor mają rozszerzenie pliku *. cshtml* i umożliwiają elegancki sposób tworzenia danych wyjściowych HTML przy użyciu C#.</span><span class="sxs-lookup"><span data-stu-id="fbed8-107">Razor-based view templates have a *.cshtml* file extension, and provide an elegant way to create HTML output using C#.</span></span> <span data-ttu-id="fbed8-108">Razor minimalizuje liczbę znaków i naciśnięć klawiszy wymaganych podczas pisania szablonu widoku i umożliwia szybkie i płynne przepływy kodowania.</span><span class="sxs-lookup"><span data-stu-id="fbed8-108">Razor minimizes the number of characters and keystrokes required when writing a view template, and enables a fast, fluid coding workflow.</span></span>

<span data-ttu-id="fbed8-109">Obecnie Metoda `Index` zwraca ciąg z komunikatem, który jest zakodowany w klasie kontrolera.</span><span class="sxs-lookup"><span data-stu-id="fbed8-109">Currently the `Index` method returns a string with a message that is hard-coded in the controller class.</span></span> <span data-ttu-id="fbed8-110">Zmień metodę `Index`, aby wywołać metodę [widoku](/dotnet/api/microsoft.aspnetcore.mvc.controller.view#Microsoft_AspNetCore_Mvc_Controller_View) controllers, jak pokazano w poniższym kodzie:</span><span class="sxs-lookup"><span data-stu-id="fbed8-110">Change the `Index` method to call the controllers [View](/dotnet/api/microsoft.aspnetcore.mvc.controller.view#Microsoft_AspNetCore_Mvc_Controller_View) method, as shown in the following code:</span></span>

[!code-csharp[Main](adding-a-view/samples/sample1.cs?highlight=1,3)]

<span data-ttu-id="fbed8-111">Powyższa metoda `Index` używa szablonu widoku do wygenerowania odpowiedzi HTML do przeglądarki.</span><span class="sxs-lookup"><span data-stu-id="fbed8-111">The `Index` method above uses a view template to generate an HTML response to the browser.</span></span> <span data-ttu-id="fbed8-112">Metody kontrolera (znane również jako [metody akcji](http://rachelappel.com/asp.net-mvc-actionresults-explained)), takie jak powyższa metoda `Index`, zazwyczaj zwracają [ActionResult](https://msdn.microsoft.com/library/system.web.mvc.actionresult.aspx) (lub klasę pochodną [ActionResult](https://msdn.microsoft.com/library/system.web.mvc.actionresult.aspx)), a nie typy pierwotne, takie jak ciąg.</span><span class="sxs-lookup"><span data-stu-id="fbed8-112">Controller methods (also known as [action methods](http://rachelappel.com/asp.net-mvc-actionresults-explained)), such as the `Index` method above, generally return an [ActionResult](https://msdn.microsoft.com/library/system.web.mvc.actionresult.aspx) (or a class derived from [ActionResult](https://msdn.microsoft.com/library/system.web.mvc.actionresult.aspx)), not primitive types like string.</span></span>

<span data-ttu-id="fbed8-113">Kliknij prawym przyciskiem myszy folder *Views\HelloWorld* , a następnie kliknij pozycję **Dodaj**, a następnie kliknij pozycję **Strona widoku MVC 5 z układem (Razor)** .</span><span class="sxs-lookup"><span data-stu-id="fbed8-113">Right click the *Views\HelloWorld* folder and click **Add**, then click **MVC 5 View Page with Layout (Razor)**.</span></span>
  
![](adding-a-view/_static/image1.png)   
  
<span data-ttu-id="fbed8-114">W oknie dialogowym **Określanie nazwy dla elementu** wprowadź *indeks*, a następnie kliknij przycisk **OK**.</span><span class="sxs-lookup"><span data-stu-id="fbed8-114">In the **Specify Name for Item** dialog box, enter *Index*, and then click **OK**.</span></span>  
  
![](adding-a-view/_static/image2.png)  
  
<span data-ttu-id="fbed8-115">W oknie dialogowym **Wybieranie strony układu** Zaakceptuj wartość domyślną **\_Layout. cshtml** i kliknij przycisk **OK**.</span><span class="sxs-lookup"><span data-stu-id="fbed8-115">In the **Select a Layout Page** dialog, accept the default **\_Layout.cshtml** and click **OK**.</span></span>  
  
![](adding-a-view/_static/image3.png)  
  
<span data-ttu-id="fbed8-116">W oknie dialogowym powyżej folder *Views\Shared* jest wybierany w lewym okienku.</span><span class="sxs-lookup"><span data-stu-id="fbed8-116">In the dialog above, the *Views\Shared* folder is selected in the left pane.</span></span> <span data-ttu-id="fbed8-117">Jeśli plik niestandardowego układu został w innym folderze, można go wybrać.</span><span class="sxs-lookup"><span data-stu-id="fbed8-117">If you had a custom layout file in another folder, you could select it.</span></span> <span data-ttu-id="fbed8-118">Porozmawiamy o pliku układu w dalszej części tego samouczka</span><span class="sxs-lookup"><span data-stu-id="fbed8-118">We'll talk about the layout file later in the tutorial</span></span>

<span data-ttu-id="fbed8-119">Plik *MvcMovie\Views\HelloWorld\Index.cshtml* jest tworzony.</span><span class="sxs-lookup"><span data-stu-id="fbed8-119">The *MvcMovie\Views\HelloWorld\Index.cshtml* file is created.</span></span>

![](adding-a-view/_static/image4.png)

<span data-ttu-id="fbed8-120">Dodaj następujący wyróżniony znacznik.</span><span class="sxs-lookup"><span data-stu-id="fbed8-120">Add the following highlighted markup.</span></span>

[!code-cshtml[Main](adding-a-view/samples/sample2.cshtml?highlight=4-11)]

<span data-ttu-id="fbed8-121">Kliknij prawym przyciskiem myszy plik *index. cshtml* i wybierz pozycję **Widok w przeglądarce**.</span><span class="sxs-lookup"><span data-stu-id="fbed8-121">Right click the *Index.cshtml* file and select **View in Browser**.</span></span>

![PI](adding-a-view/_static/image5.png)

<span data-ttu-id="fbed8-123">Możesz również kliknąć prawym przyciskiem myszy plik *index. cshtml* i wybrać pozycję **Widok w Inspektorze strony.**</span><span class="sxs-lookup"><span data-stu-id="fbed8-123">You can also right click the *Index.cshtml* file and select **View in Page Inspector.**</span></span> <span data-ttu-id="fbed8-124">Aby uzyskać więcej informacji, zobacz [Samouczek dotyczący inspektora strony](../../views/using-page-inspector-in-aspnet-mvc.md) .</span><span class="sxs-lookup"><span data-stu-id="fbed8-124">See the [Page Inspector tutorial](../../views/using-page-inspector-in-aspnet-mvc.md) for more information.</span></span>

<span data-ttu-id="fbed8-125">Alternatywnie Uruchom aplikację i przejdź do kontrolera `HelloWorld` (`http://localhost:xxxx/HelloWorld`).</span><span class="sxs-lookup"><span data-stu-id="fbed8-125">Alternatively, run the application and browse to the `HelloWorld` controller (`http://localhost:xxxx/HelloWorld`).</span></span> <span data-ttu-id="fbed8-126">Metoda `Index` w kontrolerze nie została znacznie zadziałała. po prostu uruchomiono instrukcję `return View()`, która określa, że metoda powinna używać pliku szablonu widoku, aby renderować odpowiedź do przeglądarki.</span><span class="sxs-lookup"><span data-stu-id="fbed8-126">The `Index` method in your controller didn't do much work; it simply ran the statement `return View()`, which specified that the method should use a view template file to render a response to the browser.</span></span> <span data-ttu-id="fbed8-127">Ponieważ nie określono jawnie nazwy pliku szablonu widoku, ASP.NET MVC domyślnie przy użyciu pliku widoku *index. cshtml* w folderze *\Views\HelloWorld* .</span><span class="sxs-lookup"><span data-stu-id="fbed8-127">Because you didn't explicitly specify the name of the view template file to use, ASP.NET MVC defaulted to using the *Index.cshtml* view file in the *\Views\HelloWorld* folder.</span></span> <span data-ttu-id="fbed8-128">Na poniższym obrazie przedstawiono ciąg &quot;Hello z naszego szablonu widoku!&quot; zakodowana w widoku.</span><span class="sxs-lookup"><span data-stu-id="fbed8-128">The image below shows the string &quot;Hello from our View Template!&quot; hard-coded in the view.</span></span>

![](adding-a-view/_static/image6.png)

<span data-ttu-id="fbed8-129">Wyglądają dość dobre.</span><span class="sxs-lookup"><span data-stu-id="fbed8-129">Looks pretty good.</span></span> <span data-ttu-id="fbed8-130">Należy jednak zauważyć, że na pasku tytułu przeglądarki jest wyświetlana wartość "index-my ASP.NET Application", a w górnej części strony znajduje się napis "Nazwa aplikacji".</span><span class="sxs-lookup"><span data-stu-id="fbed8-130">However, notice that the browser's title bar shows "Index - My ASP.NET Application," and the big link at the top of the page says "Application name."</span></span> <span data-ttu-id="fbed8-131">W zależności od tego, jak mały rozmiar okna przeglądarki, może być konieczne kliknięcie trzech pasków w prawym górnym rogu, aby zobaczyć do **domu**, **Informacje o** **kontakcie**, **zarejestrować** i **zalogować** się.</span><span class="sxs-lookup"><span data-stu-id="fbed8-131">Depending on how small you make your browser window, you might need to click the three bars in the upper right to see the to the **Home**, **About**, **Contact**, **Register** and **Log in** links.</span></span>

## <a name="changing-views-and-layout-pages"></a><span data-ttu-id="fbed8-132">Zmienianie widoków i stron układu</span><span class="sxs-lookup"><span data-stu-id="fbed8-132">Changing Views and Layout Pages</span></span>

<span data-ttu-id="fbed8-133">Najpierw chcesz zmienić nazwę aplikacji &quot;&quot; link w górnej części strony.</span><span class="sxs-lookup"><span data-stu-id="fbed8-133">First, you want to change the &quot;Application name&quot; link at the top of the page.</span></span> <span data-ttu-id="fbed8-134">Ten tekst jest typowy dla każdej strony.</span><span class="sxs-lookup"><span data-stu-id="fbed8-134">That text is common to every page.</span></span> <span data-ttu-id="fbed8-135">Jest on faktycznie zaimplementowany w tylko jednym miejscu w projekcie, nawet jeśli pojawia się na każdej stronie w aplikacji.</span><span class="sxs-lookup"><span data-stu-id="fbed8-135">It's actually implemented in only one place in the project, even though it appears on every page in the application.</span></span> <span data-ttu-id="fbed8-136">Przejdź do folderu */views/Shared* w **Eksplorator rozwiązań** i Otwórz plik *\_Layout. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="fbed8-136">Go to the */Views/Shared* folder in **Solution Explorer** and open the *\_Layout.cshtml* file.</span></span> <span data-ttu-id="fbed8-137">Ten plik jest nazywany *stroną układu* i znajduje się w folderze udostępnionym używanym przez wszystkie inne strony.</span><span class="sxs-lookup"><span data-stu-id="fbed8-137">This file is called a *layout page* and it's in the shared folder that all other pages use.</span></span>

![_LayoutCshtml](adding-a-view/_static/image7.png)

<span data-ttu-id="fbed8-139">Szablony układów umożliwiają określenie układu kontenera HTML witryny w jednym miejscu, a następnie zastosowanie go na wielu stronach w witrynie.</span><span class="sxs-lookup"><span data-stu-id="fbed8-139">Layout templates allow you to specify the HTML container layout of your site in one place and then apply it across multiple pages in your site.</span></span> <span data-ttu-id="fbed8-140">Znajdź wiersz `@RenderBody()`.</span><span class="sxs-lookup"><span data-stu-id="fbed8-140">Find the `@RenderBody()` line.</span></span> <span data-ttu-id="fbed8-141">`RenderBody` jest symbolem zastępczym, w którym są wyświetlane wszystkie strony dotyczące określonego widoku, &quot;opakowane&quot; na stronie układ.</span><span class="sxs-lookup"><span data-stu-id="fbed8-141">`RenderBody` is a placeholder where all the view-specific pages you create show up, &quot;wrapped&quot; in the layout page.</span></span> <span data-ttu-id="fbed8-142">Na przykład w przypadku wybrania łącza **informacje** widok *Views\Home\About.cshtml* jest renderowany wewnątrz metody `RenderBody`.</span><span class="sxs-lookup"><span data-stu-id="fbed8-142">For example, if you select the **About** link, the *Views\Home\About.cshtml* view is rendered inside the `RenderBody` method.</span></span>

<span data-ttu-id="fbed8-143">Zmień zawartość elementu title.</span><span class="sxs-lookup"><span data-stu-id="fbed8-143">Change the contents of the title element.</span></span> <span data-ttu-id="fbed8-144">Zmień wartość [ActionLink](https://msdn.microsoft.com/library/dd504972(v=vs.108).aspx) w szablonie układu z &quot;nazwa aplikacji&quot;, aby &quot;&quot; filmu MVC i kontroler z `Home` `Movies`.</span><span class="sxs-lookup"><span data-stu-id="fbed8-144">Change the [ActionLink](https://msdn.microsoft.com/library/dd504972(v=vs.108).aspx) in the layout template from &quot;Application name&quot; to &quot;MVC Movie&quot; and the controller from `Home` to `Movies`.</span></span> <span data-ttu-id="fbed8-145">Pełny plik układu jest przedstawiony poniżej:</span><span class="sxs-lookup"><span data-stu-id="fbed8-145">The complete layout file is shown below:</span></span>

[!code-cshtml[Main](adding-a-view/samples/sample3.cshtml?highlight=6,20)]

<span data-ttu-id="fbed8-146">Uruchom aplikację i zwróć uwagę, że teraz jest ona wyświetlana &quot;&quot;filmu MVC.</span><span class="sxs-lookup"><span data-stu-id="fbed8-146">Run the application and notice that it now says &quot;MVC Movie &quot;.</span></span> <span data-ttu-id="fbed8-147">Kliknij link **About (informacje** ) i sprawdź, czy ta strona zawiera również &quot;&quot;filmu MVC.</span><span class="sxs-lookup"><span data-stu-id="fbed8-147">Click the **About** link, and you see how that page shows &quot;MVC Movie&quot;, too.</span></span> <span data-ttu-id="fbed8-148">Udało nam się wprowadzić zmiany w szablonie układu i wszystkie strony w witrynie odzwierciedlają nowy tytuł.</span><span class="sxs-lookup"><span data-stu-id="fbed8-148">We were able to make the change once in the layout template and have all pages on the site reflect the new title.</span></span>

![](adding-a-view/_static/image8.png)

<span data-ttu-id="fbed8-149">Po raz pierwszy utworzyliśmy plik *Views\HelloWorld\Index.cshtml* , zawiera następujący kod:</span><span class="sxs-lookup"><span data-stu-id="fbed8-149">When we first created the *Views\HelloWorld\Index.cshtml* file, it contained the following code:</span></span>

[!code-cshtml[Main](adding-a-view/samples/sample4.cshtml)]

<span data-ttu-id="fbed8-150">Powyższe kod Razor jawnie ustawia stronę układu.</span><span class="sxs-lookup"><span data-stu-id="fbed8-150">The Razor code above is explicitly setting the layout page.</span></span> <span data-ttu-id="fbed8-151">Przejrzyj *widoki\\_ViewStart. cshtml* zawiera dokładnie te same znaczniki Razor.</span><span class="sxs-lookup"><span data-stu-id="fbed8-151">Examine the *Views\\_ViewStart.cshtml* file, it contains the exact same Razor markup.</span></span> <span data-ttu-id="fbed8-152">*[Widok\\pliku _ViewStart. cshtml](https://weblogs.asp.net/scottgu/archive/2010/10/22/asp-net-mvc-3-layouts.aspx)* definiuje wspólny układ, który będzie używany przez wszystkie widoki, w związku z tym można skomentować lub usunąć ten kod z pliku *Views\HelloWorld\Index.cshtml* .</span><span class="sxs-lookup"><span data-stu-id="fbed8-152">The *[Views\\_ViewStart.cshtml](https://weblogs.asp.net/scottgu/archive/2010/10/22/asp-net-mvc-3-layouts.aspx)* file defines the common layout that all views will use, therefore you can comment out or remove that code from the *Views\HelloWorld\Index.cshtml* file.</span></span>

[!code-cshtml[Main](adding-a-view/samples/sample5.cshtml?highlight=1-3)]

<span data-ttu-id="fbed8-153">Możesz użyć właściwości `Layout`, aby ustawić inny widok układu lub ustawić go na `null`, aby nie był używany żaden plik układu.</span><span class="sxs-lookup"><span data-stu-id="fbed8-153">You can use the `Layout` property to set a different layout view, or set it to `null` so no layout file will be used.</span></span>

<span data-ttu-id="fbed8-154">Teraz Zmieńmy tytuł widoku indeksu.</span><span class="sxs-lookup"><span data-stu-id="fbed8-154">Now, let's change the title of the Index view.</span></span>

<span data-ttu-id="fbed8-155">Otwórz *MvcMovie\Views\HelloWorld\Index.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="fbed8-155">Open *MvcMovie\Views\HelloWorld\Index.cshtml*.</span></span> <span data-ttu-id="fbed8-156">Istnieją dwa miejsca, w których należy wprowadzić zmianę: pierwszy, tekst wyświetlany w tytule przeglądarki, a następnie w pomocniczym nagłówku (`<h2>` elementu).</span><span class="sxs-lookup"><span data-stu-id="fbed8-156">There are two places to make a change: first, the text that appears in the title of the browser, and then in the secondary header (the `<h2>` element).</span></span> <span data-ttu-id="fbed8-157">Postanowisz nieco inaczej, aby zobaczyć, który bit kodu zmienia się w ramach aplikacji.</span><span class="sxs-lookup"><span data-stu-id="fbed8-157">You'll make them slightly different so you can see which bit of code changes which part of the app.</span></span>

[!code-cshtml[Main](adding-a-view/samples/sample6.cshtml?highlight=2,5)]

<span data-ttu-id="fbed8-158">Aby wskazać tytuł HTML do wyświetlenia, powyższy kod ustawia właściwość `Title` obiektu `ViewBag` (który znajduje się w szablonie widoku *index. cshtml* ).</span><span class="sxs-lookup"><span data-stu-id="fbed8-158">To indicate the HTML title to display, the code above sets a `Title` property of the `ViewBag` object (which is in the *Index.cshtml* view template).</span></span> <span data-ttu-id="fbed8-159">Zwróć uwagę, że szablon układu ( *Views\Shared\\_Layout. cshtml* ) używa tej wartości w elemencie `<title>` jako część sekcji `<head>` kodu HTML, który został wcześniej zmodyfikowany.</span><span class="sxs-lookup"><span data-stu-id="fbed8-159">Notice that the layout template ( *Views\Shared\\_Layout.cshtml* ) uses this value in the `<title>` element as part of the `<head>` section of the HTML that we modified previously.</span></span>

[!code-cshtml[Main](adding-a-view/samples/sample7.cshtml?highlight=6)]

<span data-ttu-id="fbed8-160">Korzystając z tego podejścia `ViewBag`, można łatwo przekazać inne parametry między szablonem widoku i plikiem układu.</span><span class="sxs-lookup"><span data-stu-id="fbed8-160">Using this `ViewBag` approach, you can easily pass other parameters between your view template and your layout file.</span></span>

<span data-ttu-id="fbed8-161">Uruchom aplikację.</span><span class="sxs-lookup"><span data-stu-id="fbed8-161">Run the application.</span></span> <span data-ttu-id="fbed8-162">Zwróć uwagę, że tytuł przeglądarki, nagłówek podstawowy i pomocnicze nagłówki zostały zmienione.</span><span class="sxs-lookup"><span data-stu-id="fbed8-162">Notice that the browser title, the primary heading, and the secondary headings have changed.</span></span> <span data-ttu-id="fbed8-163">(Jeśli w przeglądarce nie są widoczne zmiany, może być wyświetlana zawartość z pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="fbed8-163">(If you don't see changes in the browser, you might be viewing cached content.</span></span> <span data-ttu-id="fbed8-164">Naciśnij kombinację klawiszy CTRL + F5 w przeglądarce, aby wymusić załadowanie odpowiedzi z serwera. Tytuł przeglądarki jest tworzony przy użyciu `ViewBag.Title` ustawionej w szablonie widoku *index. cshtml* oraz dodatkowej aplikacji &quot;-Movie&quot; dodanej w pliku układu.</span><span class="sxs-lookup"><span data-stu-id="fbed8-164">Press Ctrl+F5 in your browser to force the response from the server to be loaded.) The browser title is created with the `ViewBag.Title` we set in the *Index.cshtml* view template and the additional &quot;- Movie App&quot; added in the layout file.</span></span>

<span data-ttu-id="fbed8-165">Zwróć uwagę na to, jak zawartość w szablonie widoku *index. cshtml* została scalona z szablonem widoku *\_Layout. cshtml* , a pojedyncza odpowiedź HTML została wysłana do przeglądarki.</span><span class="sxs-lookup"><span data-stu-id="fbed8-165">Also notice how the content in the *Index.cshtml* view template was merged with the *\_Layout.cshtml* view template and a single HTML response was sent to the browser.</span></span> <span data-ttu-id="fbed8-166">Szablony układów ułatwiają wprowadzanie zmian, które są stosowane do wszystkich stron w aplikacji.</span><span class="sxs-lookup"><span data-stu-id="fbed8-166">Layout templates make it really easy to make changes that apply across all of the pages in your application.</span></span>

![](adding-a-view/_static/image9.png)

<span data-ttu-id="fbed8-167">Nasz mały bit &quot;danych&quot; (w tym przypadku &quot;Hello z naszego szablonu widoku! komunikat&quot;) jest zakodowany na stałe.</span><span class="sxs-lookup"><span data-stu-id="fbed8-167">Our little bit of &quot;data&quot; (in this case the &quot;Hello from our View Template!&quot; message) is hard-coded, though.</span></span> <span data-ttu-id="fbed8-168">Aplikacja MVC ma &quot;V&quot; (View) i masz&quot; &quot;C (kontroler), ale nie &quot;M&quot; (model).</span><span class="sxs-lookup"><span data-stu-id="fbed8-168">The MVC application has a &quot;V&quot; (view) and you've got a &quot;C&quot; (controller), but no &quot;M&quot; (model) yet.</span></span> <span data-ttu-id="fbed8-169">Wkrótce przeprowadzimy Cię przez proces tworzenia bazy danych i pobierania z niej danych modelu.</span><span class="sxs-lookup"><span data-stu-id="fbed8-169">Shortly, we'll walk through how to create a database and retrieve model data from it.</span></span>

## <a name="passing-data-from-the-controller-to-the-view"></a><span data-ttu-id="fbed8-170">Przekazywanie danych z kontrolera do widoku</span><span class="sxs-lookup"><span data-stu-id="fbed8-170">Passing Data from the Controller to the View</span></span>

<span data-ttu-id="fbed8-171">Zanim przejdziemy do bazy danych i porozmawiamy nad modelami, najpierw porozmawiamy o przekazywaniu informacji z kontrolera do widoku.</span><span class="sxs-lookup"><span data-stu-id="fbed8-171">Before we go to a database and talk about models, though, let's first talk about passing information from the controller to a view.</span></span> <span data-ttu-id="fbed8-172">Klasy kontrolerów są wywoływane w odpowiedzi na żądanie przychodzącego adresu URL.</span><span class="sxs-lookup"><span data-stu-id="fbed8-172">Controller classes are invoked in response to an incoming URL request.</span></span> <span data-ttu-id="fbed8-173">Klasa kontrolera to miejsce, w którym można napisać kod, który obsługuje żądania przeglądarki przychodzącej, pobiera dane z bazy danych i ostatecznie decyduje o typie odpowiedzi wysyłanej z powrotem do przeglądarki.</span><span class="sxs-lookup"><span data-stu-id="fbed8-173">A controller class is where you write the code that handles the incoming browser requests, retrieves data from a database, and ultimately decides what type of response to send back to the browser.</span></span> <span data-ttu-id="fbed8-174">Szablony widoków mogą być następnie używane z poziomu kontrolera do generowania i formatowania odpowiedzi HTML w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="fbed8-174">View templates can then be used from a controller to generate and format an HTML response to the browser.</span></span>

<span data-ttu-id="fbed8-175">Kontrolery są odpowiedzialne za dostarczanie wszelkich danych lub obiektów, które są wymagane, aby szablon widoku mógł renderować odpowiedź do przeglądarki.</span><span class="sxs-lookup"><span data-stu-id="fbed8-175">Controllers are responsible for providing whatever data or objects are required in order for a view template to render a response to the browser.</span></span> <span data-ttu-id="fbed8-176">Najlepszym rozwiązaniem: **szablon widoku nigdy nie powinien wykonywać logiki biznesowej ani bezpośrednio korzystać z bazy danych**.</span><span class="sxs-lookup"><span data-stu-id="fbed8-176">A best practice: **A view template should never perform business logic or interact with a database directly**.</span></span> <span data-ttu-id="fbed8-177">Zamiast tego szablon widoku powinien współpracować tylko z danymi, które są do niego udostępniane przez kontroler.</span><span class="sxs-lookup"><span data-stu-id="fbed8-177">Instead, a view template should work only with the data that's provided to it by the controller.</span></span> <span data-ttu-id="fbed8-178">Utrzymywanie tego &quot;separacji zagadnień&quot; pomaga zapewnić, że kod jest czysty, weryfikowalne i bardziej konserwowany.</span><span class="sxs-lookup"><span data-stu-id="fbed8-178">Maintaining this &quot;separation of concerns&quot; helps keep your code clean, testable and more maintainable.</span></span>

<span data-ttu-id="fbed8-179">Obecnie Metoda akcji `Welcome` w klasie `HelloWorldController` przyjmuje `name` i `numTimes` parametr, a następnie wyprowadza wartości bezpośrednio do przeglądarki.</span><span class="sxs-lookup"><span data-stu-id="fbed8-179">Currently, the `Welcome` action method in the `HelloWorldController` class takes a `name` and a `numTimes` parameter and then outputs the values directly to the browser.</span></span> <span data-ttu-id="fbed8-180">Zamiast przetworzyć tę odpowiedź jako ciąg, należy zmienić kontroler tak, aby używał szablonu widoku.</span><span class="sxs-lookup"><span data-stu-id="fbed8-180">Rather than have the controller render this response as a string, let's change the controller to use a view template instead.</span></span> <span data-ttu-id="fbed8-181">Szablon widoku generuje odpowiedź dynamiczną, co oznacza, że należy przekazać odpowiednie bity danych z kontrolera do widoku w celu wygenerowania odpowiedzi.</span><span class="sxs-lookup"><span data-stu-id="fbed8-181">The view template will generate a dynamic response, which means that you need to pass appropriate bits of data from the controller to the view in order to generate the response.</span></span> <span data-ttu-id="fbed8-182">Można to zrobić, korzystając z kontrolera, który umieści dane dynamiczne (parametry) wymagane przez szablon widoku w obiekcie `ViewBag`, do którego ten szablon widoku może uzyskać dostęp.</span><span class="sxs-lookup"><span data-stu-id="fbed8-182">You can do this by having the controller put the dynamic data (parameters) that the view template needs in a `ViewBag` object that the view template can then access.</span></span>

<span data-ttu-id="fbed8-183">Wróć do pliku *HelloWorldController.cs* i zmień metodę `Welcome`, aby dodać `Message` i `NumTimes` wartość do obiektu `ViewBag`.</span><span class="sxs-lookup"><span data-stu-id="fbed8-183">Return to the *HelloWorldController.cs* file and change the `Welcome` method to add a `Message` and `NumTimes` value to the `ViewBag` object.</span></span> <span data-ttu-id="fbed8-184">`ViewBag` jest obiektem dynamicznym, co oznacza, że można w nim umieścić dowolne z nich. Obiekt `ViewBag` nie ma zdefiniowanych właściwości, dopóki nie umieścisz w nim elementu.</span><span class="sxs-lookup"><span data-stu-id="fbed8-184">`ViewBag` is a dynamic object, which means you can put whatever you want in to it; the `ViewBag` object has no defined properties until you put something inside it.</span></span> <span data-ttu-id="fbed8-185">[System powiązania modelu MVC ASP.NET](http://odetocode.com/Blogs/scott/archive/2009/04/27/6-tips-for-asp-net-mvc-model-binding.aspx) automatycznie mapuje nazwane parametry (`name` i `numTimes`) z ciągu zapytania na pasku adresu do parametrów w metodzie.</span><span class="sxs-lookup"><span data-stu-id="fbed8-185">The [ASP.NET MVC model binding system](http://odetocode.com/Blogs/scott/archive/2009/04/27/6-tips-for-asp-net-mvc-model-binding.aspx) automatically maps the named parameters (`name` and `numTimes`) from the query string in the address bar to parameters in your method.</span></span> <span data-ttu-id="fbed8-186">Pełny plik *HelloWorldController.cs* wygląda następująco:</span><span class="sxs-lookup"><span data-stu-id="fbed8-186">The complete *HelloWorldController.cs* file looks like this:</span></span>

[!code-csharp[Main](adding-a-view/samples/sample8.cs)]

<span data-ttu-id="fbed8-187">Teraz obiekt `ViewBag` zawiera dane, które zostaną automatycznie przesłane do widoku.</span><span class="sxs-lookup"><span data-stu-id="fbed8-187">Now the `ViewBag` object contains data that will be passed to the view automatically.</span></span> <span data-ttu-id="fbed8-188">Następnie potrzebny będzie szablon widoku powitalnego.</span><span class="sxs-lookup"><span data-stu-id="fbed8-188">Next, you need a Welcome view template!</span></span> <span data-ttu-id="fbed8-189">W menu **kompilacja** wybierz opcję **Kompiluj rozwiązanie** (lub Ctrl + Shift + B), aby upewnić się, że projekt został skompilowany.</span><span class="sxs-lookup"><span data-stu-id="fbed8-189">In the **Build** menu, select **Build Solution** (or Ctrl+Shift+B) to make sure the project is compiled.</span></span> <span data-ttu-id="fbed8-190">Kliknij prawym przyciskiem myszy folder *Views\HelloWorld* , a następnie kliknij pozycję **Dodaj**, a następnie kliknij pozycję **Strona widoku MVC 5 z układem (Razor)** .</span><span class="sxs-lookup"><span data-stu-id="fbed8-190">Right click the *Views\HelloWorld* folder and click **Add**, then click **MVC 5 View Page with Layout (Razor)**.</span></span>
  
![](adding-a-view/_static/image10.png)   
  
<span data-ttu-id="fbed8-191">W oknie dialogowym **Określanie nazwy dla elementu** wprowadź *Welcome*, a następnie kliknij przycisk **OK**.</span><span class="sxs-lookup"><span data-stu-id="fbed8-191">In the **Specify Name for Item** dialog box, enter *Welcome*, and then click **OK**.</span></span>   
  
<span data-ttu-id="fbed8-192">W oknie dialogowym **Wybieranie strony układu** Zaakceptuj wartość domyślną **\_Layout. cshtml** i kliknij przycisk **OK**.</span><span class="sxs-lookup"><span data-stu-id="fbed8-192">In the **Select a Layout Page** dialog, accept the default **\_Layout.cshtml** and click **OK**.</span></span>  
  
![](adding-a-view/_static/image11.png)   

<span data-ttu-id="fbed8-193">Plik *MvcMovie\Views\HelloWorld\Welcome.cshtml* jest tworzony.</span><span class="sxs-lookup"><span data-stu-id="fbed8-193">The *MvcMovie\Views\HelloWorld\Welcome.cshtml* file is created.</span></span>

<span data-ttu-id="fbed8-194">Zastąp znaczniki w pliku *Welcome. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="fbed8-194">Replace the markup in the *Welcome.cshtml* file.</span></span> <span data-ttu-id="fbed8-195">Utworzysz pętlę, która wydaje się &quot;Hello&quot; tyle razy, ile powinien.</span><span class="sxs-lookup"><span data-stu-id="fbed8-195">You'll create a loop that says &quot;Hello&quot; as many times as the user says it should.</span></span> <span data-ttu-id="fbed8-196">Pełny plik *Welcome. cshtml* jest przedstawiony poniżej.</span><span class="sxs-lookup"><span data-stu-id="fbed8-196">The complete *Welcome.cshtml* file is shown below.</span></span>

[!code-cshtml[Main](adding-a-view/samples/sample9.cshtml)]

<span data-ttu-id="fbed8-197">Uruchom aplikację i przejdź do następującego adresu URL:</span><span class="sxs-lookup"><span data-stu-id="fbed8-197">Run the application and browse to the following URL:</span></span>

`http://localhost:xx/HelloWorld/Welcome?name=Scott&numtimes=4`

<span data-ttu-id="fbed8-198">Teraz dane są pobierane z adresu URL i przesyłane do kontrolera przy użyciu [spinacza modelu](http://odetocode.com/Blogs/scott/archive/2009/04/27/6-tips-for-asp-net-mvc-model-binding.aspx).</span><span class="sxs-lookup"><span data-stu-id="fbed8-198">Now data is taken from the URL and passed to the controller using the [model binder](http://odetocode.com/Blogs/scott/archive/2009/04/27/6-tips-for-asp-net-mvc-model-binding.aspx).</span></span> <span data-ttu-id="fbed8-199">Kontroler umieszcza dane w obiekcie `ViewBag` i przekazuje ten obiekt do widoku.</span><span class="sxs-lookup"><span data-stu-id="fbed8-199">The controller packages the data into a `ViewBag` object and passes that object to the view.</span></span> <span data-ttu-id="fbed8-200">W tym widoku dane są wyświetlane w formacie HTML dla użytkownika.</span><span class="sxs-lookup"><span data-stu-id="fbed8-200">The view then displays the data as HTML to the user.</span></span>

![](adding-a-view/_static/image12.png)

<span data-ttu-id="fbed8-201">W powyższym przykładzie użyto obiektu `ViewBag`, aby przekazać dane z kontrolera do widoku.</span><span class="sxs-lookup"><span data-stu-id="fbed8-201">In the sample above, we used a `ViewBag` object to pass data from the controller to a view.</span></span> <span data-ttu-id="fbed8-202">W dalszej części tego samouczka będziemy używać modelu widoku do przekazywania danych z kontrolera do widoku.</span><span class="sxs-lookup"><span data-stu-id="fbed8-202">Later in the tutorial, we will use a view model to pass data from a controller to a view.</span></span> <span data-ttu-id="fbed8-203">Podejście model widoku do przekazywania danych jest ogólnie preferowane w porównaniu z rozwiązaniem widoku.</span><span class="sxs-lookup"><span data-stu-id="fbed8-203">The view model approach to passing data is generally much preferred over the view bag approach.</span></span> <span data-ttu-id="fbed8-204">Aby uzyskać więcej informacji, zobacz wpis w blogu [Dynamic V o jednoznacznie określonym typie](https://blogs.msdn.com/b/rickandy/archive/2011/01/28/dynamic-v-strongly-typed-views.aspx) .</span><span class="sxs-lookup"><span data-stu-id="fbed8-204">See the blog entry [Dynamic V Strongly Typed Views](https://blogs.msdn.com/b/rickandy/archive/2011/01/28/dynamic-v-strongly-typed-views.aspx) for more information.</span></span> 

<span data-ttu-id="fbed8-205">Jest to również typ &quot;M&quot; dla modelu, ale nie do rodzaju bazy danych.</span><span class="sxs-lookup"><span data-stu-id="fbed8-205">Well, that was a kind of an &quot;M&quot; for model, but not the database kind.</span></span> <span data-ttu-id="fbed8-206">Zapoznaj się z informacjami i Utwórz bazę danych dla filmów.</span><span class="sxs-lookup"><span data-stu-id="fbed8-206">Let's take what we've learned and create a database of movies.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="fbed8-207">[Poprzednie](adding-a-controller.md)
> [dalej](adding-a-model.md)</span><span class="sxs-lookup"><span data-stu-id="fbed8-207">[Previous](adding-a-controller.md)
[Next](adding-a-model.md)</span></span>
